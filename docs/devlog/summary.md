# Devlog Summary

- 2026-09-20: **Recorded the delivered finite-shoe milestone and preserved five exact reviews.** No runtime behavior changed in this docs milestone. `5e96a0c` is on main/origin with 738 tests, build/types/audit and exact 05ad/8d704 visual transfer; remote checks remain unavailable. Reviews 65/68 distinguish F35 design repair from runtime proof, and Reviews 67/69 preserve F36's rejected and corrected bank account; Review 71 accepts the two bounded geometry results. [Detail](detailed/2026-09-20_contact-native-status.md).

- 2026-09-19: **Accepted the uncertainty-aware finite-shoe integration; delivery is pending.** Review 64 accepts F34 v2: inner/outer contact regions replace nominal hull certainty; both original false passes become uncertain. The exact two-file tool remains unwired. Four fresh primary gates pass (85 files / 738 tests), with exact 05ad/8d704 visual transfer; Review 66 and root accept this bounded integration. Walking `86c08b2` is delivered; Review 63 accepts only the matched native survivor evidence and permits pipe design preparation. [Detail](detailed/2026-09-19_shoe-contact.md).

- 2026-09-19: **Accepted the read-only walking observation integration; delivery is pending.** Exact F26 baseline imports remain separate from the ordered-observation delta; Review 59 accepts its finite CPU contract. Five source/test files are unwired, and four fresh primary gates pass (84 files / 723 tests), with exact 05ad/8d704 visual transfer. Review 62 and root accept this bounded integration. Review 60 rejects shoe F34; Review 61's one diagnostic has root-observed partial results awaiting independent review. [Detail](detailed/2026-09-19_walking-observer.md).

- 2026-09-19: No runtime behavior changed. Preserved exact Reviews 54/56 and targets: v4 PID-reuse rejection and a bounded native-proof direction. Recorded facts delivery `9272500`, 685 tests, exact prior visual transfer and five archived/removed trees. Native run01 failed; contact design awaits Review 57. No contact implementation or capture is admitted. [Current disposition](../work/0_shibuya-1km/plan.md)

- 2026-09-19: **Offline historical walking facts now have an independently reviewed producer and actionable errors.** Applied the exact Review 53 component; four fresh primary gates pass, including 685 tests. Rebuilt browser bytes, served data and harness match 05ad, transferring its existing 8d704 still-image evidence without a new capture. Review 55 and root accept the bounded integration; delivery remains pending, with no source-pair or walking-support adoption. [Detail](detailed/2026-09-19_historical-walking-facts.md).

- 2026-09-19: No runtime behavior changed. Preserved exact Reviews 48–51 and their authored targets: bounded F26/F27 CPU acceptance, historical payload equality with two truthful provenance changes, and launcher v2/v3 F28 rejection. Recorded Q4 delivery `05ad1a4` and four SHA-verified tree removals. Review51 blocks GPU capture; a separate v4 cleanup design and the approved offline-facts implementation remain isolated from source/mesh adoption. [Current disposition](../work/0_shibuya-1km/plan.md)

- 2026-09-19: Applied the Review 45 preservation guard with its three source hashes unchanged. All five integration gates pass: 618 tests and hardware certificate `8d70420e07afbfa7` for 44 frames; prior visual/build evidence is preserved and owned cleanup is clear. Root accepted Review 47 and all 44 native inspections and authorized scoped delivery. Exact rejected Review 46 history remains preserved; source authority and clean-city reconstruction remain open. [Current disposition](../work/0_shibuya-1km/plan.md)

- 2026-09-19: **Scene rebuilds now refuse silent authored-paint loss before cleanup.** The isolated preservation guard admits absent or consistent published-only markings pairs; 23 refusal fixtures keep every byte and never call the writer. Both old/late-preflight mutations fail all 23, and the restored focused suite passes 27 cases. Source authority and clean-city rebuild acceptance remain open. [Detail](detailed/2026-09-19_rebuild-preservation.md).

- 2026-09-19: Applied the independently accepted terminal and capture repairs to primary with all 16 non-document hashes preserved. All five gates pass: 592 unit tests and hardware certificate `7f68c3883cc3f1ce` for 44 frames. Prior visual/build evidence is preserved; owned cleanup is clear. Root accepted Review 44 and all 44 native inspections; F25's two evidence links are repaired. This accepts the scoped components while Q1 stalls/AOI egress and Q3 temporal flicker remain open. [Current disposition](../work/0_shibuya-1km/plan.md)

- 2026-09-19: Pedestrian central routes now plan a physically clear terminal, and retirement retains any still-occupied body and lease while rendering continues. The isolated moderate run has 27 clear retirements, 27 safe reuses and no terminal holds; four over-budget central routes take the existing exit fallback. See [the detailed entry](detailed/2026-09-19_pedestrian-terminal-clearance.md).

- 2026-09-19: Review 42 caught stale bytes labelled capture-valid and release failures that skipped observer cleanup. The scoped Q3 v2 repair passes 54 focused tests and typecheck; five controls fail on the exact rejected source, and all 24 existing PNGs replay unchanged. Review 43 and primary gates remain owed. [Detail](detailed/2026-09-19_motion-capture.md)

- 2026-09-19: Repaired Q3 adjacent-frame canvas capture and actual wheel ascent; calibration proved the integer translation residual cannot diagnose scene flicker. [Record](../work/0_shibuya-1km/snapshots/q3-motion-capture-review43.md)

- 2026-09-19: **Visual certification now refuses linked worktrees before touching evidence.** Eight new cases pass; removing the CLI guard makes three fail. Independent review caught a false refusal for alternate Windows path casing, now gated and repaired. Primary integration passes all five gates, including 543 tests and hardware certificate `d11360e0649ffb15` for 44 frames; Review 38 inspects all 44 at native resolution with no blocking still-image finding. Root accepted the bounded certificate increment. Owned browser/server cleanup is verified. The two style-return approach frames remain outside the certified 44. [Detail](detailed/2026-09-19_certificate-location.md).

- 2026-09-18: **The flicker lane's crawl number is a measurement of the picture for the first time: the worst real dusk pair reads 1.351e-3 per frame against the shipped 0.005 bar, on pairs whose motion is 1–2 px inside the estimator's 24 px search, where RUN-01's pairs moved 30–35 px against a ±12 px search and the estimator answered a failed search with `0,0`.** The four fixes RUN-01 named: `motion.ts`'s gesture cut 10× (`GESTURE_PX` 4 → 0.5, `PATH_RADIUS_FRACTION` 0.05 → 0.30), so a pair's camera motion goes **1.52 m / 0.0346 rad → 0.1911–0.2016 m / 0.0042–0.0045 rad**; `capture.spec.ts` reads the pose on **both** sides of every shutter and records the midpoint, and a pair's span is measured midpoint to midpoint, so the motion and the gap it is divided by describe one window; `judge.ts` grows `SEARCH_RADIUS` 12 → 24, derives the fine pass's window from the coarse grid's own cell instead of a fixed 3 that was narrower than the grid it refined, and **fails a pair by name when the best shift lands on the search radius** rather than returning `0,0`; and `deriveStep` subtracts `performance.now()` read once at the start instead of `performance.timeOrigin`, so every frame's `tick` goes from `−1.07e11` to a plausible **13–252**. Every gate was watched red by reintroducing its defect (`docs/learning/gate-proofs.md`, five mutations with their messages; `docs/learning/defect-register.md`, four entries). **A fifth defect came out of a review of the result and it was live in `main`:** `channelDelta` read **byte** offsets while `comparePair` passed **pixel** indices, so every `changedFraction` in the lane was computed from the wrong byte — the unit frames are 192×108 and no pixel index can leave an 82,944-byte array, so all 11 unit cases passed it while the real 1280×720 pairs read **0.9703 where the true figure is 0.1871**. That, and not misalignment, is what RUN-01 published as `changed = 1.0000` on six pairs; `crawl` is unaffected (`countUnexplained` never calls it), and RUN-01's gesture diagnosis stands separately. The bar stays **0.005 and provisional**: the worst pair is 3.7× under it and two independent captures of the same pose agree to 2.4%, but calibration is blocked because the synthetic null moves the picture ~3 px a frame against the real ~0.7 and the null band is a function of that motion — a real-scene null at the real motion scale does not exist in this lane. Two bounds are carried rather than closed: the bar is written **per rendered frame**, so a *slower* screenshot makes it looser (the fix direction is to bound the pair's `unexplainedFraction` directly), and a pair now spans **20.5–23 rendered frames against the criterion's `cadenceFrames: 2`**, so anything that appears and disappears inside one frame is still invisible — the assessed way out is a second mode that grabs the frame in-page on `requestAnimationFrame` with `drawImage` into a 2D canvas, ~1–3 ms and 3.7 MB a frame against ~250 ms and ~31 rendered frames of motion for a screenshot. `ascent`'s dolly no longer moves the distance inside one run (the ticks are spaced every sixteenth gesture), recorded as a bound. Gates in the worktree: build 0 (bundle byte-identical to `main`), typecheck 0, visual **0** (certificate `422c37617ddd0cd7`, 44 frames, and `dist/` **unchanged** — same 4 files, same bundle hash), audit 0. `npm test` is **521/523**: both failures are `test/paint-support.test.ts`, which refuses a changed `data/scene/roads.mesh` against its Review 10 pin (`3fe126cd…` → `0d429c31…`) — a persisted failure on the final scene, so it is a finding about the F1 batch's scene change and not about this lane, and the pin needs re-binding deliberately. See [the detailed entry](detailed/2026-09-18_flicker-instrument-measurable.md).

- 2026-09-18: **The ground's fourteen holes are closed and the census that owns them is a gate: `npm run data:scene` now reads the written `terrain.mesh` back off disk and refuses it by name with every rim listed, the cap costs +14 vertices / +227 triangles / 3,060 bytes, and the same reader's chunk tail was counting 384 triangles twice.** The holes are PLATEAU's own — zero source triangles cover any of the fourteen centroids, while all eight control points are covered — and the register row's "+3,164 B" is wrong; the measured cost is 3,060 bytes (14 x 6 floats x 4 + 227 x 3 indices x 4). Capped: 1,214 rim edges, one rim, no holes; uncapped the gate refuses with `data/scene/terrain.mesh carries 14 holes in the ground` followed by all fourteen worst-first. `streamTinTriangles` was re-parsing the triangle each chunk ended on, so 1,101,033 source triangles were yielded as 1,101,417 and 65 exact duplicates plus 195 edges with more than two faces reached the built mesh; `mesh-doctor` now reports `duplicate=0 nonManifold=0` and the hole census does not move (14 rims, 1,441 rim edges before and after), so the cap needed no adjustment. Six mutations watched red, including the one the first version of the suite could not see — removing the cap from `buildTerrain` left all seven cases green, because they drove `capTerrainHoles` directly — and `manifest.json` now carries `closedRims` (vertex count, perimeter, apex, area per rim) so the invented surface is nameable in the payload. Branch `phase2-regen` (`f1b3bc9`, `38bb0ac`, `7ad6ed0`, `d5ead63`, `0e7d683`), **merged to `main` as `400065a`** after review 34 accepted the pavement binding; the merged revision runs green at 74 files / 535 tests, 0 failed, which is what fixes `main`'s red `paint-support` pin, and the owed 44-frame re-capture ran on it as certificate `aa2e07e9902a5dc9` binding `dfee0f12…` over 117 files / 301,782,754 B, replacing the stale `e6b7b9d8de0faf69`. **Phase 2 ran the regeneration in the primary: the payload is 117 files / 301,782,754 B at `sceneTreeDigest dfee0f12…` (from 117 / 301,674,209 at `1ebda662…`), eight files moved (`terrain.mesh` `fef57d09…` → `c39d49ed…`, `roads.mesh` `3fe126cd…` → `0d429c31…` — the brief's predicted digest to the byte — `pavements.mesh` `5d2a9150…` → `3b24c5ef…` at **+2,613** triangles where the brief predicted 123, `manifest.json` 1,521 → 4,074 B with the fourteen `closedRims`, plus `pavement-recipe.json`, `control-hardware.json`, `decorations.json` and `markings-provenance.json`), 109 files did not move, and two consecutive `data:scene` runs returned all 117 files byte-identical at `09105dd7…`.** `data:markings` **refused** — the 2026-09-18 restoration re-fetched the OSM extract, so `hero-paint.ts`'s provenance guard fires and the reviewed authored paint cannot be re-derived (a dated Overpass query returned the current database state, not the dated one); the reviewed pair was restored byte-identically from `artifacts/network/app-motion/candidate/data/scene/` and the inheritance is measured at **exactly zero** across all 204 authored-paint vertices on old against new ground and roads. That is the batch's most consequential finding, and its first statement of it named the wrong command: the silent loss is **`npm run data:scene` alone**, which calls `writeMarkings()` with the default `includeAuthored = false` (`tools/scene/build.ts:159`, `build-markings.ts:82`) and overwrote the authored pair with the unauthored one in this batch's own two determinism runs; `npm run data:markings` alone refuses by name; and a clean `npm run data:setup` re-derives both the network and the paint from the live survey, replacing the reviewed bytes with nothing comparing them. Review 34's finding B1; `docs/learning/defect-register.md` carries all three paths. `npm run visual` was run on the merged revision: certificate `aa2e07e9902a5dc9`, 44 frames, the capped scene's. Details in [the detailed entry](detailed/2026-09-18_terrain-cap-and-census-gate.md).


- 2026-09-18: **The road's grain was a spectrum problem and not an amplitude one: the near-field asphalt at `plaza-az000` now carries 7.242% of albedo between neighbouring pixels against the pavement control's 6.254% in the same frame, up from 3.580% against 5.891%, at the same luminance and with 90.3% → 56.6% of its pixels no longer indistinguishable from flat.** One change in `src/scene/surface-detail.ts`: the road's octave vector goes from `(0.35,0.45,0.6,0.85,1.2,1.7)` to `(0.04,0.075,0.175,0.5,2,5.2)` — weight moved into the 64- and 128-cell octaves, where a 3 cm texel is aggregate — and its strength from 0.45 to 0.72. Measured on the certificate's own bytes with a scratch probe that reproduced the before arm to the digit (0.304 levels, 3.580%, 82.2% of pairs bit-identical) and then on the after certificate (build `index-Dj6sqKQI.js` → `index-DRmtpVMl.js`, same GPU and driver, runs `685d0eaeb5669287` → `aded8f3aaa5dfe4c`, both binding `sceneTreeDigest 1ebda662…` over 117 files / 301,674,209 bytes), the frame's step is sub-linear in strength on the old spectrum (1.67x strength → 1.46x step, then 1.47x → 1.38x), so clearing the bar by amplitude alone needs ~0.9–1.0 and that arm's near field reads as oil-stained blotches; the fine reallocation reaches the same display step at 0.72 of the strength, and doubling only the two finest octaves reaches 5.810% at 0.72 and does not clear it. Tone is untouched to three decimals on every frame sampled (satellite noon crossing 104.026 both arms, cartographic noon crossing 155.177 → 155.176, cartographic overhead road sample 115.69 → 115.68, no frame gains a clipped pixel), the pavement keeps its joint grid and guide stripes, and the 1:1 crops settle what the numbers cannot — the cell goes from a featureless gradient to fine irregular aggregate with no speckle, no repeat lattice and no grid seam, `centre-crossing` still reads as one continuous asphalt band under crisp zebra bars and guide lines, and the one nameable texture is a soft diagonal flow in the cartographic near field, the 4x4-cell lattice at 1–2 texels per pixel, below the level at which it reads as tiling in any frame opened. The cost is coarse content: the tile's 16x16 block-mean spread 44.95 → 19.80 of 255, which is the streakiness the near field read as blotches rather than structure a frame resolved — the same mid-ground road rect at the block framing is unchanged (cartographic `block-az000` neighbour 2.770 → 2.774 with structured 26.6% both, satellite 2.706 → 2.709 with 18.0% both) — while every mip a camera between 0.0144 and 0.0886 m/px resolves is *higher* than before. `test/surface-detail.test.ts` gains a spectrum-shape case and raises the road's floors to 0.08/0.045, watched red twice (the old vector fails the floors naming 6.35%/3.97%, and it fails the shape case at `expected 1.673913043478261 to be greater than 5`). See [the detailed entry](detailed/2026-09-18_road-grain-spectrum.md).

- 2026-09-18: **The dusk ambient was the lever: the satellite near-field road comes off the black floor at `plaza-az000`, mean 17.80 → 27.78 of 255 with 0.0% of its pixels under 12, and light alone does not give it grain — the same relative structure only reaches 0.304 display levels there, and this lane measured why.** One change in `src/scene/time-of-day.ts`: `hemisphereIntensity` and `environmentIntensity` each gain a `dark`-ramped dusk fill (0.2 and 2.55), so at the preset's own −3.48° solar elevation the hemisphere goes 0.240 → 0.414 and the environment 2.805 → 5.023, and with the sun up the fill is exactly zero. Before arm certificate `14cdadbafba82120` (`index-_LQ7yEQN.js`), after arm `ebf66d041d14173b` (`index-Dj6sqKQI.js`), same adapter and driver, both binding `sceneTreeDigest 1ebda662…` over 117 files / 301,674,209 bytes — the arms differ by build alone and the primary's `data/scene` was never written (it was copied, byte-identical, into a worktree that junctions `node_modules` only). On the road rect (320,540 640x180) the neighbour step goes 0.262 → 0.304 levels and 86.8% → 82.2% of neighbouring pairs identical, which is 2.734% → 3.580% of albedo against the in-frame pavement control's 4.701% → 5.891% — so the floor is cleared and the structure improved, but 91.0% of the rect is still indistinguishable from flat against the pavement's 70.7%, and the reason is arithmetic rather than a second defect: the display step grows as about `R^0.35`, so one level of grain on an albedo-0.073 surface needs ~7x its radiance, and the grain's lever is the tile's amplitude, not the light. The same defect class moves with it: the satellite dusk approach's near field 14.5 → 26.2 mean with under-12 pixels 58.0% → 13.6%, `block-az000` 11.87% → 0.62%. Tone: the cartographic noon crossing asphalt band reads **167.2 before and 167.2 after** (mean|d| 0.04 levels), no frame in the 44 gains a clipped pixel, and the dusk crossing cells opened at 1:1 still read as dusk with zebra, yellow guides, paving grid and signage legible — the cost, recorded rather than argued away, is that the cartographic block-distance frames flatten, the largest mover being `block-az240` at +29.69 levels of mean. `test/road-tone.test.ts` gains four cases (both terms lifted within a bounded factor, the floor cleared under the whole-radiance and the pessimistic albedo-free-fixed splits, noon pinned at 0.58/1.5), watched red first with the fill at zero. See [the detailed entry](detailed/2026-09-18_dusk-ambient-fill.md).
- 2026-09-18: **The dusk criterion's second half — "no flicker or crawl over a moving sequence" — had never been judged, because no lane had ever captured two consecutive rendered frames of a moving camera; there is now a capture lane and a pure judge for it, and the judge's three failure classes have each been watched red.** `src/app.ts` feeds `cameraStill` into `post.setStill`, so TAA cannot accumulate while the camera moves in any run and the question has to be asked of the non-accumulating path; every existing capture does the opposite of what it needs, since `OrbitDriver.settle` waits for the camera to stop before the shutter opens. The new lane (`tools/flicker/`, port 4324, `npm run visual:flicker`, its own `flicker-iteration` lane root `artifacts/flicker/`) drives the canonical dusk crossing pose through synthesised pointer input and **never settles**, capturing a still per pointer gesture with the render counter read on both sides of the shutter, plus the simulation step, the pose and the PNG digest. `judgeFlicker` reports per adjacent pair the changed-pixel fraction, the digest difference, the camera travel and rotation and a crawl indicator that counts the pixels which are a hard local feature in one frame and not in the other after the earlier frame has been shifted onto the later one by a two-stage rigid-translation estimate — so motion is taken out and detail that appears or disappears one pixel against its neighbours is what remains. Bar `0.005` of the frame per rendered frame, with a control on each side: a pure 3 px-per-step translation measures **1.2e-4 to 1.6e-3**, the same movement with detail collapsed on alternate frames measures **3.7e-2 to 5.2e-2**. Three mutations watched red on `test/flicker-judge.test.ts` (11 cases): byte-identity `expected [] to have a length of 1 but got +0`, the render-counter wall, and the crawl bar weakened a hundredfold, each in `docs/learning/gate-proofs.md` — and the first mutation exposed that the frozen-scene case passed it, because the neighbour predicate's message satisfied a loose assertion. The judge also runs over the only rendered bytes on this machine, the post-chain archive (`rest-a -> motion-07`, 219 frames apart, crawl `2.39e-4`), which proves the predicates run on real pixels and is not a verdict. **The lane is `tools/` and `test/` only: no `src/`, `index.html` or `vite.config.ts` file is touched, and the build emits `dist/assets/index-_LQ7yEQN.js` at `a5b9d414…`, byte-identical to the same build of `a7f865d`, so the certificate `685d0eaeb5669287` stays bound to main and no re-capture is needed.** The per-frame step is derived in the spec from the page's own clock rather than read from the bridge — `population().ticks` is 0 for a whole `?agents=`-free run, which this lane is. All four gates green (build 0, typecheck 0, test 0 at 507/507, audit 0 with two moderate advisories below the high level); **no browser lane was run** and the first real capture is sequenced by the coordinator. See [the detailed entry](detailed/2026-09-18_flicker-instrument.md).

- 2026-09-18: **The flythrough's crowd anchor named the scored camera as if it were the camera that flies: one azimuth meaning the direction the camera looks to `aim.ts` and the direction from the target to the camera to the controls, so the exported `CROWD_CAMERA` and the flown camera are the two ends of one 26 m ray, 53.1 m apart.** The passing run's manifest settles it twice over: the opening pose's camera `(216.341, 554.452, 216.341)` is `target + (sin az, cos az) * distance * sin(polar)` at 45°, and the crowd leg's opening frame is camera `(462.439, 17.232, 442.423)` against target `(459.056, 15.200, 416.724)` at `azimuth` 0.1308997 and `distance` 26.000001 — 26.0000 m at a target-to-camera bearing of exactly 7.50°, so the flight's own bearing is 187.50° and its camera is `(462.788, 441.254)`, 1.24 m from where the run opened (the approach's 0.909 m pan residual). `aim-score.ts` builds `camera + (sin, cos) * distance`, and re-scoring the six `artifacts/crowd-aim-fix/` dumps at `(456.0, 389.7)` reproduces the anchor entry's numbers exactly (152 / 99 / 95 / 100 / 100 / 10 inside 45 m, 3.9° axis, 131 / 36 / 55 / 55 / 39 / 25 px), so `CROWD_CAMERA` is the camera the scorer accepted and `CROWD_TARGET` is the point the flight pans to. **The constants were not moved**: fitting the exported pair to the flown pose means making its own bearing equal `CROWD_AZIMUTH`, which carries the approach's pan target 53.1 m across the crowd and re-aims two legs, and the aim camera keeps 1.91 m of clearance, under the leg's 2 m floor. The fix is the truth: `CROWD_CAMERA` → `CROWD_AIM_CAMERA`, a derived `CROWD_FLOWN_CAMERA` in the controls' convention, both conventions stated in `plan.ts`/`aim.ts`/`aim-score.ts` with the manifest numbers, the crowd leg's push divided by the measured pair separation, and `aim.ts`'s usage line moved from the stale `reference-dump-t5400.json` to the six dumps the anchors were actually scored from. `test/flythrough-crowd-anchor.test.ts` pins it and the role swap in `CROWD_TARGET` exits 1 with `CROWD_AIM_CAMERA is at a target-to-camera bearing of 7.500 degrees from CROWD_TARGET where CROWD_AZIMUTH plus 180 is 187.500 … expected 3.141592653589793 to be less than 1e-9`. Gates: build 0 (bundle byte-identical to the primary's), typecheck 0, 69 files / 495 tests green, audit 0; `npm run visual` not run, a sibling lane holds the browser. See [the detailed entry](detailed/2026-09-18_crowd-anchor-convention.md) and the register entry.

- 2026-09-18: **The re-planned crowd leg was refused by its own lane — the anchor had been chosen on its best tick — and the fix is an anchor chosen on its worst.** The merged anchor `(452.07, 353.95)` held 225 walking bodies inside 45 m at tick 3,000 and **4** at 5,400. The lane's run took 127 s instead of 72 s because eight inspection lanes were loading the machine, and ticks advance with wall time, so its crowd leg captured at ticks 4,899-5,332: five of six held pairs came back byte-identical and `judgeSequence` refused the run. A leg's tick is not a property of the route, so a plan that needs a peak only works unloaded. Six dumps were generated on this revision — `probe.ts --ticks N --dump-tick N` at 3,000 / 4,000 / 4,900 / 5,400 / 6,000 / 7,200 — and every walking-way camera was scored at each of them; the anchor is now **camera (456.0, 389.7), target (459.39, 415.48), 26 m, bearing 7.5°**, chosen because it holds the most walking bodies inside 45 m at its *worst* tick: **152 / 99 / 95 / 100 / 100 / 10**, with 1.7 m figures 131 / 36 / 55 / 55 / 39 / 25 px. Measured by a fresh `npm run visual:flythrough` in the worktree: exit 0, `failures` null, **46 distinct digests of 46** (the refused run had 41), and every held pair changed by 3.6-21.2% of pixels against byte-identical before. The swing is unchanged at 0.109 rad a step against the 0.3491 cap, and no `holdToTick` was added: what moved is the ticks per step, which a hold cannot address. See [the detailed entry](detailed/2026-09-18_crowd-anchor-window.md).
- 2026-09-17: **The re-capture settles both halves: the road asphalt now shows structure where the bar measures it — the cartographic near-field road goes from 99.9% of pixels indistinguishable from flat to 63.5%, with its neighbouring-pixel difference up 14x and its mean luminance unchanged — and the black near field is NOT fixed, measuring 16.6 of 255 against a 20 floor and at the bottom of the predicted range, so the palette is the wrong lever and the dusk light on horizontal surfaces is next.** Certified runId `09e00eb5689892e0`, same adapter and driver, the same scene digest `1ebda662…` over 117 files / 301,674,209 bytes, harness untouched — so the two arms differ by build alone. Changed frames digest-verified: `sweep/satellite/plaza-az000.png` `d19e6c32…`→`70179de0…`, `sweep/cartographic/plaza-az000.png` `a2ef4b86…`→`7565ee7c…`, `hero/hero-satellite-noon-crossing.png` `486cfd6e…`→`7d9e6f09…`, `hero/hero-cartographic-noon-crossing.png` `4fc42caa…`→`3e83527d…`, `hero/hero-cartographic-dusk-crossing.png` `1c121af0…`→`27183339…`. On the near-field road band (rect 340,600 600x100, which the camera's own geometry puts at 0.017–0.029 m/px, inside the acceptance bar's 0.0144–0.0886) the cartographic road's residual median goes **0.024 → 0.365**, flat **99.9% → 63.5%**, mean neighbouring-pixel difference **0.038 → 0.531**, mean luminance 51.83 → 51.88 (the tile varies the surface around the palette's mean, which is what it was built to do); the satellite band goes 10.04 → 16.45 luminance, 100% → 96.5% flat, 0.025 → 0.132 neighbouring. Opened at native size the cartographic crop pair is decisive — a smooth blue-grey gradient becomes visible aggregate mottle — and no crop shows a repeat lattice, as the 3.1% repeat-scale energy predicted. **Inside the azimuth-45 crossing frames it is weaker for an arithmetic reason, not a second defect**: at that azimuth the camera stands on the pavement, so those frames' asphalt is the mid-ground band at ~0.07–0.3 m/px, which puts the 4 m tile at mip 2–4 where the spectrum gives 2.7–1.5% of albedo per texel, about a level; measured there, cartographic noon improves 0.477 → 0.904 residual median and 51.6% → 30.1% flat, cartographic dusk 0.325 → 0.588 and 59.3% → 44.9%, satellite noon 0.238 → 0.428 and 59.8% → 53.6% with luminance 81.7 → 92.9 from the lift, and by eye the added mottling is visible in the cartographic dusk pair and not confidently in the satellite ones. **The black near field**: the satellite near-field road reads median 10.4 → 16.6 with under-12 pixels 66.6% → 0.3%, so the black threshold is cleared and the full frame shows dark grey rather than a void, but 16.6 is under the 20 floor the change's own gate names and at the bottom of the predicted 15.6–24.6 — the two-point fit was right that the albedo-independent term (wet specular plus bloom) is most of that surface's radiance, so doubling the albedo buys six levels and not twenty. The pre-registered criterion applies rather than being restated: the palette is not this defect's lever, it is not to be tuned further, and the next change is the light on horizontal surfaces at dusk; the value is left in place only because it is right on its own terms (0x32363c is below the range asphalt occupies) and it removed the below-12 population. No regression observed: block and overhead whole-frame differences are at or under 1.2 levels with nothing over 8 apart from the intended road tone lift, the cartographic near-white look holds (noon crossing asphalt band 169.80 → 169.79), and paving, zebra bands, the yellow diamond, signage and the attribution line are present and legible in the frames opened. Still unmeasured: temporal shimmer, and a crop-by-crop look at the ground's own tile at block and overhead distances. See [the detailed entry](detailed/2026-09-17_detail-tile-invisible-and-black-road.md).

- 2026-09-17: **The appearance tile's silent-no-op hypothesis is false, the tile was invisible because its local step was two orders of magnitude short of a display level, and the black plaza near field is the road mesh rather than a light falloff — all three measured, with the pixel verification still owed.** Two commits on `appearance-2` off `2561668`. The re-inspection's leading hypothesis for a tile that is in `dist/` and absent from the pixels was an `onBeforeCompile` anchor that does not match; all four anchors (`void main() {` in both stages, `#include <project_vertex>`, `#include <roughnessmap_fragment>`) occur exactly once each in three r180's `ShaderLib.standard`, and the positive control is in the frames the reports inspected — the basket-weave paving joints at 0.6×0.4 m come from that same patch's `sidewalk` branch, which has no texture and no other source. Reproduced the inspection's figure exactly: the shipped tile moves **0.36% of albedo between neighbouring texels on the road, 0.40% on the ground**, which is 0.13 of a 0-255 level, because the renderer's curve gives ~0.36 of a level per 1% relative albedo change. Two mistakes: the amplitudes (1, 0.62, 0.38, 0.22, 0.12, 0.06) over 4→128 cell lattices decay faster than the lattices refine (a smooth 8 m hill, local step 2.3% of its own range), and **every texel sampled a lattice cell midpoint** (`u = (x + 0.5) / SIZE`), so with 128 cells each centre landed on `fx = 0.5` where smoothstep's slope is zero and the finest octave was written as the average of two lattice values. A third fact the reports did not have: the tile commit *removed* the road's previous `grain * 0.025` sine term and left 0.36%, so near-field asphalt lost about seven eighths of its structure — which is why the cartographic plaza's aimed cell measured 38% *less* structure after the tile, not because the tile is strong. `cd94f1a` corner-aligns the sampling, gives each surface a spectrum chosen by measuring that step (road rises to the fine octaves, ground stays flatter because it is mostly seen minified), sets strengths 0.45/0.30 and anisotropy 8 for the grazing-angle footprint, and extracts `patchSurfaceShader` as a pure function that refuses a source missing an anchor and names the anchor and the stage. Shipped road: **6.35% of albedo between neighbouring texels at mip 0 (p95 14.8%), 3.97% at mip 1** — 17× the old value. `a2282f8` lifts the satellite road's albedo 0x32363c → 0x474d55. **The black near field is the road, not the pavement and not the light**: the inspection tool's `left-ground`/`near-pavement` are grid positions, and at the plaza pose the camera stands on the avenue, so those regions are `roads:plateau-tran` (0x32363c) while the legible paving grid on the right is `roads:plateau-semantic-pavement` (0x999991) — a tenfold albedo difference between two meshes in one frame. Measured from the frames: satellite near-field road median **10.4** with 66.6% under 12 (the report's figure exactly), satellite right-ground pavement median 51.3, cartographic near-field road median 52.4; `lighting.ts` stops shadow casting below 8° of solar elevation and dusk is −3.9°, so no shadow can fall differently across one horizontal plane, and the road mesh's stored normals are 100% upward-facing (measured). What that lift buys is **predicted, not measured**: fitting the two certified road points to `radiance = A + K·albedo` gives A 0.0052 (the wet specular from the dusk sky plus bloom, 66% of the satellite road's radiance) and K 0.0736, so the prediction spans **15.6 to 24.6 of 255** against 10.4, and only 24.6 clears a floor of 20 — the re-capture with only the palette changed measures the albedo-driven sensitivity directly, and if it lands near the lower end the palette is the wrong lever and the dusk light on horizontal surfaces is. Gates at both commits: build 0, typecheck 0, 68 files / 494 tests green, audit 0; both new gates watched red first (anchor rename → five shader cases naming the stage; old spectrum+strength → `road: mean neighbouring-texel step is 0.41% of albedo at mip 0 (p95 1.00%)`). `npm run visual` was not run — the browser and renderer are serialized and a sibling lane held them. See [the detailed entry](detailed/2026-09-17_detail-tile-invisible-and-black-road.md).

- 2026-09-17: **The Phase B appearance batch is code-complete and gated: the ground and road carry a seeded world-space detail tile, the probe seam is in `index.html` behind an exact switch, the served manifest no longer carries a clock, and the dead `guardrailPosts` field is gone.** Four commits on `phase-b` off `b7fc563`, each with its gate watched red first and recorded in `docs/learning/gate-proofs.md`. The tile is `src/scene/surface-detail.ts`, six octaves of wrapping value noise drawn from `createRng` at a fixed seed, sampled at `worldXZ / metresPerTile` — **8 m per repeat on the ground and 4 m on the road**, chosen from the camera rather than from taste: 55° vertical at 1280×720 puts one ground pixel at 1.374 m at the overhead sweep's 950 m, so an 8 m tile is 5.8 px and its 8 m and 4 m octaves are resolved, where a 2 m tile would be 1.5 px and would mip to flat — the audit's actual finding was not "no texture" but "flat exactly where the mid and far frames look", so the scale is asserted against that arithmetic. The distance fade is the mip chain, not `smoothstep`, which is what used to turn the road's grain off as the camera pulled back. The seam's own unit case caught a flaw in itself before landing: it matched the bare path `/src/main.ts` and the comment above the seam names that path in prose, so `indexOf` found the comment and reported the order backwards. Measured on the build: `dist/index.html` 2,049 → 2,365 bytes and the entry bundle renamed `index-D7By1MWG.js` (975,237) → `index--jFOWFey.js` (976,145). The manifest's `builtAt` leaves the served payload rather than being normalised out of `sceneTreeDigest`, so the artifact itself is reproducible and the digest needs no special case a later timestamped field could defeat; `manifest.json` is 1,562 → 1,521 bytes. **The F1 terrain cap is not in this batch**: closing the ground moves `roads.mesh` (3fe126cd… → 0d429c31…, +123 triangles) and so the pavement recipe's `REVIEWED_INPUTS` and paint-support's Review 10 pins, which is a pavement-input review rather than a batch item — it sits on `phase-b-terrain-cap` (`adb9d4f`). Gates at `ddb87ac`: build 0, typecheck 0, 64 files / 470 tests green, audit 0; the primary's 116 scene files hashed identical across the isolated rebuild. The frames have not been looked at, and the coordinator sequences that run. See [the detailed entry](detailed/2026-09-17_phase-b-appearance-batch.md).

- 2026-09-17: **The flythrough's approach leg stood the camera 19 cm above the ground on its way down to a 4.5 m stand, because one capped drag a step never finished the correction and the next step's zoom then shrank what was left of the camera's offset; the vertical control now finishes inside the step, the run is green, and the recorded per-leg floor is 2.9 m.** Measured in the browser on the repaired scene: `OrbitControls` applies a drag's spherical delta with `_sphericalDelta.phi *= (1 - dampingFactor)` each frame, so the approach's step 10 asked for 0.03128 rad and the polar moved 0.0165 rad; the zoom is exact (`_spherical.radius *= _scale`, the vertical term included), so at 4.5 m of offset a 0.788 factor is 3.41 m of descent, and over the crowd's ground — 21.56 m climbing to 26.41 m along that stretch — the camera arrived under the terrain. That run recorded `approach-010` at `heightBeforeM` **0.6557 m** with a captured clearance of **3.052 m**, the same shape the archive shows at 1.07751 m and 3.10093 m. Fix: `runVerticalCorrection` iterates `standConverge` to its aim inside the step (`MAX_STAND_DRAGS` 8, `STAND_CONVERGENCE_M` 0.05 m), the aim is clamped to the leg's floor carried per step as `standFloorM` from `APPROACH_FLOOR_M` 2, held above it by `max(0.1, 0.03 * distance)` because a drag's return is read mid-gesture, and `raiseToFloor` answers the floor **between the zoom and the pan** — the pan's axes are horizontal, so a camera clear of the floor there stays clear for the step. Re-flown on the frozen tree, 46 frames, port 4323: `judgeSequence` reports **zero failures** and the manifest has no `failures` key; lowest recorded moment per leg, old then new — overview 68.084 → 57.241 (captured), approach **0.194 → 2.908**, crowd 3.640 → 2.899, ascent 4.400 → 3.550. The two frames the check named read 4.626 m and 3.431 m at their captures, and the moments that failed (the stands' 0.19419 m and 1.07751 m) are gone: those steps' stands now read 4.339 m and 3.333 m before their corrections. The six cases in `test/flythrough-stand.test.ts` drive the shipped loop, and disabling it (`while (state.commanded && …) {` → `while (false) {`, applied from a script that asserts the marker count and re-reads the file) was watched red at `expected 0.34130161639351186 to be greater than or equal to 2`, with the same leg's stand reading **-0.78 m** under the judge case; mutation, message and the run's numbers are in `docs/learning/gate-proofs.md`. The `data/scene/` the worktree junction serves was rebuilt mid-session with `npm run data:scene` because it had lost its meshes; the first "clean" run was killed and re-flown because the tree was edited under it.

- 2026-09-17: **The flythrough's clearance check read the camera only at the captured instant, so a stand that dipped to 0.19 m between shoots passed; the floor is now read over the stand's own readings too, and the recorded run fails it.** The per-frame assertion in `tools/flythrough/flythrough.spec.ts` reads the camera once, at the screenshot, so `artifacts/flythrough2/manifest.json`'s `approach-009` passed at a captured clearance of 2.68534 m while the same step's stand read `heightBeforeM` 0.19419 m (`approach-010`: 1.07751 m). `judgeSequence` now takes `clearanceFloorM` — the same `CLEARANCE_FLOOR_M` constant the per-frame assertion reads — and applies it to every moment the record kept: the captured clearance, and the stand's `heightBeforeM`/`heightAfterM` where present, failing by name and saying in the message that the claim is "no recorded moment of this leg put the camera below the leg's clearance floor", not that the path never dipped. Two new cases in `test/flythrough-frames.test.ts` (20 green); the mutation that drops the stand readings back out was watched red at `expected '' to match /no recorded moment of this leg put the camera below the leg's clearance floor/`, a record carrying no captured clearance fails instead of passing silently, and the numbers are the recorded run's own. **The recorded run now fails the check**: its lowest recorded moment is 0.19419 m against the 1.5 m floor, so it would need a floor at or below 0.194 m to pass; the approach leg's own plan floor of 2 m fails the same two frames, the dip is real, and the floor was not lowered. Recorded in `docs/learning/gate-proofs.md`.
- 2026-09-17: **The aim tool scored the crowd at a height the crowd was not at, and against a ray the renderer does not use; both are fixed, the crowd leg is re-planned onto low ground, and the clock is left alone on measurement.** The audit's constant-body-height finding reproduced exactly, but it was not sufficient: at the old anchor `(-386.198, -496.297)` the camera stands on 26.36 m of ground (driver's `groundBelow`, radius 8; the tool's 40 m box said 27.5), so its axis to the pinned 15.2 m target is **34.70 degrees down**, and the old scorer compared its bodies against `TARGET_Y_M + 0.9` — a ray 12.3 m below the real crowd — while reporting a level view. Every one of the 311 bodies in `crowd-000.png` projects to rows 636-710 of 720 and the frame at native size is clean paving. The scorer now reads each body's height off the terrain mesh, projects it into the renderer's own camera basis, and refuses a pose whose axis is steeper than 20 degrees or which holds fewer than 10 walking bodies inside 45 m (`tools/flythrough/aim-score.ts`). Re-scored against dumps regenerated on this revision — the two retained ones are **stale**, giving 42 vehicles against 56 and 2,998 of 3,000 rows differing after `5e8c266`/`7e703cd`/`42f5639` — the old anchor is refused with 0 bodies inside 45 m, and the anchor re-planned onto `(452.071, 353.949)` on 14.49 m of ground holds **225 near / 330 moving bodies, 17 vehicles, a 5.0 degree axis and a 27 px figure** at the median 43 m, way `walk:664819019:3:0:ground0:f`. Its bearing is 7.5 degrees, so the approach's swing is 37.5 degrees instead of 82.5 and 0.109 rad a step against the driver's 0.3491 cap; the swing arithmetic repaired this session is untouched. The clock is unchanged — the chosen anchor holds 316 bodies at tick 3,000 against 12 at 5,400 — so **no `holdToTick` was added**. `test/flythrough-aim.test.ts` (2 cases) pins the scorer and was watched red under a disabled pitch check. No flythrough run was started: the coordinator reserved it for the stand-repair lane. See [the detailed entry](detailed/2026-09-17_crowd-aim-scorer.md).

- 2026-09-17: **Every frame set the deliverable owns is now inspected at native resolution, the performance answer changed, and the flythrough passes.** All 44 certified frames plus both style-return frames (nine lanes, 464 crops, every digest recomputed against `complete.json`), all 38 populated frames, and the passing flythrough's 46 frames (three lanes) were opened at their own size; zero blocking render defects, with the findings severity-mapped (near-field pavement loss worst at satellite `plaza-az000`; facade softness; a dusk-crushed tower shadow in satellite `block-az060`; an untextured mid-ground in five approach frames; the crowd leg's unresolvable subject). The populated crossing pose is 14 of 14 frames byte-identical across 26.15 s of the pedestrian green with zero agents in frame — the signature criterion unmet for the routing reason, as a true depiction rather than a capture fault — and the lit red lens there is a correct vehicle-head red, superseding the old "pedestrian head in its own green" reading (the network maps zero pedestrian signal heads). Performance re-measured with the probe seam in a worktree on the quiet machine: **p50 16.7 / p95 33.4 ms at 3,000 drawn pedestrians and 59 vehicles**, so the median holds the 60 fps target where the 2026-09-16 record has 83.3/183.4; the ceiling is 3,000 by the median and 2,500 by p95. The probe seam that made the measurement possible has never existed on `main`, which is recorded as its own instrument defect with its fix owed. F1 is a ~510 m² upstream TIN hole at (+580, −404), 81.35 m outside the AOI, with a 14-hole census and its disposition left to the review. Both independent reviews found no blocking code findings; their findings are applied and merged. See [the detailed entry](detailed/2026-09-17_acceptance-evidence-and-flythrough.md).

- 2026-09-17: **The independent review of the two flythrough fixes found no blocking defect and six things the record got wrong or left unbounded; all of them are applied, each code change watched red first.** Two were real gaps in the checks. The structure metric's relative leg was guarded only by `mean > 2`, so one flat surface at mean 3 with ±1 of quantization dither scored **1.0** — every one of its 144 cells had a deviation of 1, which is 33% of its own mean — and the guard the header relied on was untested: deleting it left all 15 cases green. The leg now needs `deviation > 2` and `mean > 8` as well, the flat dark frame is a case (watched red as `expected 1 to be +0`), and approach-005 is unchanged at 17.4% because every cell that qualifies through the ratio sits above mean 16. The hold exemption was bounded by the record it exempts: six fixture frames all marked held with zero travel returned **0 failures**, a completely static camera reported as a flythrough. `judgeSequence` now takes the plan's own held-pair count per leg, derived in the spec from `LEGS`, and fails by name when a leg holds more or fewer frames than the plan, when the held frames arrive in separate runs, or when the run is not the tail of the leg — the red control's branch still returns before any of it. Five were records: approach-005's calibration is **17.4%** (25 of 144: 6 absolute + 19 relative) re-measured from the surviving frame and agreeing with that run's manifest, not the 15.3% a deleted scratch file recorded; the ascent lands **exactly on the final step** (index 10 of 0..10, the tenth carrying the last 0.0436 rad of **3.5343** rad), not "step 10 with one step spare" and not 3.538 rad, and the plan test's 0.05 tolerance is what makes that step pass; the ledger comment says the manifest is written before the **sequence** assertions only, since the per-frame clearance, renderer and viewport checks still abort the capture loop before it; `MAX_ROTATE_PX` 40 is restated at `test/flythrough-plan.test.ts:34` rather than read from the shipping code; and the `frames.ts` hash in the gate-proofs record was the CRLF form of the `934a363` blob, replaced with the landing blob's real SHA-256. One record fix the review made in passing: the plan's acceptance-status paragraph said the flythrough's `taa` 0/46 was the only TAA reading, where the eight certified agent-free hero frames in `artifacts/visual/hero/hero.json` each record `taaAccumulating: true` (25 or 32 samples) — so the hero frames evidence TAA and the populated flythrough cannot, by construction. `test/flythrough-frames.test.ts` is 18 cases; nine mutations are recorded in `docs/learning/gate-proofs.md`.

- 2026-09-17: **The flythrough's approach swing went the long way round and never arrived, which is why the crowd leg opened on a hill face; it now takes the short way, and a test flies both swings through the driver's own rotate arithmetic.** The plan interpolated the approach bearing from `OPENING_AZIMUTH` 0.7854 to `CROWD_AZIMUTH` 2.2253 through `+ 2*PI` — 7.7231 rad over steps 6..11, 1.2872 rad a step — against the 0.3491 rad a 40 px drag buys at this lane's 1280x720 canvas. The driver delivered 0.3491 a step and its `shortestAngle` arithmetic flipped sign twice, so the approach ended at azimuth 1.2872 where the crowd leg's first frame needed 2.2253, 0.9381 rad short; the crowd leg then opened about 17 m from the scored pose and `frames/crowd/crowd-000.png` was captured 0.6 m above the terrain under it, which the clearance check caught correctly. The swing is now a monotone interpolation towards `CROWD_AZIMUTH`: 1.4399 rad, 82.5 degrees, 0.2400 rad a step, landing on the crowd's bearing exactly. The stale "172 degree" claims, and the claim that the long way made the driver's shortest-angle arithmetic agree, are gone. `test/flythrough-plan.test.ts` (1 case, 0.4 s) flies `LEGS` through `rotateStepRadians` and `shortestAngle` — both now exported from `driver.ts` rather than re-derived — and read `the approach leg delivers the camera to azimuth 1.2872 rad and the crowd leg's first frame needs 2.2253 ... expected 0.9381162336791848 to be less than 0.05` against the old plan before going green. The ascent's unwrapped form was measured under the same arithmetic and converges on its final step — index 10 of 0..10, the tenth step carrying the last 0.0436 rad of 3.5343 rad of travel — so it is unchanged. Committed `67aa9c9` on `worker/flythrough-swing-fix`; the mutation and the message are in `docs/learning/gate-proofs.md`. **Correction, 2026-09-17:** this line said the ascent "converges at step 10 with one step spare"; it lands exactly on the final step, there is no spare, and the plan test's 0.05 tolerance is what makes that step pass (deleting it leaves the case green 0.0436 rad short). Made by the review-driven fixes landing, which also corrected `tools/flythrough/plan.ts` and `docs/learning/gate-proofs.md`.

- 2026-09-17: **The flythrough's sequence check judged the route by a contract its recorder never wrote, and the three adjudicated defects are now gates.** The 2026-09-16 flythrough run failed on 8 of 46 frame pairs under 1 mm and approach-005 under 5% structured, and the adjudication (`artifacts/flythrough2/adjudication-report.md`) found check defects throughout: leg-first frames recorded `cameraTravelM: 0` where the judge's own contract is `null` (four phantom pairs — the honest denominator was 42); the crowd leg's six designed hold steps were failed for being still, which is what the plan wrote them to be; and the absolute deviation-12 structure threshold read dusk tone mapping as a blank facade (approach-005: 4.2% absolute against 24.3% of cells over 0.25 relative contrast). Leg-first travel is now `null`; the hold steps carry `holdsCamera`, exempt from the travel floor and required instead to show a living scene — digest differs from the previous frame, render counter and population ticks advance, failing by name — while the red control still requires every pair still, held included; `structuredFraction` (now `tools/flythrough/structure.ts`) counts a cell on deviation > 12 or on deviation > 2 with mean > 8 and deviation/mean > 0.3, so approach-005 scores 17.4% and a blank sky still scores 0; and `manifest.json` is written before the `judgeSequence` assertion, carrying a `failures` field on a failing run, so a failed run no longer destroys the pose ledger — the sequence assertions only, since the per-frame clearance, renderer and viewport checks still abort the capture loop before that write. Seven mutations watched red in `test/flythrough-frames.test.ts` (15 tests, 0.4 s), with three entries in `docs/learning/gate-proofs.md` and calibration in `artifacts/flythrough-fix/scratch-calibration.txt`. **Correction, 2026-09-17:** this line first said the structure leg was `deviation/mean > 0.3` with `mean > 2` (the relative leg now also needs deviation > 2 and mean > 8, because a ±1 dither on a flat dark surface satisfied the old form in every cell), recorded approach-005 at 15.3% (the surviving bytes give 17.4%, 25 of 144 cells, and the run's own manifest agrees; the scratch calibration it cited is gone), and said the manifest is written before the assertions (it is written before the sequence assertions, and the in-loop checks still fail before it). The review-driven fixes landing made all three corrections and added two cases (18 tests): the flat dark surface with dither, and the hold exemption bounded by the plan's own `holdsCamera` steps rather than by the record's flag.
- 2026-09-17: **The 44-frame appearance gate left SwiftShader and the whole gate now takes about five minutes instead of three hours.** The owner's 2026-09-16 instruction — "if you can use GPU, don't use CPU" — overrode the recorded trade of machine-independent certified pixels for a roughly fifty-fold tax, and `playwright.config.ts` now pins `--use-angle=d3d11` with `workers: 3`, one per specification file. Measured on the committed tree: `npm run visual` end to end in **283.2 s and 309.8 s** on consecutive runs, against the software lane's **4,438.2 s for ten hero captures** in the run the coordinator stopped; the capture lane alone is 144 s at `workers: 3` against 238.9 s at `workers: 1`, and `--fully-parallel --workers=4` measures 114.7 s without being adopted. A one-pose phase probe on both arms puts the difference where it belongs: on software one capture drew frames at 1,035 ms each, waited 64.9 s for tiles, spent 342.7 s in controls corrections and 55.7 s inside one screenshot; on hardware the same phases are 207 ms, 1.1 s, 2.7 s and 232 ms. What that costs is stated rather than glossed: the certified frames are this adapter's pixels, so the certificate now names the renderer, the GPU and the driver version (`tools/visual/gpu-identity.ts`, read at `--begin`) and refuses a frame set drawn on another adapter. `playwright.hardware.config.ts` and `npm run visual:hardware` are deleted — two lanes claiming to be the appearance gate was the outcome to avoid — and the `hardware-iteration` lane becomes `frame-budget` for the one instrument that still uses it. The renderer assertion was made to go red twice (swiftshader launch args, with and without the lane's hardware switch) in about a minute each, and the pixel difference between the preserved software frames and the hardware ones at the same pose is 2.4–8.4% of pixels beyond 6 summed over RGB (mean absolute channel difference 0.4–1.0 of 255) against 90.5% for a real style change on the same metric and 0.000% for a file against itself. Also fixed: `requestedGpu` recorded `"software"` in the first promoted certificate because the wrapper runs in its own process and never saw the lane's declaration. See [the detailed entry](detailed/2026-09-17_appearance-lane-on-the-gpu.md).
- 2026-09-16: **The `tile-memory` red the unit-gate lane recorded as a mystery was its own control arm, and the timeout it hid behind is now the message.** `test/tile-memory.test.ts` spawned `test/fixtures/lru-disposal.mjs` twice on a 2,000 ms `spawnSync` budget and parsed stdout without checking it existed, so a child killed at the budget arrived as `SyntaxError: Unexpected end of JSON input`. The line that threw at 20:18 is `13:15`, `JSON.parse(control.stdout.trim())` — the `--fractional` arm, whose budget is *meant* to expire and which needs only its first line inside it — and not the `repaired` arm the earlier record named (`artifacts/flake/gate-R6.log:171-177`); that entry now carries a dated correction. Reproduced deliberately with the control's budget at 50 ms, and the mismatch is provable from the code as well: at a 5 ms budget the repaired arm fails earlier, on `ETIMEDOUT`, and never reaches a parse. No arm parses an empty string now, and every way a child can produce nothing has a message naming the command line, the budget it missed, the child's measured cost on this box and what to do. The budgets come from that child measured under the loaded suite — 66-152 ms to exit, 66-112 ms to the control's first line — so `CHILD_BUDGET_MS` is 15 s where 2,000 ms was 13x the same measurement, while the hang stays a 2,000 ms window *after* the first line, which is what keeps the case at its old 2.2 s. Ten single-file runs and four `npm test` runs green at CPU 47-100% (60 files, 442 tests, exit 0, no watchdog error in any log), three red mutations watched to fire, and no assertion about the LRU totals changed. See `docs/learning/gate-proofs.md`, "The tile-memory case reports a stalled child as a stall, not as a JSON parse error".

- 2026-09-16: **`npm test` was red on this machine for two reasons, and neither was the product.** `test/visual-evidence.test.ts` costs 1.09 s alone and 10.06 s inside the loaded 59-file suite, where both cases died as `Test timed out in 5000ms`; a phase probe put the file's own work at 2,185 ms for the `beginVisualRun` scene digest (84 files, 214,114,015 bytes), 956 ms for the 44 frame decodes an accepted pass makes and 818 ms to write the fixture, none of it removable from a test file because the digest's mounts and the decode count live in `tools/visual/**`. Each case now names a 60 s ceiling derived from those measurements, and a third case checks the derivation sits between two and four times the work — the old 5000 ms default fails it by name. Separately, `npm test` exited 1 on runs where every test passed, with `Errors 1 error — [vitest-worker]: Timeout calling "onTaskUpdate"`: Vitest arms a 60,000 ms watchdog on every worker-to-runner RPC, and `test/pedestrian-overlap.test.ts` is one synchronous case running 60,000 ticks in 47.6-92.9 s, measured firing that watchdog in three of three default-concurrency runs, each time in the seconds after it finished. Bounding workers to half the cores was tried and **rejected on measurement** — it fired anyway at 16 workers, with peak node processes only falling from 100 to 48 — so the tick loop now turns the event loop every 2,000 ticks and `vitest.config.ts` is unchanged. Six runs on the final code revision: five green and exit 0, one red for an unrelated `tile-memory` child-process stall, and no watchdog firing in any of them. See [the detailed entry](detailed/2026-09-16_unit-gate-bounds.md).

- 2026-09-16: **The verdict capture was stopped on purpose at three of its ten hero frames, because a capture running while two `src/` landings are pending can never bind the final revision.** The vehicle-run gate rebased onto the current tip at `7329dec` and green 5 of 5, and the population tick lever with its behaviour-preservation proof, both arrived within a minute of each other; a certificate binds the bytes the revision builds, so running to certification at 22:30 would have bought frames the next run's own `hero.spec.ts` deletes as it starts. Its ledger is preserved under `artifacts/verdict-run5-aborted/` rather than its frames. The gate landing corrected three of its own numbers on the way in: 28,923 clean lane samples rather than the 28,594 its mutation's own run reports, zero samples exactly on the centreline rather than the 20,610 its header claimed (the p50/p95/p99 of 0 is `toFixed(4)` rounding, which is what made the false claim look corroborated), and a lane control that is not purely presentational because `placeVehicle` writes the pose `vehicleFootprint` reads back. The tick lever's first result is four behaviour-preserving repairs proved digest-identical over 3,600 ticks with a 4.6× cut to the gate row, and one rejection: the ring-ordered k-nearest neighbour query cuts bodies visited from 99.3 to 62.3 but leaves the avoidance row a wash at 7.67 against 7.59 ms while moving the crowd's queue from 656 to 833 — a change that buys no time and moves behaviour. The machine was at a measured 100% of 32 logical cores from a sibling `3d-maker` checkout even after our browser lane died, so the decisive arms are read with their load beside them.
- 2026-09-16: **The human set's repair was a paragraph a person checked with their eyes, and it is now a command with a red control per defect class.** `node tools/agents/verify-human-set.ts` (`npm run data:agents:set`) requires the 33 files the runbook names — 3 variants x 3 LODs x {glb, positions.f16, normals.f16} plus 3 manifests and 3 `-source.blend` — checks each one non-empty, parses every manifest, requires every file a manifest names to exist beside it at the digest it records, and exits non-zero naming what is missing, empty or disagreeing, so it can be the bake step's own check rather than a report. The variant names come from `HUMAN_ASSET_URLS` rather than being restated, and `--root` points it at a synthetic tree because `data/scene/agents` holds no human set and nothing may write into a served mount while the verdict capture runs. Twelve synthetic cases were run through the CLI: one green, and eleven reds covering a missing asset, an empty file, a manifest that is not JSON, a stem, a mismatch, a malformed digest, a dropped LOD, a missing blend, a path instead of a name, and a stray file. **Two of the eleven found defects in the check itself** — `bytes` read as a per-file length (a false red on every LOD of a correct bake) and a malformed digest read as "nothing recorded" (a false green on a corrupt manifest) — which is the argument for a control per class rather than one. `test/agent-human-set.test.ts` holds 14 cases, green in 6.8 s, and the CLI's two exit codes are asserted through a subprocess. See [the detailed entry](detailed/2026-09-16_human-set-check.md).

- 2026-09-16: **The verdict lane's hero spec committed the style row it was already on, and it cost a 25.7-minute capture.** Its style-return block pressed `End` to walk the listbox; the registry orders cartographic first and satellite second, and the style being left was satellite, so `End` landed on the selected row — and a closed control that moves onto its current option is not a move, so it correctly did nothing and `data-style-id` stayed `satellite` against an expected `cartographic`. The defect arrived with the gate-fixes rewrite, which flipped the capture order and the return direction with it, and whose author could only call the spec code. `Home` is the fix (`48c19c5`), verified before paying for another run by an 8-second probe that drives the real control and reports `rows: ["Cartographic","Satellite"]` and `Enter/Home/Enter -> cartographic`. The narrower lesson is the one that matters: the check made before the failed run was that hero's *first* assertions passed — a minute into a spec whose failure was twenty-five minutes in. Recorded in `docs/learning/gate-proofs.md`.

- 2026-09-16: **The 44-frame inspection signs off single-pixel detail it cannot see, and there is now an instrument for it.** `artifacts/frame-inspection/report.md` records its own bound — every 1280x720 frame came back as a **1066x600 preview** — so the aliasing and thin-geometry class was never inspected at 1:1, and round 30's D4 (the World style panel composited into every frame at x 1010..1275, y 4..140) is in no inspection at all. `node tools/inspect/crops.ts --frame artifacts/visual/hero/<frame>.png` decodes through `tools/visual/png.ts` and writes eight named 1:1 crops that **tile the frame exactly once each**, plus a manifest carrying each crop's rectangle, byte length, SHA-256 and frame digest, so a review binds to bytes and re-cutting from another frame strands it. The first tile check counted only *uncovered* pixels and **passed a region cut one pixel to the right**, because a later region tiled over the corruption before the comparison: coverage is now counted, every pixel exactly once, and that same mutation fails naming `pixel 691201, column 1, row 540`. The first region set was freehand and did not tile — rows 360..539 of the right column belonged to no region — so the rectangles are now derived from three columns and three bands. **The three frames the assignment named were deleted at 19:16:02 by a new capture run** (`runId cc8bc42d2368926e`, build `index-BFH5WFVP.js`), so the digests in `plan.md` are the previous run's; verification ran on the surviving sweep frames, and no `npm test`, `npm run build` or `npm run visual` ran under the capture. See [the detailed entry](detailed/2026-09-16_frame-crop-instrument.md).
- 2026-09-16: **The flythrough's route was aimed from a cell centre, 59 m from the pose the scoring tool picks, and a bearing half a turn out.** `node tools/flythrough/aim.ts --dump artifacts/flythrough2/reference-dump-t5400.json` scores 611,904 poses over 3,187 walking-way positions and reports three standable anchors; the route now stands on the highest-walker one, camera `(-386.198, -496.297)` with target `(-406.825, -480.469)` (26.0000 m, 127.50 degrees), against the **old** pair's 59.1 m and a camera 108.8 m away. The tool's azimuth is the direction a camera **faces** and the controls' is the direction **from target to camera**, so its "az 307" is 127.50 degrees flown, an inversion rather than a rounding difference. Deriving `CROWD_DISTANCE_M` from the tool's own two points then exposed a latent defect: the ascent ladder's zooms were computed against a hard-coded 24 m opening while the leg opens at 18.8 m, so the climb finished at **485.7 m instead of 620 m** and the flight never came home. `npx tsc --noEmit` exit 0, `npx vitest run test/flythrough-frames.test.ts` 12/12 green, no sequence check changed, route arithmetic re-derived by `artifacts/flythrough2/verify-route.ts`. **No frame of this route has ever been captured**, and the flight still waits on the verdict capture and `npm run data:agents`. See [the detailed entry](detailed/2026-09-16_flythrough-route-scored-pose.md).
- 2026-09-16: **The certificate never bound the harness that drew the frames, and the gate's new pointer half could not be told from a dead one.** Round 31's B4 and B3, fixed on `worker/gate-fixes` while the verdict capture owns the CPU and port 4319. `harnessTreeDigest` in `tools/visual/verify-output.ts` pins the lane's own files — `tools/visual`, the two lane configs, `vite.config.ts` and the preview mount — into `run.json` at `--begin` and re-derives them at `--end` before a frame is read; the pre-fix state is that check removed, red on **2 of 39** cases, and the pin and the presence check have their own red controls because every other case writes its own `run.json` and would stay green while real runs failed hours later. `hero.spec.ts` and `style-picker.spec.ts` now complete the pointer direction with a real press on the option row, assert `aria-expanded` on the control between the two presses, and keep the keyboard path as its own switch, because `move()` commits a *closed* control on an arrow key: a five-arm probe with the control's `click` registration deleted shows the old `click()`-then-arrow sequence **still passing**, the new opening assertion failing by name, and a press that opens the list without pressing a row committing nothing — 23.1 s, and the paired assertions cannot see a spec that swaps its own option press for a key press, which is a review property rather than a checked one. `src/ui/style-picker.ts`'s native-select measurement is re-cited to round 27, which carried it, and the built bundle is byte-identical across that edit. A Windows PowerShell `Get-Content`/`Set-Content` round-trip double-encoded `gate-proofs.md` in `4a7448d` — 240 characters — and it was repaired and re-verified against the file's parent blob in the commit that follows. See [the detailed entry](detailed/2026-09-16_harness-binding-and-pointer-half.md).

- 2026-09-16: **The dusk post chain's TAA could never engage, because the camera it waits for could never stop.** `artifacts/gate-timing/REPORT.md` recorded `taaAccumulating: false, taaSamples: 0` on all eight hero frames, and the post chain was not at fault: `src/app.ts` decides the camera holds still by comparing `camera.matrixWorld` against `1e-7`, and an `OrbitControls` camera on damping decays by 0.92 a frame and stops only when the arithmetic runs out of significand — at the shutter it was gliding at 8.96e-5 m a frame, **0.0014 of a pixel**, and it crossed the bar 200 frames after the last pointer input, 82 frames *after* the shutter. `src/render/controls-rest.ts` ends the glide when the residual could not move the picture by one pixel, leaving the `1e-7` bar untouched; raising that bar was refused because it makes the predicate agree with the damping instead of making the damping stop. At the shutter `false / 0` → `true / 32`, first accumulating frame 460 → 185. `test/camera-rest.test.ts` goes red on the mutation with a named message, and `tools/post-chain/motion.spec.ts` measures the anti-smear property from the PNG bytes: zero of 711 frames accumulated samples from more than one camera pose. Measured on hardware only — the SwiftShader arm is an argument, not a measurement. See [the detailed entry](detailed/2026-09-16_taa-could-never-engage.md).

- 2026-09-16: **`git status --porcelain` is not a measurement of unlanded work, and it read as the largest piece of it here.** Under `core.autocrlf=true` a stale stat cache makes git name files as modified whose content equals HEAD: `artifacts/network/milestone-20260911` reported **78 modified files** with an empty `git diff HEAD` for every one, because its branch was merely behind main. The instrument is `git diff HEAD`; the rule is in `docs/policies/local-rules.md`. That mattered because reclamation deletes on this reading: 42 stale worktrees were removed, and 33 of the 45 census entries were named dirty against only 32 real diffs, 139 files named against **101 that differed**. Everything unique was captured first — `artifacts/reclaim/preserved/`, 45 captures, 565 files, 9.57 MB, with all 32 patches verified to `git apply --check` against their own captured HEAD. `artifacts:size` went `{"totalMb":27015.1,"entries":99,"reparse":104}` → `{"totalMb":22537.7,"entries":97,"reparse":39}`. Three worktrees were destroyed before a do-not-touch order arrived and were **restored** from their captures with `git status` reproducing line for line; the fourth protected payload was never touched. Two capture defects would each have lost work silently: PowerShell `>` writes UTF-16LE, which `git apply` refused while the script logged success regardless, and `2>$null` on a native command swallows `git diff` entirely. See [the detailed entry](detailed/2026-09-16_worktree-reclaim.md) and `artifacts/reclaim/RECORD.md`.

- 2026-09-16: **The scene digest binds the served bytes now, and the check that carried the old bound was a clock race.** `f4e96f0` replaced `path\0size\0floor(mtimeMs)` with `path\0sha256(bytes)` over the 137 files and 371,228,426 bytes of `data/scene` + `data/network` **as the payload stood before that day's rebuild — the payload the running capture pins is 84 files and 214,114,015 bytes, digest `ddd21ee1b4feb7f7…`, because the human agent set is missing; see the entry below.** and three documents still told the old, false bound; they are corrected here, the history one with a dated marker rather than a silent rewrite. The red control reinstates the pre-`f4e96f0` algorithm verbatim with both arms' mtimes pinned so they agree, and the gate **issues its certificate while the served bytes have changed** — `All 44 fresh native-resolution frames survived`, for a city that moved under it — where the shipped digest refuses the same run with `The served scene data changed during capture`. The unit case that guards it was worse than "passing": with the defect back it went **red 4 of 24 runs** here against **1 of 12 recorded on `main`**, a race on whether the two writes shared a clock millisecond, and it is deterministic under the content digest. Cost measured: 425-447 ms per call over the real payload, about 0.9 s of the three-hour gate run. See [the detailed entry](detailed/2026-09-16_scene-digest-binds-bytes.md).

- 2026-09-16: **The vehicle spacing gate's proof existed only in its own header.** `test/vehicle-spacing.test.ts:27` and `5e8c266`'s message both say the mutation "is recorded in `docs/learning/gate-proofs.md`", and on `f4e96f0` that file had no such entry — the 42 unlanded lines were sitting in `artifacts/spacing/wt`. Reinstating the origin-to-origin separation now fires on this revision: `kei#15 into taxi#17, 0.120 m of oriented-box overlap at a -4.009 m origin gap` from tick 475, exit 1 in 2.96 s, and the entry is landed with its bound. The class check found the other three "recorded in `docs/…`" claims in `test/` and `src/` do hold. The defect register gets the interpenetration defect for the first time: 120 overlapping pairs among 71 active bodies, deepest longitudinal overlap 4.147 m at a 0.500 m origin gap, and a note that the same figure is written three ways in three places. The pedestrian half is still ungated on `main`. See [the detailed entry](detailed/2026-09-16_missing-gate-proofs.md).

- 2026-09-16: **Most of the crowd was never anywhere.** A rendered lane found that **1,700 to 1,990 of the 3,000 "active" pedestrians share a position with another body**, in piles of 60 to 108 at single coordinates, one frozen for 78 simulated seconds. Aiming a camera at the densest cell by body count photographed a street with five people, because the 217 bodies in the frustum occupied **19 distinct positions**; aiming by distinct positions instead found **239 distinct bodies, all moving**. The app's own counts agree with an offline probe to within two bodies at 51 of 52 frames, so this is the model rather than the instrument. That makes `rendered.pedestrians = 3000` a slot count that three lanes and the coordinator all reported as a body count: "the crowd covers 0 pixels" and "the crowd interpenetrates" are both true and their mechanism is plainer than crowding. A distinct-position count is being added beside the instance count, and the stacking is being traced — a pile of 60 to 108 at one coordinate is a placement or lifetime defect, not a crowd effect. See [the detailed entry](detailed/2026-09-16_the-crowd-was-never-anywhere.md).

- 2026-09-16: **The vehicle fleet reached 153 only by braking its own spacing gate.** The bus could never spawn, and the cause was a planner heuristic asking the wrong question — `canHalt` asked whether the *junction section* is longer than the body where the contract asks whether a footprint radius of *route* lies before the gate. Correcting it took drawn vehicles from a peak of 94 with zero buses to a peak of 153 with 45, mix unchanged, `crossed` 311 to 437. Applied on top of the spacing fix, `test/vehicle-spacing.test.ts` went **red: 5 overlapping pairs at tick 2775**, so it was reverted rather than landed with a red gate. The two are coupled: more bodies need more room than the same 213 controllers give, which is the contract's own sentence — its policy "is not evidence that 200 vehicles have acceptable throughput" — measured.

- 2026-09-16: **The visual gate's certificate could be issued for runs that never ran, five ways.** All five findings the instrument review accepted were still live on `main` at `0ae46df`, and the previous fix attempt (`artifacts/gate-hardening`, base 22 commits behind) had never been merged. Each is now a check watched to fire: the chain clears the previous `complete.json` in a `--reset` step *ahead* of the build (the old order left the last run's certificate on disk through a failed build, reproduced end to end rather than argued), the certificate requires three hardware lifecycle records naming their renderer for this run's own start, it binds a digest of the served `data/scene` + `data/network` payload (same build bytes, one scene file changed: `519a5e65f4a8…` → `89db4ac9f585…`), the lifecycle lane asserts an empty page-error list *and* reads the page's own `pagehide` cleanup record, and both capture specs plus the wrapper refuse a pixel lane whose renderer is not SwiftShader. The measurement that shaped finding 4 is the one to keep: on Chromium 153.0.8010.12 a throw inside a `pagehide` handler reaches neither `pageerror`, nor `console`, nor CDP `Runtime.exceptionThrown`/`Log.entryAdded`, while the same throw from a click handler is reported normally — so the listeners the review asked for would have closed the finding's letter and left its hazard open. See [the detailed entry](detailed/2026-09-16_gate-integrity.md) and `artifacts/gate-integrity/REPORT.md`.

- 2026-09-16: **The frame is not the problem, the simulation is.** At 1920x1080 with 3,000 pedestrians the GPU frame costs 0.900 ms p50 and 3.600 ms p95 against a 16.667 ms interval, and the population curve is flat from 1,000 to 4,000 walkers, so the renderer holds the target with about 13 ms of slack and the population is not the term. The whole-app interval is 54 to 98 ms with the simulation spending 33 to 69 ms of it, pinned at the loop's five-step catch-up cap: once the fixed step sits near the interval the loop multiplies it, so the step has to come down far enough that catch-up cannot engage. Two instruments lied on the way: `EXT_disjoint_timer_query_webgl2` is advertised and 3,906 of the 4,382 queries the app's own context resolved answered 0 ms (the bare context rejects the same query outright on all three ANGLE backends), and a first population curve measured an almost empty scene reported cost falling with population while all three gates passed, because the renderer draws what has spawned rather than what was requested. See [the detailed entry](detailed/2026-09-16_frame-time-is-not-the-frame.md).

- 2026-09-16: **The population now goes to the crossing's block.** Routing pedestrians portal-to-centre and terminating them there — the contract's own disjunction allows it, and the 513-933 m shape is admitted by the new 1,000 m `MAXIMUM_CENTRE_ROUTE_LENGTH_M`, not by the 700 m `MAXIMUM_ROUTE_LENGTH_M` that still bounds the exit shape where portal-centre-exit is 1,172 m — took pedestrians from 0 crossed and 0 completed to **1703 and 309** at the judged 360 s window, vehicles from 118/66 to **311/203**, and the nearest walker from 336.278 m to 176.768 m, with 28 actors inside 200 m where before there were none in 21,600 ticks. A terminus *on* the scramble is impossible under the passage rule because it is a single compound, so 31 m is the delivered graph's floor. See [the detailed entry](detailed/2026-09-16_centre-routes.md) and commit `e23c29f`.

- 2026-09-16: **ORCA was inventing collisions, and that was why walkers never moved.** `orcaHalfPlane`'s region test is true for every non-overlapping pair beyond radius times horizon, so it built a cut-off-circle constraint for pairs not on a collision course and `linearProgram`'s fallback zeroed the preferred velocity. A 116-case battery against a brute-force predictor: 84 artificial reductions before, **0** after. Pedestrian crossings went 0 to 813, permanently frozen walkers 1771 to 922. See commit `7c441d5`.

- 2026-09-16: **The lifecycle gate was green because a walker leaked first, and the guard that looked like the fix is inert.** `leakRetiredBody` never reached the boundary retirement path — the only path any vehicle in the fixture retires through — so 24 vehicle retirements with the mutation on behaved exactly like the control's 24. Guarding `retireSlot` there, as the reachability lane did, changes nothing: `JunctionAdmissions.retireBoundary` clears the active byte itself, so that call is already dead on the shipped path. The mutation now restores the presence the authority cleared and stops the boundary path re-planning the slot in the same phase, and a third case drops the pedestrians, requires the failure to name the retired vehicle by slot, and fails in 6.4 s when the seam is reverted. The halt capture landed with it: 147,420 vehicle requests against 28,035, slot 20 from 0 to 2,929, and 42 of 65 active vehicles that had never asked the authority down to 2 of 84 — at the cost of throughput, which fell from 14,067 grants and 54 completions to 9,884 and 33. See [the detailed entry](detailed/2026-09-16_gate-repair-and-halt-capture.md).

- 2026-09-16: **The city's signal lenses were a constant, and the fix is visible in five pixels.** `updateSignals` was only ever called with `[]` at construction, so every lens held the "no group active" colours while the simulation ran; at a pose 45 m east of the crossing, where `scramble:vehicle:3`'s head faces the camera, a 30 s capture across green, amber, clearance and the pedestrian stage went from **1 distinct digest in 16 frames** to **3**, with the only changed pixels in the head's lens row (445 and 351 pixels, x 670-708) and 0 pixels changed inside any single phase. Also fixed: `bridge.population()` published but undeclared (the harness cast is gone, `tsc` exit 0, and the compiler now rejects that class in both install paths), the LOD counts that reported one variant instead of the population (`{near: 1, medium: 1, far: 1}` where the population was `{3, 2, 1}`), and the ~19 simulated seconds the fixed step spent advancing a world whose renderer's assets had not arrived (attach tick 1,075 → 11, via a start gate on the population's clock only). Two findings from the same lane did not reproduce: the "pedestrian signal red during the green" is a vehicle head, because no control in this network is tagged `traffic_signals=pedestrian` and no pedestrian lens exists anywhere; and the fleet is not all placed on the boundary at tick 1 (18 active at attach). See [the detailed entry](detailed/2026-09-16_render-defects.md).

- 2026-09-15: **The gate is slow because its settle budget counts the wrong thing.** Same predicate, same pose, same build: 12 frames and 200 ms on the RTX 4090 against 58 frames and 134.6 s on SwiftShader, a 171x difference in one crossing pose — while the deadline is a fixed 300 s of wall clock and the software lane's per-frame cost moves 370x within a single run. That mismatch failed a real gate run after 1 h 46 m with "71 frames advanced, 0 quiet intervals", a budget failure its message could not distinguish from a renderer stall. Also measured: a screenshot is 71.7 s with 25 frames drawn inside it, a sweep capture costs 220 s median, and TAA never engaged before any shutter in the completed run, so the cheaper settled-frame path the post chain designs for is never reached. The answer is not to move the verdict off software but to stop paying the verdict price for iteration: a hardware lane whose frames are never evidence, a budget denominated in the frames the predicate consumes, and fewer verdict runs. See [the detailed entry](detailed/2026-09-15_gate-slow-defect.md).

- 2026-09-15: **The 53.63 GiB under `artifacts/` was duplication, not gate frames.** Nine worker worktrees carried a full copy of the regenerable `data/` (12.9 GiB) and the network observer evidence existed three to five times over — with every PNG inside each copy the same 312,318-byte image, 126 times per case. Trace retention was already failure-only and is 2.8% of the total, so nothing changed there. 28.4 GiB reclaimed, 25.19 GiB left, with `npm run artifacts:size` and a new rule under `## Gate artifacts live in artifacts/` so a copied input or an unmeasured junction is caught rather than assumed. See [the detailed entry](detailed/2026-09-15_artifacts-storage-cleanup.md).

- 2026-09-15: **The population cost probe measured the CPU side of the 3,000-pedestrian target for the first time, and the answer was not the frame rate.** Admission costs 2.409 ms median against the design's 0.5 ms assumed budget, and the same tick granted 27 of 2,582 requests, so 80.8% of the population queues; the pose path holds at 1.062 ms p95. Two vehicle routes have a mapped stop line nearer the gate than half the longest vehicle and no offset policy exists. The owner's commit-early rule now carries a testable transfer criterion — identical bundle bytes, unchanged scene data — which this commit used instead of a three-hour visual gate. See [the detailed entry](detailed/2026-09-15_population-cost-and-commit-eagerness.md).

- 2026-09-15: **The lifecycle check was split by renderer after attribution showed the fifteen-second bound was measuring SwiftShader, not the application.** Three arms put the application's own `pagehide` work at 2.1-4.3 ms while the replacement document's first script waits about 27.87 s; the pixel lane stays on SwiftShader and the lifecycle check now runs on the hardware renderer, repeated three times, failing by name on a software rasteriser. Both red controls are recorded, and the 2026-09-08 "36.5 seconds" claim is annotated as unverified. Native inspection of the 44 frames and the merge remain open. See [the detailed entry](detailed/2026-09-15_lifecycle-gate-split.md).

- 2026-09-13: **Independent review accepted bounded facade preparation and B0 witness repairs, while confirming the evaluator slowdown.** F17–F20 now have finite repair evidence; native facade/city acceptance was not supplied. The ABBA kernel comparison is 263.45 versus 103.10 ms, with correctness retained and no cause diagnosis. See [the detailed entry](detailed/2026-09-13_facade-witness-cost.md).

- 2026-09-13: Preserved separate evaluator, retirement, tiny witness and ABBA preparation reviews, including original F15/F16 failures, their focused repair and open B0 F17/F18/F19. Exact pose correspondence did not establish a speedup; tiny loss delivery did not establish city cleanup, and its failed outer report remains evidence. See [the detailed entry](detailed/2026-09-13_retirement-pose-evaluation.md).

- 2026-09-13: Preserved independent gait progression, sampled cadence and 3,000-agent cost reviews, plus the visibility observer's original F13/F14 failures and bounded CPU repair. Complete finite gait traces do not establish naturalness or affordable production poses; passing vehicle response records did not make occluded frames acceptable. See [the detailed entry](detailed/2026-09-13_gait-visibility.md).

- 2026-09-13: Review 15 records accepted R2 response-completion verification. A same-collector capacity comparison reproduced both large-body inspector evictions with default buffers and verified the exact three responses with expanded buffers. The 44-view graphics gate and eighteen vehicle executions remain incomplete; the walk initialization experiment remains rejected. See `detailed/2026-09-13_completion-evidence.md`.

- 2026-09-12: The first default software visual run exposed a still-damping camera baseline and a lifecycle setup timeout. Exact traces, six native frames and owned cleanup are preserved. Atomic fresh-frame observation, a strict pre-dropdown baseline and measured setup budgets pass focused CPU controls; the repaired full visual gate remains pending. See `detailed/2026-09-12_graphics-diagnostics.md`.

- 2026-09-12: Review 10 caught full-plugin UV validation bypass and paint jumping 7.55 m between stacked surfaces. Source-bound regressions, measured paint-only seam repair and eight native hero views now verify the bounded fixes; V4 vehicle-only setup and unchanged hardware placements are rebound. Canonical contracts moved to `docs/reference`. The isolated graphics candidate still awaits all five gates and 44 native frames. See `detailed/2026-09-12_graphics-diagnostics.md`.

- 2026-09-12: Review 8 independently reproduced the native-reviewed C5 pavement through the ordinary producer; root accepted that bounded recipe. Source-bound emission removed false wall glow, and native C9 review accepted authored daylight/dusk environment grading after C8 isolated added blue. A recycled-PID cleanup defect prompted retained-handle ownership checks; natural exit and fake-CIM evidence remain distinct. The isolated two-style candidate awaits reviewed vehicle setup, all five gates and 44 native frames. Source colour/detail and populated graphics remain open. See `detailed/2026-09-12_graphics-diagnostics.md`.

- 2026-09-11: The reviewed Phase 6 network milestone adds the separate OSM graph, shared deterministic admission, supported vehicle hulls and atomic boundary lifecycle. Reviews caught broken continuation, invented physical meaning, adjacent-controller deadlocks and terminal leases; source fixtures and mutation gates now cover them. Populated traffic, continuous steering/contact and graphics acceptance remain pending. See `detailed/2026-09-11_network-milestone.md`.

- 2026-09-11: Renderer recovery added shared-geometry Cartographic/Satellite controls, source-based street details, a bounded native frontage texture exception and a proved LRU disposal fix. Native review rejected several pavement constructions; source ownership now preserves duplicate aliases, uncovered LOD2 regions and distinct lower passages. Eight-frame candidates remain diagnostic; the final 44-frame run, populated integration and performance are pending. See `detailed/2026-09-11_renderer-recovery.md`.

- 2026-09-06: **Phases 2 and 3 — the placeholder is gone and real Shibuya renders, after three defects that each looked like a city** — `npm run data:scene` places MLIT's 67 3D Tiles into the world frame offline and builds PLATEAU's 2.5 m terrain TIN (183,188 triangles, 81,052 inside the box, relief 8.71 to 36.35 m, ground at the crossing 15.20 m) and its `tran` road surfaces (3,248 roads, 1,173 at LOD3, the rest draped, 79,244 triangles). All **1,740** buildings in the box are in the scene — 1,676 LOD2 and 64 LOD1, 532,315 triangles — and every one matched PLATEAU's own CityGML by `gml_id` with **zero** disagreements about height, level of detail or whether a height exists, and a **median 0.93 m** between where the pipeline placed it and where the CityGML's own roof-edge centroid puts it. **Loader: `3d-tiles-renderer`**, because the tileset already carries the LOD hierarchy and because 551.9 Mpx of texture (2,943 MB decoded) makes an eviction policy mandatory rather than optional; the ellipsoid it assumes is paid off offline, so no coordinate over about 2 km reaches the scene. **Three defects, none of which produced a blank frame or a red test.** glTF is Y-up where `CESIUM_RTC` is Z-up and nothing in the file says so — without the turn every building sat a median of **68 m** from its own batch table and the city rendered lying on its side; with the turn folded in and the tileset not declaring `gltfUpAxis: "z"` the library applied its own on top and it tilted again. A tile cache sized from the 137 MB on disk rather than the decoded bytes could not hold one 4096×4096 leaf, so the traversal deadlocked at the root and the app drew seventeen decimated buildings **while reporting itself loaded, idle and error-free** — and every pixel floor in the visual gate passed on it. And a road ring reaching past the terrain has nothing to drape onto, so PLATEAU's flat-at-zero LOD1 outlines dropped a sheet of asphalt to sea level past the edge of the world; 461 polygons now dropped. **Six of design.md's claims were wrong and are corrected in place**: the LOD1 buildings are in the same tileset, the sentinels read `null` not −9999, atlases go to 4096×4096 not 2048×2048, the count is 1,740 not 1,741 and its last digit is a property of the centroid rule, the Draco decode works, and the up-axis fact was missing entirely. One held exactly — `_zmin`/`_zmax` are orthometric, and the difference from the ellipsoidal geometry recovers the geoid undulation from the data alone at **36.786 m** against a published 36.877 m. **Two new gates, both made to go red** (`test/sentinel.test.ts` over real PLATEAU batch-table bytes, `test/placement.test.ts` over the ECEF and up-axis transforms), plus the visual gate's first check on the scene rather than the framebuffer, which caught two of the three defects above. Building textures are capped at 1024 px at load, which is what lets the whole box sit at leaf detail in 335 MB. All five gates green; all **eighteen** frames opened at 1280x720.
- 2026-09-06: **Phase 1 — the data is on disk, verified and attributed, and four plan items were wrong** — `npm run data:fetch` pulls the PLATEAU Shibuya-ku FY2025 CityGML (649,322,807 bytes, SHA-256 `f7437469…31d`, verified by length before hash), extracts the AOI members, runs Overpass (1,211,346 bytes, 7,915 records, `osm_base 2026-09-07T03:23:56Z`) and fetches the GSI cross-check tiles, all into a gitignored `data/`. Truncating the archive by 4,096 bytes made it say so and re-download. Four plan items were corrected before any data was touched: item 5 named the FY2020 Tokyo 23-ku bundle instead of Shibuya-ku FY2025; item 7's terrain source was wrong twice over, since PLATEAU ships its own 2.5 m TIN in the same archive and the right GSI layer would be the 1 m tile API; item 10 assumed a converter that has no Windows CLI; and **item 15 rested on a false premise** — parsed independently here, **1,678 of 1,741 AOI buildings (96.4%) are textured LOD2**, with a perfectly clean split, so procedural facades became the treatment for the untextured 3.6% and item 35 was appended for de-lighting the baked daylight out of the rest. Item 23 gained an ODbL 4.6 constraint: the OSM-derived graph stays in files of its own. **Conversion path decided empirically**: two `.b3dm` tiles from MLIT's own 3D Tiles build were parsed against the CityGML and win on per-building attributes (a 63-key batch table cross-checking exactly on Hikarie 173.6 m/34 and Shibuya Stream 171.3 m/35) and on addressability (`_BATCHID` plus a five-level hierarchy, 67 tiles over the AOI), so the untested Windows Rust build was not taken. **Two new gates, both made to go red**: `test/aoi.test.ts` caught a transposed EPSG:6677 axis pair, `test/elevation.test.ts` caught both a 36.877 m geoid slip and a Terrain-RGB misdecode. The attribution surface is in the running app and the blank-frame luminance spread it governs was re-measured at 2.20 against a floor of 6. All five gates green; all twelve frames looked at again at 1280x720.
- 2026-09-06: **the `app-id` gate was made to go red, and Phase 0 is closed out in the plan** — put the original two-part defect back (preview port to Vite's 4173 default, `reuseExistingServer` on) while aoe2's `vite preview` happened to be live on 4173, so this was the real collision and not a staged one. Playwright reused aoe2's server exactly as before, and the harness refused in 462 ms with `artifacts/visual/` empty, where Phase 0 got a ten-minute timeout. Four of the visual gate's failure paths are now proved red and three are not; `docs/learning/gate-proofs.md` has the mutation and the message. The wrong-app error now names the port, what it found there and what would satisfy it. `plan.md` checks off items 1 to 3 and records the contracts Phase 0 set — world frame, origin subtracted at load, scene and agent homes, `RenderLoop`'s `onFixedStep` and `onFrame` — plus a warning that frames are **not byte-reproducible**, measured again here as zero of twelve frames byte-identical across three runs of one build. All five gates green; all twelve frames looked at again at 1280x720.
- 2026-09-06: **the repo has code, and a visual gate that has been made to go red** — Phase 0 landed a Vite + three.js + TypeScript scaffold on Node 24, a Playwright sweep that drives OrbitControls with synthesised pointer and wheel events and writes 12 frames at 1280x720, and a filled-in `Gates` section. The sweep's first run photographed **a sibling repo's app**: `vite preview` defaults to 4173 in every repo in the fleet, aoe2 already held it, and `reuseExistingServer` attached to it happily — now fixed by port 4319, `reuseExistingServer: false`, and an `app-id` check before capture. Frame floors were guessed at 150 distinct colours and measured at 91 to 160, so they were reset from measurements. Three deliberate mutations proved the gate red, recorded in `docs/learning/gate-proofs.md`. No map data, no PLATEAU, no agents — Phases 1 to 10 are untouched.
- 2026-09-06: **maps joined the fleet and the Shibuya deliverable got a plan** — the canon landed in `AGENTS.md`, `docs/work/0_shibuya-1km/` was allocated, and its `plan.md` now carries the settled stack (three.js + Vite), the settled geometry sources (PLATEAU LOD2 + OpenStreetMap, with Google Photorealistic 3D Tiles rejected because its terms forbid bulk caching and derivative works), the 1 km AOI box, and 34 steps across 11 phases. No code yet — Phase 0 blocks everything, and three dependencies are open: pedestrian models, vehicle models, and target agent counts.
