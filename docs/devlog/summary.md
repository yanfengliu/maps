# Devlog Summary

- 2026-09-06: **the `app-id` gate was made to go red, and Phase 0 is closed out in the plan** — put the original two-part defect back (preview port to Vite's 4173 default, `reuseExistingServer` on) while aoe2's `vite preview` happened to be live on 4173, so this was the real collision and not a staged one. Playwright reused aoe2's server exactly as before, and the harness refused in 462 ms with `artifacts/visual/` empty, where Phase 0 got a ten-minute timeout. Four of the visual gate's failure paths are now proved red and three are not; `docs/learning/gate-proofs.md` has the mutation and the message. The wrong-app error now names the port, what it found there and what would satisfy it. `plan.md` checks off items 1 to 3 and records the contracts Phase 0 set — world frame, origin subtracted at load, scene and agent homes, `RenderLoop`'s `onFixedStep` and `onFrame` — plus a warning that frames are **not byte-reproducible**, measured again here as zero of twelve frames byte-identical across three runs of one build. All five gates green; all twelve frames looked at again at 1280x720.
- 2026-09-06: **the repo has code, and a visual gate that has been made to go red** — Phase 0 landed a Vite + three.js + TypeScript scaffold on Node 24, a Playwright sweep that drives OrbitControls with synthesised pointer and wheel events and writes 12 frames at 1280x720, and a filled-in `Gates` section. The sweep's first run photographed **a sibling repo's app**: `vite preview` defaults to 4173 in every repo in the fleet, aoe2 already held it, and `reuseExistingServer` attached to it happily — now fixed by port 4319, `reuseExistingServer: false`, and an `app-id` check before capture. Frame floors were guessed at 150 distinct colours and measured at 91 to 160, so they were reset from measurements. Three deliberate mutations proved the gate red, recorded in `docs/learning/gate-proofs.md`. No map data, no PLATEAU, no agents — Phases 1 to 10 are untouched.
- 2026-09-06: **maps joined the fleet and the Shibuya deliverable got a plan** — the canon landed in `AGENTS.md`, `docs/work/0_shibuya-1km/` was allocated, and its `plan.md` now carries the settled stack (three.js + Vite), the settled geometry sources (PLATEAU LOD2 + OpenStreetMap, with Google Photorealistic 3D Tiles rejected because its terms forbid bulk caching and derivative works), the 1 km AOI box, and 34 steps across 11 phases. No code yet — Phase 0 blocks everything, and three dependencies are open: pedestrian models, vehicle models, and target agent counts.
