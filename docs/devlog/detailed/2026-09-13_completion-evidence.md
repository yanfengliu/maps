# 2026-09-13 — completion evidence and experiment bounds

The result checker initially trusted response flags; its next repair still accepted missing cleanup arrays and contradictory runtime evidence. Independent reviews exposed both classes. The real old PowerShell writer serialized zero/one results as null or an object. The wrapper already returned nonzero for known live leftovers; the defect was the standalone checker's false completion claim. R2 now checks explicit arrays, complete typed source/runtime records, valid digests and the actual retained prefix of the one permitted Chromium debug-log append. Root and the independent reviewer each passed 44 response and 110 completion checks; the reviewer also passed six actual serializer controls. [Review 15](../../work/0_shibuya-1km/reviews/15_implementation.md) preserves the authored re-review and exact source target.

The capacity driver's cancellation repair separately passed independent review after the exact old function with deferred APIs started B while A's acquisition remained unresolved. The repaired driver blocks later work, closes late resources and seals copied evidence. One actual headless A/B then ran: with identical dedicated CDP collection and three ordinary responses, default parameters evicted both JSON bodies while expanded parameters verified all three lengths and digests. Both pages consumed the expected inputs. All 7,202 bound records stayed unchanged; fresh observed process and port closure was empty. This is a finite two-input measurement, not full-city collection or a reproduction of the older Playwright response-body API path. A rejected absolute output argument is retained as a pre-launch failure; no browser had started before its correction.

The original vehicle run retained seven native frames for one incoming-kei case before response collection failed; the other seventeen executions were not completed. Graphics produced eight hero and two return views before the unchanged 15-second navigation check failed, leaving 36 sweep views unrun. Its observation-only replay also failed navigation. Neither partial run is a whole gate; Review 11 remains open.

The walk initialization experiment makes each selected flight consistent but discrete duration selection still creates 32.4/48.6 mm placement jumps at 2 m/s across phase boundaries. Its single corpus completed 11 cases and 34 rows of a twelfth: 837 observed rows and 31 cases unrun. The ongoing prediction's 38.291045259 mm plus 3 mm exceeds the unchanged 35 mm cap, even though the retained current pose is valid. The initialization remains rejected. The retained erratum corrects the original production-240-Hz wording: 240 Hz describes the unintegrated pilot corpus; production defaults to 1/60 second. No production controller, threshold or original report was rewritten.
