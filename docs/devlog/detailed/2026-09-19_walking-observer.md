# Devlog — 2026-09-19

## Observe the existing walking calculation before coupling a foot controller

**Timestamp:** 2026-09-19 22:50–23:14 PDT, preparation and primary verification.

**Action:** Applied the exact five-file candidate accepted by Review 59. Four previously unmerged F26 files supply the pure path, source-bound support query, corridor helper and original 24-test file. The observer changes only `walking-path.ts` and adds fourteen observer tests. The complete import patch and the separate observer delta remain recoverable in ignored integration evidence. Production wiring, source selection and controller behavior are unchanged.

**Result:** `advanceWalking` can call one optional diagnostic observer after calculating its legacy result. Detached, frozen intervals expose move, unverified geometric turn and held/done/unspent remainders, with physical/source distances and relative tick times. A callback exception propagates; it is not an admission transaction. The path remains `productionReady: false`. The source is applied and its four primary gates plus exact visual transfer pass. Independent Review 62 and root accept the exact integration; delivery remains pending.

**Reasoning:** A final body pose cannot reconstruct an update that moved, turned and then became held. Recording those actual operations preserves the evidence needed for later causal foot checks. It does not make the existing current-tag surface query a production source policy or put raw OSM in the browser. The candidate is outside the observed browser import closure. A controller must still define zero-duration events, moving admission, handback and atomic rejection separately.

**Validation:** The author reports 38 focused tests, typecheck, exact retained-baseline replay over 1,255 advances and 1,416 intervals, and two discriminating observer mutations. Independent Review 59 ran 62 test executions (including replay of the same 24 original designs), typecheck, a 4,102-advance / 4,324-interval matrix and callback controls. Four deliberately corrupted records failed the review instrument; those are not four source mutations. These are finite analytic checks, not city, foot, visual or performance evidence. Fresh primary build, types, 84 files / 723 unit tests and high-threshold audit pass on Node 24.12.0; audit retains two moderate Vitest advisories without a dependency change. The rebuilt four-file dist, 117 served files, 24 harness files, 88-file browser closure, 44 original images and three lifecycle records match exactly, transferring 05ad/8d704 still-image evidence without a new capture. All 21 gate-target files stayed fixed. Four original gate processes were joined, with no matching primary tool process remaining in the read-only census.

**Code reviewer comments:** Review 62 independently verifies the exact integration, gate receipts and visual transfer, with no material new finding. Root accepts that bounded disposition. Review 59 found no new material observer defect. It confirms that the callback runs after the original calculation and cannot mutate it through the detached argument; unrelated closure side effects remain possible. Review 59's initial strict primary-byte comparison stopped on CRLF differences. This integration records both raw hashes and exact CRLF-to-LF content equality for all 24 dependencies, without changing any source bytes.

**Notes:** Exact Reviews 57–62 and their authored snapshots are retained together. Review 57 admits bounded observer work, not source assignment or whole-controller contact. Review 58's original run01 rejection is preserved. Root accepts run02 only as bounded repeated-accounting evidence: the native FINAL self-report is PARTIAL/2, its OS exit was not recorded because wrapper formatting failed with exit 1, and three survivor negatives remain unvalidated. Review 60 rejects the shoe observer for F34: nominal clipped vertices can make an uncertainty-aware hull falsely pass. `shoe_contact_repair` owns a separate v2. Root accepts Review 61 for one diagnostic by `native_survivor_execution`; root observed actual run03 tool/OS exit 2, three matched candidate PASS and three survivor EXPECTED_RED, separate baseline28 PARTIAL and completed cleanup. Independent native results review remains pending. This does not replace run02 or admit the primitive, pipe, launcher or capture. No shoe code, native source/binary or controller is imported by this milestone.
