# Pedestrian terminal clearance, 2026-09-19

The assumed safe terminal was not safe. A central route ended on `walk:664532522:1:0:ground0:r`, which has no junction ID but remains inside the scramble's original 39 m primitive. A read-only replay of the prevention candidate observed slots 95, 138 and 133 retire at ticks 37400, 38166 and 38208 with entered leases. `cancelPending` correctly refused those leases, after which the population removed the bodies and their plans anyway. Its next-generation refusal was the symptom, not the cause.

The candidate preserves the selected central prefix and appends the shortest complete directed sections needed for a full-body terminal outside every physical primitive. All three observed cases need the same 6.313157353948492 m successor. The planner tests an envelope containing all permitted right offsets for the actual body size, with a 0.1 mm allowance for float32 pose rounding in this local world. It keeps the original 1000 m and edge budgets. Body-size fits retain the prefix's original RNG choice, and only the latest fit per portal is cached.

The tick now observes the actual published position and yaw after integration, including at the terminal, before retirement. An occupied terminal keeps its body, plan, generation and authority, records why it is blocked, and allows the render loop to continue. The old pre-integration footprint was a separate error: it paired new route progress with old geometry. A focused test now compares every observed footprint with the live pose exactly.

The isolated 150/0/45000-tick run at seed 5970698 reports 27 completions, 70 releases, 150 active pedestrians and zero authority violations. All 27 observed retirements had physically clear footprints and no lease; all 27 reuses had no inherited lease. No terminal hold fired. Four initial central routes from one portal cannot fit the required tail inside the unchanged budget: 995.1548899314295 + 6.313157353948492 = 1001.4680472853779 m. They take the existing explicit exit fallback, reducing initial diagonal routes from 69 to 65. Of the other initial slots, 144 retain their original planned prefix and two remain unplanned in both arms.

Three mutations fail for the intended reason: removing the tail prevents the real tick's two required completions; bypassing the terminal guard removes the still-occupied body; restoring the cached footprint reports the preceding position. The final focused gate has 24 passing tests across terminal, lifecycle, tick-order and boundary files, and typecheck passes. The full gate, visual review and independent candidate review remain integration work. This is a lifecycle repair, not resolution of the crowd's stalls.

Pedestrian AOI egress remains a distinct gap. The public authority already supports bound pedestrian retirement after the body clears the boundary, and its eleven boundary tests pass, including both controlled walking exits. The population does not use that pedestrian binding/egress path. Generic `observe` cannot release a terminal conflict occurrence without an outside occurrence. This candidate does not claim otherwise or cancel that lease. Evidence, frozen hashes and the separate egress proposal are retained in `artifacts/lease-retirement/artifacts/lease-evidence/REPORT.md`.

Review 39 rejected the first candidate for F22: the central prefix cache and appended tail ignored the current caller's `viable` edge constraint. The repair checks the whole prefix and every tail successor, bypasses fitted-plan cache reads for constrained calls, and keeps a constrained fresh choice out of the unconstrained prefix cache. Eight new tests fail against the rejected source and pass after the repair, including a forbidden shortest tail with an allowed alternative and a callback whose allowed set changes between calls. The final focused result is 32 passing tests plus typecheck. No city run was repeated: the ordinary pedestrian caller supplies no callback, and this repair leaves that path's route choice and tick implementation unchanged. The exact revised target is in `artifacts/lease-retirement/artifacts/lease-evidence/REPORT-v2.md`; Review 39 remains the rejection of the preceding bytes.
