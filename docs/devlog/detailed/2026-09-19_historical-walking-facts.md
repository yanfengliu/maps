# Devlog — 2026-09-19

## Equal derived graphs do not recover the original source response

**Action:** Implemented an isolated offline producer and artifact-only CLI for the complete accepted walk roster. The artifact binds the reviewed historical replay, current filter-helper bytes and all ordered network payload fields. It separates the historical query cutoff, response watermark and raw hashes. It writes 1,738 mapped edge records across 561 ways plus two explicitly authored diagonal records. The browser receives no new loader or raw OSM import.

**Result:** The real artifact is 255,664 B, SHA-256 `e56babad1c2f687aac40313002e87f9e88b402839158031f2ad806f8f800c9b9`. V1, independent Review 52 and v2 produce the same bytes. Missing or mismatched bindings, duplicate/foreign rows and forbidden source classes refuse. Historical eligibility does not select a current mesh sheet, fill in an absent level or authorize step locomotion. The unrecovered original raw response and strict paint-source guard remain unchanged.

**Validation:** Review 52 independently checks the complete real roster, a literal eight-row oracle, six actual-byte source/payload controls and four observed-red mutations. This relies on independent Review 49 for the earlier replay premise; graph equality alone is not evidence of equality of every original source tag. No shared data, generator, source adoption or walking runtime changed.

## Safe refusal still needs an actionable error

**Action:** Repaired only the CLI and error tests after Review 52 found F29. Read and output filesystem failures now name the operation, bounded path and valid OS code, retain the underlying cause and state what satisfies the required read or destination. The producer and types remain byte-identical. Containment, link refusal, validation-before-write and exclusive creation retain their earlier semantics.

**Result:** The focused suite passes 67 cases and typecheck passes. All 17 new F29 cases fail against the exact frozen old CLI. Review 52's unchanged missing-input/write-denial probe now reports recovery in all three cases. Two probes are actual native missing reads; the EACCES case is injected, not an ACL experiment. Existing-output bytes remain intact.

**Code reviewer comments:** Review 52 rejected the original bare ENOENT messages despite safe refusal. Its report and exact original design/report targets remain preserved. Review 53 independently accepts the four-file component with F29 resolved; root accepts that scope. The author's initial spy setup failed on an immutable Node ESM namespace; a test-only forwarding mock corrected the instrument, with that failed attempt retained separately.

**Notes:** The original 60-file candidate and 17 watched inputs were verified unchanged. F29's scope is contextual synchronous I/O errors; neither this repair nor the separate source-sheet study grants source/support adoption. Integration and its own verification are separate from these focused results.

## Integration kept the browser-visible world unchanged

**Timestamp:** 2026-09-19 21:00–21:01 America/Los_Angeles, primary gates; no commit.

**Action:** Applied the exact four source files and five review/snapshot files under root's sole-writer authorization. Preserved the existing visual tree and complete dist as 109 SHA-verified files / 71,137,154 B before rebuilding. The plan's historical body and all 86 earlier review/snapshot files remain unchanged.

**Result:** Build, typecheck, 82 files / 685 tests and the required high-threshold audit all exit 0. Two moderate Vitest advisories remain; no dependency changed. All three certified build hashes, the separately retained source map, 117 served files, 24 harness files, 88 actual primary import-closure files, 44 original PNGs and three lifecycle records remain exact. Certificate `8d70420e07afbfa7` still belongs to `05ad1a4830e5876be03116e468199370504a6693`; this is a byte-bound transfer, not a fresh capture.

**Validation:** The first preservation instrument incorrectly expected only three total dist files; the existing source map made that assertion fail before gates. The corrected check retains all four while keeping the certificate's three-member check. A worktree-to-primary raw closure check then exposed CRLF-only checkout differences. The exact closure instrument ran on primary and bound its actual bytes; normalized equality explains the earlier representation mismatch without changing any source. Both stops are recorded as instrument assumptions, not code failures. The first restricted process scan lacked CIM access and proves nothing; the subsequent available read-only scan found no known gate IDs, direct descendants, task-marker processes or port-4319 listeners. No browser/server was launched.

**Notes:** Review 55 independently accepts the exact integration with no new material finding; root accepts that scope. Final staged byte checks and delivery remain pending. This check inherits the original agents-OFF still-image bound and establishes no new population, support, source adoption, motion or performance result.
