# 2026-09-19 — Adjacent frames and the comparison model

The old screenshot lane could neither capture adjacent frames nor reject its real 22-frame gaps. Its quadratic allowance hid a collapsed-detail positive above 14.14 frames. RAF canvas copies closed that capture gap on the RTX 4090 without changing the renderer. A released drag stopped too soon, then a held drag made OrbitControls ignore wheel input. Sustained input with release around wheel events produced twelve adjacent non-accumulating frames for both orbit and ascent.

The tighter capture did not close flicker acceptance. Every actual pair exceeded the unchanged 0.005 bound. Unchanged synthetic textures under fractional translation, rotation and two depth layers also exceeded it, proving that an integer translation classifier cannot attribute these residuals to the scene. The component records an unresolved scene verdict. Evidence and exact native frame hashes are in [the Q3 record](../../work/0_shibuya-1km/snapshots/q3-motion-capture-review43.md).

Review 42 found two public-contract failures outside those calibration tests. Stale copies failed the judge but still set the manifest's capture-valid bit. A rejected pointer release could also skip the active observer wait and hide the input error. The separate v2 repair assigns capture refusals where they are detected, feeds them into the real manifest, and waits for observation inside a nested finally while retaining all errors. Five regression controls fail on the exact rejected source; the repaired seven-file suite passes 54 cases and typecheck. Replaying the same 24 hash-bound PNGs through the emitted manifest changes no pair measurement or residual verdict. The original patch and rejected review remain evidence; independent re-review and integration gates are still owed.
