# Graphics diagnostics, 2026-09-12

Root subsequently accepted Candidate 9's authored environment grade after verifying all 80 input and eight frame hashes and opening all eight native 1280×720 views. Its manifest is `f0925ce615b2548a29de5a88fb2082661718cfb0c82ff3188bd51e99fdb8420a`; input manifest is `68066c0f0135206b5830157ddc68706630503c81e23e436d122c1ca046c2e7a2`. The promoted sky change retains 10% environment chroma above 10° solar elevation and 45% below −3°, with a smooth transition. Exposure, albedo, emission eligibility and texture budgets stayed fixed. The noon road control changed from `39,47,61` to `44,47,50`; the blank-wall control changed from `40,65,83` to `46,64,71`. Noon approach mean changed only from 72.046968 to 72.108997. This removes much of the added blue while retaining dusk colour; it does not recover measured surface albedo or solve inherited green/blur. Camera position differed by at most 2.151 mm, and 85 of 86 source samples matched. The same non-control `(541,235)` UV-seam sample changed sides; named wall/roof/road controls did not. Several transitive harness/config paths were omitted from the pre-run 80-input binding and recorded afterward, so they do not gain retroactive pre-run proof. Final candidate evidence must bind the complete closure.

The normalized retained-handle runner used for C9 had been read and accepted by root. All eleven retained identities exited naturally, with no termination fallback. One console process outside the configured executable allowlist was recorded but never targeted; a fresh query found it absent. Fresh inventory found no task browser/server and port 4319 closed. Six fake-CIM checks cover lexical Windows path normalization and reused/exited-parent rejection. These observations preserve the distinction between reviewed termination code and an exercised termination path.

Review 8, SHA-256 `58bdd2d4846a8523b22220ab538cbf62e5f5a6dd26731ff70e905d2966a198c9`, independently ran fresh terrain/roads followed by the ordinary pavement recipe in production order. The rebuilt mesh and source publication matched C5 exactly, all 64 frozen records matched, and 13 focused checks passed. Root read the report, inspected the independent replay and verified seven handoff records before accepting producer freeze `14d88e56b5c736249f45db014cac975e640ead698d4b5594337865345a2cad71`. This acceptance preserves the original strict-support failure and source/classification limits. It does not extend the ground oracle or admit all published pavement into the walking network.

An isolated graphics worktree on base `279908488a29bd729e78656dade259638b98a669` now holds the proposed two-style milestone. Its initial 101-file overlay explicitly excludes active human/core/steering work and keeps only the shared normal-material registration hook. Typecheck passes. Fresh setup still needs the accepted standalone vehicle-only build/verify closure and regenerated hardware binding; placeholder commands are preparation, not proof that setup works. Full default 44-frame capture and all five exact-candidate gates remain pending.

Later in this session, the ordinary pavement producer was extracted from the C5 prototype and wired into `data:pavements` and `data:scene`. An isolated replay reads pinned raw PLATEAU, terrain and roads, without recovery-artifact inputs, and reproduces the complete C5 file exactly. Its 12 focused source/overlay/precision/recipe tests and typecheck pass. The unchanged three seam rays pass, both material interior mutations fail, and the strict 6,757-point result remains failed at the same 17 boundary misses and three exterior-road observations. The first per-array diagnostic mistakenly hashed whole shared backing buffers; its preserved output is labelled, and corrected byteOffset/byteLength hashes independently match each actual array. Full-file equality was unaffected. Source review and publication remain separate from this reproduction evidence; see `docs/reference/pavement-recipe.md`.

Candidate 8 changed only the frozen environment bake to equal-channel linear luminance. Root verified all 80 input and eight frame hashes and inspected all eight native views. Manifest SHA-256 is `7e1a631df51433ede8bde8e244933cf76f8900d80e48fda87faa90e252d27fca`; input manifest is `2e7614df6b34d6cd09ded9be93c79e93b148e7e559ad30a29ba31b330bf658e4`. The noon road control changed from RGB `39,47,61` to `45,47,49`; a blank wall changed from `40,65,83` to `46,64,69`. Noon approach mean luminance was nearly constant, `72.046968` to `72.105241`. Full neutral removes added blue but also loses useful dusk colour and cannot remove inherited photographic green/darkness. It is not promoted. Live sky source remains coloured; `dist` contains this explicitly isolated diagnostic until the next build. Camera position differs by at most 4.306 mm, and 85 of 86 source samples match; one non-control pixel at `(541,235)` crosses a UV seam and must not be treated as an exact per-pixel albedo comparison. Named wall, roof and road controls are unaffected.

Candidate 8 used the independently reviewed retained-handle wrapper and exited naturally. Its six admitted identities all closed, fresh checks found no task browser/server and port 4319 was closed. No fallback termination ran, so this does not prove complete descendant/fallback coverage. The allowlist used `C:\WINDOWS\System32\cmd.exe`; a case-sensitive comparison could omit another spelling, but the omitted C8 inventory was not retained and that cause is not retrospectively proved. The next wrapper uses explicit lexical, case-insensitive comparison of configured absolute DOS paths and records omissions. Six fake-CIM tests pass, including case variants, different paths, relative/device-path negatives and the original recycled-parent controls. The C8 helper and original wrapper bytes remain preserved; no PID-based retry was made.

C5 composed the accepted source-owned hero overlay and proved lower-passage clipping with retained presentation elsewhere. Its published pavement is SHA-256 `5d2a915047b0e8719e624f14f600c81c0a6c6342c4e8ce5b383bdfa987bcbc24`, 12,974,860 bytes and 461,912 triangles. All three original oblique rays hit pavement ahead of asphalt. The final composition retains 17 tiny Float32-boundary misses and three exterior road-tolerance observations in the strict probe; those are explicitly classified within the independently reviewed precision bounds, not relabelled as a strict pass. Source coverage, lower-level identity and both true interior mutation failures remain in the retained ledger. The current scene still needs its bounded composition promoted into the ordinary reproducible pavement recipe; this capture is not proof that data:scene reproduces it.

Root opened all eight C5 hero frames and six additional oblique frames at 1280×720. The hero digest manifest is `ea96b08f286350d9bf5e5f7e7fe1128e2006de9760113e2a0aa72e416cc47cc2`; the oblique manifest is `44f230eaf2846e6e026962325f8dd09c83e4c44b62030a58182cdafcacd5474e`. The seam, rounded foliage, calmer windows and style dropdown were accepted as progress. Broad cyan-white facade patches at dusk, blue-green daytime appearance and a pole inside the carriageway remained material findings.

Two frozen one-variable arms separated the photographic emission term from environment illumination. Disabling only derived emission removed broad wall glow; authored signs remained, while the dusk approach mean fell from 58.88 to 35.60. Grayscaling only the PMREM input reduced blue shadow light with nearly unchanged noon approach mean, 72.50 to 72.56. Neither arm was promoted as a final treatment. The two hero manifests are `35b8ebb266616e1ebeaa58ea04f868ac55b87b46871ef0727d0330102b428838` and `30aebf24e1ad3ab8a60338c6fa09a943a7acb72bec520f198d74baf33a75df43` under the retained renderer recovery directory.

The mapped control at node 6219685053 had been rendered literally as a pole at logical road position `(27.935674,15.191631,4.031744)`. A pavement-only search first offered 84 of 119 placements, but that result omitted walking and vehicle body checks. Adding those checks reduced the current recipe to 23 placements and 96 explicitly unplaced records. This is a disclosed coverage limit. Source positions, graph paths and signal authority remain unchanged; the separate artifact labels every physical base and head as authored presentation.

The named pole's new base is `(28.621587,15.263378,-3.687843)` and its head is `(28.400325,19.863378,-1.197653)`. The nearest declared pedestrian-corridor gap is 0.727615 m. Ninety-three overlapping supported fleet poses put the highest body corner at 18.676580 m; the actual head bottom leaves 1.011798 m clearance. These are local samples, not a continuous route proof. The recipe uses each class's actual bounds and axle contacts, scale 1, ±0.05 m model wheel travel, 0.5 m path samples and a 0.25 m horizontal sampling margin. A pose with missing wheel support forbids placement instead of supplying a guessed head height. Layer selection stays near the route's declared level and does not take the highest surface at the same XY.

Candidate 6 captured eight controls-driven native views on the observed RTX 4090 D3D11 renderer in one minute. All eight were opened individually by the implementation owner and root reviewer, with every hash verified. Its manifest is `fe9a1ed3460bda5809d045cb3d2d9f90abfae4580b3a5df761a9c0362168435e`; frozen inputs had zero changes, and there were zero browser errors. East and low views show the authored pole on pavement with a short overhead arm. The source/build/data copies remain frozen under `artifacts/recovery-realism-20260908/candidate6-probe/`. Root accepted the bounded pole and seam result, while photometry remained rejected. All twelve tracked processes and preview port 4319 were confirmed gone after finally cleanup.

Read-only facade rays now record processed texture RGBA, UV and surface normal. Corresponding unmodified source atlas bytes were extracted for comparison. Seventy native-atlas samples reproduce the live RGB exactly after applying the current de-lighting operator. At the blank wall in approach pixel `(432,224)`, source RGB `87,105,119` becomes albedo `91,112,115` with emission mask `124/255`, while noon framebuffer RGB is `44,70,87`. The adjacent `(460,241)` sample is `89,107,122` to `92,113,119`, mask `136/255`, then `45,70,89`. The image already carries blue photographic illumination; the material/light stages add another visible colour contribution. This comparison does not establish a surveyed neutral reflectance. Bounded source-visible sign regions and explicit bright-wall negatives remain the next mask change.

The new loader hashes the exact road and pavement bytes already fetched and the canonical encoding of the loaded network, rejecting stale geometry, widths or control metadata. A fleet digest is also recorded; the future shared actor factory must compare it with the displayed vehicle manifest before spawning actors. City-only startup does not load the fleet solely for this assertion. Root found a malformed-record case that still threw a property error: the frozen validator reads `sourceId` from null. The repaired validator names the malformed input and `data:hardware` rebuild command; the old and repaired messages are retained in `hardware-malformed-control.json`, SHA-256 `1ae817d388ba34417aaf2602225d33ce06eada530a55f0f96d95055d418b0c75`. Eight focused hardware tests pass after the fix; these do not replace native or populated acceptance.

Candidate 7b replaces broad photographic emission with three authored sign interiors bound to exact source tile bytes, GML identity, UV polygon, plane and outward normal. Root independently inspected all eight native frames, verified their hashes and all 81 frozen input hashes, and accepted removal of false wall emission within this bound. Manifest SHA-256 is `0011861acf772e8692f5cf9aea0683b38b082321c770a87cbd76d717bdf67838`; input manifest SHA-256 is `61051ec0b619b7c58f9f06dea171564a3b1dcee7315bf6715af8235f8d1ebb07`, under `candidate7b-emission/`. The same controls-driven views preserve albedo, illumination, exposure and geometry; daytime darkness and blue-green colour remain unresolved. Three signs are sparse coverage, not complete nighttime Shibuya. Source anchors and a corrected provisional negative label are recorded in `docs/reference/facade-emission.md`.

The first Candidate 7 attempt failed in production startup: the new fetch hook called `new URL(url)` on the relative root tileset `/scene/buildings/tileset.json`. The trace recorded `TypeError: Failed to construct 'URL': Invalid URL` from `FacadeTexturePlugin.fetchData`; the harness then timed out with an active loop but no ready scene. This was a production hook defect, not a failed measurement instrument. A parsing-only base now handles relative requests while the original URL remains the fetch argument. The failed build, trace and cleanup remain in `candidate7-emission/`; root/non-selected passthrough and selected relative/absolute matching are covered by the six-test emission file.

Candidate 7b cleanup exposed a separate ownership failure. The PID-only ancestry tracker admitted system processes 8812/csrss, 4624/winlogon, 26020/fontdrvhost and 11712/dwm through historical parent PID 20264, later reused by this capture. All four were created at 08:13:28 local, while capture began at 16:22:18. The wrapper attempted Stop-Process and labelled the attempts `cleaned`; fresh inventory showed those processes still alive. They were foreign processes and were preserved, not retried. All fifteen actual capture node/browser processes were absent and port 4319 was closed. The original misleading cleanup JSON remains counterevidence. The replacement classifier requires exact executable/PID/creation identity, a current parent and ordered creation times. Five fake-CIM tests pass, including the old closure admitting all four system rows and younger children of exited/reused parents being refused. Proposed runner wiring retains actual process handles before admitting children, terminates through held handles only, distinguishes attempts from confirmed exits and has an overall deadline; live use remains subject to root review. Fake snapshots do not establish live cleanup acceptance.

## F8/F10 repair and final candidate closure

Review 10 exposed two defects that smaller tests missed: facade processing replaced original texture UV metadata before validation, and physical paint selected an unrelated upper pavement layer. F8 now validates the actual source map through the full plugin; its original six failing cases and repaired thirteen-case pass are preserved. F10 source-level selection and finite triangle checks resolve the 7.545902 m paint sheet. Initial strict paint omitted fourteen diagonal stripes, including central parts; measuring actual opposing road edges showed eleven 4.263–9.221 mm cracks. The approved paint-only seam repair restores those eleven and leaves three explicitly omitted endpoints. Root inspected all eight hardware hero views and accepted the central continuity within those framings; no support mesh or network bytes changed.

The accepted standalone V4 vehicle recipe built through ordinary `data:vehicles`, and `data:vehicles:verify` checked all twelve wheels. Rebinding hardware changed only its fleet digest to `5b29efc7977949db1611273c8fb294b3ab23a1a55c8f42316cad9b5309c94a01`; all 119 placement/unplaced records remain identical. The candidate excludes unready human/runtime and simulation files. Current supporting contracts moved to `docs/reference`; historical reports retain original targets, and the serialized network-method locator intentionally stays byte-stable.

The eight-view run bound 16,711 inputs before and after. Its raw comparison remains RED because the exact installed Chromium `debug.log` appended 660 bytes of ANGLE performance warnings. The prior 23,950-byte prefix matched and the other 16,710 records were unchanged. A separately reviewed binder retains the raw difference and classifies only an exact-path append/new log as runtime output; source, executable, prefix, truncation and unrelated-log mutations still fail. Thirteen retained process identities exited normally, and fresh checks found all fourteen observed IDs absent with port 4319 closed. This does not prove fallback termination. The next actual 44-frame run must use the final reviewed candidate; no full-gate pass is claimed here.
