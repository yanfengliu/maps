# 2026-09-11 — reviewed network milestone

The first graph interpreted turn-only labels at internal sections, broke degree-two road continuations, and gave lateral links incompatible conflict sections. Review 0 also caught movement corridors being treated as physical pavement, crossing paint and signal hardware. The repaired graph retains source-backed physical arrays separately, carries turn permissions until an actual choice, preserves explicit restrictions and validates parallel outside-conflict lane changes. Review 1 accepted that scope.

Vehicle bodies then exposed a failure absent from point-actor checks. A 4.6 m car on way 46770374 crossed a 0.231534 m controller gap; bus and opposing mixed cases on ways 87250220 and 1377702569 could hold one authority while waiting for another. Fixed-point grouping preserves original physical disks and retains route commitments through internal gaps, mapped controls and repeated route occurrences. One pass was insufficient: a union exposed another close pair. Review 3 accepted the interior flat-body repair but found boundary termination could retain an active lease forever.

F5 now binds actor, kind, stable slot, generation and complete route before activation or retirement. An incoming body receives admission before becoming active. A terminal actor remains active and committed until its complete supported hull lies outside the canonical projected AOI; retirement removes presence and lease synchronously after all validation. Six physical-only incoming tails need authority even though their semantic route starts outside that controller. Review 5 independently accepted the lifecycle, corrected walking-exit instrument and supported 3D body projection.

The earlier pedestrian graph census had used entrance IDs as exits; its six reported failures were reversed terminal conventions. Correct actual exits give 408 successful pedestrian passages and 3,444 vehicle passages. The separate true controlled-pedestrian terminal failure was still real and is gated. The original report and instrument remain preserved rather than rewritten.

The supported bus includes 10.565 m length, 3.28 m collision width and its actual 3.3245 m body height, plus ±0.05 m wheel travel. Its orientation-independent diameter is 11.5803634 m. The engineering limit rose from 11.1 m for flat rectangles to 11.6 m for supported 3D bounds, with 0.5 m stop gap and 12.5 m route threshold. Grouping changed 216 to 213 controllers; the scramble union grew from the previous largest 157.087 m bbox diagonal to 211.029 m, while keeping all 401 original disks, four sectors and 88/116-second empty timing. This does not establish acceptable queues or throughput with 200 vehicles.

The offline builder exposed a Node Buffer decoder bug: Buffer.slice retained its backing slab where Uint8Array.slice had copied. The old decoder returned a first position of 7.185598589700907e+22 instead of zero. The shared decoder now copies the byte view; ordinary browser arrays, nonzero-offset subarrays and Buffer pass one regression contract.

Delivery uses an isolated main-based worktree with exactly reviewed source overlays. It retains the baseline app and eighteen-view SwiftShader harness; the dirty two-style graphics/asset work stays separate. Build, typecheck, full unit, visual and high-severity audit are the five commit gates. Native frame inspection and their exact hashes are recorded in the milestone handoff before integration. No later-phase traffic/contact/graphics acceptance is inferred from this commit.
