# Devlog — 2026-09-19

## Vertex uncertainty changes which shoe points can carry a load

**Timestamp:** 2026-09-19 23:50–2026-09-20 00:03 PDT, preparation and primary verification.

**Action:** Applied the exact two-file finite-shoe observer accepted by Review 64, with exact Reviews 63/64 and their targets. The pure tool receives complete caller-bound shoe triangles, explicitly selected fixed upward support planes and an exact load query. It is not called by production code. Walking observations are already delivered as `86c08b2` and are not re-imported.

**Result:** F34's nominal clipped hull could pass when permitted shoe-vertex error removed its supporting contact. V2 constructs inset guaranteed-contact and expanded possible-contact regions. Passing needs a resolved inner hull and a separate displacement margin; loads between the guaranteed and possible decisions remain uncertain. Both original false passes now remain uncertain while the actual displaced/rounded negatives fail. Physical 15 mm contact and 5 mm drift thresholds are unchanged. The source is applied; four primary gates and exact visual transfer pass. Independent Review 66 and root accept the exact integration; delivery remains pending.

**Reasoning:** A nearly parallel intersection can move much farther than the vertex error. A margin applied only to a nominal hull does not account for changed contact membership. The inner/outer construction propagates the declared error through both signed normal bands, support edges and hull decisions. Point and segment possible hulls remain meaningful uncertainty. The existing double-precision and area guards are finite arithmetic conventions, not a directed-rounding proof.

**Validation:** Review 64 independently repeated 15 unit cases, focused types, 49 actual-source outcomes, the complete 369-observation boundary record and 56 additional API observations. Exact old source with the preserved regression file failed four cases and passed nine; all eight original cases stayed green. The source-controlled payload and denominators remained exact. Fresh primary build, types, 85 files / 738 tests and high-threshold audit pass on Node 24.12.0; two moderate Vitest advisories and the build chunk-size warning remain. Rebuilt four-file dist, 117 served files, 24 harness files, the unchanged 88-file browser closure, 44 images and three completed lifecycle records match exactly. This transfers original 05ad/8d704 agents-OFF still-image evidence without a new capture or certificate. All 109 retained/current visual/build files were rehashed without duplicating the archive. All eleven gate inputs held; the four gate children and runner exited, and the read-only census found no matching primary tool process.

**Code reviewer comments:** Review 60 found F34; its rejected bytes and counterexamples remain unchanged. Review 64 accepts only fixed selected upward height fields, an exact load query and declared per-vertex packing error. Uncertain collision geometry, omitted geometry and actual source membership remain caller obligations. Review 66 independently verifies the exact integration, four gate receipts and visual transfer with no material new finding. Root accepts that bounded disposition; delivery remains pending.

**Notes:** Review 63 and root accept the six matched F32 observations from run03, which has actual tool/OS exit 2 and separate baseline28 PARTIAL. Historical labels and run02's missing OS receipt remain intact. `native_survivor_execution` owns minimal pipe design only; this milestone imports no native source, binary or launcher. `causal_support_prefix` owns a separate conditional nine-case study. No source policy, real plant lifecycle, anatomy, force, continuous collision, gait, controller, city stance or natural-motion acceptance follows. No queued lesson is retired.
