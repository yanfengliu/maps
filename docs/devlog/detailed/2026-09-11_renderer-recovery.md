# Renderer recovery, 2026-09-08 to 2026-09-11

The inherited software visual gate passed while the city showed washed-out facades, floating bright signs and almost no readable street hierarchy. Its two specifications logged 22 captures, but the sweep erased the four new hero files. Eighteen new baseline images survive, and older hero images retain their original provenance. Specifications now own separate output directories; a final wrapper checks all 44 expected frames and unchanged build bytes. The complete integrated run is still pending.

The owner added Cartographic and Satellite styles through a generic World style dropdown. Both styles retain the same camera and loaded geometry, and capped photographic atlases stay cached during style changes. Native control tests exercise pointer focus and keyboard selection, preserve query parameters and compare complete camera poses. A separate fixture exercises a third registry entry. Candidate 4 passed eight hardware hero captures on the observed RTX 4090 D3D11 renderer; those frames remain diagnostic, with pavement continuity, foliage and facade appearance still under repair.

The first street-detail implementation confused movement topology with physical surfaces. Tactile guidance paths became wide curbed corridors across Hachiko Square; unmarked crossing ways acquired paint; inferred signal authorities became excessive poles. Review rejected the actual pixels. Physical source crossings, tactile extent and mapped control nodes now drive paint and hardware, and PLATEAU semantic surfaces own pavement. Candidate 4 removed the false corridors and pole forest. This does not establish final pavement continuity.

Navigation then hung during ordinary disposal. Removing disposers changed the symptom but was rejected as a repair. The actual `3d-tiles-renderer` 0.5.2 LRU stored fractional mipmap estimates; removal order left a positive 1.1102230246251565e-16 remainder after every item was gone. Rounding each existing tile estimate up to a whole byte fixes termination without changing upstream code or skipping cleanup. A bounded child reproduces the fractional hang, and the ordinary controls-driven dusk-to-noon sequence passed with all disposers restored. The gate proof records its exact bounds.

The initial texture policy capped every atlas at 1024. Fixed geographic priority first raised 17 near-crossing tiles to 2048; ray hits proved the actual hero facade received that cap. A frozen A/B comparison then changed only one observed source atlas from 2048 to its native 4096. Native inspection recovered TSUTAYA lettering and glazing edges enough to justify the extra 67,108,864 decoded bytes. The production selector uses final-leaf geographic bounds containing the observed frontage, not a tile filename. The source audit requires exactly one native tile and atlas. Its current full-source estimate is 561,075,583 texture bytes including mipmaps plus 73,324,734 geometry bytes. These are estimates, not measured whole-process GPU allocation or proof of the 3,000-person/200-vehicle performance target.

Supported original pavement vertices did not imply supported interiors. Candidate 3 showed asphalt through the plaza; adaptive face subdivision repaired those pixels and exposed a different oblique tear in candidate 4. An independent exact-overlay prototype passed 167,710 vertical probes while missing all three fixed oblique rays. Its naive risers closed those rays but produced 1.419 million triangles with internal walls. A different global X/Z weld closed the same rays by incorrectly joining levels 16.365 m apart. Both mechanisms remain rejected counterevidence.

The source audit then found duplicate LOD descriptions and 29,287 exact polygon aliases. Approved selection retains full source identity and original height, merges only exact full-XYZ same-level rings within one parent road, preserves every LOD3 surface, and subtracts only that parent's LOD3 coverage from LOD2. The full-source selection preserves 1,599.561189074 square metres of mixed-road fallback. The maximum per-triangle area residual is 1.2325358511589002e-9 square metres under the published double-precision bound. The new source-only publication does not alter terrain, roads or the rendered pavement. A thirteen-ring fixture pins the hero and both named extreme conflicts, including seven aliases and the original height triplets.

At the worst overlay conflict the published sidewalk is 14.806487 m while terrain is 19.603313 m. Source dates and a ground-section tag initially suggested a terrain disagreement, but independent GSI reads 18.98 m at 5 m resolution and 19.77 m at 1 m resolution. Exact PLATEAU bridge surfaces overlap the same point at 17.564967 m and 18.564964 m; OSM also identifies a lower road and elevated railway there. This supports a separate lower passage, not permission to lift the sidewalk through the bridge or lower the terrain. Full layer classification and support/edge construction remain open. Creation dates alone did not establish changed ground.

The post stack now has an adapter for registered animated normal materials, so the ordinary GTAO normal pass can use the same VAT deformation and alpha cutouts as agent rendering. Material arrays and scene overrides restore even when the draw throws. The unit proof covers this swap, while actual moving AO, depth, shadows, temporal AA and populated performance remain integration work. No final graphics acceptance, five-gate completion or code commit is claimed by this session.
