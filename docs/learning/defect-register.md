# Defect register

## 2026-09-16 — the lifecycle conservation gate's red path was unreachable, and it read green

Observed symptom (gate-repair lane): `test/population-lifecycle.test.ts` reported itself green on `main`, the case `goes red when a retired body is left present` included, and that green carried no evidence at all about the boundary retirement path. The case that exists to make `populationInvariants.leakRetiredBody` fire could not fire for a vehicle: the mutation guarded `retireSlot` in `finishVehicle` and in `retirePedestrian`, and the boundary retirement path in `lifecycle()` retired its body unguarded.

Investigation (`artifacts/gate-repair/REPORT.md`, all figures below are that report's): a census probe over the shipped population, unchanged except for the mutation switch, ran vehicles only — 40 of them, 1,200 warm-up ticks then 4,800, no pedestrians — and reported 24 boundary retirements in the window with the mutation on, identical to the control's 24, and no failure at any tick. `retiredInPlace` was 0 in every run taken, so `finishVehicle` never retires a vehicle at all. With the fixture's 60 pedestrians the mutation does fail, at tick 1330, on a walker, with a message that names no body — `71 of 72 slots agree between a present body and a planned route` — which is not the path the case is named for. The obvious repair is inert: guarding `retireSlot` on the boundary path changes nothing, because `JunctionAdmissions.retireBoundary` clears the slot's active byte itself (`src/network/admissions.ts:161`), so that call is already a no-op on the shipped path. The repair that works restores the presence the authority just cleared (`table.poses.vehicles.active[slot] = 1`) and stops the boundary path re-planning the retired slot in the same phase, because `lifecycle()` drains its plan queue in the tick it retires and re-planning healed the leak before any conservation reading could see it.

Root cause: a mutation keyed to the two retirement sites its author was reading rather than to the envelope this fixture's vehicles actually retire through, on a gate whose green could not be told apart from a mutation that never ran. The gate and the claim in its header were wrong together, which is the pair a reader is least able to separate.

Checked from now on: the third case in `test/population-lifecycle.test.ts`, `goes red on the boundary retirement path, which is the only one its vehicles retire through`, drops the pedestrians so the boundary envelope is the only vehicle retirement path left, requires the failure to name the retired vehicle by slot, and was watched to fire — with `main`'s seam restored it exits 1 with `the leak mutation retired a vehicle through the boundary envelope and nothing noticed: expected null not to be null` while the two cases above it stay green. The gate, the mutation and both messages landed on `main` in `27b5957`, off `86b5a70`; `artifacts/gate-repair/wt` is that lane's own copy. **This entry said the opposite until 2026-09-16** — "not merged to `main`", "on `main` this case does not exist and the defect is unfixed" — because `7c6bd95`, `27b5957`'s immediate child, copied the lane's record byte-identically including its worktree status line. On `main` at `04be817` the case is `test/population-lifecycle.test.ts:89-123`, the repaired seam is `src/agents/population/tick.ts:641-642,745,756,1423,1437`, and `npx vitest run test/population-lifecycle.test.ts` passes 3 tests in 2.3 s. The record is `docs/learning/gate-proofs.md` and `artifacts/gate-repair/REPORT.md`, SHA-256 `4FA2FF66328B3045D2EBA01EEBEC9EB1FCDBFFD1D4E7FA5303D7D433912B5EF7`. Bound: one delivered network at seed `0x5b1b0a`, 12 vehicles and no pedestrians, 1,200 warm-up ticks and 4,800 mutated, `retiredInPlace` 0 throughout; no fixture reaches the `finishVehicle` in-place path, so that half of the same guard stays code rather than evidence.

## 2026-09-16 — every traffic signal in the city showed a constant

Observed symptom (populated capture lane, finding 5.1): a signal head shows a lit red lens during the phase the simulation says is that approach's green, and 14 frames spanning the whole scramble pedestrian green are byte-identical, lens included. Anyone reading traffic behaviour off those frames is reading a constant.

Investigation: `StreetDetails.updateSignals` exists to colour lenses from `JunctionAdmissions.signalSnapshot()`, and the only call in the repository was `updateSignals([])` at construction (`src/scene/street-details.ts:225` in the base revision). Nothing ever passed live state. The app had no read-only accessor for the clock: `JunctionAdmissions.signalSnapshot()` existed and was unused. The lens cache also compared its key against `""`, so an empty state list never matched and every frame would have rewritten every lens in the city — which is why the call had to be made affordable before it could be made correct.

Root cause: missing wiring between a model that existed and the scene that renders it, in the frame path rather than the simulation. No test could see it: the model was correct and the lens colouring function was correct; nothing joined them.

Checked from now on: `test/signal-lenses.test.ts` gates the colour mapping (active group bright, amber for the preceding approach with red off, every other lens red, the same phase not rewritten), and the rendered check is `tools/render-defects/signal-frames.spec.ts` at a pose where a head faces the camera — 45 m east of the crossing at 6 m above the ground, where `scramble:vehicle:3` is 16 m away and face-on. The gate is the pair: the same capture against a tree with the wiring reverted produces one digest in sixteen frames and zero changed pixels across green, amber and red, and against the fix produces three digests and 445 changed pixels confined to the head's lens row. Bound: one junction, one pose, one build, hardware renderer, `?agents=1`. A population-free run still shows every lens red forever, because the signal clock only exists with a population; that is the reviewed 44-frame appearance and is deliberately unchanged.

## 2026-09-16 — the reported LOD counts were one variant's, not the population's

Observed symptom (finding 5.6): `population().rendered.near/medium/far` disagreed with `rendered.pedestrians` beside it. The lane's tables had to say "of the variant the bridge happens to report", and the near/medium instance caps are recorded as hypotheses whose judge is exactly these numbers.

Investigation: `HumanRenderer.drawn` was assigned inside the loop over human variants (`src/agents/render/humans.ts:179-181`), so each variant overwrote the previous one's counts and the reported numbers were whichever variant the loop finished on. There are three variants, so the reported number was about a third of the truth at each level, per variant rather than per level.

Root cause: an accumulator that was written as an assignment in a nested loop.

Checked from now on: `test/human-lod-counts.test.ts` drives the real `HumanRenderer.update` with synthetic pose buffers and level parts and asserts the counts are the population's, that they agree with the instance counts left on the level meshes, and — new and worth keeping — that the instance caps are per variant, so the population's near count can reach `variants × nearInstances`. Mutation: restoring the assignment fails all four cases with the defect's own numbers (`{near: 1, medium: 1, far: 1}` against a population of `{3, 2, 1}`).

## 2026-09-16 — the first ~19 simulated seconds of population were never drawn

Observed symptom (finding 5.4): the agent renderer attaches at population tick 1,189-1,238, while the fixed step has been advancing the population since the network arrived. Everything simulated in that window — the first vehicles placed on the AOI boundary among it — is never on screen.

Investigation: it is asset loading. `agents.attach` runs as soon as the network is in, and `createAgentRenderer` then loads three human variants (three LODs each, two VAT textures and a GLB per LOD) plus the fleet, about 40 MB, while the loop's fixed step steps the population at 60 Hz throughout. Measured attach ticks on 2026-09-16: 1,075 and 1,087 against a tree with the fix reverted, 6, 11 and 16 with it. The 200-vehicles-at-tick-1 part of the finding did not reproduce: `vehicles.active` was 18 at the attach tick.

Root cause: two clocks that started at different times — the population's, which began when its graph was loaded, and the picture's, which could not begin until its assets had arrived.

Checked from now on: `test/loop.test.ts` asserts `holdFixedSteps` stops the fixed step without stopping the frame, leaves `simulatedSeconds` at zero, and does not pay the held wall clock back as a burst on release (mutation: removing the accumulator guard fails with `expected 28 to be +0`). `tools/render-defects/signal-frames.spec.ts` records the attach tick and the active/drawn counts at that moment in every arm, so the number is re-measured by any capture rather than remembered. Bound: one machine, one build, hardware renderer; the attach tick is the evidence and no captured frame yet shows a vehicle appearing at the boundary.

## 2026-09-08 — navigation hangs while disposing a refined city

Observed symptom: the hero capture could render its dusk views, then navigation to noon stalled while leaving the page. Removing selected disposers changed the failure, but omitting cleanup was not an acceptable fix. The diagnosis isolated the actual `3d-tiles-renderer` LRU path: fractional mipmap estimates accumulate and subtract in different orders, leaving a positive remainder after every item is removed. The removal loop then has no item left to advance it.

Repair: wrap the existing tile byte estimator with `wholeByteAccounting`, taking a conservative integer ceiling per tile. Every ordinary disposer remains active. `test/tile-memory.test.ts` exercises the actual upstream zero-budget unload in a child process: the reintroduced fractional control times out after two seconds; repaired accounting removes all three items and returns zero. `tools/visual/lifecycle.spec.ts` drives both styles and crossing/approach camera poses through normal controls, then navigates to noon with a 15-second navigation bound. The repaired sequence passed in 36.5 seconds during candidate 2. The child proof covers the reordered fractional accounting class; the browser sequence covers that loaded navigation path, not every possible unload or device. Annotated 2026-09-15: that 36.5 s run recorded neither its rasteriser nor the scene it drew, and no raw record of it was found, so it cannot be read as evidence that this check passes on SwiftShader today; see the 2026-09-15 entry below.

## 2026-09-08 — movement paths became false physical streets

Review of candidate 1 found an X-shaped raised sidewalk through the crossing, repeated paint and a forest of inferred poles. Movement graph edges were being treated as physical pavement and traffic-hardware authority. Source tags instead identified narrow tactile paths and explicitly unmarked crossings. Rendering now consumes separate source physical records: zebra only where marked, tactile strips only where continuous paving is sourced, and poles at mapped control nodes. PLATEAU semantic pavement is a separate source. `test/realism.test.ts` checks unmarked/unknown crossing exclusion, no inferred sidewalk ribbon and full-width supported paint. Native candidate 4 removed the false corridors and pole forest. Physical pavement continuity remains open below.

## 2026-09-08 — supported pavement vertices hid interior asphalt and oblique cracks

Native candidate 3 showed asphalt cutting through Hachiko paving although its original vertices were above the ground. Exact camera-ray replay identified road triangles ahead of the paving at two recorded pixels. Independent midpoint-based subdivision corrected those rays but candidate 4 exposed a second narrow tear. At two of the new rays the modified pavement had no intersection at all: independent face subdivision and lift left shared-edge gaps. An independent exact-overlay experiment passed 167,710 vertical support probes while still missing the three oblique rays, proving that vertical support alone is insufficient. A global X/Z weld closed those rays by incorrectly joining stacked levels up to 16.365 m apart and was rejected.

Status: unresolved. The required check now includes connected oblique edges, interior terrain ridges, transverse paint, source layer transitions, stacked levels kept separate and source coverage. `test/semantic-streets.test.ts` currently gates only semantic selection and the interior-ridge class; it does not establish continuity. Source ownership and competing construction evidence remain retained under the active renderer recovery handoff. No pavement acceptance or lesson retirement is claimed.

## 2026-09-08 — one visual specification erased another specification's evidence

The baseline run logged four hero plus eighteen sweep captures, but the sweep removed the shared output directory. Only eighteen new baseline images remained; older saved hero images are separate older evidence. Each specification now removes only its own directory. `tools/visual/verify-output.ts` checks all 44 expected images after the complete run and binds their timestamps, dimensions, hashes and unchanged build bytes. `test/visual-evidence.test.ts` proves the wrapper rejects a missing sibling image, altered image bytes, stale manifest and changed build. It does not judge appearance; final integrated capture and inspection remain pending.


## 2026-09-11 — network routes and physical facts disagreed with source meaning

Independent review found internal and cross-way continuations treated as forbidden turns, lateral links inside conflicts, and movement corridors misread as painted crossings, tactile pavement and physical signal heads. These were implementation errors, not source-unreachable routes. The repaired source graph and separate physical records preserve IDs, restrictions, mapped marks and unknowns. `test/network-review.test.ts` and source fixtures cover both directions, actual branches, tactile/shared nodes and stop metadata. Reviews 0 and 1 record the rejected and accepted scope.

## 2026-09-11 — adjacent controllers and AOI terminals retained deadlocked leases

Independent controller traces showed car/bus bodies bridging close authorities, commitments released in internal physical gaps, and complete legal AOI routes rejected or permanently occupied at their terminal point. Fixed-point compound grouping, route-occurrence commitments, exact body clearance and bound-slot activation/retirement now cover these classes. `test/network-compound.test.ts`, `test/network-boundaries.test.ts` and the full-source admission/boundary tools include actual source cases and deliberate regression failures. Reviews 3 and 5 distinguish accepted controller/lifecycle bounds from unimplemented continuous traffic and contact dynamics.

## 2026-09-11 — valid Node Buffer decoded header bytes as mesh positions

The offline graph builder exercised the same mesh decoder with Node Buffer and received a position of 7.185598589700907e+22 instead of zero. Buffer.slice shared the backing storage where browser Uint8Array.slice copied. `test/network-mesh.test.ts` covers ordinary bytes, nonzero offsets and Buffer; the copied byte view fixes the shared decoder rather than only its offline caller. The failing old decoder remains documented in the network contract and gate proof.

## 2026-09-12 — bright photographic walls became light emitters

Native C5 dusk approach views showed broad white facade and roof-edge panels glowing cyan-white. The photo mask treated brightness and saturation as emission authority, so pale concrete and cladding qualified along with signs. A frozen zero-derived-emission arm removed the wall glow while authored signs remained; its approach mean fell from 58.88 to 35.60, so that arm is diagnostic, not the final lighting treatment. A separate neutral-environment arm reduced daylight blue cast with almost unchanged mean luminance, isolating another colour contribution. Candidate 7b restricts photographic emission to three source-bound panels and zero elsewhere. Root inspected all eight native views and verified their hashes plus 81 frozen inputs, accepting the bounded false-wall-emission fix. `test/facade-emission.test.ts` covers four positive source observations, five blank-wall/roof/fascia negatives, wrong atlas/LOD identity, displaced planes, reversed normals and shader eligibility wiring. Broader nighttime illumination, source colour and whole-scene graphics remain open; this is not final lighting acceptance.

## 2026-09-12 — logical traffic-control nodes became poles in the carriageway

C5 east and low native views exposed a pole at OSM control node 6219685053 inside the road. A mapped traffic-control node locates logical control, not necessarily its physical pole or head. The new separate `control-hardware.json` recipe preserves every source node and records authored pavement bases, head positions and unplaced reasons. It currently accounts for 119 records with 23 placements and 96 unplaced cases after discrete footing, walking-corridor and locally supported fleet-body checks. `test/control-hardware.test.ts` covers distinct source/render positions, body height on slopes, absent wheel support, stacked layers, route obstructions, stale input digests and actual instance placement. Root inspected all eight C6 native views and accepted the observed pavement footing; continuous populated clearance and displayed-fleet digest binding remain pending.

## 2026-09-12 — good sole contact hid a mismatched full-body reference pose

[Review 6](../work/0_shibuya-1km/reviews/6_implementation.md) accepted the F6 finding: bind-joint corrections were added to already-posed idle VAT. It also accepted F7 after native profiles showed crouch and static upper-body motion. The 378-case flat-contact result remains within its 3.583476 mm drift and 9.132385 mm stance-height bounds; those observations do not establish whole-body reference fidelity. [Review 7](../work/0_shibuya-1km/reviews/7_implementation.md) records the completed commuter-male/near F6 proof: evaluated reference matrices and full source skin influences correct the mismatch and top-four truncation. Root verified 163 handoff digests and inspected selected native pairs. This is a bounded diagnostic, not a production repair. The future production gate must compare independently evaluated full drawn geometry with its matching palette, then check identity, controlled joint perturbation and a mismatched-reference negative; a foot-only or self-subtracting identity check is insufficient. F7 naturalness remains rejected.

## 2026-09-12 — recycled parent PID classified Windows system processes as capture-owned

Candidate 7b's PID-only descendant closure followed a historical parent PID 20264 into four system processes created eight hours before capture. The wrapper attempted cleanup against csrss/winlogon/fontdrvhost/dwm and incorrectly labelled attempts as `cleaned`; fresh inventory proved all four still alive. They were preserved and no retry was made. All actual capture node/browser processes were gone and preview port 4319 was closed. `test/process-ownership.test.ts` replays the actual older rows and a younger reused/exited-parent class: the old closure admits all four system rows, while the new exact-identity/current-parent classifier rejects them. Root reviewed the retained-handle runner: a new child needs a matching live pinned parent, exact creation/name/configured executable identity is rechecked while handles are retained, and fallback termination acts through those handles. Cleanup attempts, confirmed exits, remaining processes and omitted identities are separate outcomes. Six fake-CIM checks pass. C9 observed eleven retained identities exit naturally and one out-of-allowlist console process disappear without intervention; fresh checks found no task browser/server and port 4319 closed. No fallback termination ran, so natural cleanup does not prove that path. The original misleading JSON remains counterevidence.

## 2026-09-12 — F8 replacement textures hid unsupported original UV mapping

Review 10 ran the real `FacadeTexturePlugin.processTileModel` over the pinned data488 tile and found that replacement DataTexture defaults erased original channel, flip and UV-transform evidence before emission eligibility checked it. The repaired candidate validates the original texture first, including a manually managed matrix. The complete-plugin test keeps the supported real tile positive and rejects channel 1, flipY, offset, rotation, repeat and nonidentity manual matrix. The original plugin failed six cases while seven passed; repaired source passes all thirteen. Direct helper tests alone had missed the ordering. This is bounded source-panel eligibility, not complete facade emission coverage.

## 2026-09-12 — F10 crossing paint jumped between stacked road levels

Review 10 reproduced a real paint triangle at OSM way 1419311959 spanning 7.545902 m vertically over about 0.56 m horizontally. The source path remains near 21.6 m, while the old highest-X/Z callback chose pavement at 28.9 m. The candidate now chooses existing road/pavement within 0.5 m of source elevation, rejects ambiguous layers and never falls back blindly to terrain. Finite 0.25 m subdivision checks edges and triangle midpoints/centroids; invalid whole parts are explicitly omitted. Eleven central hero misses were measured as 4.263–9.221 mm cracks between opposing actual road edges, not floating-point error. A separate paint-only bridge admits at most 10 mm gaps with at most 1 mm edge-height disagreement. Source geometry and authoritative support are unchanged. `test/paint-support.test.ts` gates the original actual crossing, all generated zebra/tactile triangles, named seams and adverse holes/layers/boundaries. Root accepted all eight native hero views within their stated coverage; the final 44-view gate remains pending.

## 2026-09-12 — malformed hardware prerequisites escaped useful validation

A null hardware record threw TypeError before the validator could name `data:hardware`; malformed fleet prerequisite objects likewise needed validation before property access. The candidate validates complete record/object and vehicle-manifest classes first and names the ordinary `data:vehicles` preparation command. `test/control-hardware.test.ts` covers null, scalar, array and missing-field inputs alongside stale digests. The accepted V4 rebind changes only the fleet input digest, preserving all 119 authored placement/unplaced records. Future populated startup must still compare its actual displayed fleet manifest and scale against this clearance input.

## 2026-09-12 — camera preservation compared a still-damping baseline

The first default SwiftShader run failed a style-preservation assertion after camera Y moved 12.827539615 mm between frames 373 and 379, beyond the unchanged 5 mm bound. Pre-click observations were already drifting. The old OrbitDriver read status and camera in separate browser tasks, counted repeated reads of one frame as quiet intervals, and used a radius-normalized threshold that did not establish millimetre stability. A replay through unchanged installed OrbitControls, using the recorded final pointer drag and a schedule calibrated only to the pre-click observation, reproduced the held-out six-frame displacement within 6.1 nm; absolute pre-click Y differed 88.1 micrometres because prior residual and event/update interleaving were unrecorded. This is causal CPU evidence, not a browser pass.

The repair reads epoch, status and camera atomically and counts only fresh-frame intervals. Ordinary capture settling keeps its angular/relative scale, including observed translation. Immediately before each forward and reverse dropdown comparison, a separate 80 micrometre world-path bound must hold for three fresh intervals and twelve advanced frames. The actual camera rig's enabled 0.08 damping is checked; in the pinned no-input update path this leaves less than 1 mm of residual travel. The independent post-switch 5 mm checks are unchanged. Tests cover duplicate frames, translation, radius, inconsistent pose metadata, epoch/counter regressions, real upstream damping and measured stall/total time. Actual repaired SwiftShader flow remains pending.

The lifecycle test independently exhausted its old 120 s total budget during setup: the first ready:true observation was frame 22 about 4.48 s after navigation, initial refinement ended about 95.30 s after navigation, and only two of twenty-one first-zoom wheel calls completed. It never reached the 15 s navigation assertion. Named finite setup stages now separate refinement and input time from that unchanged navigation bound. Longer software capture budgets are verification costs, not a hardware performance claim.

## 2026-09-15 - navigating away from the refined city stalls about 28-30 s on a software rasteriser

Observed symptom: on a machine drawing with SwiftShader, leaving the refined city for another URL freezes the page for roughly half a minute before the replacement document starts. The 15 s navigation bound in `tools/visual/lifecycle.spec.ts` fails, and the user-visible effect is the same stall.

Investigation: an init script timed the application's own `pagehide` listener on the absolute page clock while a stage localisation and three probe arms measured where the interval sits. The application's cleanup (`picker.dispose()` plus `app.dispose()`) spans 2.4 ms with no context loss, 4.3 ms of app work when `WEBGL_lose_context.loseContext()` is forced inside `pagehide` (that handler then spans 30,453 ms), and 2.1 ms on the hardware arm. The replacement document's own document-start script does not run until +27.87 s, and the stage localisation showed the wait failing at 15,016 ms even with `waitUntil: "commit"`. On the hardware renderer the same unchanged check navigates in 32-110 ms.

Root cause: Chromium's destruction of the outgoing page's WebGL resources on the renderer main thread under SwiftShader. The mechanism - deleting resources versus draining in-flight work - is not separated by this evidence, and the cost is not shown to be independent of the scene's resource count (the 2026-09-13 trace of the same failure recorded 65 loaded tiles, `cachedBytes` 630,155,033, `gpuBytes` 317,190,172). The application's own cleanup is excluded as the cause; it is milliseconds in every arm.

How it is checked from now on: the lifecycle check runs on the hardware renderer the deliverable targets (`playwright.lifecycle.config.ts`, part of `npm run visual`), records the unmasked renderer string each run, and fails by name if that string names a software rasteriser. A machine with no usable GPU reports the check unavailable and red - never a skip and never a pass. The software baseline stays reproducible as disclosed counterevidence through the ignored diagnostic config and the preserved trace, and this bound must be re-measured when the scene changes materially.
