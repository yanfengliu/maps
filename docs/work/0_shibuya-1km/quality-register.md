# Quality work register — the Shibuya 1km deliverable

Status: active, 2026-09-17. Owner: the lead (coordinator). This register is the prioritized list the quality goal asks for: what is fixed, what is prescribed and why it waits, and what is rejected. Its evidence home is the plan's dated status block; the audit that produced the ranking and the file:line verification is `artifacts/quality-audit/register.md` (ignored), summarized here so a reader of `main` sees it.

The organizing rule is the certificate: the 44 certified frames and their native inspection are the deliverable's headline evidence, so a change that moves `dist/index.html`, the bundle hashes, or `data/scene` strands that evidence and must be batched into a single re-capture rather than landed alone. Phase A is everything that does not; Phase B is the one batch that does.

## Fixed, with evidence

| Item | Where | Evidence |
| --- | --- | --- |
| The flythrough's sequence judge counted pairs that did not exist, had no concept of a planned hold, and read a dark facade as blank | `f06aaf8` | 7 mutations watched red; 3 gate-proofs entries; the run then failed honestly on the repaired check |
| The approach leg's swing could not converge (7.7231 rad commanded against a 0.3491 rad/step cap), so the crowd leg opened on a hill face | `39ca459` | `test/flythrough-plan.test.ts` watched red; the passing run's `crowd-000` at azimuth 2.2250 against the scored 2.2253, 3.64 m clearance |
| The review's six findings and the record corrections it caught | `f160fe9` | each verified against the real bytes; the structure metric's dark-surface flaw reproduced with a two-pixel-block dither, `approach-005` unchanged at 17.4% |
| The session's two independent reviews were only in ignored artifacts | `6929b2a` | tracked as [round 32](reviews/32_implementation.md) and [round 33](reviews/33_implementation.md) with provenance and dispositions |
| Three record claims the audit falsified: guardrails are placed; TAA cannot accumulate while moving; the certified payload is not agent-free | `368f92c` | `vegetation.ts` builds four `guard_rail` ways 364–460 m out; `src/app.ts` feeds `cameraStill` into `post.setStill`; `data/scene/agents` holds the four vehicle assets in the certified payload |
| The clearance check read only the instant of the shot, so a path dipping to 0.194 m between captures passed | `999a15b` | `judgeSequence` now asserts the floor over every moment the record keeps (captured clearance plus the stand's own two heights) and fails a frame that carries none; watched red; 453 tests green. **It is not a free win**: the archived run does not pass it — `approach-009` stood the camera 0.194 m above ground and `approach-010` 1.078 m — so the check landed with the route defect it exposes recorded and the sequencing repair below in flight. The floor was not lowered |

## Phase A — in flight or prescribed, none of it recapture-bound

| Item | Change | Why it matters | State |
| --- | --- | --- | --- |
| The aim scorer scored bodies at a constant 15.2 + 0.9 m, ignoring terrain, and never checked standability | `tools/flythrough/aim.ts` (+ `plan.ts` re-plan, tests) | This is why the passing flight's crowd leg points 34.7° down at pavement: the anchor was chosen by a scorer that believed a 27.5 m knot stood at 16 m. Low-ground anchors do exist — `aim.json`'s own best-overall pose sits on 14.97 m ground | in flight |
| The clearance check reads only captured instants, so a path dipping to 0.194 m between frames passes | `tools/flythrough/{frames.ts,flythrough.spec.ts}` + tests | The check's claim should be "no recorded moment of this leg put the camera below the floor", asserted over the record's own stand readings | **landed** (`999a15b`) |
| The approach leg's vertical control stands the camera 0.19 m above ground mid-step: between frames 008 and 009 the ground rises 4.85 m while the camera descends, the stand corrects once per step, and `holdClearance` only nudges before the capture | `tools/flythrough/driver.ts` (+ the approach leg's stand parameters if needed) | The frames are fine but the path is not, and the new check is right to fail it; the leg's own floor is 2 m | **landed** (`3491a39`); the worst recorded moment is 2.908 m |
| No lane has captured consecutive rendered frames, so the dusk criterion's flicker/crawl half is judged from pairs 285–2,202 ms apart | a capture mode judged on the non-accumulating path (the app cannot accumulate TAA while moving) | The criterion's temporal clause must be judged on the path that exists; a video or consecutive-frame capture is the missing instrument | prescribed |
| **The flythrough's capture ticks depend on wall-clock load, so any anchor scored at one tick is fragile.** The re-planned crowd leg was scored at tick 3,000 and the flight reached it at tick **4,899** because the run took 127 s of wall clock against the previous run's 72 s (eight lanes were loading the machine; the sim advances with real time). The chosen knot holds 316 bodies at 3,000 and 12 at 5,400, so the held frames photographed a static scene and the lane failed its own check with "5 of 6 held frame pairs show a scene that went still" and 41 distinct digests of 46 | re-plan onto an anchor whose walking-crowd count holds across the whole window a loaded and an unloaded run can produce (measured at 3,000/4,000/4,900/5,400/6,000/7,200), preferring a broad plateau over a high peak | A route aimed at a time-varying population has to be robust to the time it actually arrives, not the time it was scored at | in flight |
| The crowd leg's unlisted findings: no contact shadow under the knots (`castShadow` is near-level only, `renderedNear` 0), a quarter of the frame carrying no image, `standAt`'s 0.75 m tolerance accepting 3.64 m for a 3 m ask | lane-level, tools | Small, but they are why the crowd frames judge less than they could | prescribed, partly served by the aim re-plan |
| `guardrailPosts: 0` is a dead field read by nothing | `src/scene/street-details.ts:230` (delete) | It reported nothing either way and misled a criterion note; removing it is a `src/` change, so it rides the Phase B batch | **landed** (`4e71f19`) |

## Phase B — the one batched re-capture

Nothing here lands alone. Each moves what the certificate pins, so they land together, then all five gates run once, the 44 frames are re-captured once, and they are re-inspected once.

| Item | Change | Cost, measured | Why batched |
| --- | --- | --- | --- |
| The mid-ground ground and road materials carry no texture map (`terrain.ts:48` "ground", `roads.ts:52` "road" are bare `MeshStandardMaterial`; ground albedo is a ±3% sinusoid at ~20 m, road grain fades above 0.015–0.08 m/px) | `src/scene/surface-detail.ts` + `surface-materials.ts` — a seeded 128×128 tile sampled in world space, the mip chain as the distance fade | the largest single visual-quality gap recorded; the tile is 8 m per repeat on the ground and 4 m on the road, chosen so the coarsest octaves are still resolved at the overhead sweep's 1.374 m/px | It is also the cause of the certified plaza near-field wash-out, so it is one change with two findings. **Landed (`68a4775`) and measured as NOT delivering on 2026-09-18.** The tile is in the certified build (`mapsDetailMap` in `dist/index-DpreZClu.js`) but the inspection of the re-captured frames (runId `fb07b11ea3b8296f`, eight lanes) does not find it in the pixels: the hero approach lane's old-vs-new crops differ only by dither (structure energy within 1.2%, whole-frame mean difference 0.004–0.28/255, *smaller* than the difference between two frames of the same capture); the cartographic plaza lane measures the tile's neighbouring-texel step at **0.40% of albedo** (road 0.36%) because its octave amplitudes decay faster than its lattices refine, so it is a smooth field rather than texture, and in the one cell it was aimed at (`az000` left-ground) it *removed* 38% of the local structure; the satellite plaza frames are pixel-indistinguishable from the superseded capture. **Road asphalt is still flat in all four hero crossing frames** (54.3/95.8/93.0/79.7% of asphalt pixels indistinguishable from flat, against 9.9–13.8% structured on paving in the same frames, and a 4 m repeat spans 45–280 px at those poses so it is not mip fade). **`plaza-az000`'s near-field pavement is still black** — 66.6% of pixels below luminance 12, 98.3% of 8×8 blocks dead flat — while the same frame's right-ground shows a full paving grid, so the material reaches one side of that camera and not the other; the satellite plaza lane grades this blocking and carried over unchanged. One lane (cartographic block) reports ground structure present and measured 15–35× the sky control; a second (satellite overhead) reports structure but no spectral peak at either tile scale; both are consistent with the base material carrying it rather than the tile. Supplying the structure the finding asks for, or reverting the tile as insufficient, is the next appearance item. **Targeted re-inspection of the new certificate's changed cells (runId `14cdadbafba82120`, artifacts/inspection-gpu3/): the change is a half-fix and is recorded as one.** The satellite near-field road at `plaza-az000` is no longer black (0.3% of pixels under luminance 12 against 66.6%, mean 17.80, a soft gradient) but remains a featureless dark fill; the cartographic cell shows fine even mottle at 1:1 at about a quarter of the amplitude the pavement draws in the same frames (1.79%/1.57% neighbouring-pixel step against 2.6–3.7% for pavement — the in-frame control), and both hero mid-ground asphalt bands are flat fills. So the blocking defect stands half-fixed: **the near-field road is lifted, not given structure.** The same lane re-read the material labels: at the plaza pose `left-ground`/`near-pavement` are the ROAD (`roads:plateau-tran`), while the legible paving grid in `right-ground` is the pavement, so the earlier inspection's "missing paving tile" pointed at the wrong surface. Bound: 8 of 44 frames; the unaffected frames' last full native inspection remains `artifacts/inspection-gpu2/` against the superseded build |
| The F1 terrain hole and its 13 siblings | `tools/scene/build-terrain.ts` capping + a watertightness gate in `npm run data:scene` | F1 is 15 rim vertices, 89.5 m perimeter, 30×25 m at (579.0, −403.1), 80.43 m outside the AOI; the cap costs +227 triangles, +14 vertices, +3,164 B (0.069%), and the census gate refuses uncapped terrain by name with all 14 rims listed | **Removed from this batch on 2026-09-17 and prescribed separately.** Capping the ground moves `roads.mesh` (`3fe126cd…` → `0d429c31…`, +123 triangles because roads over the holes now drape instead of being dropped) and the pavement stage refuses it, because `tools/scene/pavement-recipe.ts`'s `REVIEWED_INPUTS` binds the old terrain and roads and `test/paint-support.test.ts` pins both plus `pavements.mesh` as Review 10 artifacts. Moving those pins changes a reviewed Candidate 5 artifact and the digest its recipe doc records, which needs a fresh review of the pavement input binding — a review project rather than a batch item, against a hole 80 m outside the AOI whose four in-AOI siblings are hidden under buildings. The cap code and the census gate exist and work on branch `phase-b-terrain-cap` (`adb9d4f`); they land with that review, not before it. |
| The frame-budget lane cannot measure the delivered build: the probe seam has never existed on `main`, and **no seam shape is pin-free** | `index.html` + a query-gated `tools/frame-budget/probe-entry.ts` + a unit case asserting the script line | measured: `dist/index.html` 2,049 → 2,365 bytes and the entry bundle renamed `index-D7By1MWG.js` (975,237) → `index--jFOWFey.js` (976,145), because Vite merges the probe into the entry chunk and `verify-output.ts` pins every `dist/assets` file | Same reason; the certificate must be re-issued anyway. **landed** (`d2babb8`) |
| The served scene's digest is a build identity: `data/scene/manifest.json` carries `"builtAt"`, so every `data:scene` run moves `sceneTreeDigest` with byte-identical geometry, and a certificate's scene binding can never be re-derived after a rebuild | `tools/scene/scene-manifest.ts` — the timestamp leaves the served payload rather than being normalised out of the digest | It is why the 2026-09-17 junction incident could not be undone: the pre-incident digest is unrecoverable by construction, and a reviewer cannot tell a rebuild from a real change. The served `manifest.json` is 1,562 → 1,521 bytes | Batched with Phase B; the certificate is re-issued there anyway. **landed** (`ddb87ac`) |

## Rejected or deferred, with the reason

- **Satisfying the dusk criterion's TAA clause over a moving sequence.** The app cannot do it: `src/app.ts` feeds `cameraStill` into `post.setStill` and `post.ts` resets the sample index, so accumulation and motion are mutually exclusive by construction. The criterion has to be judged on the non-accumulating path, or the app's stillness policy has to change — which is a product decision, not a quality fix.
- **Landing F1 or the probe seam alone.** Each strands the certified frame set and the week's digest-bound inspection; the batched re-capture is the cheap way to have both.
- **The signature shot's routing fix.** It needs `src/agents` work plus a fresh populated capture, and the plan already records the measurement it must carry (the crossing change's after-arm at the acceptance population). It is a scope decision for the owner, not a quality-lane change.
- **Re-capturing the 44 frames without a Phase B change.** The current certificate's build and harness bindings re-derive exactly, so a re-capture would buy new frame bytes and strand the inspection for no evidential gain.

## The bound on this register

Every cost figure above is the audit's measurement, not an estimate, and every recapture dependency was verified in `vite.config.ts`, `index.html` or the digest algorithm rather than assumed. What the register does not claim: that the Phase B batch is free of risk — a materials change touches every frame and could move the hero look enough to invalidate the tone judgments of round 30 and this session's inspection, which is exactly why the batch is re-inspected rather than assumed.
