# 1km x 1km animated Shibuya model

Status: active
Owner: root orchestrator
Created: 2026-09-06
Updated: 2026-09-19

## Current queue — 2026-09-19

The root orchestrator owns this queue and final acceptance. This section supersedes the older status claims and unfinished checkboxes below when they disagree; those passages remain historical evidence. Recovery started from clean `main` at `a9b5c3ee1278c33f847b26ecddf52de540ad5785`. No gate, capture or source rebuild was run for this documentation update.

The last recorded accepted appearance baseline is the merged terrain cap (`400065a`, Review 34) and certificate `aa2e07e9902a5dc9`, with all 44 frames inspected at native resolution and no blocking appearance finding. The old “cap unmerged”, “capture owed” and “frame change unattributed” claims are superseded. The two approach-pose `style-return` frames remain lane evidence outside that certificate's 44; documenting that boundary is the chosen scope, with no expansion of the certificate set. Historical evidence does not certify the changes now in progress.

The latest recorded performance is p50 **16.7 ms** / p95 **33.4 ms** at 1920×1080, 3,000 pedestrians and **59 drawn vehicles**, on the i9-13900KF / RTX 4090, one seed and one window per arm. The median meets the target within that bound; p95 does not. The owner's accepted **200-vehicle infeasibility** remains separate from measuring the next population revision. The signature crossing and moving-camera flicker criteria remain unmet.

| Unit and state | Owner and dependency | Evidence and acceptance | Next action |
| --- | --- | --- | --- |
| Q1. Pedestrian stall — active | `population_stall`; root accepts. Base `a9b5c3e`; preserve contact and admission contracts. | `artifacts/signature-routing/STALL.md` and the quality register locate ORCA's zero-velocity fallback. Require `committedAtScramble > 0` on pedestrian green, at least 338 completions at 3000/200, and at most 142 worst overlapping pairs at 150/0 in the pinned windows. Those counters do not replace native motion/contact inspection. | Reproduce with shipped population instruments, repair the fallback, prove the regression check red, and hand off a reviewed candidate for Q5–Q7. |
| Q2. Durable visual certificate — active | `certificate`; root accepts. Workspace `artifacts/certificate-primary/wt`; independent of Q1. | The wrapper currently writes a removable worktree's ignored certificate. Accepted design: CLI refuses `--reset`, `--begin` and `--end` in a linked worktree before any mutation, names the primary, and keeps primary certification available. | Implement and test both refusal and primary paths; correct certificate documentation to distinguish the two return frames from the certified 44. |
| Q3. Dusk motion evidence — queued | Root to assign after a worker slot is free; serialize browser/GPU use. | The current crawl predicate is quadratic in frame gap and cannot fire at the measured 20.5–23-frame cadence. The recorded ascent emits no wheel input. Require actual adjacent rendered-frame evidence, a non-vacuous calibrated bound, real orbit/ascent input and native inspection of the moving non-accumulating path. | Repair the capture/judge together, correct the falsified `channelDelta`, shutter and refine-window comments, and document crop names as screen rectangles. Preserve the old measurements and demonstrate positive and negative controls. |
| Q4. Rebuild and source authority — proposal owed | Root owns the proposal; source-contract approval precedes any authority change. Use private data, never the shared served tree. | The reviewed OSM extract is missing. `data:scene` can silently drop authored paint; `data:markings` refuses mixed provenance; `data:setup` re-derives a new network and paint. Clean-rebuild acceptance is unresolved for these current inputs. | Prepare the smallest reviewed proposal described below, including a named refusal for silent paint loss. Do not relax the source guard or replace accepted network bytes as routine cleanup. |
| Q5. Populated and style acceptance — queued | Root to assign after Q1; Q4 only if a source change is needed. | Re-use `visual:populated`, its aim probe, `visual:flythrough` and the actual style control. Require visible lane/turn/boundary events, meaningful governed vehicle entry for the signal clause, pedestrian crossing on green with natural contact, and preserved camera/progress through pointer and keyboard switches on the final scene. An empty corridor or zero governed entries cannot pass. | Inspect native moving sequences in both styles. Check the authored west-approach paint from a close view; identify the carried satellite-block az060 surface before proposing a material or light change. Record unresolved fixture limits rather than inventing coverage. |
| Q6. Final population performance — queued | Root to assign after Q1 and final render changes; timing owns a quiet machine. | Re-use the landed frame-budget probe at 1920×1080. Name revision, served inputs, hardware, seed, window, load, drawn counts, p50/p95, catch-up and authority results. Retain the accepted 200-vehicle exception. | Re-measure the delivered population revision and state the supported bound; do not inherit the earlier timing or reopen the rejected 200-vehicle trade. |
| Q7. Review and verification — queued per increment | Root owns acceptance; independent read-only reviewer. Depends on each candidate's scoped checks; final pass depends on Q1–Q6. | All five gates precede each code commit. Run one visual/browser lane at a time, issue the certificate in the primary, inspect every certified frame at native size, and cover affected populated/flythrough flows. Reviews bind the exact integrated revision and material fixes get focused re-review. | Freeze each meaningful candidate, review and run its required gates; re-capture after bundle/data/harness changes and explicitly prove any permitted gate transfer. Check every acceptance criterion with its evidence and remaining limits. |
| Q8. Integration and cleanup — queued per accepted increment | Root; depends on Q7 for the increment. | Completion requires accepted changes on `main`, an authorized push with available remote checks inspected, and no abandoned branch/worktree or task-owned process. Old evidence for unresolved issues stays recoverable. | Merge and push each accepted unit, preserve cited evidence before worktree removal, enumerate/unlink reparse points safely, remove integrated worktrees, and close only owned browser/server processes. |

Q4's source boundary is exact: reviewed `network.json` `314fac84…` records extract `9923a9ae…` at `2026-09-07T03:23:56Z`; the available extract is `976d848e…` at `2026-09-18T01:49:46Z`. Authored `markings.mesh` `efdba9cb…` (388,992 B) was restored, with all 204 authored vertices proving unchanged support after the cap. The smallest safe proposal preserves those accepted bytes and first refuses a rebuild that would silently discard the paint. A separate content/support check recording both vintages needs independent review and source-contract approval before replacing the provenance guard. Choosing a new network vintage is broader and requires fresh dependent evidence. The proposal is owed; no source-policy change is approved by this queue.

## Problem and outcome

Build a roughly 1 km by 1 km model of Shibuya, Tokyo that runs in real time in a browser, leans photoreal, and has pedestrians and vehicles moving through it.

The area of interest is a 1 km box centered on Shibuya Scramble Crossing at 35.6595 N, 139.7005 E, which gives 35.6550–35.6640 N and 139.6950–139.7060 E.

That box covers Hachikō Square, Shibuya 109, Center Gai, Shibuya Scramble Square, Miyashita Park and lower Dōgenzaka.

The deliverable is judged by what the rendered frames look like and how the scene behaves while you move through it. A green test run is not evidence for any of it.

## Scope

Included: everything in the eleven phases under Implementation steps, from the toolchain and its visual gates through data, pipeline, static scene, materials, lighting, the shared network graph, vehicles, pedestrians, performance and acceptance.

The owner's 2026-09-08 scope adds two switchable 3D world styles: Cartographic, inspired by the clean default Apple Maps view with finer scene detail, and Satellite, improving the existing phototextured world. A World style dropdown draws its options from an extensible registry. Both styles share the same world geometry, simulation state and camera; changing style preserves the current camera and running agents. This is an appearance choice within the Shibuya deliverable.

Excluded, and these are non-goals rather than deferred work:

- Not a map viewer.
- Not a navigation or routing product.
- Not a survey-grade reconstruction of the real place.
- No second location. Nothing past this deliverable is decided.

Ownership boundaries inside the work:

- Phase 0 blocks everything. Nothing else starts until the scaffold, the Playwright visual harness and the `Gates` section exist.
- Phase 6 produces a shared contract. The lane, sidewalk and crossing graph plus the signal phase model is the interface both agent workstreams build against, so it must be documented and frozen before Phases 7 and 8 run in parallel.
- Phases 4 and 5 are where the deliverable is won or lost. Treat them as the main body of work, not as polish after the geometry lands.

### Current delivery status and ownership — 2026-09-13

The root orchestrator owns scope, shared contracts, canonical status and final acceptance. Implementation and independent review run in bounded worker assignments. The approved goal is the whole Shibuya deliverable through Phase 10, with autonomous goal mode across the managed work. The owner instructed the team to continue until it is done, overriding the default automatic-repair cap for this scope. This adds no second location or product surface.

**The table below was written on 2026-09-12 and every row of it is superseded; it is left in place as the record of what was believed then, and this replacement is what a reader should use as of 2026-09-16.**

| Scope | Current state, 2026-09-16 | Owner and dependency |
| --- | --- | --- |
| Phases 0–3 | Unchanged and complete: the AOI, the data pipeline, the static scene and the first visual baseline are on `main`, and the recorded five-gate and eighteen-frame acceptance belongs to that baseline. | Root orchestrator preserves the baseline and its evidence. |
| Phases 4–5 | **The graphics candidate landed** as G1 `0098253` and is on `main`: both world styles, photographic facades, emissive signage, road markings, signals and street furniture. The dusk preset's post chain had never accumulated — the app's stillness witness was stricter than any damped camera could satisfy — and that is fixed (`src/render/controls-rest.ts`), so TAA engages at the shutter where it previously had none. **What remains is the capture for this revision**, which is running, and its native inspection. | Renderer worker implements and verifies; root orchestrator accepts the integrated result after independent review. |
| Phase 6 | The reviewed network milestone is on `main` at `1ca4552`, the contract is frozen in `docs/reference/network-contract.md`, and one signal phase model drives both populations through the design's own tick order. Authority violations are gated, and the temporal half of signal obedience is **unmeasurable in the current fixtures** because no vehicle ever reaches a governed junction. | Network worker owns the accepted graph and admission API; root orchestrator accepts integrated traffic. |
| Phases 7–8 | The agent and vehicle cores (`10da8ea`), the human manifests (`0045b30`) and the population (`388a73c`) are on `main`, and the crossing change (`a800cee`) with its pedestrian-overlap gate (`831de61`) lands walkers on the scramble's diagonal. **The crowd still interpenetrates**, bounded at half rather than at zero and traced to queues rather than the walking model, and **no bus is ever drawn** in any run because every bus is refused at spawn for its footprint radius. | Network worker owns simulation mechanics; asset worker owns provenance, geometry and animation; root orchestrator accepts shared contracts. |
| Phases 9–10 | **Measured, and the performance target does not hold**: the frame interval at 1920×1080 with 3,000 pedestrians and 59 drawn vehicles is p50 83.3 ms against a 16.67 ms target, with the cause attributed to a tick of p50 75.7 ms and 44.1% of it in a neighbour query visiting 97 bodies where ORCA uses 8. Both halves are now owner decisions — keep `main` green at the fleet it carries and record 200 vehicles as measurably infeasible; record 3,000 pedestrians as measurably infeasible with the ceiling stated. Phase 10 acceptance — the certified 44-frame sweep, its native inspection, the controls-driven flythrough, and the independent review — is **owed**, and the capture that produces the frames is running. | Root orchestrator owns measured performance, whole-deliverable acceptance and merge to `main`. |

The target already pinned in `src/world/frame.ts` is 3,000 animated pedestrians and 200 vehicles at 60 fps and 1920×1080. It is an acceptance target, not a measured result. Completion requires every acceptance criterion below on the final integrated revision, all five repository gates, native-resolution inspection of the rendered sweep and controls-driven moving sequences, independent review with material findings resolved, and merge to main. Work left uncommitted or on a branch is incomplete.

Owner disposition, 2026-09-12: root accepted [Review 8](reviews/8_implementation.md), SHA-256 `58bdd2d4846a8523b22220ab538cbf62e5f5a6dd26731ff70e905d2966a198c9`, after reading its independent production-order replay and verifying the seven handoff files and rebuilt mesh/source hashes. Acceptance binds producer freeze `14d88e56b5c736249f45db014cac975e640ead698d4b5594337865345a2cad71` and the exact C5 pavement, not a new support policy. The original 6,757-probe strict failure, three unchanged passing seam rays, retained source heights and unclassified regions remain explicit. Later geometry changes require another review. The authored report is unchanged.

Owner disposition, 2026-09-12: root accepted C9 manifest `f0925ce615b2548a29de5a88fb2082661718cfb0c82ff3188bd51e99fdb8420a` as the current lighting increment. Environment chroma retention is 10% above 10° solar elevation and 45% below −3°, with a smooth transition. This is authored grading; albedo, exposure and texture budgets did not change. Its 80-input manifest omitted several transitive harness/config paths before capture, which were recorded afterward and cannot inherit pre-run binding. Final candidate verification must bind the complete source/build/data/harness closure before and after the run. C9's eleven retained process identities exited naturally; fallback termination was not exercised.


Owner disposition, 2026-09-12: main is `d3699133aa9185543fdfc51997f76385be011285`, the reviewed documentation checkpoint for reports 8, 9, 10 and 13; its parent is `2799084`. The isolated graphics candidate deliberately retains base `2799084` and imports authored history as an explicit document delta. Root accepted Review 12 F9 within its source/support bounds after checking 85 reviewer and 94 author pins and 36 focused tests; populated raw-V4 motion is still unverified. Review 11's 190-input standalone/hardware copy has no new finding. Neither proof closes the full simulation or performance criteria.

Owner disposition, 2026-09-12: F10 source and native acceptance binds `artifacts/graphics-milestone/f10-followup.json` (`68dd6ce33865c61eb717725b82f94d5d803f40cc33668ffd3a5d7421aff59ca7`) and the eight-frame hero manifest (`6a2390d0a6d5cc0adaa867a49f2bac8bdae4b7c291c39c48f591036ff83b018f`). Root inspected every original 1280×720 frame. The central diagonal is continuous; omitted endpoint parts 0, 44 and 45 are not material in crossing/approach views. Off-centre way 1419311959 is triangle-gated, not close-viewed. Of 1,333 total paint parts, 1,158 are placed and 175 explicitly omitted across 75 features; all 100 features retain paint. The 28 paint-only seam queries do not alter network or pedestrian/vehicle support. Final 44-view surrounding appearance remains pending.

Owner disposition, 2026-09-13: [Review 15](reviews/15_implementation.md) is on main at `e93dfa9c3aca45526ad8cee31caaaf876b33fca8`, following the Review 14 checkpoint `7ec174246983d4122bd60ab1c46ada04d55709ac`. Root and the independent reviewer each passed 44 response and 110 completion controls; the reviewer also passed six actual serializer controls. Acceptance covers authentic result-artifact consistency. The old v3 failure was response-body evidence collection, not a demonstrated failure of its one observed vehicle flow. Review 11 remains open, with the full 44-frame graphics gate unfinished.

The separately reviewed capacity correction completed one headless A/B run, bound by `artifacts/network/app-motion-native/observer-capacity-r1/run-02-evidence/handoff.json` (SHA-256 `31a2bdd5fe667449d3a6fc218e7540f411a217c72582bf9885349f8b8fb34db9`). The same dedicated CDP collector, two JSON files and HTML were used in fresh sequential browsers. Default buffers evicted both large JSON bodies; expanded parameters verified all three exact responses. Both pages consumed the two expected files. All 7,202 source/runtime input records stayed unchanged and fresh cleanup found the observed processes absent with port 4319 free. This demonstrates that finite two-input comparison, not the prior Playwright response-body path or the full-city concurrent request pattern. Eighteen completed vehicle executions remain required.

The earlier rejected walk initialization experiment and stepping erratum are retained at `artifacts/agents/gait-cycle-spawn-fix/report.md` (SHA-256 `7d1c2f0191ca5e742fc3fc39d8cb264193ac1340c6007498561c437b95a56e9b`) and `erratum-stepping.md` (`3b5a526cfd3885ec84106531b7402b3636326e2ea406000078f44b6095443726`). The selected flight removes the original derivative mismatch, but discrete duration selection causes 32.4/48.6 mm placement jumps at 2 m/s across phases 0, 0.5 and 1. Eleven cases and 34 rows of a twelfth completed, totaling 837 observed rows; 31 cases never ran. The ongoing prediction is 38.291045259 mm plus a 3 mm margin against the unchanged 35 mm cap, although the current retained pose remains valid. The experiment is rejected. Its 240 Hz belongs to the unintegrated pilot corpus; production's fixed step remains 1/60 second. No production gait or populated acceptance follows.

Owner disposition, 2026-09-13: the preceding canonical checkpoint is `636bff7dca10f00dc3ada892ebb7005cfa815c92`. Root accepts the finite gait progression and cost reviews preserved in [Review 16](reviews/16_implementation.md), and the bounded CPU visibility repairs in [Review 17](reviews/17_implementation.md). These document-only records do not integrate their ignored candidates. Review 11 remains open; no missing round or whole work-docs continuity pass is implied.

The current gait candidate is controller `a81813b2c503f338cd8070b6c548f5801f0d0835b7512a5f313b72e4aa0cbb26`. Independent execution completed 43 cases and 18,523 rows at 240 Hz, then 43 cases and 4,663 selected rows at 60 Hz with exactly 4,620 updates. The latter does not observe the 13,860 omitted rows or between-tick poses. Seventeen successful decisions omit some rejected-successor diagnostics; selected-pair evidence remains intact. Naturalness, arbitrary future commands and production integration remain unaccepted. The phase-independent initializer and its ongoing failure are preserved as an earlier, separate target.

The independently reviewed cost experiment measured complete 3,000-agent batches over 120 ticks after 12 warmup ticks. Median/p95/maximum is 3.315 / 13.221 / 23.009 ms for A updates, 96.988 / 109.623 / 133.123 ms for B poses, and 101.412 / 120.269 / 144.613 ms for same-tick update plus pose. A/B equality covers the emitted 19-double state projection only. Palette byte counts are cumulative materialization, not resident memory. Twelve synchronized streams repeated on one flat-ground rig do not establish heterogeneous or whole-scene performance; the 3,000-pedestrian/200-vehicle target remains unchanged.

The canceled ordinary vehicle run is retained at `artifacts/network/app-motion-native/observer-final/run-01-owner/partial-handoff.json`, SHA-256 `003bc8127b48ff9aa60005c1e17213750371b91cc54a3b7b9b5fb5ede742c55e`. Root viewed complete building occlusion in incoming-kei-side1 and bend-kei-side0 moving frames while the old speed/rendered-count predicate passed. F13 now refuses visible unknown deformation before static-box exclusion; F14 binds wheel samples and first hits to the exact target instance. The 1,200,000-test CPU variant completes the retained 60-ray request, but all source replays remain unsupported without actual draw membership, interpolation and UI metadata. The prior 500,000-test refusal is retained, and no pixel acceptance follows.

Owner disposition, 2026-09-13: the preceding canonical checkpoint is `7f8aeac51357654a4d39d3e6ecc5f8ac0bda79b6`. [Reviews 18–23](reviews/18_implementation.md) preserve six separate authored rounds in sealed timestamp order. Root accepts the finite evaluator correctness, retirement and tiny preparation scopes, and the focused ABBA F15/F16 repair. F17/F18/F19 from the original B0 city instrument remain open pending a reviewed successor. These are documentation and ignored preparation milestones, not a graphics or simulation code merge. Review 11 remains open and the grand goal remains active.

The retirement helper now has independent CPU ownership/API evidence and a separately observed 64×64 real-extension delivery/event. The tiny run's added outer reporter failed; its historical closure ledger is missing despite later fresh observed process/profile/port closure. Neither that small-context observation nor the screenshot-disabled negative run proves native memory reclamation, whole-city disposal, the unchanged 15-second navigation or 60-second replacement check. The full 44-view graphics gate remains unavailable.

The reusable gait evaluator matches the original finite position contract with all 43 sampled cases, full-state/input nonmutation and retained destination/source checks. Its initialization/update controller, complete influences and Float32 source stores remain unchanged. F15/F16 repair concerns timing-instrument failure propagation and completion evidence; it does not improve the pose algorithm. Root accepted the sealed owner ABBA comparison as a finite negative measurement: full-state/pose correspondence held, but same-tick update-plus-pose medians were 103.145 / 263.538 / 263.364 / 103.054 ms for original/new/new/original, respectively. The ratio of mean arm medians is 2.555×; this is one four-arm sequence, not a confidence interval or a cause diagnosis. Each arm ran 3,000 states through 12 warmup and 120 measured ticks on twelve repeated streams, with a 120-second hard child cap and 110-second cooperative bound inside one ten-minute lease. All four children closed without timeout, error or retry. The reviewed r1 repair patch remains `883fe2cce49e0b885fee14c42e6d767ec00db1a55f81a68cef80fbc9d866449b`; lease SHA-256 is `8821062151eb06fd97ad1efc2782e1258ea5990599993215414f36de7b30cdf7`. The measurement owner report and handoff remain at `artifacts/agents/gait-pose-abba-r1/benchmark-report.md` (SHA-256 `c4443c24165088f1db0706b8697865ae92eedab460d6e5fb6a1985569fab230c`) and `benchmark-handoff.json` (`4d53117141a53811a37d6887dac449a5666e149ebdc455f49df6ebd9df5d1b31`). The optimization hypothesis is rejected; independent result interpretation remains separate and pending. Validation-heavy callback times around 500/662 ms are not production FPS. One rig and twelve repeated streams remain distinct from heterogeneous city work, GPU passes, naturalness and the unchanged 3,000-pedestrian/200-vehicle target.

Owner disposition, 2026-09-13: the preceding documentation checkpoint is `edd448edf524371a247d15983ea99ede13a87ff3`. [Review 24](reviews/24_implementation.md), [Review 25](reviews/25_implementation.md) and [Review 26](reviews/26_implementation.md) retain three separate independent reports in recorded seal order. Root accepts their finite CPU source, instrument-repair and result-interpretation scopes. Historical Review 23 keeps its original open disposition; this later repair resolves F17/B01, F18/B02, F19/B03 and the subsequently discovered F20/B04. Review 11 remains open. The broader goal remains active, with no source or graphics code merged by this checkpoint.

The facade increment binds three material patches to the actual admitted tile, image, geometry and UV data while preserving the existing photographic gain expressions. The prepublication cleanup assurance limit and rejected null-KTX hypotheses remain explicit. CPU checks do not supply native shader compilation or appearance acceptance. The repaired B0 witness retains the earliest matching noon snapshot, refuses first admission after observed completion, uses the exact canonical debug-log key and rejects unfinished oversized EOF. Those finite controls do not establish the ordinary city navigation or replacement criteria.

The independent ABBA interpretation reconstructs the input and row populations, all forty recorded summary sets and eleven negative arithmetic controls. The original/new/new/original kernel medians are 103.145250 / 263.538000 / 263.363600 / 103.054500 ms, with ratio 2.555296987508426 for the means of arm medians. The slowdown is accepted as measured; its cause remains unknown. Full state/pose correspondence is separate from performance. The proposed 24-tick paired CPU profile has not run. Production gait and the unchanged 3,000-pedestrian/200-vehicle target remain unintegrated.

Owner disposition, 2026-09-15: four documentation units are on main at `f705e29`, `14e790f`, `cc108bd` and `ecb7dd1`, and are pushed. They move the Phase 6 network contract to `docs/reference/`, preserve review rounds 2, 4 and 12 with their recorded format corrections, record the 2026-09-13 status with the renderer-recovery and graphics-diagnostics devlog sessions, and take the candidate's newer copies of the graphics-diagnostics and facade-emission records. Some of those records name code and evidence that land only with the graphics candidate; each of those commit messages carries the list.

The graphics candidate is still not on main, and its blocker is unchanged. Its `tools/visual/lifecycle.spec.ts` failed its own `page.goto('/?time=noon', { timeout: 15_000 })` after an 18.4-minute preparation, with the renderer's main thread unavailable for about 13.7 seconds. The fixed reproduction, the disqualifiers and the branch registry for that repair are held in `artifacts/lifecycle-repair-20260915/CONTRACT.md` and stay there while an attribution lane measures where the block sits. `AGENTS.md` and `docs/policies/local-rules.md` describe the candidate and land with it. About 137 paths stay uncommitted in the primary checkout: the candidate's code, the later network admission and passage work, and the agent, human and vehicle workstreams.

Owner disposition, 2026-09-15: the lifecycle check's red fifteen-second bound was attributed to SwiftShader's teardown of the outgoing scene rather than to the application, whose own `pagehide` work spans 2.1-4.3 ms while the replacement document's first script runs at about +27.87 s (forcing `loseContext()` inside the handler moves the same ~30 s inside it). The gate is now split by renderer. `playwright.config.ts` is the pixel lane: hero, style-picker and sweep on SwiftShader, `lifecycle.spec.ts` excluded, and the launch-arg switch that could have moved the lane to hardware removed. `playwright.lifecycle.config.ts` runs that spec on the hardware renderer with the same URL, preparation, viewport and 15 s / 60 s bounds, records the unmasked renderer string per run, and fails by name if it names a software rasteriser. `npm run visual` runs one build, the pixel lane, the lifecycle lane with `--repeat-each=3`, then the wrapper, and the wrapper deletes any earlier `complete.json` when a capture run begins so a failed run cannot leave a previous success artifact behind. **That last guarantee is false on `main` and was corrected on 2026-09-16**: `package.json`'s `visual` script runs `npm run build` *before* `node tools/visual/verify-output.ts --begin`, and `tools/visual/verify-output.ts:21` is where the deletion happens, so `&&` stops the chain at a failed build and the previous run's `complete.json` survives it. The deletion itself still works, and `test/visual-evidence.test.ts:13` still pins it; what is unpinned is its place in the chain.

The first corrected run completed on 2026-09-15: the four pixel-lane tests took 3.0 h (each style sweep 1.1 h, hero 43.8 min) and wrote all 44 native frames; the three hardware lifecycle runs navigated in 108 / 104 / 109 ms with replacement at 5,630 / 4,660 / 4,686 ms on an RTX 4090 over D3D11; the wrapper reported that all 44 fresh native-resolution frames survived the complete visual gate with matching hashes. Evidence: `artifacts/lifecycle-attribution/` (report, proposal, independent review with nine conditions), `artifacts/lifecycle-repair-20260915/gate-run/` (both red controls and the full-run log), the new entries in `docs/learning/gate-proofs.md` and the defect register, and the drafted amendment to the repair contract's software requirement at `artifacts/lifecycle-repair-20260915/AMENDMENT-DRAFT-20260915.md`, which the coordinator records. Nothing here changes a bound, the preparation, the URL or any acceptance criterion; the SwiftShader result stays in the record as disclosed counterevidence. Next gate: native inspection of all 44 frames at their own size, then independent review of the corrected instrument, then merge to main.

The graphics candidate is now on main, and the blocker above is closed by it. Owner disposition, 2026-09-15: the candidate landed as `0098253` on `main`, from `672c73f`, at tree `57a5fe3e20b3e3beb34050eea1357d0d4a693316`, and is pushed. The 44 candidate frames are bound to it: the capture at `complete.json` SHA-256 `629786433e5ab372debdd2367f55e59b1f211da40f5418fdcecb177bc89e198d` came from `dist/assets/index-BP1Vm0F-.js` at `0d99838361a2597053e2eb6f0fdf72dd6a9aa53b31433cc5e540a9c4df1f451c`, and that revision builds to exactly those bytes. The plan text above, which still describes the candidate as unmerged and its blocker as unchanged, is superseded by this paragraph; it is left in place as the record of what was believed before the merge. Native inspection then opened all 44 frames individually at their own size and found no blocking defect (`artifacts/frame-inspection/report.md`, consolidated from four read-only lanes of nine frames each plus the eight hero frames). Its disclosed bound is that the image tool returns 1066x600 previews of the 1280x720 sources, so single-pixel aliasing and temporal crawl are outside what it signs off. The findings it carries forward are outer-terrain ground holes, a facade scribble at dusk, one sign artwork reused on several facades in a frame, and near-field pavement wash-out at grazing angles. Two lane findings were adjudicated as over-calls: the "untextured slab" in `plaza-az120.png` and `plaza-az180.png` is the road surface, correctly paved and toned, and the pale low-rise district in the satellite overhead frames is rooftop lighting under a low sun rather than a missing atlas.

Independent review of the corrected two-lane instrument reports no blocking finding (`artifacts/instrument-review/review.md`, SHA-256 `0fb106584c8dff4d306940673672be7f730a5c2dfbc25456f176d4bb47ac699f`). Its five accepted material findings were described here as "taken into the hardening unit that follows", and that is corrected on 2026-09-16: on `main` at `04be817` **none of the five was fixed**, and each is named here with where it lived then. (1) The previous `complete.json` survived a failed build, because `package.json`'s `visual` chain ran `npm run build` before `node tools/visual/verify-output.ts --begin` with the deletion inside `beginVisualRun`. (2) `complete.json` carried no lifecycle evidence, and no step of the wrapper read `artifacts/visual/lifecycle/`. (3) The build-hash binding excluded `data/scene`, so identical build hashes did not determine the frames. (4) The lifecycle lane attached no `page.on("console")` and no `pageerror` handler. (5) The pixel lane's SwiftShader identity was recorded in the manifests and never asserted. **All five are now fixed in the `artifacts/gate-integrity` worktree on branch `worker/gate-integrity`, off `0ae46df`**, with each check watched to fire and its mutation recorded in `docs/learning/gate-proofs.md` and `artifacts/gate-integrity/REPORT.md`. The chain is now `--reset` (ahead of the build) → `npm run build` → `--begin` → both lanes → `--end`, the certificate carries both lanes' renderers plus a digest of the served scene data, the lifecycle lane asserts an empty page-error list and a completed `pagehide` teardown record, and both capture specifications fail in seconds on a renderer that is not SwiftShader. Nothing is committed; the first full gate run after this lands is what certifies the spec-side assertions.

The stale `artifacts/gate-hardening` worktree on branch `worker/gate-hardening` remains as it was, base `e561d75` 22 commits behind `main` and no commits of its own (`git log main..worker/gate-hardening` is empty). Its `REPORT.md` records that nothing was committed, pushed or merged, so a reader must not cite that worktree as a fix on `main`. What it established and this unit kept is its measurement that a `pagehide` throw is invisible to Playwright; what was re-derived against today's tree is all of its code, because `verify-output.ts`, `orbit.ts`, `hero.spec.ts`, `sweep.spec.ts` and `lifecycle.spec.ts` had each changed since its base and `budget.ts`, `progress.ts` and `lane.ts` did not exist then.

Three further units landed and are pushed on the same day. `620b662` adds `tools/agents/population-cost.ts`, the first measurement of the real per-tick population cost: on this machine, 3,000 pedestrians and 200 vehicles cost 2.409 ms median for admission (4.8 times the design's assumed 0.5 ms budget), 0.526 ms for pose composition, 1.007 ms for a simulation substrate that contains no motion model, and 4.346 ms for a whole tick. Admission throughput, not frame rate, is the first thing this population breaks: a median 27 grants against 2,582 requests, and two vehicle routes cannot be driven at all because a mapped stop line sits nearer the gate than half the longest vehicle and no offset policy exists. **Correction, round 31's review:** the "80.8% of the population queues" this paragraph used to give is not derivable from 27 against 2,582 — that pair is 99.0% — and the figure the review could reproduce is 2,586 of 3,200 active. The 2,586-of-3,200 reading is the one to quote, and the older pair is left standing as what was written at the time. `0045b30` gives the delivered human assets the version-2 contract the runtime requires; the asset bytes were never wrong, only the four manifests were stale, and the repair leaves every GLB and VAT texture bit-identical while `tools/agents/verify.ts --humans-only` keeps the accepted bounds (worst sole drift 0.001167 m against 5 mm, worst absolute contact 0.009148 m against 15 mm). `10da8ea` lands the agent and vehicle cores with both renderers, the offline certified-geometry tools and eleven test files. All four commits were pushed, and the working tree is clean.

The gate for the graphics landing was re-run after the 2026-09-15 documentation landing, because the earlier run's evidence was captured against the candidate worktree rather than against the merged revision. That run's own log is preserved at `artifacts/verdict-run4/visual.log`, and round 31's review read it: it **completed on 2026-09-16T13:08:43Z**, wrote 44 fresh native-resolution frames with matching hashes, and **its npm header shows the pre-hardening chain — no `--reset` — and it ends on the frame-only line with no `Certified run` line.** The claim this paragraph used to make, that it "certified … three hardware lifecycle runs on the RTX 4090 over D3D11, navigation 158 to 177 ms and replacement 5.7 to 8.6 s", is **false and is withdrawn**: `966930e`, which added `--reset` and `lifecycleEvidence`, landed at 09:47:29 local, three and a half hours after that run finished, and the three records it did leave read 158, 168 and 158 ms rather than a 158-to-177 range. What that run establishes is 44 fresh frames against a stated build; it does not establish a certificate, and the certificate owed to this deliverable is the one the current capture produces. Two earlier attempts failed on wall-clock bounds inside the instrument rather than on the application, and both bounds are fixed: the settle deadline is now denominated in the frames its predicate consumes, and each capture spec's ceiling is derived from the captures it makes.

**That certificate does not bind to the current revision**, which an independent review established in round 29: its build is `index-Wbd7IHvh.js` from a tree at `7c6bd95`, and three source commits landed during its four-hour capture. The 44 frames whose digests it records are intact on disk, and the native inspection recorded earlier binds to a different capture again (`index-BP1Vm0F-`). So the acceptance requirement that a sweep and a native inspection bind to the final revision is **open**, and a final capture on the frozen revision is owed once the landings in flight settle.

Owner disposition, 2026-09-16: `f4e96f0` lands and is pushed the last way a certificate could carry the wrong city's identity. `sceneTreeDigest` in `tools/visual/verify-output.ts` folded in each served file as its path, its size and its modification time; it now folds in the path and the SHA-256 of the bytes. **The payload figures in this paragraph are the ones measured that day, before the incident below: 137 files and 371,228,426 bytes, about 0.4 s per call and so about 0.8 s per three-hour gate run. The payload the 2026-09-16 capture actually pins is 84 files and 214,114,015 bytes, digest `ddd21ee1b4feb7f7…`, because the human agent set is missing from the rebuilt `data/` — see the paragraph on that below, which round 31's review established.** What that bound could not see is a tile rewritten in place at the same length and the same modification time, and the check that was supposed to catch it could not see it either: the case in `test/visual-instrument.test.ts` rewrites a three-byte file and passed whenever both writes landed in one clock millisecond, which it did in 11 of 12 runs on `main`. So this retires an intermittent gate, not a broken one. The content digest makes the case deterministic, the test is unedited at 31 cases, `npm test` was 53 files and 384 tests at that revision, `npm run build`, `npm run typecheck` and `npm run audit` are green, and the build bytes are unchanged because `tools/` is outside the bundle graph.

Owner decision, 2026-09-16, taken with both alternatives in front of them: the 200-vehicle half of the performance target is not to be bought by leaving `main` red. Keep `main` green at the fleet it can actually carry and record 200 as measurably infeasible in this network, named as such wherever the target is quoted. The measurement it rests on: the planner correction that let the bus spawn took the peak from 94 bodies and no buses to 153 with 45, mix unchanged and crossings 311 to 437, and on top of the spacing fix it turned `test/vehicle-spacing.test.ts` red with 5 overlapping pairs at tick 2,775, so it was reverted rather than landed. The cause is room, which is the network contract's own sentence — its policy "is not evidence that 200 vehicles have acceptable throughput" — and the binding constraint is that a bus whose collision width is 3.28 m cannot fit the lanes this network gives it. The 3,000-pedestrian half of the target remains a target to meet, not one to record as infeasible.

Round 29's four blocking findings are disposed as follows, and the disposition is why the acceptance criteria below stay open rather than a reason to call them closed. **B1**, that vehicles interpenetrate and nothing measures spacing, is closed on `main`: `5e8c266` made the measure part of the type, so `Leader.kind` is `body` for another vehicle — its gap being the free distance between the two collision envelopes — and `halt` for a point on this body's own route, which keeps the contract's `stopGapM`; `test/vehicle-spacing.test.ts` then measures oriented collision boxes re-derived from the published pose buffers and the delivered envelopes rather than from the law's own idea of a gap, and requires every overlapping pair it does find to be head-on between lane centrelines within the two bodies' half lengths. Its residual is the drawn count, which is the owner's decision above. **B2**, that the signature shot is unreachable and the crowd interpenetrates, and **B3**, that the frame interval and the vehicle half of the population fail, are being worked against `f4e96f0` by named lanes. **B4**, that nothing binds to the integrated revision, is owed by this coordinator and is paid after the landings settle, not before them.

One correction to the record above, found while routing that work: `test/vehicle-spacing.test.ts:24` and the message of `5e8c266` both say the spacing mutation "is recorded in `docs/learning/gate-proofs.md`", and on `f4e96f0` **it is not there**. The entry exists as +42 uncommitted lines in `artifacts/spacing/wt`, and that is the second time a claim about this file has outrun the file. It is being landed with a red control reproduced on the current revision, and the whole class of "recorded in gate-proofs" claims is being checked rather than this instance alone.

The component of the crowd fix that closes **46,615 overlapping pedestrian pairs to 253, deepest penetration 0.540 m to 0.023 m** was built and deliberately withheld from `5e8c266`, whose message records why: it deadlocks the crowd, crossings falling to 205 and completions to **0**, because `blocked` forces `ramp = 0`, which short-circuits `orcaVelocity` so that a pressed walker holds velocity (0,0), with the four largest queue heads being two walkers facing each other on the two directions of one way. It waits on the representation change its own author named — **a per-section lane index carried in the route** — and it sits uncommitted in `artifacts/spacing/wt` with `test/pedestrian-overlap.test.ts`, `tools/agents/pedestrian-overlap.ts` and `tools/agents/pedestrian-flow.ts`. `artifacts/crowd-stacking/wt` holds a second such payload: the placement tally that makes the drawn count honest, its spec, and the stall and stacking instruments. Both payloads are copied to the ignored `artifacts/unlanded/` — the tracked diff and the untracked files — so that removing those worktrees cannot lose them, and both are assigned to the lanes now working on the crossing and the crowd.

The **controls-driven flythrough is not on `main` at all**, and that is a third such payload: `artifacts/flythrough/wt` and `artifacts/flythrough-crowd/wt` hold an unlanded prototype of it — `playwright.flythrough.config.ts`, a populated variant, and `tools/flythrough/{driver,plan,flythrough.spec,probe.spec}.ts` — and nothing about it is committed. Phase 10's item 33 requires it, so a lane is now landing it on the current revision, running it with `?agents=1`, and making its check go red when the input path is not driven, because a flythrough check that passes while nothing moves the camera proves nothing. All three payloads together are 45 files and 589,435 bytes under `artifacts/unlanded/`.

Owner disposition, 2026-09-16 (evening): the revision is frozen at `30c7b01` and the 44-frame verdict capture is running against it, expected to take about three hours from 17:35 local. The revision carries twelve landings since `f4e96f0`, all pushed, each with the four non-visual gates green when it landed: the scene digest bound to the served bytes, the dusk post chain's stillness fix, the crossing's black-surface attribution, the population URL seam and tick-phase observer, the World style control driven by a real pointer and keyboard, and the gate proofs and register entries for the defects those fixes answer. Every acceptance claim that rests on the 44 frames is owed only after the run certifies.

Two pixel-lane specifications were changed by this coordinator at freeze time, and that is recorded rather than left implicit. `tools/visual/hero.spec.ts` and `tools/visual/style-picker.spec.ts` read the World style control as a native `<select>` — `locator("option")` and `toHaveValue` — and the shipped control became a button plus an in-page listbox in `c11f6b5`, so both assertions described a control that no longer exists; the capture failed on the first of them within thirty seconds. The replacements drive the real control and read the option rows as elements in the document, and assert the selection through the `data-style-id` the control writes onto itself. Driving the real control is the stronger check and is why the control changed at all: a native select's popup is drawn by the browser process, outside the renderer's hit testing, so no synthesised pointer input can reach one of its options. This change carries no independent review, and the first full run after it is what certifies the spec-side assertions.

One lane destroyed the primary checkout's `data/` and `node_modules/`, and both are rebuilt. `git worktree remove --force` was run on a comparison worktree without first unlinking the `data` and `node_modules` junctions inside it, and git's recursive delete followed them into the primary — the failure `docs/policies/local-rules.md` names. Nothing in `main`, in any commit or on any branch was harmed. `data/` was rebuilt with `npm run data:setup`: the PLATEAU archive had already been re-fetched and verified at 649,322,807 bytes, and the outputs deterministic from it came back at their accepted hashes — the vehicle manifest as `5b29efc7977949db1611273c8fb294b3ab23a1a55c8f42316cad9b5309c94a01`, which is the accepted raw-V4 manifest, and roads and pavements as `3fe126cd…` and `5d2a9150…`, which are their Review 10 pins. The Overpass extract is live data and could not be re-fetched byte-identically, so `test/paint-support.test.ts` failed by name against its standing pin, which is the behaviour `AGENTS.md` requires of it. The reviewed `network.json` survived byte-identical in three older worktrees and was restored from one of them, returning the pin `314fac8433…`, and the network's dependents were rebuilt against it; the suite is 56 files and 401 tests green. **The OSM extract on disk is still today's**, so a future `npm run data:network` would produce a network that is not the reviewed one and would fail that pin again: that is correct, and it is recorded here rather than discovered later.

The goal's performance measurement has been taken and it does not hold. At 1920×1080 with 3,000 pedestrians and 59 vehicles drawn, the delivered frame interval is **p50 83.3 ms, p95 183.4 ms** — about 12 fps — while the frame's own cost is p50 7.7 ms, so rendering is not the limit. The fixed step is: the population tick costs p50 75.7 ms per frame and 152 of 262 frames hit the loop's five-step catch-up cap — **round 31's review could not reproduce that pair and bounded it to 143–202 of 262 from the record, and the artifact it came from, `artifacts/frame-budget/…`, was destroyed by the coordinator's own worktree cleanup, so the number is quoted as the lane reported it and is no longer independently checkable; the qualitative claim it carries, that a majority of frames hit the cap, is what the paragraph rests on** — so the simulation sets the interval. The tick's measured phase split accounts for the whole tick: the per-body avoidance solve is **44.1%** (5.521 of 12.524 ms), request assembly 17.7%, integration 10.4%, the gate pass 9.1%, placement 7.5%, vehicles 7.2%. Inside the avoidance solve the named defect is the neighbour query: `CellGrid.neighbours` returns every body within the 8 m radius — measured mean 97, p95 114 per body — while `PEDESTRIAN_DYNAMICS.neighbours` is 8, so ORCA uses 8 of 98 and the query visits all of them. Same instrument, measured ceiling: 500 and 1,000 drawn pedestrians are both vsync-clamped at a 16.7 ms interval and 3,000 gives 83.3 ms, so the largest population that fits lies between 1,000 and 3,000 and is not pinned. The tick scales at roughly 4.2 µs per pedestrian per tick, which is 12.6 ms of a 16.67 ms frame at 3,000. The lane landed the instrument, the additive phase observer and the `?pedestrians=`/`?vehicles=` seam, did **not** move the tick cost, and built no gate over it, so the tick budget is an unmet criterion reported as one rather than dressed as a pass. The owner's earlier decision governs the vehicle half; the pedestrian half now has the same shape of answer available, and the lane's three honest options are a lower drawn population, a coarser avoidance model, or moving the crowd's neighbour search off the critical path.

The signature criterion is worse than it was recorded, and the change that answers it is unlanded. Measured on `f4e96f0` over a 72,000-tick window at the pinned network and the population's real seed: the worst sample carries **982 distinct positions for 3,000 active bodies**, the largest stack is **205 bodies at one centimetre**, one sample holds **227,214 overlapping pairs**, **no walker ever entered the authored diagonal**, and the nearest any walker came to the crossing was **31.228 m** — that last number reproducing what three earlier lanes recorded, on a different instrument, which is the control that the instrument and the network are the ones claimed. Against that, `worker/scramble` at `7a9538c` gives a walking route a lateral position and builds the crossing into the route shape, and the repository's own `population-route-mix.ts` reads **1,580 of 3,000 slots driving a crossing route, against 0**. It is **not landed**: the lane got no after-arm run, because the hold issued during the data loss killed it, so the crossing's occupancy, the changed tree's distinct-position count, its completions, its frames, and a possible per-tick cost from two trig calls per sample are all unmeasured. Landing a population change that routes walkers into a compound which has jammed four times before, with no measurement of whether it jams again, is the change this plan's own history says not to make. It waits on its branch with its instrument and lands with its after-arm or not at all. **Its branch has since been rebased onto `main` at `d1e340a` and typechecks clean there** — it was 36 commits behind, and the rebase applied with no conflict, so the only thing standing between it and a landing is the measurement it still owes, not an integration problem. **The after-arm does not need the human agent set, and an earlier version of this paragraph said it did: `createPopulation` takes the network, the *vehicle* manifest and the settings, and reads no human asset at all — the delivered human GLBs and manifests are loaded by the renderer for a `?agents=1` browser run, not by the simulation.** That was measured rather than reasoned: `node tools/agents/crowd-spread.ts --pedestrians 3000 --vehicles 200 --ticks 600 --sample-every 200` runs green in the crossing branch's worktree right now, with the human set absent, reporting a tick p50 of 11.589 ms and its own `verdict: "pass"`. So the measurement can run the moment the capture releases the machine, in parallel with the Blender bake rather than after it, and only the flythrough and the populated lane wait on the humans.

Two recorded claims are corrected rather than carried. The far-LOD defect — "the far level renders no pedestrian at any distance" — is **not supported by its evidence**: measured from the preserved frames, 99 m with the crowd on against the crowd off changes 201 pixels (0.0218%), and two crowd-on frames two seconds apart differ by 4 pixels at 99 m and 7 at 70 m, with per-level counts of `0/0/3000`. That is a crowd which has not reached the crossing by tick 1,500, not a far level failing to draw, and a decisive test needs bodies in frame at tick 35,000 or later with the per-level counts beside them. And the controls-driven flythrough exists as an unlanded lane: `worker/flythrough2` **defines a 46-frame route over four legs through the real controls on the hardware renderer**, with a red control that raises the same pointer and wheel events with zero deltas, and its own measured aim — at the browser's real default seed the crowd's dense knots all sit on the edge of the AOI rather than at the crossing, and vehicles thin from 59 at tick 2,400 to 14 by tick 20,000, so a flight has to leave early to have any traffic in it. **"Carries 46 frames" would be false and round 31's review caught this paragraph saying it: no frame of that route has ever been captured, because the lane was stopped by the hold issued during the data loss and has not run since. What exists is the driver, the route, the frame-sequence checks and their twelve unit cases, typechecked and committed; what does not exist is a single photographed frame.** The branch has since been rebased onto `main` at `ef07fd7` and typechecks clean there, like the crossing change, so both unlanded branches are integration-ready and neither has been photographed. The review also found that `tools/flythrough/plan.ts` cites `artifacts/flythrough2/aim.ts` and `aim.json`, neither of which exists — the tool is `tools/flythrough/aim.ts` with its output defaulting to `tools/flythrough/aim.json` — which is a citation to fix when it lands rather than a reason to hold it.

The verdict lane's own cost is now the critical path, and it is a defect to be measured rather than scheduled around. `playwright.config.ts` runs the pixel lane at `workers: 1` with a reason — that several browsers would fight for one software rasteriser and it would not be faster — and that sentence is reasoned rather than measured. The two style sweeps and the hero specification are independent, so running them concurrently on a 32-core machine may turn three hours into about one. It was deliberately not tested before this freeze. The first thing owed after the capture is the two-arm measurement of it, because a second capture is likely: the crossing change has to land with its after-arm, and any landing invalidates the frames.

**Owner instruction, 2026-09-16: the three-hour verdict lane is over, and this paragraph is where that lands.** The owner's words were "why does it take 3 hours? That is ridiculous and unacceptable", followed by "if you can use multi-core, don't just use single core. If you can use GPU, don't use CPU." **The measurement that makes it a defect rather than an opinion**: the capture running against `22ef994` held **7.7 of 32 logical cores continuously**, which is roughly **54 core-minutes of CPU rasterization for every 1280×720 frame**, while an **RTX 4090 (driver 32.0.16.1664) sat idle** — forty-four frames at 6.8–8.0 minutes each. **The plan's own record already knew the size of the tax and had chosen to pay it**: `docs/policies/local-rules.md` carries the deliberate exception that the 44 appearance frames stay on SwiftShader because "a pixel set captured on one renderer cannot inherit a review written on another", beside the measurement that `npm run visual:hardware` returns 44 frames in about **3.6 minutes** against the software lane's **302.4 s per frame**. That is a recorded trade — machine-independent certified pixels for a roughly fifty-fold tax — and the owner has now overridden it, so the coordinator stopped the run rather than spend the remaining two to four hours on the sweep. **What was preserved is the software arm of the comparison the fix needs**: the eight hero frames, the style-return frame, the per-capture intervals and the frame manifest, under `artifacts/verdict-run6-stopped/`. A lane owns the change: promote or rebuild the appearance gate on the hardware renderer, run the three independent specification files concurrently rather than at `workers: 1`, measure both arms with their load, make the renderer assertion still fail in seconds on the wrong renderer with the mutation recorded, and rewrite the rule that this instruction supersedes rather than contradicting it. **What is genuinely lost is stated rather than glossed**: certified frames stop being machine-independent and become this GPU's pixels, so the certificate has to name the renderer, the GPU and the driver, exactly as the lifecycle lane already does, and every review stays bound to the digest of the bytes it inspected. The appearance review was owed for this capture in any case, so no review already written is invalidated by the change.

**It landed, and the numbers are the whole argument: the gate went from about three hours to about five minutes.** `f741fa7`, merged as `740beca`, 31 files, +726/−364, and it is the largest single reduction in this deliverable's cost of verification. **The whole gate certified twice, at 283.2 s and 309.8 s**, against an estimate of three to three and a half hours for the same forty-four frames. The per-capture split, measured with one instrument against both renderers at the same pose and on the same build, is where the time went: frames 1,035 ms → 207 ms, tile refinement 64.9 s → 1.1 s, the controls' own corrections 342.7 s → 2.7 s, settle 26.4 s over 190 frames → 0.22 s over 204, screenshot 55.7 s → 0.23 s, and ten hero captures **4,438.2 s → 88–91 s**. Concurrency was measured rather than assumed, and the old comment's claim — that more browsers "would fight for one software rasteriser and it would not be faster" — is answered rather than deleted: same tree, same four tests, `workers: 1` **238.9 s**, `workers: 3` **144.0 s**, which is what landed, and `--fully-parallel --workers: 4` a further **114.7 s** that was measured and deliberately not adopted because adopting it would have edited the config after the certificate had bound the tree. **What hardware rendering changes about the pixels is stated rather than glossed**: on the plan's own metric, 2.4–8.4% of pixels move beyond 6 summed over RGB between the preserved software frames and the hardware frames at the same pose, with a mean channel difference of 0.4–1.0 of 255, against **90.5%** for a real style change and 0.000% for a file against itself — and both arms agree exactly on sun position, Tokyo clock and `tiles.cachedBytes`, so the difference is edge coverage and resolve rather than a different scene. Native crops of the crossing pair are side by side in `artifacts/gpu-lane/pixel-diff/`. **What replaced machine-independent bytes is a binding, not a promise**: `tools/visual/gpu-identity.ts` reads the adapter and driver at `--begin` (the vendor's `nvidia-smi` first, Windows' device record as the fallback, and the certificate says which answered) and `certifyVisualRun` refuses a frame set drawn elsewhere, so the refusal for a software fallback and the refusal for another adapter were each watched red before this landed. **Rasterization is no longer the critical path, and what replaced it is named**: page boot and first tile refinement cost 30–36 s per specification file across three page loads, each capture 1.4–4.3 s, and the PNG checks 30–35 s per file, with GPU duty at 15–41% throughout. **Three sentences above are superseded by this and are left standing as the record of what was believed**: the split-by-renderer paragraph whose `playwright.config.ts` description says the 44 frames run "on SwiftShader" with "no switch that could move this lane to hardware"; the review paragraph's "both capture specifications fail in seconds on a renderer that is not SwiftShader"; and the earlier disposition that quotes `npm run visual:hardware` returning "44 frames in about 3.6 minutes" — that script and its config are **deleted**, because two lanes claiming to be the appearance gate was the outcome to avoid. Four findings are carried rather than fixed: orphaned `chrome-headless-shell` processes recur when a parent pipeline is cut early (12 and then 16 were cleared by hand, none remain) and the cheap fix is a `--reset`-time check against `tools/visual/process-ownership.ps1`; the FLEET-CANON block in `AGENTS.md` still calls `npm run visual` a three-hour command because it is generated from `../fleet/FLEET.md` and must be changed at source; the fully-parallel arm is measured and unadopted; and `src/render/renderer.ts` still advises SwiftShader flags in its headless-boot error, which remains correct advice for a machine without a GPU.

What Phase 10 still owes, in the order it is owed: the capture that is running now and its certification; native-resolution inspection of all 44 frames, each bound to its digest, by lanes that each own a subset; the flythrough landed and run on the frozen revision, with its frames inspected at their own size; the style switch, already verified in the live app by `c11f6b5` and owed a frame-level record; and an independent read-only review of the integrated revision with its material findings resolved. Nothing is complete until the capture certifies and the review passes.

**Superseded on 2026-09-17: every item in the list above is now done or in flight, and the list is kept as the record of what was owed when it was written.** The capture certified on `654fcc5` as runId `94fc1aa78b0af8f9`; all 44 frames were inspected at native resolution and are bound to their digests, and the two `style-return` frames were opened beside them as lane evidence rather than as certified frames — they are at the **approach** pose, not the crossing, and the certificate does not list them (the Phase 10 entry below carries the two measurements that establish it); the flythrough was landed, failed its own check, was adjudicated twice, repaired in both the check and the route, and passed a run of 46 frames whose inspection is in flight; the style switch has a frame-level record (the dusk round trip restored every scene crop byte-identically); and two independent reviews are running against `39ca459`. The current status is the dated block at the end of this document.

**Round 31's** independent review found, in its first pass, that the rebuilt `data/` is **53 files and 157,114,411 bytes smaller than five documents claim**, and it named the cause: the delivered human agent set under `data/scene/agents/` is gone. `npm run data:agents` is **not** part of `data:setup`, so the post-incident rebuild could not restore it; that directory now holds only the three vehicle GLBs and `vehicles.json`, while `src/world/agent-assets.ts` requests `commuter-male.json`, `office-male.json` and `commuter-female.json`, and `tools/agents/bake-human.py` writes 33 files into it. The figures the documents quote — **137 files, 371,228,426 bytes, digest `0222c0566d507e4b…`** — describe the payload as it was before the incident and are corrected to the payload the running capture actually pins: **84 files, 214,114,015 bytes, digest `ddd21ee1b4feb7f7eb1e2bcda4c39bede72757756fc582686aa488870257d500`**, which the review reproduced independently from disk as `data/scene` 83 files / 203,874,605 plus `data/network` 1 file / 10,239,410.

**That repair is deliberately queued rather than done.** The verdict lane is unaffected — a run without `?agents=` switches the population off and never loads the human assets, which is why the capture that is running now is still valid — but adding those 33 files to `data/scene/` while it runs would move the very digest the certificate re-derives at `--end`, and the run would be refused after three hours of frames. So the human set is restored **after** the capture certifies, by `npm run data:agents` once the MPFB and MakeHuman archives the builder needs are re-fetched into `data/agents/`, and the scene digest is re-measured then. Until that happens every `?agents=1` lane is blocked: the populated lane, the crossing change's after-arm measurement, and the controls-driven flythrough, which the review correctly identified as the next thing to fail on missing assets.

**The style round trip is now measured rather than asserted, and it has a control — and the frame's target changed when the return block was fixed, which corrects this paragraph's own record.** The comparison below belongs to the previous capture and to the previous specification: that run's fifth frame was `style-return/return-satellite-dusk.png` (`56e337704d37…`), taken after the hero block switched the style away to cartographic and back again, and comparing it against `hero/hero-satellite-dusk-approach.png` — same pose, same style, the frame the round trip set out to restore — moved **0.345% of sampled pixels beyond a threshold of 6 summed over RGB, with a mean absolute difference of 0.369 out of 255**. As a control on the instrument, the same comparison between the satellite and cartographic frames at that pose moves **83.58% of pixels with a mean difference of 32.948**, so the metric is sensitive to a real style change and the round trip is 89 times closer than that. **The specification fixed at `48c19c5` presses `Home` rather than `End` in the return block, and `Enter/Home/Enter` reaches the registry's first row, so the current capture's fifth frame is `style-return/return-cartographic-dusk.png` (`94fd0b6a59a1a93f…`, written 21:06:57) and the round trip it records is satellite → cartographic rather than cartographic → satellite.** Its comparison is therefore against `hero/hero-cartographic-dusk-approach.png` at the same pose, and that comparison is owed rather than inherited: the assertion order in the spec is what makes the frame what it is — `beforeReturn.style.id` must be `satellite`, the listbox must open on Enter, and the committed value must be `cartographic` — so the frame exists only because that sequence passed.

Native inspection has begun against the running capture rather than waiting for it, so a defect would be found while the run can still be stopped and a clean continuation is left in the record. **Seven frames are open — the four dusk hero views, the dusk style-return, and the noon pair — each bound to the digest of the bytes inspected:** `hero-satellite-dusk-crossing.png` `f6b5ed939e00…`; `hero-satellite-dusk-approach.png` `ea7e9f048b8b…`; `hero-cartographic-dusk-crossing.png` `4de4eedd81f5…`; `hero-cartographic-dusk-approach.png` `d11f829f101a…`; `style-return/return-satellite-dusk.png` `56e337704d37…`; `hero-satellite-noon-crossing.png` `3d207940a52e…`; `hero-satellite-noon-approach.png` `8cff9202aec3…`. The noon pair is the daylight counterpart of the dusk pair at the same two poses: the crossing reads with its zebra bands and diagonal against a bright block, the signage is present but no longer the dominant light in the frame, and the facades show their photographic content plainly rather than as dusk silhouettes. The foreground roofs at this framing are the mottled aerial photography stretched over LOD2 geometry, which is the known PLATEAU limitation item 35 exists for and not a new finding. The crossing frames show the scramble's diagonal arms in yellow, the zebra bands on both approaches, legible emissive signage (`TOKYO`, `SHIBUYA`, `TSUTAYA`) against the sunset band, the attribution line, and **no black surface across the crossing** — the defect `d00c84b` fixed. The approach frames show the block obliquely from above, coherent massing, buildings on the ground, and the crossing legible in the middle distance. Both pairs are controlled comparisons across the two styles at one pose: the framing is identical, the photographic facades give way to generated window grids, and **every road marking survives the switch**. **The noon frame is the strongest single still for the markings criterion**: under daylight the zebra bands, both diagonals and the plaza paving all resolve without the dusk grade over them, which corroborates round 30's crop-level adjudication on an independent frame from the frame the adjudication examined. One observation to carry: at the approach framing the cartographic style carries far less emissive signage than satellite — a handful of boards against a spread — so the "reads as Shibuya rather than a generic city" property at that distance is carried mostly by the satellite style, which is a palette difference to weigh rather than a defect to fix. All six carry the known near-field pavement wash-out at grazing angles and roof shadow away from the sun, which are recorded findings rather than new ones, and none is adjudicated beyond the bound the earlier inspection stated: any read of these frames through the image tool returns 1066×600 previews of 1280×720 sources, so single-pixel detail, aliasing and temporal crawl are outside what that reading signs off — which is why the round-trip comparison above was made from the pixels themselves and not from a preview. **What remains is the rest of the hero specification — its ledger records six of ten captures — and both sweep specifications, which have not started; those carry the sweep's eighteen views per style, the bulk of the set.**

The human set's repair is **known-good rather than hoped for**, checked on 2026-09-16: `tools/agents/build.ts` fetches both archives itself from their pinned URLs and refuses anything that does not match its recorded length and SHA-256 — `mpfb2-80919fa.zip` at 44,851,738 bytes from `codeload.github.com`, and `makehuman-system-assets.zip` at 280,737,770 bytes from `files.makehumancommunity.org` — into `data/agents/source`, then drives Blender with `data/agents/blender-user`. **Both were re-checked live while the capture ran: the MakeHuman pack answers HTTP 200 with `Content-Length: 280737770`, exactly its pin, and the MPFB archive answers HTTP 200 as `application/zip` with no length header, codeload streaming it — that one is a commit-addressed GitHub archive, which is immutable, and the builder verifies its SHA-256 before use in any case.** Nothing in that chain needs a byte that was destroyed, so `npm run data:agents` restores the set the day the capture releases the machine, and the scene digest is re-measured and recorded then.

Round 31's review also established, as its fourth finding, that the certificate binds the build and the served scene and **nothing binds the harness** — while the plan requires the complete source, build, data and harness closure before and after the run — and that this stopped being theoretical when `5c32286`, a `tools/visual/**` commit, landed twenty minutes into the running capture. The capture is not invalidated, and that is now a measurement rather than an argument: the lane's Playwright process started at **17:35:33**, which is `run.json`'s own `startedAt` to the second, its preview server at 17:35:34 and its browser at 17:35:35, and no process of that run has restarted since — the only later process on the machine is the coordinator's own command. Playwright resolves its test list and module graph at startup, so the specs that commit touched were loaded before it existed, and the frames written after it (`hero-cartographic-dusk-crossing.png` at 17:59:37 and `hero-cartographic-dusk-approach.png` at 18:06:20) come from that same process. What is missing is the certificate's ability to say so, which is the finding; a lane is fixing it by digesting the instrument the lane actually runs, pinning it at `--begin` and refusing at `--end` with both values named, in the shape the scene digest already uses.

**Evidence was destroyed by the coordinator's own worktree cleanup on 2026-09-16, and the record says so.** Six worktrees whose branches were all fully merged were removed — `reclaim`, `render-state`, `frame-budget`, `post-chain`, `style-ui`, `paint-review` — with their `data` and `node_modules` junctions unlinked first and the primary verified after each. Six branches deleted, which is progress on this plan's own "nothing left on a branch or worktree" requirement. But a lane writes its evidence under `<worktree>/artifacts/`, which is ignored and therefore invisible to both `git status` and the ancestry check that said the branches were merged, and removing them took the bytes with them: **paint-review's six frames, its ten 1:1 crops, its nine decoded reference tiles and its `analysis.json`; frame-budget's `interval.json`, `sweep.json`, `phases-baseline.json` and `phases-subsplit.json`; and render-state's eight arms, twelve frames and six records.** Round 30's review cites every one of those crops by path and digest, and the paragraph above quotes those performance numbers, so both now rest on artifacts that no longer exist: the review is not wrong, its evidence is simply no longer inspectable, and its frames had to be re-captured anyway because they predate `d00c84b`. `post-chain`'s and `style-ui`'s evidence survived only because those two lanes happened to write into the parent directory. The rule this earned is in `docs/policies/local-rules.md`: whether a worktree may be removed is not the same question as whether its branch is merged, and the untracked files under it must be enumerated first — `artifacts/reclaim/preserve-worktrees.ps1` is the instrument. No further worktree is removed until that check has been run on it, including the three that remain live and the four payload trees.

The 36 sweep frames carry the least risk of the three specs, and that was checked rather than assumed while the run was still in its hero phase: `tools/visual/sweep.spec.ts` touches no part of the app's DOM. It sets its style through `/?style=` in the URL, drives the camera through `OrbitDriver`, and reads `window.__mapsHarness` — its assertions are camera-pose tolerances, drawing-buffer size, building tiles and drawn geometry, frame count and size, per-frame stats and an empty console-error list. The defect class that killed `hero.spec.ts` three times, a spec asserting against a control the app had changed shape, cannot reach it, and the third spec, `style-picker.spec.ts`, was re-run standalone and passes. So the remaining risk in this run is the two failures already known — the wall-clock ceilings and the settle budget — rather than a fourth stale assumption.

**Four branches landed and are pushed at `0dfc092`, and the verdict capture is deliberately held until the machine is quiet.** Landed in one batch, each gated on the merged tree: the **gate fixes** (`cb266f7`, round 31's B3 and B4 — the certificate now binds the harness through `harnessTreeDigest`, and the style switch completes its pointer half with a real press on the option row), the **crossing change** (`a800cee`, a lateral position per route occurrence plus a real separation half-plane for an overlapping pair), its **gate** (`831de61`, `test/pedestrian-overlap.test.ts`, the longest case in the suite at ~36 s), and the **flythrough** (`0dfc092`, the lane, the aim tool, its scored output and a route standing on a scored pose). The suite went 384 tests in 53 files to **422 in 58**, all green, with typecheck, build and audit green on the merged revision.

**Why the capture is held rather than started.** At 18:45 the machine was at 66–82% CPU with eight browser processes, and none of them is this repository's: a **sibling checkout (`3d-maker`) is running its own Vite server and Playwright suite** (`node tools/test.js`, `out/wh/scratch/boxes.mjs`). The previous capture died of exactly that condition — a browser that could not create a context in thirty seconds — and the per-test budget is now two minutes, which softens the failure but does not create cores. A SwiftShader lane sharing the machine with another browser suite does not take slightly longer; it takes several times longer, for both. So the run starts when the machine is quiet, and the check is the same one that caught it: system CPU and the count of `chrome-headless-shell` processes, neither of which can be inferred from this repository's own state.

**The crossing change is landed with measured evidence rather than with its author's claim.** Its author could not run the after-arm before landing, and the gate lane supplied one at a smaller scale with two mutations that separate the change's two halves: at 150 pedestrians over 60,000 ticks, on `main` **0 of 150** bodies ever entered the authored diagonal, the largest one-centimetre stack was **7** with 135 distinct positions, **19.33%** of the crowd shared a position and **148 of 150** bodies were in a deep interpenetration; on the change, **2** bodies entered the diagonal (nearest approach 0.121 m), the largest stack was **2** with 149 distinct positions, sharing fell to **0.67%** and deep interpenetration to **53 of 150**. Forcing `sampleRoute`'s lateral offset to zero reds the stacking claims and leaves the diagonal green; forcing `walkToCentre` to the direct descent reds only the diagonal. **Two honest limits are carried with it**: the change still interpenetrates — the gate bounds deep interpenetration at half the crowd, not at zero, and a gate demanding none would be red — and the residual is **in the queues rather than the walking model**, since at the final tick all 27 deep pairs are between two stalled bodies, and at 300 pedestrians 143 of 300 are queued with a 110 s longest wait, piled 450–650 m from the origin early in their routes. That is a different root cause and is not claimed as fixed.

**Those seven digests are now historical and must not be read as the current frame set.** They were taken from the *previous* run, and this run's `hero.spec.ts` clears its own output directory as it starts, so those bytes no longer exist anywhere: the paragraph above is the record of what was inspected and concluded, not a description of what is on disk now. **Inspecting a capture in flight is stranded by a restart, and that is now measured rather than assumed.** The four dusk hero frames of the earlier run were inspected at their own size and bound to their digests — and then the run was restarted at 19:16:02 after the return-switch defect, and `hero.spec.ts` clears its own output directory as it begins. Those bytes, and the crops cut from them, no longer exist anywhere. **The observations stand** — what was seen in them was seen, and the conclusions below rest on it — but their digests now describe deleted files, and a later reader must not treat them as describing what is on disk. The lesson for this deliverable's own workflow: **a mid-run inspection is only valid until the run restarts, so the inspection that counts is the one bound to the final certified capture**, and inspecting early is worth doing only for its defect-catching value, never as acceptance evidence. The instrument to do the final one properly now exists: `tools/inspect/crops.ts`, landed at `179f775`, cuts eight regions that **tile the frame exactly once each** so that opening all eight is a native review of the whole frame rather than a sample, and binds each crop and the frame itself by SHA-256 in a manifest, so a review bound to those digests is stranded by regeneration instead of silently inheriting it.

The run in flight carries its own digests and will be inspected as a set when it certifies.

**Owner decision, 2026-09-16 (evening), on the pedestrian half of the performance target.** The 60 fps target is measured and does not hold, and the owner has decided it is to be **recorded as measurably infeasible at 3,000 drawn pedestrians rather than pursued with a design change**, with the ceiling stated instead. The measurement it rests on: at 1920×1080 with 3,000 pedestrians and 59 vehicles drawn the frame interval is **p50 83.3 ms / p95 183.4 ms**, while the frame's own cost is only **p50 7.7 ms**, so rendering is not the limit; the tick is **p50 75.7 ms per frame** with **44.1%** of it in a per-body avoidance solve whose neighbour query visits a measured **97 bodies per body where ORCA uses 8**, and 152 of 262 frames hit the loop's five-step catch-up cap. The ceiling is **bracketed and not yet pinned**: 500 and 1,000 drawn are both vsync-clamped at a 16.7 ms interval and 3,000 is not, so the largest population that holds 60 fps lies between them — and the plan now owes pinning it rather than leaving it a bracket, which is a measurement and not a design change. This sits beside the earlier decision on the vehicle half — keep `main` green at the fleet it can carry and record 200 as measurably infeasible — and together they mean the criterion is reported with a measured cause and a stated bound rather than met. The three design levers the measuring lane named remain recorded, and **one of them has been mis-sized by this plan and by that lane, which is corrected here**: the neighbour-policy change was dismissed because "3.8 ms does not close a 12.5 → 16.7 ms gap", and that comparison is wrong. The tick does not have to fit the whole frame budget — it has to fit *alongside rendering*, which the same measurements put at p50 7.7 ms, so the tick needs to come down from 12.5 to roughly **8 ms, a 36% cut**, and the measured saving is 3.8 ms. Worse, the system is in a catch-up spiral rather than a simple overrun: a frame that cannot fit its due steps runs the loop's maximum of five, which makes the frame longer, which makes more steps due, and the equilibrium lands at p50 83.3 ms with 152 of 262 frames at the cap. That means there is a **cliff rather than a slope** — below roughly one step plus rendering, the loop is stable at 60 fps and only one step comes due per frame; above it, it collapses to the cap. A lever that removes 3.8 ms of a 12.5 ms tick is therefore the right size to matter, and the question of whether it clears the cliff is a measurement nobody has made. A lane is on it. **What that lane is doing, so a later session can pick it up rather than restart it:** branch `worker/tick-cost`, worktree `artifacts/tick-cost/wt`, with A/B arms under `artifacts/tick-cost/arms/` — a `baseline` copy of `cell-grid.ts` beside a `ring` variant — component-level probes under `probes/` and their raw output under `raw/`. It is modifying `src/agents/population/cell-grid.ts` and `pedestrians.ts`, which is the neighbour-query lever rather than the allocation hunt listed second below, and it was told to report a lever that does not move the number as a result rather than to argue it away. **Any source change it makes must land after the capture certifies, not before**, because the certificate binds the build bytes and main's source currently builds to exactly them. **The complementary lever is the frame's own 7.7 ms, and it is deliberately not being measured at the same time.** The cliff sits at one tick plus rendering, so cutting either side moves it: if the per-frame cost fell from 7.7 ms to 3 ms the current tick would already fit, and the sunset post chain at 1080p — bloom, SSAO and TAA over the full viewport — is the obvious candidate. Two lanes measuring timing on one machine produce two wrong answers, and the running capture already owns it, so the tick lane goes first and the frame-cost lane follows on a quiet machine. The earlier instancing work took this app from 22.5 to 56.5 fps, which is the evidence that the frame's own cost is movable rather than fixed.

**The tick lever's first result is one accepted change and one rejected one, and both are the lanes' own numbers rather than the coordinator's hopes.** The lane profiled the tick afresh and found four behaviour-preserving repairs: the route's edge walk is a linear scan of `route.edges` from index 0 several times per body per tick where `route.starts` makes it a binary search; `gateAtOrAhead` and `gateBeyond` built a fresh closure per call where `route.gates` is already in ascending occurrence order; `buildPassage` rebuilt a `Map` of every lane in the network, thousands of entries, to resolve the handful of ids one route names, which is **8.9% of the tick's sampled self time**; and `sampleEdge`'s arc table went through a `WeakMap` once per call where `sampleRoute` samples one edge three times in a row. **Those four are exact, and that is proved rather than argued**: `population-run.ts --digest-only --ticks 3600` gives `aee25d45a714988834dfc38d696522d50903f415f7aabe77ec3ab907230140a3` on the base and **the same digest** with them. The fifth change, a ring-ordered k-nearest neighbour query returning the 8 **nearest** where the grid returned the 8 **lowest slot indices**, is **rejected, and the lane rejected it on its own measurement**: the avoidance row is a wash — means **7.67 against 7.59 ms** — because the walk visits 99.3 bodies instead of 62.3 but the solve then builds 14,747 real half-planes against 1,416 degenerate ones, and it changes the crowd (`queued` 656 → 833) and the run digest (`ce9deb4787bf…` against `aee25d45a714…`). **A change that buys no time and moves behaviour is not a trade.** What the accepted four buy is **not yet measurable as a whole-tick figure**: on a machine the capture was loading at 100%, interleaved arms read base medians 20.896 / 26.299 ms against 16.559 / 27.801 ms, which is −21% paired one way and +6% the other, and the lane quoted the mean ratio of **1.064** while stating plainly that every absolute millisecond there is contention. The one decisive row is the gate: **8.63% and 7.80% of the tick on base against 2.07% and 1.50% with the four, a 4.6× cut, consistent in both pairs**, with the request row at 4.76 against 3.48 ms. So the decisive arms decide whether this lands, and the coordinator stopped the verdict capture to give them the machine: **a few percent does not clear the cliff**, which needs the tick down from 12.5 ms to roughly 8 ms, and if the arms say the whole tick barely moves then that is the result to record.

**The post-capture runbook, in order, so that executing it needs no re-derivation.** (1) Confirm the certificate: `artifacts/visual/complete.json` exists, names both lanes' renderers, the build bytes, the scene digest and the harness digest, and that scene digest is `ddd21ee1b4feb7f7…` for the agent-free payload. (2) Restore the human set with `npm run data:agents` — it reads the archives already fetched and pin-verified in `data/agents/source` and refuses a mismatch, and `tools/agents/build-human.py` writes **into `data/scene/agents`**, which is a served mount and therefore the reason this waits for certification. **Verify the bake rather than assuming it**: three variants (commuter-male, office-male, commuter-female) each at three LODs with `glb`, `positions.f16` and `normals.f16` beside a manifest, plus the three source blends — **33 files** — and `src/world/agent-assets.ts` requests `commuter-male.json`, `office-male.json` and `commuter-female.json` by name. Then re-measure `sceneTreeDigest()` and record the new payload, because that digest will no longer be the one the certificate binds, which is correct: the 44 frames are of the agent-free city and the population never loads without `?agents=`. (3) Run the crossing change's after-arm on its rebased branch — it does **not** wait for the human set, measured above — with `node tools/agents/crowd-spread.ts --pedestrians 3000 --vehicles 200 --ticks 72000 --sample-every 600`, then `crowd-occupancy.ts` with `--sample-every 120 --warmup 6000`, then `population-run.ts --ticks 21600`, and compare against the `f4e96f0` column — 982 distinct positions, a 205-body stack, 227,214 overlapping pairs, 0 on the diagonal, nearest 31.228 m, crossed 7,172 / completed 1,753. **This step is already done and is kept only for the wider window it names.** The crossing change landed at `a800cee` and its gate at `831de61`, with the after-arm supplied at 150 pedestrians over 60,000 ticks: diagonal entries **0 → 2**, largest one-centimetre stack **7 → 2**, crowd sharing a position **19.33% → 0.67%**, deep interpenetration **148 of 150 → 53 of 150**, and two mutations separating the lateral offset from the crossing descent. What has never run is the same measurement at the **acceptance load** — the three commands above, whose baseline column on `f4e96f0` is 982 distinct positions, a 205-body stack, 227,214 overlapping pairs, 0 on the diagonal and a nearest approach of 31.228 m. Running it would strengthen the record and cannot alter the landing; it does not block anything. The human set is not needed for it, and it and the Blender bake both want the machine's cores, so they are ordered rather than parallel. (4) **Already done, and what replaces it.** The gate fixes landed at `cb266f7` — the certificate binds the harness, and the style switch completes its pointer half — and the frame-crop instrument landed at `179f775`. What is still unlanded is the **vehicle-run gate** on `worker/vehicle-gate` at `cd3cbd9`, which is green with a real lane-clause red control but needs a rebase (its `tick.ts` hunk does not apply to current `main`, tested) and carries a source change, so it must land before a capture rather than after one; and the **tick-cost lane's** work on `worker/tick-cost`, which is the performance question the owner asked to have pursued. (5) Inspect the remaining frames at native size, each bound to its digest. (6) Clear every branch and worktree, running the untracked-evidence check first — the rule the loss above earned. **Two of these steps have a lane already standing by rather than needing a fresh brief.** The independent reviewer that produced round 31 is resumable by agent id **`c2fcdcf1-66cc-4522-8c42-7f63bad370d3`**, and its own report lists the commands it deferred under the capture: `npm test`, `npm run typecheck`, `npm run build` with a bundle hash, `npm run visual:smoke`, `npm run artifacts:size`, `population-route-mix.ts` on both arms, `crowd-spread.ts`, `vehicle-spacing.ts`, `npm run data:scene` on a scratch tree — which is what would name the ~20 files and ~117 MB of the old payload that the human set does not account for — `data:agents`, the flythrough config, a native-select re-probe, and the two-arm `workers` measurement that would settle whether the three-hour pixel lane can be run concurrently. It also offered to inspect the 44 frames at native resolution as a second pair of eyes on the pixels rather than on the record. Sending it back in costs one message and saves re-deriving all of that.

**The rest of the runbook, and the one rule that orders it.** Steps (4) to (10), all after certification, and **the browser lanes run one at a time** because they are the resource the whole plan is sized around: (4) **verify the bake by name** with `npm run data:agents:set`, which must report `33 of 33` and exit 0 rather than being assumed from the builder's own exit status; (5) re-derive `sceneTreeDigest()` and record the new payload, which will no longer be `ddd21ee1b4feb7f7…` and is correct to differ, because the certified frames are of the agent-free city and the population never loads without `?agents=`; (6) run **the populated lane**, `npm run visual:populated`, whose frames are the rendered evidence for populated vehicles and pedestrians on one shared signal clock, and open them at their own size rather than through a contact sheet; (7) run **the controls-driven flythrough**, `npm run visual:flythrough` on port 4323, on this revision, and inspect its frames at their own size — no frame of that route has ever been photographed, so this is the first time its 46-frame sequence exists rather than being asserted; (8) inspect **all 44 certified frames** at native resolution with `node tools/inspect/crops.ts --frame <path>`, whose eight regions tile the frame exactly once each and whose manifest binds every crop and the frame itself by SHA-256, dispatched across lanes that each own a subset so that opening all eight is a review of the whole frame rather than a sample; (9) **re-measure the performance record on this revision**, because the tick lever changed the code the old numbers describe, and **pin the ceiling** the owner's decision asks for — the largest drawn population that holds a 16.7 ms interval, which is currently only bracketed between 500 and 1,000 — accepting that this box is loaded by a sibling checkout and recording the load beside every number; and (10) obtain the **independent read-only review** of the integrated revision and resolve its material findings. Steps (9) and (6) must not overlap each other or any capture: one measures wall clock and the other is a capture, and the plan's own rule is that two timing lanes on one machine produce two wrong answers.

**The download half of that repair is already done, and doing it early was safe because the claim was measured rather than assumed.** `data/agents/` lies outside the two mounts `sceneTreeDigest` walks, so the archives could be fetched while the capture runs: both are now in `data/agents/source` and **each matches its pinned SHA-256 exactly** — `makehuman-system-assets.zip` at 280,737,770 bytes and `b542127a8e25…`, `mpfb2-80919fa.zip` at 44,851,738 bytes and `038e9f01ae39…` — and the certified scene digest was re-derived immediately afterwards and is **unchanged at `ddd21ee1b4feb7f7…`, stable across two calls**, which is the proof that the download touched nothing the certificate binds. What remains for the repair is only the Blender bake, which `npm run data:agents` will run without re-downloading because the archives are present and verified, and which does write into `data/scene/agents/` and therefore does move the digest — so it waits for certification, and its new payload is measured and recorded then.

**The flythrough's open aim item is closed: the tool has run, and the anchors are scored camera positions rather than an unscored cell.** Round 31's review found that the route's crowd anchors were the measured knot's 25 m cell, because `tools/flythrough/aim.ts` needed the walking-way network and the terrain, both of which the data loss had removed. Both are back, and the tool ran on the retained tick-5,400 dump — the default seed's population, unchanged on `main` because the crossing change is not landed — scoring **611,904 poses over 3,187 candidate camera positions on walking ways**. Each result is an actual standable pose with its way id: the **best overall** balance is camera `(344.1, 396.5)` at 15.1 m ground looking to `(365, 381)` on azimuth 128° at 26 m, giving **395 moving pedestrians across 395 distinct positions with 12 vehicles** in frustum, on `walk:1086844875:0:0:ground0:f`; the **most walkers** are at the north-west knot the earlier lane used, camera `(-386.2, -496.3)` at 27.5 m ground looking to `(-407, -480)`, giving **415 moving across 487 distinct positions and no vehicles**; and the **most traffic** is camera `(292.8, 426.4)` at 17.5 m ground looking to `(319, 426)` on azimuth 90°, giving **22 vehicles with 313 moving pedestrians**. The route's street legs can now be aimed from those three rather than from a cell centre, and the tool's output sits at `tools/flythrough/aim.json` in that worktree — which is also the path the review found `plan.ts` citing as non-existent, so the citation now resolves where the tool actually writes.

**The gate-fixes lane has committed and been pre-read, so its landing is a merge rather than an investigation.** Two commits on `worker/gate-fixes` off `024c44d`: the harness closure is a named `HARNESS_ROOTS` — `tools/visual`, the two configs that select the capture lanes and pin their renderer, port, viewport and bounds, and the preview mount that decides what they are served — with everything deliberately outside it named and justified (the app's sources reach the frames through `dist/` and are bound by the build hashes; `data/` by the scene digest; the other `tools/` lanes this chain never runs; the Draco decoder nothing here binds). It is pinned at `--begin` and re-derived at `--end` in the shape the scene digest already uses. The pointer half is rebuilt on a shared `tools/visual/style-control.ts` so both specs drive the same control the same way, which is the review's own prescription. The change also touches `src/ui/style-picker.ts` — the mis-citation — so **it is inside the bundle graph and lands after the capture**, not before: a source change under a run whose certificate pins build bytes is exactly the class the gate-transfer rule exists to refuse.

**Criterion 153 was tested clause by clause, and the result is two real gaps and one clause that cannot be measured at all.** A lane built `test/vehicle-run.test.ts` with a pose-derived measurement (`tools/agents/vehicle-run-metrics.ts`) and one additive seam, and its first execution — which I ran before merging rather than after — is **red in three of five cases**, so the gate is **not landed** and its author is calibrating it. What it established on the way is worth keeping regardless:

- **Hold lanes was genuinely uncovered and now has numbers**: 28,923 samples, the largest offset of a drawn body from the lane its plan names is **0.0216 m** against a 0.05 m tolerance, and 20,610 samples are exactly zero.
- **Turn and spawn/despawn were uncovered too**: 46 completed turns, 150 lane-section changes at junctions, a maximum swing of 102.5° and a minimum of two ticks on the turn lane; **34 entries, every one at an `portals.vehicleEntry` lane**, and **6 exits, every one at an exit portal**, with `retiredInPlace` 0.
- **The signals clause is half gated and half unreachable.** `test/population-authority.test.ts` already gates `status.authorityViolations` and asserts the population never names `canEnter`, so that half is not duplicated. The temporal half — does a body enter a signalized junction only on its movement's green — measured **zero governed entries**, and that is not a fixture-size artefact: 60, 200, 400 and **3,000** vehicles all give 0 governed entries against 3 to 37 junction-disk entries. **The driven corridors contain no signalized junction's governed approaches at all.** The gate reports this in its own `unmeasuredClauses` and fails if a later change makes it measurable, so it cannot pass quietly.

**Two findings from that work stand on their own.** First, **the bus class is never drawn, in any run**: every bus is refused with a named reason — `vehicle class bus at scale 1.002 has no AOI entry portal whose junction sections are long enough for its 5.541 m footprint radius` — so the 3.280 m body the vehicle criterion worries about against 2.5 m and 3.0 m lanes has **no representation in the population at all**. That is consistent with the planner's own `viable` filter rather than a separate defect, but it makes that clause unmeasurable as written and it is the mechanism behind the earlier "60 of 200 drawn" observation. Second, **the vehicle population drains**: mean active bodies at 60 vehicles is 24.24 over 20 s, 21.47 over 60 s and **8.96 over 240 s**, finishing with 4. A slot is only re-planned when it retires, so the population decays toward zero — why the gate's window is 1,200 ticks. That is a bounded observation and not yet a diagnosis.

**The worktree pile is nearly gone, and the last clause of this plan's own requirement is close to satisfied.** From eleven worktrees and six branches earlier in the day, the repository is down to **two worktrees and three branches**: `main`, `worker/flythrough2` and `worker/vehicle-gate`. Three landed branches were deleted with their worktrees, then the four protected payload trees and a stray detached helper. **The evidence check the earlier loss earned was run before every removal**, and it caught something worth the pause: the enumeration reported 45 files of "evidence" inside `artifacts/spacing/wt`, including files belonging to another lane, which would have been the signature of `<wt>/artifacts` being a junction to the primary's own tree — in which case the listing was my own artifacts directory and I would have been about to delete real files while "preserving" nothing. It is a real directory in all five cases (`reparse=False`), so the listing was genuine and the explanation is that lanes ran tools from their worktree root. All 54 files and 0.95 MB are preserved under `artifacts/worktree-evidence/` before any tree was touched; `data/` was verified steady at 10,350 files and `node_modules` working after each removal; and reparse points fell from 32 to 10 with `artifacts:size` at 22,040 MB against the 27,015 the reclamation pass started from.

**The vehicle-run gate is green on its branch with a real lane-clause red control, and it is held rather than landed for two measured reasons.** `worker/vehicle-gate` at `cd3cbd9`: 5 cases pass in 3.5 s, re-run by me rather than taken on its author's word, and the lane clause now has a demonstrated control — adding one lane index to the drawn pose puts a body **1.3500 m** off its lane centreline against a 0.05 m tolerance — alongside the boundary arm (`34 of 34 vehicles appeared somewhere other than a vehicle entry portal's own first occurrence`) and the vacuity guards. Its author also removed one of its own claims on re-measurement: the 122 samples that appeared to justify excluding a section-end band were **retiring bodies on the portal's outward line**, which the gate already excluded, and with them excluded the widest offset is 0.0216 m in *every* band, so the exclusion is gone. Two reasons it does not land today. **It conflicts**: its `src/agents/population/tick.ts` hunk does not apply to current `main`, which I tested in a scratch worktree rather than assuming, so it needs a rebase before it can land at all. **And it carries a source change**, which is the more consequential one: main's source currently builds to the exact bytes the running capture pinned — verified, `index-BFH5WFVP.js` / `FE74488F…`, byte-identical — so landing a `src/` change afterwards would leave the certificate binding a build that the final source no longer produces. That is the B4 class of gap, and it is the whole reason the runbook orders landings before the capture rather than after. The gate waits for a rebase and for a capture that can be run after it.

**If the run is not alive when the next session starts, re-run it — nothing else needs to change.** The capture was started as a background job of the coordinating session, and while it survived many rounds of this one, nothing guarantees a background process outlives the session that owns it. So the first check is not "read the certificate" but "is it still rendering": `chrome-headless-shell` processes exist, the newest file under `artifacts/visual/` is recent, and `run.json`'s `runId` is `f79a011fcacc03b1`. If those hold, wait for `complete.json`. If they do not, kill nothing and run `npm run visual` again: the machine is otherwise idle, `main`'s source builds to the bytes the run pinned, and the only cost is the three hours. **Nothing has been left running that could compete with it** — the job list was checked, the after-arm measurement that starved an earlier attempt is killed, and the sibling repository that held the machine earlier went idle and was measured rather than assumed.

**The run, identified precisely so that check can be made rather than guessed at.** `runId` **`f79a011fcacc03b1`**, begun **2026-09-17T01:47:35Z** (18:47:35 local) against `main` at `c70f26f`, pinning `dist/assets/index-BFH5WFVP.js` at `fe74488f12cc…`, the served scene at `ddd21ee1b4feb7f7…` over 84 files and 214,114,015 bytes, and — for the first time in any run — the harness at `6673c9e4…` over 22 files. **Main's source was verified to build to exactly those pinned bytes** in a scratch worktree, so the certificate this run writes will bind a revision that the source on `main` still produces. Its first frame, `hero-cartographic-dusk-crossing.png` at 18:55:37, is also the first evidence that the gate-fixes lane's rewritten hero specification captures: its author could only call it code, and the frame's *name* confirms the documented flip that makes the cartographic pair go first. At the pace this lane has shown — seven to ten minutes a frame for hero, then the two eighteen-frame sweeps — certification lands several hours after the session's own continuation budget ends, which is why the runbook above is written to be executed rather than remembered.

**Round 30's finding D4 is adjudicated, with 1:1 pixels rather than a preview — and the crops it was adjudicated from no longer exist.** That round recorded that the World style control is composited into every captured frame at roughly x 1010–1275, y 4–140, that the composited pixels are identical across poses, and that no inspection had ever mentioned it. A 1:1 crop of that rectangle, 320×540 and returned at the size it was written, showed the control rendering cleanly: a dark panel, legible `World style` heading, the `Cartographic` combobox with its caret, and the description text over the city beneath. A second pass by the lane that built the crop instrument, cutting `right-ui.png` and `centre-crossing.png` from a satellite dusk frame, read the panel at its own bounds and reported the same with more detail — the label, a button reading `Satellite` with a caret, and the description over a photographic facade with a window grid and a red awning, with zebra bands and a yellow lane line below. **Those files were deleted by the same restart that cleared the frames**, so the digest this paragraph once quoted describes bytes that no longer exist. **It is re-anchored on a frame that does**: the instrument run against this run's own `hero-cartographic-dusk-crossing.png` — itself `08fd85de5ff4cf946f967599c2915320eefb9049e8304d327655d7e5fd29ecb4` — cut `right-ui.png` at 320×540, **SHA-256 `b6d60b22ba447dcdc6ff117b80d36fa6f6bd812e10721b2c5502d5e45f1a6415`**, and at 1:1 it shows the panel over the street: the `World style` heading, a combobox reading `Cartographic`, the description line, and behind it a tree whose foliage renders as individual leaf cards over the plaza paving and a zebra band. **The finding: it is the application's own UI, correctly drawn, and it is in every frame by construction** — the harness photographs the page, so the page's chrome is part of what it photographs. That makes it not a defect and not an accident, but a fact an inspector must account for: every one of the 44 frames carries roughly a 265×136 rectangle of UI in its top right, and a review that judges the city without noticing the panel is judging a frame nobody sees. The same run of the instrument also proved something about the instrument itself: **its eight regions tile the frame exactly and reassemble to the source byte for byte, 3,686,400 bytes compared**, so opening all eight is a complete native review rather than a sample of one.

**The hero spec's final thresholds were checked against real frames before the run that would have paid for them.** The spec's last assertions compare frames rather than accept them: for every pose and time, the two styles' frames must differ by more than **5** in `signatureDistance`, and for every pose and style, dusk and noon must differ by more than **8** — both of which run after an hour of rendering and had never been tested against a measured value. The style pair was checkable without waiting, because the older sweep frames under `artifacts/visual/sweep/` are satellite/cartographic pairs at the same pose: **18 pairs, minimum distance 18.5, median 35.45, maximum 51.28, none below the threshold** — 3.7 times the headroom the assertion needs, so that one is safe. The **dusk-versus-noon threshold of 8 remains untested**, because it needs two times of day and no two-time pair exists on disk; it is the one assertion in that spec still running on faith, and the frames that will settle it are the run's own. Bound: this measured an older run's bytes and says nothing about the current capture's frames beyond the threshold's plausibility.

**How the final 44-frame inspection is to be done, so the method does not have to be invented at the time.** It is a native-resolution inspection, and the instrument is `node tools/inspect/crops.ts --frame <frame>`, landed at `179f775`. Its eight regions tile the frame exactly and reassemble to the source byte for byte, so **opening all eight is a review of the whole frame** and opening one is a sample; each crop carries its own SHA-256 and a world-scale note in metres per pixel, and the manifest binds the review to the frame's digest so regenerating the frames strands the review rather than inheriting it. The work is to be split across lanes that each own a subset — the previous inspection used four lanes of nine frames plus the hero frames, and that shape is sound — with one rule: **a lane inspects at 1:1 and reports what it saw, not what it measured**, because the point of the crop instrument is that a person can look at pixels rather than at statistics. What to look for, from the findings this deliverable already carries: the crossing's diagonal arms and zebra bands in every frame that contains them; the World style panel in the top right of all of them, which is application chrome and must not be mistaken for a defect; the pale roof treatment under a low sun; near-field paving wash-out at grazing angles; facade atlas softness, which a preview cannot separate from renderer aliasing and a 1:1 crop can; and anything floating, missing or black. **What no crop can tell anyone** — the bound to state in the review — is temporal crawl, TAA behaviour and aliasing over a moving sequence, because a still has no time axis; those belong to the controls-driven flythrough instead. And the frames themselves carry the capture's own bound: one renderer, one build, one scene, one instant each.

**The run before this one was restarted because of a defect in the instrument, not in the application.** It reached the hero spec's style-return block after 25.7 minutes and four captures and failed there: the block pressed `End` to walk the listbox, the registry orders cartographic first and satellite second (`src/world/styles.ts`), and the style being left was satellite — so `End` landed on the row already selected, and a closed listbox that moves onto its current option is not a move, so the control correctly did nothing. `Home` is the fix, landed at `48c19c5`, and it was verified on the real app by an 8-second probe before this run began: `rows: ["Cartographic","Satellite"]`, `Enter/Home/Enter -> cartographic`. The four frames that run had written were cleared when this one started, which is the concrete reason a mid-run inspection is stranded by a restart. The defect and its red control are recorded in `docs/learning/gate-proofs.md`.

**The vehicle-run gate is now rebased, verified by the coordinator, and still held — for one reason only.** `worker/vehicle-gate-rebase` at `40ca6d3` rebases onto `main` with **zero conflicts** — checked with `git merge-tree`, whose first reading I had to redo because PowerShell's `Select-String` is case-insensitive and matched prose containing the word "conflict" rather than merge conflicts — and its gate **passes 5 of 5** when I ran it myself, both red-control arms firing: the lane clause at 1.3500 m against a 0.05 m tolerance, and the boundary arm at `34 of 34 vehicles appeared somewhere other than a vehicle entry portal's own first occurrence`. So the conflict that made it unlandable is gone, and it survives the crossing change's effect on the population. **It still cannot land today, and the reason is the certificate**: it carries a source change, `main`'s source currently builds to exactly the bytes the running capture pinned, and landing it now would leave that certificate binding a build the final source no longer produces. **It lands before the next capture, not after this one.**

**When to expect the certificate, from the run's own pace rather than from an estimate.** The plan's earlier projection was 22:30 from a capture that began at 19:16:02, and that capture was stopped at three hero frames by the decision below, so the projection is superseded rather than missed. What survives is the pace it measured: a first hero capture 9.4 minutes in including the initial tileset refinement, then 6.0 and 8.0 minutes, which is the same cadence the previous successful run set. The next certificate is owed by the final capture, which starts once the pending source landings are in, and the check that distinguishes a working run from a stalled one is the ledger's `completed` count and the newest file under `artifacts/visual/`, not the clock.

**Round 111's decision: the capture at `cc8bc42d2368926e` was stopped on purpose at 3 of its 10 hero captures, and the reason is arithmetic rather than a defect.** Two lanes reported within a minute of each other, and both carry `src/` changes: the vehicle-run gate rebased onto the current tip at `7329dec` and green 5 of 5, and the population tick lever with its behaviour-preservation proof. A certificate binds the build bytes the revision produces, so a capture running while two source landings are pending **can never bind the final revision** — it is the same defect round 29 found in the previous certificate, and running it to certification at 22:30 would have bought frames that the next run's own `hero.spec.ts` deletes as it starts. Its remaining two and a half hours were therefore worth more as a machine than as pixels. What was preserved is its ledger rather than its frames, under `artifacts/verdict-run5-aborted/`: `run.json`, `hero/captures.json` and the three frames, so the record of the run and of the pace it measured survives its own restart. **The sequence from here is land, then measure, then capture once**: the vehicle gate is landed at `7329dec`, the tick lever lands if its decisive arms hold, the performance record is re-measured on the resulting revision rather than quoted from the superseded one, and only then does the final capture run. **Two readings from the stopped run are worth keeping.** The machine was at a measured **100% of 32 logical cores across repeated samples** even after our own browser lane died, and three of the four browser instances on it belong to the sibling `3d-maker` checkout running `tools/drive.js`, `tools/devices.js` and `tools/shimmer.js` — another participant's work, not mine to stop. The final capture therefore runs under load that is not ours to clear, and the ledger is the instrument that says whether it is working: the stopped run produced hero captures at 19:25:26, 19:31:29 and 19:39:28, intervals of 6.0 and 8.0 minutes, which is the pace the previous successful run set, so a slower cadence rather than a failure is what load looks like here.

**Two more landings and one gate defect, all in the same hour.** The human set's repair stopped being a paragraph a person checks with their eyes: `ee112fd` adds `tools/agents/verify-human-set.ts`, run as **`npm run data:agents:set`**, which requires the bake's 33 files, checks each non-empty, parses every manifest, and requires every file a manifest names to exist beside it at the digest and summed byte length the manifest records, reporting what is missing, empty or disagreeing **by name** and exiting 1 so it can be a chain step. It was proved against **12 CLI runs, one per case**: green at `33 of 33`, exit 0, then red for a removed file, a zero-byte file, truncated JSON, a digest mismatch, a malformed digest, a `bytes` mismatch, a dropped LOD, a missing source blend, and a path where a file name belongs, with the report's own sentences recorded; a stray unlisted file correctly **does not** fail, because the vehicle fleet shares that directory. Two of those cases found bugs in the checker itself — a per-file `bytes` comparison invented **27 findings on a correct bake**, and a malformed digest read as "nothing recorded" let a corrupt manifest **pass** — which is the second time in one session that a check could not fail. Its landing is certificate-neutral and that was measured rather than argued: the commit builds `dist/assets/index-BFH5WFVP.js` at `fe74488f12cc…`, byte-identical to the pair the stopped capture pinned, and `sceneTreeDigest()` re-derived from the worktree is still `ddd21ee1…` at 84 files. **The gate defect is the more serious find**: `npm test` on `ee112fd` printed `Test Files 59 passed (60)` with `Tests 439 passed (441)` in one run and `Errors 1 error` with **exit code 1** in another, and the error is Vitest's own worker-RPC watchdog — `[vitest-worker]: Timeout calling "onTaskUpdate"`, a 60-second deadline that a starved box misses — so on this machine the unit gate cannot tell a pass from a failure. A lane owns it, and until it is fixed the claim "all five gates green for the exact revision" cannot honestly be made, which is why the final capture waits on it rather than running alongside it. The runbook's step 2 is now `npm run data:agents && npm run data:agents:set`.

**The revision is frozen at `22ef994` and the final capture is running against it** — runId `ed0cb4e7a244fcba`, started 20:31:25 local, four non-visual gates green on that exact revision: `npm run build` producing `dist/assets/index-B3qA34nq.js` at 975,254 bytes and `b50735b70fca…` with the stylesheet unchanged at `11dd032c51b5…`, `npm run typecheck` exit 0, **`npm test` 60 files and 442 tests passed with no `Errors` line and exit 0**, and `npm audit` exit 0. The capture's own `run.json` pins the scene at 84 files and `ddd21ee1b4feb7f7…` and the harness at 22 files and `ff5a53559456253c…`, both unchanged from the stopped run, so nothing in tonight's landings moved either bound; `dist/index.html`'s hash moved from `1d5e9cc7…` to `782e2dde…` for the ordinary reason that the bundle it names changed.

**The tick lever landed at `42f5639` and it does not clear the cliff.** Four repairs are behaviour-preserving rather than merely careful: the route's edge walk becomes a binary search over the non-decreasing `route.starts`, `gateAtOrAhead` and `gateBeyond` lose a fresh closure per call, `buildPassage` stops rebuilding a map of every lane in the network to resolve the handful of ids one route names — **8.9% of the tick's sampled self time** — and `sampleEdge`'s arc table is memoised in front of its `WeakMap`. The proof is a digest, twice: `population-run.ts --digest-only` returns `aee25d45a714…` on the base and on the changed tree at 3,600 ticks, and at **21,900 ticks** — 365 simulated seconds, 3,000 pedestrians and 200 vehicles — both arms return `fc328812c3c6a8…` with every counter equal: pedestrians queued 2,247, completed 219, crossed 180; vehicles active 16, completed 163, crossed 264. **The attributable gain is one row**: `plan.pedestrians.gate` falls from 1.826 / 1.733 / 1.509 ms to 0.326 / 0.329 / 0.314 ms, a **5.4× cut with no overlap between arms**, taking it from 8.5–9.7% of the tick to 2.0–2.1%. The whole-tick arms — base 21.474 / 20.075 / 15.607 against preserve 16.153 / 16.667 / 14.753, and 23.63 against 18.24 on the long pair — move the right way in every adjacent pair, and **no whole-tick figure is quoted**, because on this machine the drift between arms is the same size as the effect; taken at 17–23% on the plan's 12.5 ms baseline the tick lands near 10.4 ms against the roughly 8 ms that fits alongside the frame's own p50 7.7 ms, so the criterion's conclusion is unchanged. The rejected candidate is recorded rather than dropped: a ring-ordered k-nearest neighbour query cuts cells opened from 25 to 5.17 and bodies visited from 99.3 to 62.3 per query with 98% of queries stopping early, and moves the avoidance phase by **+0.011 ms** (7.67 against 7.59) because the eight nearest build **14,747 real half-planes where the arbitrary eight built 1,416** — no time, a changed crowd (`queued` 656 → 833), a changed digest — so `cell-grid.ts` is byte-identical to `main`'s and the candidate is kept at `artifacts/tick-cost/arms/ring.cell-grid.ts`.

**The population's throughput at the acceptance population is not what the sentence above says, and both of its errors were settled by reproducing the revisions rather than by argument.** `node tools/agents/population-run.ts --pedestrians 3000 --vehicles 200 --ticks 21600` gives, at `966930e` — the revision the devlog names — pedestrians **309 completed and 1,815 crossed** with vehicles **203 and 311**, and at `a800cee`, the crossing change, and again at `HEAD` it gives **219 completed and 180 crossed** with vehicles **163 and 264**, the digest `86178c88…` identical at both of the latter. So the figures were true when written, are not true now, and **the boundary is the crossing change**: same tool, same window, same pinned inputs (`network 314fac84…`, `vehicles 5b29efc7…`), clean worktrees, and 360 s and 365 s windows agreeing exactly, so it is neither a window artefact nor noise. **The sentence also paired two different counters.** `crossed` counts crossing *events* — `counters.crossed.* += 1` fires each time `admissions.observe` reports a release, and one actor contributes several, measured at **2.0 events per vehicle over 6,000 ticks** — while `completed` counts finished routes once per actor, so "1,815 crossed and 309 completed" is an event count set beside a route count and **1,815 cannot be read as 1,815 people**. Any successor sentence must not reuse 1,815 or 311. **A third record fails outright**: `artifacts/crossing-crowd/REPORT.md` reports **1,642 crossed and 309 completed** for that change's own after-arm, and the landed code gives 180 and 219 — reproduced twice, never obtained, so that figure is withdrawn rather than carried forward. **What this means for the criteria is a judgement the rendered frames must inform, not the counters**: the crowd's queueing is already this plan's recorded residual (2,247 of 3,000 queued at 365 s with a 341 s longest wait, piled hundreds of metres from the origin), the after-arm's diagonal evidence is thin at 2 entries in 60,000 ticks at 150 pedestrians, and whether the signature shot shows walkers on the scramble is exactly what the populated lane's frames answer. Nothing here is fixed tonight, and nothing here is hidden: it is the one measured regression the deliverable carries.

**Three integration defects came out of landing that work, and each is a claim-against-artifact gap rather than a slip.** (1) The lane reported "`npx tsc --noEmit` exit 0" and the merged revision did not typecheck: both frame-budget probes read `GridCounters.stoppedEarly`, a counter only the **rejected** ring variant had, because the arm switch and the landing were two states of one tree and only the measured state had been typechecked end to end. The coordinator's own first read of it showed two errors in one file **because the output was piped through `Select-Object -Last 2`** — the other four were hidden above the cut — and the fix, `22ef994`, reverts `grid-walk.ts` to `ee112fd` and keeps `avoid-cost.ts`'s least-contended-minimum row while dropping its four ring references. The rule this earns is narrow and worth keeping: **a gate's output is read whole or not at all**. (2) A lane's mutation script wrote into the **primary checkout** instead of its own worktree and left `test/visual-instrument.test.ts` with seven uses of an undefined `FRAME_SET_BUDGET_MS`, which the lane reported as an urgent hazard in someone else's tree and which was found to be its own; the file was restored with `git checkout --` and the gate re-run. Every mutation arm from now on names the absolute path it writes and checks it is inside its own worktree before writing. (3) The unit gate could not distinguish a pass from a failure: `npm test` printed `Errors 1 error` and exited 1 on an all-green run, and the cause is Vitest's own worker-RPC watchdog — `[vitest-worker]: Timeout calling "onTaskUpdate"`, a 60-second deadline — fired because one synchronous case runs 60,000 ticks in 47–93 s and its worker never reaches Node's poll phase. The suggested fix was tested and **falsified**: bounding workers to 16 did not stop it. What fixed it was making that case turn the event loop every 2,000 ticks, after which six full-suite runs went five green with zero watchdog firings, and the file-cost half was fixed separately by giving two cases ceilings derived from the work they do — `beginVisualRun` hashes the whole 214,114,015-byte scene inside the call at 2,185 ms, and the second case decodes 44 native frames twice — with a third case checking that each ceiling still describes its own work. Also found and not fixed by that lane: `test/tile-memory.test.ts` can fail as `SyntaxError: Unexpected end of JSON input` when its `spawnSync` child misses a 2,000 ms budget on a stalled box, reporting a timeout as a parse error; a lane owns it.

### Blockers (unresolved dependencies)

- Review 5 accepted the network lifecycle and supported-body contract; the reviewed milestone is on main at `1ca4552ceee25e34ac62335114bbb5377a4d6470`. Phase 7/8 still require continuous legal trajectories, level-aware contact, measured receiving capacity and populated integration. See `reviews/5_implementation.md`.
- Pinned MPFB and MakeHuman system CC0 assets supply three adult clothing/body variants and baked idle/walk animation. Human format v2 requires selected-scene draw parts and world-baked VAT. A1/support repairs remain frozen; root verified 34 source/evidence hashes and reran 13 tests. Review 6 preserves the 378-case flat-contact result (3.583476 mm maximum planted drift and 9.132385 mm stance height) while rejecting full-body reference mismatch and crouch/static arms. Review 7 completes only the commuter-male/near reference proof: matching evaluated bind matrices plus full skin influences recover source correspondence, with a controlled perturbation and paired rendered evidence. Root independently verified all 163 handoff digests and inspected selected native pairs. F7 naturalness and production integration remain rejected/pending; no new production bake or phase-settled strip is accepted. Pose normals and wheel residual buffers remain unchanged. Shipping cross-pass GPU, natural motion, world contact and populated LOD quality remain unaccepted. See `reviews/6_implementation.md` and `reviews/7_implementation.md`.
- The Japanese vehicle mix uses accepted original, unbranded kei hatchback, tall taxi and city-bus V4 recipes. The standalone `data:vehicles` / `data:vehicles:verify` closure uses the one measured fleet authority without human archives or baking. Candidate ordinary build/verify produced the exact accepted three GLBs and raw manifest `5b29efc7977949db1611273c8fb294b3ab23a1a55c8f42316cad9b5309c94a01`; hardware was rebound to those bytes, with all placement geometry unchanged. Review 12 separately accepted the bounded F9 wheel-offset/source support repair; integrated raw-V4 motion, natural steering, populated contact, all five code gates and merge remain pending. This does not claim a licensed branded JPN Taxi model was obtained.
- The 3,000 animated pedestrian / 200 vehicle performance target is set in `src/world/frame.ts`; its Phase 9 measurement remains pending.

## Approach

These decisions are settled. Reopen one only with a reason that is new, not with a preference.

**Target: a three.js + Vite web app, running in real time in the browser.**

Chosen over Blender, over Unreal plus Cesium, and over Godot. It matches `town_3d`'s stack, so the fleet already knows it. Animated agents are natural in it. And Playwright gives cheap screenshot gates, which is exactly what the canon's visual-verification rule asks for.

Current supporting contracts: [network](../../reference/network-contract.md), [facade emission](../../reference/facade-emission.md) and [bounded pavement recipe](../../reference/pavement-recipe.md). The primary checkout also retains the in-progress human asset contract at `docs/reference/agent-assets.md`; that unready pipeline is excluded from this graphics candidate.

**Geometry source: Project PLATEAU LOD2 (MLIT, PDL 1.0 with CC BY 4.0 permitted) plus OpenStreetMap (ODbL).**

Chosen over Google Photorealistic 3D Tiles. The Maps Platform terms forbid bulk caching and derivative works, so that data cannot be baked into a simulated world however good it looks. This is a licensing decision and it is recorded here so it stays findable, rather than being rediscovered later by someone who notices the tiles are prettier.

**Where the realism actually comes from.**

PLATEAU supplies accurate geometry and, in this AOI, photographic texture on 1,678 of 1,741 buildings — 96.4%, every one of them LOD2, with the untextured 3.6% being exactly the LOD1 set. That corrects the premise this plan was originally reasoned from, which said PLATEAU supplies almost no texture; item 15 and new item 35 are the items rewritten to match. Realism is still won in the material and lighting passes, because the texture that exists is daytime aerial photogrammetry with shadows baked into the albedo and no emissive channel anywhere.

Shibuya is defined by its illuminated signage. Without emissive billboards the scene reads as a generic Japanese city no matter how accurate the geometry underneath it is.

That is why emissive signage (item 16) is the single highest-value item in the plan, and why dusk is the hero lighting preset: it gives the best realism per unit of effort and it forgives LOD2's weaknesses.

**Pipeline shape.**

The geospatial work is offline, deterministic and cached. Its outputs stay gitignored and regenerable, under the canon's blob ceilings, so the repository holds the recipe rather than the bytes.

**Gate frames are not byte-reproducible, and no phase should assume otherwise.**

The visual harness aims the camera through the real controls and accepts a pose within 0.012 rad of the one it asked for, and damped controls come to rest at a slightly different residual on every run. Measured on 2026-09-06: three runs of the same build produced no frame that was byte-identical in all three, with settled azimuths drifting up to 3.5e-4 rad between runs and distinct-colour counts moving by a few either way. Pixel-exact goldens need a tighter settle tolerance bought first; until some phase pays for it, frames are reviewed by eye and compared by content, never diffed byte for byte.

## Acceptance criteria

Each of these is checked by looking at a rendered result or watching the scene run. None of them is satisfied by a passing test.

- [x] The AOI renders end to end — terrain, buildings and road surfaces across the full 1 km box — inspected from several angles and zoom levels, each frame at native resolution. (Phases 1–3)
- [x] The ground is not flat: the Shibuya valley and the Dōgenzaka slope are visible in the rendered terrain, and building footprints sit on it without floating or sinking. (Phases 2–3)
- [x] Buildings hold up close: window grids follow floor counts and the PBR materials respond to light, checked at street level and from above. (Phase 4)
- [x] A dusk frame of the crossing reads as Shibuya rather than as a generic Japanese city, because the emissive signage and neon are there. (Phases 4–5)
- [ ] Road markings match reference imagery, including the scramble's diagonals, with signals, guardrails and street furniture in place. (Phase 4)
- [ ] The dusk preset renders with ACES tone mapping, bloom carrying the neon, SSAO and TAA, and holds still — no flicker or crawl over a moving sequence. (Phase 5)
- [ ] Vehicles hold their lanes, obey the signals, turn, and spawn and despawn at the boundary, watched over a run rather than sampled in one frame. (Phase 7) — **partly met and partly unmeasurable as written.** Lanes, turns and boundary spawn/despawn are measured over a run: a drawn body sits at most 0.0216 m off the polyline of the lane its plan names across 28,923 samples against a 0.05 m tolerance, 46 turns complete with 150 lane-section changes at junctions and a maximum swing of 102.5°, and 34 entries and 6 exits all occur at the portals they should with `retiredInPlace` 0. **The signals clause cannot be measured in this fixture**: zero governed entries at 60, 200, 400 and 3,000 vehicles, because the driven corridors contain no signalized junction's governed approaches — the gate that discovered this reports it rather than passing. **That gate is now landed**, at `7329dec`: `test/vehicle-run.test.ts` and `tools/agents/vehicle-run-metrics.ts`, green **5 of 5** on today's population with both red controls firing — the lane clause at `a drawn body sat 1.3500 m from the centreline of the lane it is on, over a 0.05 m tolerance`, and the boundary clause at `34 of 34 vehicles appeared somewhere other than a vehicle entry portal's own first occurrence`. Its landing also corrected three of its own claims, and the corrections are the reason to trust the rest: the header had quoted **28,594** lane samples, which is its *mutation's* reading, where the clean run reports **28,923**; the claim that 20,610 of those sit exactly on the centreline is **false — zero samples are exactly zero**, and the report's `p50/p95/p99` of 0 is `toFixed(4)` rounding, which is what made the false claim look corroborated; and the lane control is **not purely presentational**, because `placeVehicle` writes the drawn pose that `vehicleFootprint` reads back for the authority comparison, so displacing the pose displaces collision envelopes and the mutated arm draws 33 entries against the clean run's 34. The lane's own honest limit is carried: the control reddens its own clause, so it is valid, but its sample count is not a second observation of the unmutated window. **The rebase that made it landable also settled a disagreement rather than stepping around one**: `git diff 8748566 334e984 -- src/agents/population/tick.ts` is empty, so the conflict the coordinator expected does not exist, and the replay was verified by comparing the replayed commits' blobs against the originals rather than by trusting a clean exit. And **the bus class is never drawn at all**, refused at spawn for its footprint radius, so the 3.280 m body this criterion worries about has no representation in the population.
- [ ] Pedestrians avoid each other and surge diagonally across the crossing on the signal phase — the signature shot — with no visible interpenetration and no sliding feet. (Phase 8) — **half met, and the half that is met is now gated.** The surge is real on the crossing change landed at `a800cee`: walkers enter the authored diagonal and the crowd stops standing in one place, measured at 150 pedestrians over 60,000 ticks as diagonal entries **0 → 2**, largest one-centimetre stack **7 → 2**, distinct positions **135 → 149**, crowd sharing a position **19.33% → 0.67%**, with two mutations separating the lateral offset from the crossing descent. **Interpenetration is not eliminated**: deep interpenetration fell **148 of 150 → 53 of 150**, and the gate bounds it at half the crowd rather than at zero, so a check demanding none would be red. The residual is **in the queues, not the walking model** — every deep pair at the final tick is between two stalled bodies, and at 300 pedestrians 143 of 300 are queued with a 110 s longest wait, piled 450–650 m from the origin. Unmeasured at the acceptance population.
- [ ] Vehicles and pedestrians run off the same clock: one signal phase model drives both, and no frame shows traffic moving through the scramble while pedestrians are on it. (Phase 6) — **the clock half holds; the frame half is unmeasurable on the vehicle side for the reason above.** One phase model drives both populations, and the authority half is gated. But no vehicle ever enters a governed junction in this fixture, so "no frame shows traffic moving through the scramble" cannot be tested by watching traffic: there is none to watch at a governed section.
- [ ] 60 fps at 1920×1080 in a running build with 3,000 animated pedestrians and 200 vehicles, the target pinned in `src/world/frame.ts`; name the measured hardware, build and timing window. (Phase 9) — **measured, and it does not hold.** p50 83.3 ms / p95 183.4 ms, about 12 fps, with 59 of 200 vehicles drawn; the frame's own cost is p50 7.7 ms so rendering is not the limit, the tick is at p50 75.7 ms per frame, and 44.1% of that tick is a neighbour query visiting a measured 97 bodies per body where ORCA uses 8. The owner's decision of 2026-09-16 governs the vehicle half: keep `main` green at the fleet it can carry and record 200 as measurably infeasible.
- [x] The scene rebuilds from a clean checkout: delete the derived geospatial outputs, re-run the pipeline, render again, and compare with the frames above. (Phase 2)
- [x] The running app shows its attribution: PLATEAU (PDL 1.0, with CC BY 4.0 permitted — the licence name here was imprecise and is corrected), OpenStreetMap (ODbL) and GSI. (Phase 1)
- [x] A multi-angle, multi-zoom sweep and a flythrough driven through the real controls both come back clean, an independent review passes, and the work is merged to main. (Phase 10) — **met on 2026-09-17, and the sweep half is not clean: the re-inspection of the current certificate found the terrain hole F1/H8 visible at 1:1 in the overhead frames of both world styles.** The sweep's 44 frames and the flythrough's 46 were each inspected at native resolution with every digest verified; the flythrough's own judge reports zero failures and its re-aimed crowd leg resolves, carrying two frame-level findings; two independent reviews found no blocking finding; the fixes are merged to `main`. See the acceptance criteria status subsection for the sweep's terrain-hole finding and the state of its fix, for the crowd leg's stale aim-evidence dump and its crushed near ground on superseded frames, and for the flicker half of line 268, which stays open.
- [ ] Cartographic and Satellite each hold up at street, block and aerial distances. The actual World style dropdown switches both ways with pointer and keyboard input, preserves camera and simulation progress, and takes its options from a registry that can accept later styles. (Phases 4–5, 10)

### Acceptance criteria status, 2026-09-17

The list above is the criteria; this subsection is where each open one stands after this session's evidence. The dated blocks that follow are kept as the record of what was believed when they were written; where they disagree with this subsection, this subsection is current.

- **Road markings** (line 267) — **met, corrected on 2026-09-17 after the audit re-derived the guardrail claim.** The markings half rests on round 30's 1:1 adjudication plus this session's inspection of all 44 certified frames: the scramble's diagonals and every arm's zebra bands are present and legible in every frame that contains them, the yellow diagonal guide lines read at street level, and the markings survive the satellite↔cartographic switch at both hours. **The guardrail half is met too, and the earlier "not met" note was wrong**: `src/scene/vegetation.ts` builds four `guard_rail` ways from `decorations.json`, 364–460 m from the crossing — the "nearest source barrier 295 m away" that note rested on is the nearest bollard, not a barrier. `guardrailPosts: 0` in `src/scene/street-details.ts:230` is real but is a dead field read by nothing, so it reported nothing either way; it is removed in the quality work rather than cited. The one bound that stands is the reference's **0.486 m/px** ground sample distance, which means the thin-paint half cannot be adjudicated against the photograph at all.
- **Dusk preset and "holds still over a moving sequence"** (line 268) — **TAA is evidenced and the flicker half is now framed correctly, but the criterion is not closed.** The stillness fix (`src/render/controls-rest.ts`) works, and the certificate's own manifest records it: all eight agent-free hero frames in `artifacts/visual/hero/hero.json` have `taaAccumulating: true`, seven at 32 samples and one at 25. **The audit then established a fact that changes how this criterion can ever be read: TAA cannot accumulate while the camera is moving, in any run** — `src/app.ts` feeds `cameraStill` into `post.setStill` and `post.ts` resets the sample index — so "TAA over a moving sequence" is not a thing this app does, and the criterion's temporal-accumulation clause and its moving-sequence clause cannot both be satisfied by one capture. The flicker/crawl half therefore has to be judged on the non-accumulating path (adjacent frames of a moving camera) or from a video, and the passing flythrough gives 46 such frames. What it does not give is the spacing to do it confidently: the audit corrected this document's numbers — the 0.65–1.98 s and 39–119-rendered-frame figures are the **approach leg only**; the run's widest gap is **2,202 ms** (ascent) and the crowd leg's held pairs are **285–675 ms**. No lane has yet captured **adjacent** rendered frames — the flicker lane's closest pair spans 20.5–23 — which is the missing capture mode; the lane exists and the in-page capture that would close the gap is assessed and unimplemented, and the item is Phase A work in the quality register. The one measured positive stands: with the camera held, 918,000+ of 921,600 pixels are byte-identical across five frames, so crawl is excluded *there* rather than merely unseen. **Corrected 2026-09-18:** the flicker lane's crawl class cannot fire at the span that lane achieves — the predicate is `unexplainedFraction > 0.005 × frameGap²`, so past a `sqrt(1/0.005) = 14.14`-rendered-frame span it needs more than the whole frame, and the lane's measured spans are 20.5–23 — so its worst pair (**1.3506e-3** per rendered frame) is a measurement and not a class enforced, and a `no failures` line from that lane must not be read as the crawl bar having passed. Two further bounds its review established the same day: the per-shutter span is **14–18** rendered frames, the same regime as RUN-01's 16–18, so the claim that the shutter span grew is backwards; and **`ascent` emits no wheel event at all**, so the criterion's "or an ascent" pose is not exercised.
- **Vehicles** (line 269) — unchanged in its measured half, and the populated inspection adds two coverage findings: at the `corridor` pose **no vehicle is visible in any of 8 frames** over 14.1 simulated seconds at a pose built to watch one hold its lane through a signal (16 active, `vehiclesCrossed` flat), and at the `boundary` pose the portal is hidden behind rooftops while the manifest shows **no spawn or despawn occurred in the window** (lifecycle counts constant). The signal-obedience clause remains unmeasurable in this fixture for the recorded reason — zero governed entries at 60, 200, 400 and 3,000 vehicles.
- **Pedestrians and the signature shot** (line 270) — **the walking half is now evidenced in pixels and the crossing half is met in the negative.** `artifacts/inspection-populated/REPORT-poses.md` records pedestrians walking at 1–2 m/s, mid-stride, feet planted, no interpenetration at 3× in the `walkers` series; `REPORT-crossing.md` records **14 of 14 frames byte-identical across 26.15 simulated seconds of the pedestrian green with zero pedestrians and zero vehicles in frame**, verified as a true depiction rather than a capture defect (3,000 rendered, 0 within 60 m, crossing events accumulating elsewhere) and attributed at the time to the routing bound. **Superseded 2026-09-18: that attribution is falsified on `HEAD`.** `MAXIMUM_ROUTE_LENGTH_M = 700` (`src/agents/population/routes.ts:85`) is read at one site, `routes.ts:528` inside the legacy `walk()`; a spawn asks for a centre route first (`tick.ts:638`, defined `tick.ts:486-490`, bounded by `MAXIMUM_CENTRE_ROUTE_LENGTH_M = 1_000` at `routes.ts:113`), so the cap still binds the portals whose centre plan is refused — 14 of the 31 drawn portals, about 1,365 of 3,000 slots — but it is not the measured blocker. The criterion stays **not met**, and the measured cause is that walkers do not reach the crossing and freeze at a governed junction: at the acceptance population (3,000/200, seed 5,970,698) over 45,000 ticks, `walkersEnteringDiagonal` **1**, peak within 60 m of the origin **1**, median closest approach **500.661 m**, `maxStoppedWalkers` **2,992 of 3,000** (mean 2,556.9), longest pedestrian wait **726.07 s of a 750 s run**, and **0 of 6 pedestrian-green windows** hold anyone on the diagonal — the single traverse falls on the amber/vehicle stages. The mechanism is now located to one expression, and it is not capacity: the **no-admissible-velocity fallback** in the ORCA solver, `src/agents/population/pedestrians.ts:314-321`. Every half-plane is `dot(v, point) >= w`, and when `w > 0` while `dot(preferred, point) < 0` the ratio `w / value` is negative, `scale` goes negative, and `Math.max(0, scale)` clamps the velocity to **exactly zero**; the fallback can only shorten the preferred direction and never turn it, so a constraint demanding motion against that direction is unsatisfiable and the body simply stops. The census at tick 44,000 with 150 pedestrians and zero vehicles: **148 of 150 bodies at exactly zero speed — 133 by that fallback, 14 halted at a gate and 1 unclassified** — and of the 60 holding a lease **all 60 are solver-zeroed and none is gate-halted**, the gate capping them being a different authority 59.9-462.6 m ahead. The isolating control is what makes it credible: the only two moving bodies have **zero overlapping neighbours** and walk at 1.17 m/s, while every frozen body has at least one. The arithmetic on one slot: a neighbour 0.1 mm inside the collision circle demanding 0.2 mm/s of separation gives `dot = -0.0253` and `w / dot = -0.0059` → zero, and magnitude is irrelevant because the binding `w` across the census runs 0 to 0.812 m/s while the largest `dot` among bindings is -0.0000859, so not one binding half-plane was satisfiable by shortening the preferred velocity. The fixed point reproduces bit-identically: slots 6, 14 and 19 hold `travelledM` at 43.226, 36.442 and 36.134 across 39,000 ticks while their lease's `heldSeconds` climbs 28 → 678 s. Two earlier candidates are falsified — the body-ahead gate does not exist, which is the defect register's new entry, and same-compound re-entry is refuted by `gateBeyond`'s own comment (`tick.ts:926-940`), which records this exact freeze as a defect that function already fixed; gate halting is real but minor at 14 of 148 and is a consequence of the frozen lease-holders rather than a cause. The test the fix must meet is recorded rather than negotiated: walkers enter the diagonal **on the pedestrian green** (`committedAtScramble > 0`), `pedestriansCompleted` stays at or above **338** at 3000/200, `worstOverlappingPairs` stays at or below **142** at 150/0, and the criterion stays **unmet** until then. **The cause is located, not yet demonstrated**: arm (3) — shrink the ORCA collision radius until no pair overlaps, predicting the zero-speed count falls from 148 of 150 to at most 15, falsified if it stays materially above 15 or the previously frozen bodies keep a fixed `travelledM` — is pre-registered and running. Driving the crossing share from 51.1% to 93.9% is **rejected by measurement**: it buys 1 → 3 diagonal entries and 1 → 4 peak-within-60 m while completions fall **338 → 2**, overlapping pairs rise 142 → 324 and deepest penetration 0.4909 → 0.5137 m. The distance floor is part of the picture rather than a detail: the fastest legal walk from the nearest portal to a central crossing is 500.038 m, **454.6 s = 27,276 ticks** at the 1.1 m/s pedestrian cadence. Evidence: `artifacts/signature-routing/STALL.md` (16,100 B, sha256 `56626a1c…`) and `BRIEF.md` (39,323 B, sha256 `2036fac2…`), both still being revised while this was written, with `stall-census-p150-v0.json`, `stall-trace-p20-v0-t45000.json` and `occ-p3000-v200-t45000-before.json`.
- **One clock for both populations** (line 271) — unchanged: the clock half holds and is gated; the frame half cannot be tested on the vehicle side because no vehicle enters a governed junction in this fixture.
- **Performance** (line 272) — **supersedes the paragraph in the list above.** Re-measured on this revision with the machine quiet: **p50 16.7 ms / p95 33.4 ms** at 3,000 drawn pedestrians and 59 vehicles, so the **median holds the 60 fps target** where the list records 83.3/183.4; the fixed step is p50 11.8 ms (was 75.7), catch-up frames 28.0% (3 at the cap), the simulation tracks wall clock, and 0 authority violations. The ceiling is **3,000 by the median** and **2,500 by p95**; no measured population fails the median and the first failure is above 3,000, unmeasured. The bounds are named in the dated status block at the end of this document (one machine, one seed, one window per arm, the certified build plus an observation-only probe seam, load tabulated). The vehicle half remains the owner's recorded infeasibility — 59 of 200 drawn.
- **Phase 10 sweep, flythrough, review and merge** (line 275) — **met, on the evidence below, with the sweep's material finding and the flythrough's two frame-level findings recorded rather than hedged.** All 44 frames were inspected at native resolution by nine lanes with every frame digest verified against `complete.json`, and the two `style-return` frames were opened beside them as lane evidence — **those two are at the approach pose, not the crossing, and they are not certificate-bound**: `complete.json` lists 44 entries (8 hero + 36 sweep) and `tools/visual/verify-output.ts` never names `style-return`, while `artifacts/visual/hero/hero.json` records the two digests in `styleReturns[].sha256`, and the pose is measured rather than assumed (`return-cartographic-dusk` differs from `hero-cartographic-dusk-approach.png` by a mean of **0.38** luminance levels against **30.70** held against the crossing frame, and its frame mean 92.63 sits on the approach frame's 92.36 rather than the crossing frame's 117.59) — and **the "zero blocking defects" line this bullet used to carry described that earlier round and is superseded**. The re-inspection of the current certificate (runId `e6b7b9d8de0faf69`) found the terrain hole F1/H8 visible at 1:1 in the overhead sweep in **both world styles**: four of the six cartographic azimuths (`artifacts/inspection-final/REPORT-cartographic-overhead.md` §3 — visible at az000, az060, az180 and az240, out of frame at az120, and not separable from the background at az300) and four of the six satellite azimuths (`artifacts/inspection-final/REPORT-satellite-overhead.md` §4), whose own verdict says outright that this supersedes the standing adjudication that the hole would not be visible in satellite. Its core samples the scene's own background colour — exactly `rgb(22,24,33)` at az060 and az180, a level either side at az000 and az240 — so the ground has a gap and the sky shows through it, not a shadow or a prop; reconstructing the recorded pose puts the documented world point (579.0, −403.1) within **1.9 px** of the observed blob at every azimuth it is visible; its exact-background bounding box is identical to the 2026-09-15 measurement at az060 and az180 and within one pixel on every side at az000 and az240; and `artifacts/terrain-holes/REPORT.md` (2026-09-15) is the root cause and census — the holes are in PLATEAU's own DEM TIN, 14 interior rims, and only the overhead frames show them. **The fix is capping the interior rims with a watertightness gate** — the census, the cap and the refusal live in `tools/scene/terrain-watertight.ts` and `npm run data:scene` refuses an uncapped scene by name — **Superseded 2026-09-18: the fix is implemented, verified against the served bytes and not merged, and the certificate this entry names is now knowingly stale.** Branches `phase2-regen` in the primary checkout and `worker/f1-terrain-cap` in its worktree both sat at `0e7d683` when this was written (off `4efaea6`, five commits: the cap and census gate, the TIN reader fix, its gate proof, the pavement-input re-binding and the session record), and the served `data/scene` has been regenerated with the cap — the closing dated block below carries the digests, the census and what remains. The parked `phase-b-terrain-cap` at `0c76789` is the superseded first attempt. What is owed is review 34 of the re-bound pavement inputs, the merge to `main`, the one batched 44-frame re-capture and its one re-inspection; until that re-capture runs, every inspection bound to runId `e6b7b9d8de0faf69` is stranded, because the scene the app serves is no longer the scene that certificate's binding describes (`artifacts/f1-cap/BRIEF.md` §1, §7 and §8). The road-material question is not adjudicated here and a later pass owns it. The controls-driven flythrough came back clean as a run: exit 0 on `39ca459`, 46 frames over four legs, its shipping sequence judge reporting zero failures, its frames inspected by three more lanes at native resolution with **no blocking render defect**, and the approaching leg closes on a paved footway with bloom that never clips. **The crowd caveat is superseded: the re-aimed leg's own inspection finds the subject resolves.** `artifacts/inspection-flythrough2/REPORT-crowd.md` is that lane's record of the current crowd leg, and its §4 verdict is **yes** — heads, limbs, torsos and feet legible, measured at roughly **35-45 figures in the column plus 10-15 separated walkers, so about 45-55 distinguishable bodies** at 1.4-1.8 m and 7.6 head-heights, no figure stretched, squashed or collapsed, no visible interpenetration and feet planted. Its §6 records that the held frames stay live rather than static — the same bodies translate rigidly at 1.05-1.34 m/s while six of the eight crops are byte-identical, so the camera is not what changed — and its §8 records **"Blocking: none"**. Two frame-level findings replace the caveat. **D2**: the aim-evidence dump the run's manifest carries is a stale population — `artifacts/flythrough2/aim-run-worker.json` names `artifacts/flythrough2/reference-dump-t5400.json`, dated 2026-09-16, which puts 82 of its 101 near bodies standing still and none to the right of frame column 500, where the frames show walkers out to frame x ~820 — and that is the dump-provenance class already recorded in `docs/learning/defect-register.md` in the entry "the flythrough's aim scored a population that no longer existed, and nothing marked the dump stale", whose gate that entry still records as owed (the same stale citation is also the second half of the register's 2026-09-18 crowd-anchor entry). **D3**: the near ground in those frames is crushed to black (`left-ground` mean luminance 11.84 with 11 distinct colours, `near-pavement` 12.33, flat in every held frame), which is a statement about **superseded frames** and not a live defect. The run's manifest records `capturedAt` `2026-09-18T01:49:09.620Z` and the crowd frames were written 18:48:10-18:48:27 local on 2026-09-17; it carries build hashes rather than a git revision. The two appearance changes those frames predate both landed that evening: the dusk ambient fill `d2a2f57` at 19:43:50 and the road's grain spectrum `e384e28` at 20:18:55. The current certificate's own inspection of the near field (`artifacts/inspection-final/REPORT-satellite-plaza.md` §5 and §7, runId `e6b7b9d8de0faf69`, the same certificate the sweep finding above is bound to) records the black end gone rather than carried forward — frame means 56.8-92.2, pixels under luminance 3 at 0.00-0.02% and under 8 no more than 0.05% — and no lane has re-flown the flythrough's own pose since either change, so D3 is not claimed fixed at that pose. Two independent read-only reviews returned **no blocking code findings**: `artifacts/review-flythrough-fixes/r1.md` on the two fixes (preserved on main as [round 32](reviews/32_implementation.md); red control preserved, swing exact, driver refactor behaviour-preserving, five recorded mutations reproduced) and `artifacts/review-acceptance-evidence/r2.md` on the evidence chain (preserved as [round 33](reviews/33_implementation.md); the certificate's build, harness and agent-free scene digests all re-derived exactly; the certified hero frames' TAA read corrected; the F1 census reproduced independently). R1's six non-blocking findings and R2's record corrections are applied on `f160fe9`, merged and pushed, with the four non-visual gates green there and the build reproducing the certificate's bundle. **The caveats, recorded rather than hidden**: the sweep's terrain hole above, with the state of its fix; the crowd leg's two frame-level findings — its stale aim-evidence dump (D2) and its crushed near ground on frames the evening's lighting changes superseded (D3) — against a crowd that now resolves; and the flicker half of line 268, which is unanswerable from these frames and is why that criterion stays open. R2's two blocking findings are criterion-unmet statements, not defects: the signature shot (line 270) and performance as written (line 272), both recorded above. **Updated 2026-09-18: the fix is merged, the certificate is re-issued, and eight lanes re-inspected its 44 frames with no blocking defect anywhere — the finding this entry carries is closed.** `400065a` merges `phase2-regen` to `main` (five commits off `4efaea6`); review 34 is landed as `docs/work/0_shibuya-1km/reviews/34_implementation.md`, its one blocking finding was in the record rather than the code and is applied; and all five gates are green on the merged revision — build 0, typecheck 0, `npm test` **74 files / 535 tests, 0 failed**, audit 0 and `npm run visual` 0, recorded in `docs/devlog/detailed/2026-09-18_terrain-cap-and-census-gate.md`. The certificate is runId **`aa2e07e9902a5dc9`**, `certifiedAt` `2026-09-18T04:47:44.725Z`, binding `sceneTreeDigest dfee0f12bb2e87c1defa7c41c0400e0df4f98ab829fd346d5d689da0c406873c` over 117 files / 301,782,754 B at `a73d718a…` / `c890299c…` / `11dd032c…`, both lanes on the RTX 4090 at driver 616.64, **279 s** for the 44 frames; all 44 frame digests verify (44 match, 0 mismatched) and lanes independently reproduced the scene digest and the three build hashes. **In the frames**: every one of the fourteen interior rims is shaded ground wherever it appears and the count of exact-background pixels inside the rim polygons falls from **940 / 746 / 214 / 606 / 242 / 446** per frame to **0** in the six cartographic overhead frames, with the satellite overhead set's enclosed background islands falling from 4 / 3 / 3 / 8 / 9 / 13 to 0, and the served mesh is **183,350 triangles at 1,214 boundary edges in one loop with 0 duplicate and 0 non-manifold edges** against the pre-cap mesh's 1,441 edges in 15 loops. The **+3.25 m over about 42 m²** at H1 and **+1.74 m over about 19 m²** at H2 spill predictions were reproduced exactly from a pre-cap mesh recovered by stripping the cap's 227 triangles and **do not read**: the luminance step across the cap's own boundary is at or below the same frame's ordinary ground texture at every azimuth (`az300` p99 75.3 / max 82.9 against a control p99 87.3 / max 204.7; `az060` mean 3.48 against 7.65) and the outer silhouette is unmoved. Three bounds limit what these frames are evidence for, and the dated block below carries them with the owed list: the authored west-approach road paint is present and correctly placed but not legible at any certified pose, H1 and H2 are visible only in the overhead pair, and the satellite-block az060 framing still carries a shadow-side plane statistically indistinguishable from that frame's sky.
- **The two world styles** (line 276) — **the frame half is met and measured; the "simulation progress" half is not measured under a switch.** Both styles hold up at street, block and aerial distance across 36 inspected frames (window grids follow the storey rhythm in cartographic; photographic facades resolve in satellite), the dropdown is driven by real pointer and keyboard input in the spec and was verified live in `c11f6b5`, the registry is extensible by construction, and the **round trip restored the view**: seven of eight dusk crops byte-identical to the approach frame with only the style control's focus ring differing. **A switch made while the population is running is now measured rather than claimed**: `artifacts/style-switch-sim/REPORT.md` drives a pointer switch and a keyboard switch with 3,000 pedestrians and 56 vehicles running and returns **met within this bound** — the fixed step advances (+91 and +73 ticks) and never resets, stalls or goes backwards, no cumulative counter dips across 24 samples spanning the press, the population never detaches and never shows tick 0, and the post-switch camera holds the five-millimetre bound this repository already names. Its bound is one build (`a73d718a…`/`c890299c…`/`11dd032c…`), one **pre-batch** served scene (`1ebda662…`), one seed (9137), one pose and one switch in each direction; what it cannot answer is whether the *same named body* continued along its route, which would need a read-only `bodies()` observation the frozen bridge does not publish. **Updated 2026-09-18: the 36 sweep frames this entry rests on have been re-inspected against the capped scene, two styles at three distances, and both styles pass with no blocking defect.** The cartographic and satellite lanes each report the storey rhythm, the road, kerb and pavement boundaries and the crossing's paint holding at plaza, block and overhead distance, with the same known PLATEAU limitations (mottled aerial photography over LOD2 roofs, facade-atlas softness at grazing angles) and no new defect — the two block-distance lanes' largest finding is that the terrain, road, pavement and markings changes of the F1 batch are **invisible at that framing** (a pose-tolerant residual whose maximum 32×32-block mean is 0.2 luminance levels with zero blocks above 2 of 920, against the pre-batch arms' own 0.097–0.219 floor). The cross-preset material test still separates the two styles on their own fine structure: the satellite-plaza lane's masked fine-band r is 0.993 / 0.926 / 0.868 / 0.854 with pavement controls at 0.952–0.994, and the two az240 pavement cells return −0.933 / −0.947, the sign reversal the adjudication records. `style-switch-sim`'s bound above still names the **pre-batch** served scene `1ebda662…`, so that measurement describes the scene the previous certificate bound and not the one the current certificate binds.

### Visual acceptance state, 2026-09-15

Two criteria above have evidence from the 44-frame capture and are settled here rather than left blank; the rest stay open with the reason.

**Settled.** Buildings hold up close (line 145) and the dusk frame reads as Shibuya rather than a generic Japanese city (line 146), both on the 44 frames of the capture bound to G1. The facade criterion is carried by the hero and plaza frames at street level and by the block and overhead frames from above: window grids follow floor counts, the generated cartographic windows sit on the storey rhythm, and the PBR response separates sunlit from shaded faces at both times of day. The dusk criterion is carried by `hero-satellite-dusk-crossing.png` and `hero-cartographic-dusk-crossing.png`: the crossing's zebra and diagonal arms are legible, the emissive signage reads (`TOKYO`, `SHIBUYA`, `TSUTAYA`), and the sunset band carries the sky. Both rest on frames the coordinator opened individually, with the bound recorded in `artifacts/frame-inspection/report.md`: the image tool returns 1066×600 previews of the 1280×720 sources, so single-pixel detail is outside what that review signs off. Nothing in these two frames depends on that detail — they were judged on structure, signage and tone, which survive the downscale.

**Deliberately still open, with the reason rather than a blank.**

- Line 148 (the dusk preset's post chain, and "holds still — no flicker or crawl over a moving sequence") is **not** satisfied at the time of writing, and the reason is measured: `artifacts/gate-timing/REPORT.md` found `taaAccumulating: false, taaSamples: 0` on all eight hero frames, reproduced on both renderers, so the temporal accumulation the criterion names never engaged. The frames also cannot answer "over a moving sequence" at all — that needs the controls-driven flythrough, which is Phase 10. This is the clearest case in the plan of a criterion whose mechanism is present in the code and absent from the pixels.
- The road-markings criterion — "Road markings match reference imagery, including the scramble's diagonals, with signals, guardrails and street furniture in place", which this entry used to cite as "Line 147" and which sits at line 161 — is **now adjudicated rather than partly carried**, by round 30 at 1:1. Met with measured evidence: both scramble diagonals exist at their authored and OSM bearings and widths, the GSI reference confirming the junction's shape and position to 5.6–7.8 m; the zebra bands are on every arm meant to carry them, stripe period measured from pixels at 1.10 m against an authored 1.05 m; the tactile strips sit on OSM's own tactile paths; three of the four mapped controls within 80 m are placed; street furniture is present for what the source carries. **Not met: guardrails** — `src/scene/street-details.ts` returns `guardrailPosts: 0` as a literal while every sibling member is tallied from live records, and the nearest source barrier is 295 m away, so the one field a reader would check cannot report a count. The reference's ground sample distance is **0.486 m/px**, not the 0.6 this entry's round assumed: zoom 18 is 0.5972 m/px in projected metres and ground metres are that times cos(35.6595°). At 0.486 m/px a 0.5 m stripe is one pixel and a 0.15 m line is a third of one, so the criterion's thin-paint half cannot be adjudicated against the photograph at all — that is a bound, stated rather than crossed. Round 30's frames predate `d00c84b`, so its six frame digests describe pre-fix bytes; re-capturing them is owed if this criterion's evidence is to sit on a revision carrying the crossing fix.
- Lines 149 to 152 and 155 to 156 stay open, and the reason has changed rather than lapsed. The population now completes routes — pedestrians 1,815 crossed and 309 completed, vehicles 311 and 203 over 360 s at 3,000 and 200, **and every one of those four figures is superseded: see the correction that follows this list** — but round 29's independent review found three of these criteria failing on their own subject: vehicles interpenetrate (120 overlapping oriented-box pairs, deepest 4.147 m) and nothing measured spacing; the crowd overlaps (46,615 pedestrian pairs) and nobody is on the crossing, because the scramble is a single compound that no route may terminate inside; and the running app delivers 10 to 18 fps with 60 of 200 vehicles drawn, because the bus class cannot spawn anywhere in this network. The signature shot is reachable without a contract change — 17 of 33 portals have a legal route crossing the diagonal, nearest approach 0.078 m — and the blocker is the population's route distribution. **Superseded 2026-09-18: the distribution clause is falsified on `HEAD`.** 1,533 of 3,000 slots (51.1%) already drive a route that traverses at least half the authored diagonal, and the measured blocker is a held-admission stall at `junction:walk:665322367:0:0:ground0`; the route-distribution lever is a measured dead end. See the signature-shot entry in the status subsection and the closing dated block.

## Implementation steps

Thirty-five items across eleven phases. The numbering is stable and append-only — refer to items by number, and add a new item at the end of the list rather than renumbering. Item 35 is a Phase 4 item added in Phase 1 and so sits out of numeric order under that phase.

### Phase 0 — Toolchain & gates (blocks everything)

- [x] 1. Vite + three.js + TypeScript scaffold; `.nvmrc` pinned to Node 24
- [x] 2. Playwright visual harness that drives OrbitControls and never assigns camera pose directly — the canon names direct state-setting structurally blind, with `scenes` as its case study
- [x] 3. Fill the currently-empty `Gates` section of `AGENTS.md` with commands actually run in this repo

### Phase 1 — Data & provenance

- [x] 4. Fix the AOI to the box above
- [x] 5. PLATEAU **Shibuya-ku FY2025** CityGML LOD2 for the covering tiles — five years newer than the Tokyo 23-ku FY2020 bundle this item first named, spec v5, a tenth the download, and it adds LOD3 roads and street furniture
- [x] 6. OSM via Overpass — lanes, crossings, signals, sidewalks, rail
- [x] 7. Terrain from **PLATEAU's own 2.5 m TIN**, which ships in the same archive, same CRS, same vertical datum, 80,896 triangles for the AOI — Shibuya is a valley and Dōgenzaka means slope; flat ground reads as wrong immediately. If GSI is used at all it is as a cross-check, through the 1 m `dem1a_png` tile API, not the 5 m mesh download this item first named
- [x] 8. Record licenses (PLATEAU CC-BY 4.0, OSM ODbL, GSI) and build an in-app attribution surface

### Phase 2 — Geospatial pipeline (offline, deterministic, cached)

- [x] 9. Project to JGD2011 / Japan Plane Rectangular CS IX (EPSG:6677), local origin at the crossing so floats stay small
- [x] 10. Buildings from **MLIT's pre-converted 3D Tiles**, not from a local CityGML conversion; unwrap b3dm to glTF, georeference, clip to AOI. The PLATEAU GIS Converter this item first named has no prebuilt Windows CLI — the release is a GUI installer and the CLI ships only for Linux and macOS, so it needs a Rust build on a path upstream never CI-tests. The decision and the sample it rests on are in the Outcome section below; the CityGML archive is still fetched, because terrain and the authoritative attributes come from it
- [x] 11. Terrain mesh from the DEM; snap building footprints to it
- [x] 12. Tile the output for culling and LOD; outputs stay gitignored and regenerable, under the canon's blob ceilings

### Phase 3 — Static scene

- [x] 13. Terrain, buildings and road surfaces rendering
- [x] 14. Camera, controls, first visual-gate baseline

### Phase 4 — Realism pass (where the deliverable is won)

- [ ] 15. Facades — PLATEAU's photographic texture is the albedo base for the 96.4% that has one, with generated roughness and metalness over it; floor-count-derived procedural window grids are the treatment for the untextured 3.6% and for hero close-ups where the aerial texture visibly fails, not the default for the AOI
- [ ] 16. Emissive signage and neon — the single highest-value item in the plan
- [ ] 17. Road markings including the scramble's diagonals, signals, street furniture, guardrails
- [ ] 18. Wet-asphalt reflectance, sidewalk paving
- [ ] 35. De-light the PLATEAU facade textures — they are aerial photogrammetry with daylight and hard shadows baked into the albedo and no emissive channel, median 10 KiB, typically a 512x512 atlas holding a small oblique crop. Left alone they paint midday shadows onto a dusk scene. Appended here rather than inserted because the numbering above is stable and append-only

### Phase 5 — Lighting & post

- [ ] 19. Time-of-day sun and sky, with dusk as the hero preset — best realism per unit effort, and it forgives LOD2's weaknesses
- [ ] 20. ACES tone mapping, bloom to carry the neon, SSAO, TAA

### Phase 6 — Network graph (shared contract; must freeze before Phases 7 and 8 run in parallel)

- [x] 21. Lane-level road graph plus sidewalk and crossing graph from OSM — reviewed offline network milestone; inferred widths and missing ground remain disclosed in [the canonical network contract](../../reference/network-contract.md).
- [ ] 22. Signal phase model driving vehicles and pedestrians from one clock — shared deterministic controller and admission API reviewed; both populated consumers remain unimplemented.
- [x] 23. Document and freeze the schema — it is the interface both agent workstreams build against. Constraint: the OSM-derived network graph stays in its own files, never fused with PLATEAU geometry. The render is a Produced Work with no share-alike, but this graph is a Derivative Database, and publishing the app obliges offering recipients that derived database or a description of the method (ODbL 4.6). Keeping them separable is nearly free now and expensive to unpick after the schema freezes

### Phase 7 — Vehicles

The reviewed network milestone is on main; integrated simulation remains pending. Original, unbranded kei hatchback, tall taxi and city-bus source recipes are selected. Steering, shipping appearance and contact validation remain open; see the current delivery status above.

- [ ] 24. IDM car-following with MOBIL lane changes
- [ ] 25. Signal obedience, turns, boundary spawn and despawn
- [ ] 26. Instanced rendering

### Phase 8 — Pedestrians

The reviewed network milestone is on main; integrated simulation remains pending. Pinned CC0 adult character sources are selected. Production animation, naturalness and shipping motion/contact validation remain open; see the current delivery status and `reviews/7_implementation.md` for the bounded reference proof.

- [ ] 27. ORCA/RVO2 local avoidance with a spatial hash
- [ ] 28. Scramble-crossing behavior driven by the signal phase — the diagonal surge is the signature shot
- [ ] 29. Thousands of animated characters via baked vertex animation textures, since three.js instancing does not do skinning natively

### Phase 9 — Performance

Pending integrated vehicles and pedestrians. The target is 60 fps at 1920×1080 with 3,000 animated pedestrians and 200 vehicles; performance has not yet been measured against it.

- [ ] 30. Draw-call and LOD budget; 60 fps at 1080p under target agent counts
- [ ] 31. Profile, fix, re-measure

### Phase 10 — Acceptance

- [ ] 32. Multi-angle, multi-zoom sweep with each frame inspected at native resolution
- [ ] 33. Real-controls flythrough exercised over time
- [ ] 34. Independent review, then merge to main

## Outcome

Phase 0 is complete and merged to main. Items 1, 2 and 3 delivered a Vite + three.js + TypeScript scaffold on Node 24, a Playwright harness that drives OrbitControls with synthesised pointer and wheel events and writes twelve frames at 1280x720, and a filled-in `Gates` section in `AGENTS.md` naming five commands that were all run here.

Four of the visual gate's failure paths have been made to go red on purpose; three more are written but unproved. `docs/learning/gate-proofs.md` holds the mutation and the message for each.

Phase 0 also set the contracts every later phase builds against. These are contracts and not conventions: changing one changes every system downstream of it.

- **The world frame**, in `src/world/frame.ts`. Scene units are metres, Y is up, the world origin is the Shibuya Scramble Crossing at 35.6595 N, 139.7005 E, +X is east and +Z is south. Everything that places anything reads it from there rather than restating it.
- **No global coordinate reaches the scene.** `planeRectangularToWorld` swaps EPSG:6677's northing-first axes and subtracts the origin at load time, so no eight-million-metre northing ever lands in a float32 vertex buffer. Projecting to EPSG:6677 is Phase 2's offline job, and Phase 2 supplies the origin.
- **Homes for the geometry.** `src/scene/terrain.ts`, `src/scene/buildings.ts` and `src/scene/roads.ts` for those three, `src/agents/agents.ts` for pedestrians and vehicles. Each holds a named seam and a comment saying which phase fills it.
- **Two hooks on `RenderLoop`**, in `src/render/loop.ts`. `onFixedStep` runs at a constant rate and is where Phases 7 and 8 both register, so one clock drives vehicles and pedestrians and Phase 6's signal phase model can drive both from it. `onFrame` runs once per drawn frame with the real elapsed time, for anything that should look smooth rather than be reproducible.

### Phase 1

Items 4 to 8 are done. The data is on disk, verified, attributed, and reachable by one command; the scene is still a placeholder, because rendering it is Phase 3.

`npm run data:fetch` gets everything into a gitignored `data/`. The PLATEAU archive is 649,322,807 bytes, SHA-256 `f7437469d85b1d4a85f2141671b08bbb84d6e05cb15ad2a8b4e8f6a28e67831d`, pinned in `tools/data/manifest.ts` and checked by length before hash on every run. Truncating it by 4,096 bytes was tried: the script reported "4096 bytes short of the expected 649322807 — a truncated download" and re-fetched. The Overpass extract came back at 1,211,346 bytes and 7,915 element records, `osm_base 2026-09-07T03:23:56Z`.

**The AOI is one definition now**, in `src/world/aoi.ts`, and `src/world/frame.ts` takes the world origin from it rather than restating it. `tools/data/manifest.ts` builds the archive member list from the mesh codes there, and `tools/data/overpass-query.ts` builds the Overpass bbox from the same bounds.

**Two new gates, both proved red.** `test/aoi.test.ts` pins the projection and the composed chain from degrees to scene metres, and `test/elevation.test.ts` compares PLATEAU's terrain against GSI's independent survey at the crossing. Both mutations and both failure messages are in `docs/learning/gate-proofs.md`. These catch the two defects that would otherwise be invisible: a mirrored city, and terrain uniformly 36.877 m too high.

**Attribution is in the running app**, not only in a file. `src/ui/attribution.ts` replaces the placeholder in `index.html` at boot and throws if the element is missing, so the scene cannot draw without its credits.

**The conversion path changed, and item 10 with it.** Two `.b3dm` tiles covering the crossing were fetched from MLIT's own 3D Tiles build and parsed against the CityGML the fetch downloads. The pre-converted tiles carry a 63-key batch table per tile — `bldg:measuredHeight`, `bldg:storeysAboveGround`, `bldg:usage`, `gml_id` and per-feature bounding boxes — which cross-checks exactly against the CityGML on two named buildings: Hikarie at 173.6 m and 34 storeys, Shibuya Stream at 171.3 m and 35 storeys. They carry `_BATCHID` per vertex and a five-level hierarchy of 730 tiles, 67 of them over the AOI, which is item 12's culling unit already built. Texture is a 2048x2048 WebP atlas per tile, and the 75 tiles covering mesh 53393596 carry 314.6 Mpx against that cell's 184.4 Mpx of source JPEG, so the route is not resolution-limited. The converter would instead have needed an untested Windows Rust build and would have emitted one GLB per feature *type*, which is the wrong granularity for item 12. Full evidence in `design.md`.

**The plan's texture premise was wrong and is corrected.** Parsing all four building files here independently reproduces the research figure: 1,741 buildings in the AOI box, 1,678 with LOD2 and a texture, 96.4%, with a perfectly clean split — no textured LOD1 and no untextured LOD2. Item 15 was rewritten and item 35 appended for de-lighting.

**Provenance lives in `design.md`**, distilled from a read-only research pass and marked where this phase re-measured rather than carried a claim.

What Phase 1 did **not** do, and Phase 2 should not assume: nothing has been rendered, the Draco decode in those tiles has never been exercised, and the 3.6% of buildings that are LOD1 live in a separate tileset that must be merged on `gml_id`. The last of those turned out to be false — see below.

### Phases 2 and 3

Items 9 to 14 are done. Real Shibuya renders: PLATEAU's 2.5 m terrain TIN, its `tran` road surfaces, and all 1,740 buildings in the box from MLIT's own 3D Tiles, with photographic facades, standing on the ground.

**The loader decision: `3d-tiles-renderer`, against a tileset the pipeline has already moved into the world frame.** The tileset is a real five-level `REPLACE` hierarchy with geometric errors from 316 down to 0, which is item 12's culling and LOD unit already built; what a purpose-built loader would have had to add is screen-space error selection, a download queue and an eviction policy. The last is not optional here — 551.9 megapixels of texture over the 67 AOI tiles is 2,943 MB decoded — so a loader of our own would have grown the same LRU cache with less testing behind it. The cost is three transitive dependencies this project does not use and a library that assumes tiles sit on an ellipsoid; the second is paid offline, where `npm run data:scene` rewrites every `region` bounding volume as a `box` in world metres, so the browser holds no coordinate over about two kilometres and Phase 0's frame contract is kept exactly.

**Item 11 says "snap building footprints to it" and nothing snaps anything.** That is deliberate and it is a better result than snapping would be: the terrain TIN and the buildings come out of the same PLATEAU survey, so a building's own `_zmin` is already the ground under it. What the pipeline does instead is *reconcile* the two — it recovers the vertical datum shift from the tiles' own geometry and refuses to build if the two sources disagree — and the measured spread between a building's lowest decoded vertex and its stated ground is 0.094 m over 2,607 buildings. A snap would have hidden that disagreement rather than measured it. The visual gate checks the outcome from every framing: the lowest building geometry drawn sits at 10.80 m against ground that runs 8.71 m to 36.35 m.

**Three things this phase found that a passing test would not have.**

- **glTF is Y-up and `CESIUM_RTC` translates in a Z-up frame.** Nothing in a b3dm says so and `design.md` did not either. Placed without the turn, every building landed a median of 68 m from where its own batch table puts it and the city rendered lying on its side over the right street pattern. Placed with the turn folded into the tile matrix *and* the tileset not declaring `gltfUpAxis: "z"`, the library applied its own on top and it tilted again.
- **The tile cache counts decoded bytes.** Sized from the 137 MB the tiles weigh on disk, it could not hold a single 4096x4096 leaf, so the traversal never refined past the root and the app drew seventeen decimated buildings over the whole ward while reporting itself loaded, idle and error-free.
- **PLATEAU's road polygons below LOD3 are flat at z = 0**, as `design.md` warned, and the part it did not warn about is what happens at the edge: a road ring reaching past the terrain has nothing to be draped onto, so it stays at sea level and drags asphalt out past the edge of the world. 461 polygons were dropped for that.

**Corrections to `design.md`, which is the project's provenance.** Six of its claims were wrong or incomplete and each is now marked in place: the LOD1 buildings are in the same tileset rather than a separate one; the sentinels read `null` in the tiles rather than −9999; the atlases run to 4096x4096 rather than 2048x2048, which is 5.5 times the memory; the AOI holds 1,740 buildings by this repository's own count rather than 1,741, and the count is sensitive to the centroid rule because 25 buildings sit within two metres of the boundary; the Draco decode now works and is exercised; and the up-axis fact above was missing entirely. One claim held exactly — `_zmin` and `_zmax` are orthometric where the ECEF geometry is ellipsoidal, and the difference between them recovers the geoid undulation from the data alone at 36.786 m against a published 36.877 m.

**Two new gates, both proved red**, in `docs/learning/gate-proofs.md`: `test/sentinel.test.ts` over a real PLATEAU batch table, and `test/placement.test.ts` over the ECEF and up-axis transforms. The visual gate gained a third check that has fired on two real defects — the bounding box and triangle count of the building geometry actually in the scene, which is the first thing in this repository that looks at the scene rather than at the framebuffer.

**What Phase 4 inherits, and should not rediscover.** Building textures are capped at 1024 pixels on the longest side at load time, in `src/scene/texture-budget.ts`, because the alternative is a scene that cannot hold the area of interest at leaf detail; the table of what each cap costs is in that file and the number is one constant. The 64 LOD1 buildings render as untextured white, which is item 15's stated job. Nothing here touches materials or lighting.

Phase 4 is next.


### Phase 6 network milestone

Review 5 accepts the separate OSM graph, deterministic signal/reservation admission, supported 3D vehicle footprint and atomic boundary lifecycle. Earlier reviewed failures and all four exact contract snapshots remain permanent. The graph has 5,094 nodes, 4,855 vehicle sections, 1,740 walking edges and 213 controllers; all 401 original conflict disks and every portal remain. The 11.6 m supported body bound enlarged the scramble union to a 211.029 m bounding-box diagonal without inventing a larger physical disk. Source lane/width inference, 47.196 m of undrapable walking path and the single no-U-turn dead-end demand exclusion remain explicit.

The accepted module is available through `data:network` and `/network/network.json`; this milestone retains the main baseline application and its eighteen-view SwiftShader visual harness. It does not claim the pending two-style 44-frame graphics gate, rendered network decorations, animated agents, natural traffic throughput, actual wheel/pavement contact or continuous boundary steering. Items 24–29 and all populated/visual acceptance criteria remain open. Phase 6 item 22 closes only when both running populations use this one clock.

## Coordinator status, 2026-09-17 — the Phase 10 evidence is complete, two flythrough defects are fixed and merged, and the re-measured performance changes the answer at the acceptance population

`main` is `39ca459`, pushed, and it is the only branch. Two merges landed this session, each with the four non-visual gates green on the merged revision and the production build reproducing the certificate's bundle byte for byte (`dist/assets/index-B3qA34nq.js`, `b50735b70fca…`) because neither touched `src/`: `f06aaf8`, the flythrough sequence check repaired per the flythrough2 adjudication (8 files, +388/−73), and `39ca459`, the approach swing repaired per the clearance adjudication (6 files, +235/−23). Every worktree this session used was removed with its `data` and `node_modules` junctions unlinked first and its evidence enumerated before deletion; the merged branches were deleted. Nothing is left on a branch.

**The native inspection of the certified set is complete, and it is the evidence this deliverable has been missing.** All 44 frames were cut into eight tiling 1:1 crops each, opened at their own size by nine read-only lanes, and every frame digest was recomputed against `artifacts/visual/complete.json` (runId `94fc1aa78b0af8f9`) with every crop digest recomputed against its manifest: zero mismatches, zero blocking defects. The two `style-return` frames were opened beside them as lane evidence — they are at the **approach** pose and carry no entry in the certificate, as the 2026-09-18 re-inspection measured (the Phase 10 entry above carries the two measurements). What the reports carry, by severity: near-field pavement detail loss at the closest framing, severity-mapped by azimuth (severe in satellite `plaza-az000`, whose `left-ground` and `near-pavement` are uniform black; notable at `az120`/`az180`; worst instance is cartographic `plaza-az000/left-ground`, one flat colour across a 320×180 cell); facade atlas softness, severe in satellite `plaza-az060`'s close facade; a dusk-crushed tower shadow filling about a third of satellite `block-az060` as near-featureless black, where cartographic at the same pose keeps structure — a photographic-dusk tone reading, not a geometry fault; a grazing-angle wash band across satellite `az060`'s carriageway; a plain white kiosk-scale cuboid and an untextured recessed ground-floor panel as notes; and the known PLATEAU limitations (mottled aerial photography over LOD2 roofs, pale roof treatment under a low sun) confirmed where expected. The scramble's diagonals and zebra bands are present and legible wherever a crossing arm is in frame, road markings survive the satellite↔cartographic switch at both hours, and the fixed black-surface-across-the-crossing defect is explicitly absent. **The style round trip is measured rather than asserted**: seven of the eight dusk crops of `return-cartographic-dusk` are byte-identical to `hero-cartographic-dusk-approach` and the eighth differs only by the keyboard focus ring on the style control; at noon the content matches with a ±1-level renderer dither. The reports are under `artifacts/inspection-gpu/`.

**The populated set is inspected, and it answers the signature criterion in the negative for a reason the pixels make plain.** All 38 frames were opened and judged, with 1:1 and 3–4× crops for detail. `walkers` is a living population: pedestrians walking at 1–2 m/s, mid-stride, feet planted, no interpenetration, a fourth walker entering and crossing the frame. `corridor` shows two packed knots of pedestrians standing shoulder-to-shoulder for all 14 simulated seconds — the recorded queueing problem, in pixels — and **no vehicle in any frame** at a pose whose stated purpose is to watch one hold its lane through a signal. `boundary` changes by 2 pixels across 14.2 s, its portal is hidden behind rooftops, and the manifest's lifecycle counts are constant, so no spawn or despawn occurred to be seen: the criterion is not covered by pixels at that pose. `crossing` is **14 of 14 frames byte-identical** across 26.15 simulated seconds spanning the whole pedestrian green, with zero pedestrians and zero vehicles in frame: that is a true depiction of a crowd that cannot reach the scramble — 3,000 rendered, 0 within 60 m of the camera, crossing events still accumulating elsewhere (1 → 77) — not a capture defect, and the routing bound is recorded (`MAXIMUM_ROUTE_LENGTH_M` 700 m against a 505 m nearest portal and a >1,000 m round trip; no centre route nearer than 31.228 m). **Superseded 2026-09-18: `MAXIMUM_ROUTE_LENGTH_M` is read at one site inside the legacy `walk()`, and the measured blocker is a held-admission stall rather than this routing bound; see the signature-shot entry in the acceptance criteria status subsection.** **One recorded misreading is corrected**: the lit red lens at that pose is a *correct* red — vehicle head `scramble:vehicle:2` during the pedestrian green and the clearance that follows it; the scramble's cycle is 6,960 ticks with the capture ending at 6,862 — and the network maps **zero pedestrian signal heads**, so the old "pedestrian head red in its own green" reading cannot hold. Reports are under `artifacts/inspection-populated/`.

**The performance answer changed, and the new measurement is bounded openly.** With the tick lever landed and the machine quiet, the acceptance state at 1920×1080 with 3,000 pedestrians and 59 vehicles drawn delivers **p50 16.7 ms / p95 33.4 ms** — 59.88 fps from the median, so **the median frame holds the 60 fps target at the acceptance population**, against the p50 83.3 / p95 183.4 ms recorded on 2026-09-16. The fixed step is p50 11.8 ms (was 75.7), the frame's own cost p50 5.6 ms, 28.0% of frames run catch-up steps and 3 frames (0.3%) sit at the five-step cap (a majority did before), and the simulation tracked wall clock (20.13 s in 20.58 s). The ceiling is pinned at the top of the plan's bracket: **the largest drawn population holding a ~16.7 ms median is 3,000 itself**, no measured population fails the median, and the first failure lies above 3,000 and is unmeasured; a secondary boundary is recorded — **2,500 is the largest whose p95 also stays within one vsync quantum** (16.8 ms), while 3,000's p95 slips a full frame to 33.4 ms. The bounds are named: one machine (i9-13900KF, RTX 4090 over D3D11), one seed (5970698), one window per arm with no repeats, and the numbers describe the certified build **plus an observation-only probe seam**, because the seam that gives the lane its frame boundary has never existed on `main` — that instrument defect is recorded in the register as its own entry, with its permanent fix owed. Load is tabulated per arm (12–42% CPU, against the 66–77% the old numbers ran under), which is part of why the comparison is stated rather than claimed. This **supersedes the "measurably infeasible at 3,000" reading for the median**: the pedestrian half is now reported as met at the median with **no margin left** — the state would not hold on a slower machine and the p95 tail is one frame — and the vehicle half remains the owner's recorded infeasibility, untouched by these numbers (59 of 200 drawn). Report at `artifacts/frame-budget/REPORT-20260917.md`.

**The flythrough is repaired in both halves and has passed a run.** The first run failed its own check with two complaints that were both the check's: leg-first frames were recorded with zero travel where the interface already says `null` (four phantom pairs), and the crowd leg's documented six-step hold — whose travel is genuinely sub-millimetre once damping dies — was counted as an undriven camera, while an absolute luminance threshold read a textured dusk facade as a blank frame (4.2% of cells against a 5% floor, where 24.3% carried relative contrast above 0.25). `f06aaf8` fixes all three and writes the pose ledger before the **sequence** assertions — the per-frame clearance, renderer and viewport checks still abort the capture loop before that write, and the manifest's `failures` field is the sequence judge's failures, not every failure — with seven mutations watched red. The second run then failed honestly on the repaired check: the approach leg commanded its swing the long way round (7.7231 rad, 1.2872 rad a step) against the 0.3491 rad a step the driver's 40 px rotate cap actually delivers, so the bearing ended 0.9381 rad short and the crowd leg opened on a hill face about 17 m from the scored pose — which the per-frame clearance check caught as a camera 0.58 m below the ground reference. `39ca459` makes the swing a monotone 1.4399 rad interpolation the short way (0.2400 rad a step), leaves the ascent unchanged because its unwrapped form was measured to converge, and pins both through the driver's own exported rotate arithmetic in `test/flythrough-plan.test.ts`, watched red against the old plan. The third run passes: exit 0 in 1.7 minutes, 46 frames over four legs, the crowd leg opening at azimuth 127.5° on the scored pose with 3.6 m of clearance, the held pairs accepted on the scene-stays-alive rule, and TAA reporting 0 of 46 accumulations, which is by construction in a populated run — the agent-free hero frames in `artifacts/visual/hero/hero.json` are where TAA is evidenced. Its frame inspection is in flight.

**The independent review's non-blocking findings are applied (2026-09-17, `worker/flythrough-review-fixes` off `144766b`).** Six findings, two of them real gaps in the checks and four recorded-number or record-wording defects. Corrected numbers: approach-005's calibration is **17.4%** (25 of 144 cells, 6 absolute + 19 relative), re-measured from the surviving `artifacts/flythrough2/frames/approach/approach-005.png` and agreeing with that run's manifest, against the 15.3% recorded from a deleted scratch file; and the ascent lands **exactly on its final step**, index 10 of 0..10, the tenth carrying the last 0.0436 rad of **3.5343** rad of travel, against the "step 10 with one step spare" and "3.538 rad" recorded before — its plan test passes that step only because the tolerance is 0.05. Two gaps closed and each watched red: the structure metric's relative leg gained an absolute floor (`deviation > 2`, mean above 8) because a flat dark surface with ±1 dither scored 1.0 in every cell, and the hold exemption is now bounded by the plan's own `holdsCamera` steps rather than by the record's flag, so a set of frames that marked every one of itself held no longer exempts the whole route. `test/flythrough-frames.test.ts` is 18 cases with nine mutations recorded in `docs/learning/gate-proofs.md`.

**One newly adjudicated scene defect is recorded and deliberately not fixed here.** The cartographic overhead lanes' black patch is a ~510 m² hole in the terrain at about (+580, −404), roughly 80 m past the AOI's east edge: PLATEAU's own TIN has no triangles there, so it is an upstream data gap, one of 14 interior holes the census found (4 inside the AOI on the Shibuya River line, hidden under buildings; 10 in the margin ring). It is visible only in cartographic because through the hole both styles see the sky dome's near-black below-horizon colour, which contrasts with cartographic's pale ground (37.6 against 82.9) and is indistinguishable from satellite's olive (25.7 against 30.3). The minimal fix (cap interior boundary loops over their own rim vertices in `tools/scene/build-terrain.ts` and refuse to ship uncapped loops) and the gate that should own it (`npm run data:scene`'s watertightness census) are recorded; it is **not landed** because it moves the scene digest and would strand the certified frame set, so the disposition is the independent review's to make.

**What remains before this deliverable can be called accepted**: the two independent read-only reviews of `39ca459` — one of the two flythrough fixes and their tests, one of the acceptance evidence chain — with their material findings resolved; the inspection of the passing flythrough's 46 frames at their own size; and this document's acceptance criteria re-marked against the evidence above. The reviews are running.

**All three of those are now done, and their outcome is recorded here.** Both reviews returned **no blocking code findings**. `artifacts/review-flythrough-fixes/r1.md` verified that the red control still requires every travel pair to be still (held frames included, and it returns before the hold exemption), that the null-travel rule applies only to pair-less leg-first frames, that a dead hold fails by name on digest, render counter and ticks, that the driver's extracted rotate arithmetic is the pre-fix inline code verbatim, that the swing ends exactly on `CROWD_AZIMUTH` (0.2400 rad a step = 27.50 px against a 40 px cap), and it reproduced five recorded gate-proofs mutations red with their recorded messages. Its six non-blocking findings — the structure metric's relative leg allowing a flat dark dithered surface to score as structure; the hold exemption not being self-limiting; the spec comment overstating where the pose ledger is written; the ascent's "one step spare" and 3.538 rad figures; an unreproducible 15.3% calibration; and two trivial record claims — are all applied on `f160fe9`, each verified against the real bytes before the change (the review's own repro mechanism was corrected: the metric samples every second pixel, so the flaw needs a two-pixel-block dither, and the shipped guards leave `approach-005` at 17.4%, 25 of 144 cells, exactly what the run's manifest recorded). `artifacts/review-acceptance-evidence/r2.md` re-derived the certificate's build hashes, its harness digest (5 roots, 23 files) and its scene digest exactly, and established the scene's real identity: the certificate matches the tree it was captured against — 84 files, 214,114,015 bytes, `ddd21ee1…` — and today's served tree is larger by exactly the 33 restored human-agent files, which the certified frames cannot depend on because the verdict lane runs without `?agents=`. It reproduced the F1 census independently (515 m² uncovered at 1 m sampling; 14 interior loops; `x = +580` is 81.35 m outside the box), confirmed all 58 crop manifests and 464 crops recompute, and corrected two record readings — the certified hero frames' TAA (now fixed in this document) and the populated capture's noon time, which the plan's own paragraphs did not name. Its two blocking findings are criterion-unmet statements rather than defects: the signature shot and performance as written.

**The passing flythrough's 46 frames were inspected at native resolution by three lanes, and the findings are the record's most useful new material.** `artifacts/inspection-flythrough/REPORT-overview-ascent.md` passes both legs with no blocking defect: the bearing is genuinely held across the overview, the climb is monotone from 17.8 m to 110.1 m above target, the mip transition resolves as a smooth gradient, and across 20 adjacent pairs with 17–441 m of travel there is no whole-frame discontinuity, so no popping. It carries one low finding that contradicts a sentence of this plan — the flight is **not** a closed loop: `ascent-010` measures 857.4 m out against the leg's own 620 m aim (38% over) and `overview-000` is 159 m short of its 543 m aim, because the driver's per-step corrections are capped (a 0.25 rad polar step delivered 0.1322 with `settled: false`); the pixels are unaffected, the claim is. It also records that exposure is constant at the dusk preset's own value in all 22 frames, so the ascent's stated "exposure change" does not exist in the pixels, and that it opened 36 of the 176 crops at 1:1 — a bound it states rather than glosses. `artifacts/inspection-flythrough/REPORT-approach.md` passes with defects: clearance falls 60.84 → 3.50 m with a 2.685 m minimum, tiles are idle with none pending or failed at all twelve captures, the swing lands within 0.846° of its planned 82.5°, and bloom never clips (0.01% of pixels at any channel ≥ 250, with the paving grid still readable inside the brightest glow). Its medium finding is that the ground and road materials carry **no texture at all** — `terrain.ts` and `roads.ts` use surface materials with no map, the ground's only albedo variation is a ±3% sinusoid at about 20 m, and the road's grain fades out above 0.015–0.08 m/px, which is this leg's own band — so five of twelve frames are mostly a blank surface where the leg should be resolving detail (mid-ground sd 1.8–3.4 against 10.15 on a roof deck at a similar mean, so it is the surface and not the tone curve). It also records that the clearance check samples only captured instants — the stand records show the camera 0.194 m above ground at step 9 (`settled: false`) and 1.078 m at step 10, captured at 2.685 m and 3.101 m, so the path dips inside the 1.5 m threshold between frames and the check cannot see it — and that no pedestrian casts a shadow anywhere in the leg because `castShadow` is set only for the near level while `renderedNear` is 0 in all twelve frames. `artifacts/inspection-flythrough/REPORT-crowd.md` is the one that grades blocking on the leg's **purpose** rather than on the render: the render is stable and artifact-light (five held frames share one pose to the bit, 918,000+ of 921,600 pixels identical, no crawl, bloom pop or shadow swim), but the subject never resolves — two near-black clumps of 36×69 and 40×48 px in the top 80 rows, no head, limb or foot legible at 1:1 or at 6×, the centre 640×360 band byte-identical across five frames, and `renderedNear` 0 throughout. A rigid-shift search over ±14 px between `crowd-008` and `crowd-011` returns dx = dy = 0 with MAD 2.75/1.43 luminance while 12.1% and 6.6% of the knots' pixels change in place, so the knot does not walk; it rearranges where it stands, which is the queueing problem confirmed at pixel level. The mechanism is arithmetic from the manifest's own pose: the orbit target's y is pinned at the app's origin datum of 15.200 m while the local ground is 26.36 m, making the view axis 34.7° down with the frame's top edge 7.2° below horizontal, so the whole frame is ground 1.9–28.8 m ahead. No input can flatten that axis — panning is horizontal by construction — so **the crowd leg's questions about shape, interpenetration, feet and crowd-reading are unreachable at a high-ground anchor with this camera model**, and the report's prescription is a re-shoot rather than an edit: aim at a knot on low ground, or change the rig's fixed target datum, and add a consecutive-frame capture. That is recorded as a coverage finding; it is not repaired here, and it does not change any criterion's verdict, because the signature criterion's blocker is the routing that keeps the crowd off the crossing (line 270).

**The quality work that follows this acceptance round is tracked in [quality-register.md](quality-register.md)**, with the audit's file:line verification behind it (`artifacts/quality-audit/register.md`, ignored). It organizes the remaining work by the certificate: Phase A is everything that leaves the certified frames valid — the aim scorer that picked the crowd anchor by believing a 27.5 m knot stood at 16 m, the clearance check that read only captured instants, the missing consecutive-frame instrument, the crowd leg's smaller findings — and Phase B is the one batch that moves what the certificate pins (the untextured mid-ground and road materials, the F1 terrain-hole capping with its watertightness gate, and the frame-budget probe seam, which the audit measured to be pin-moving in every possible shape because Vite merges it into the entry chunk and `verify-output.ts` pins every `dist/assets` file). It records what is rejected and why: satisfying the dusk criterion's TAA clause over a moving sequence is impossible in this app by construction, and landing any Phase B item alone would strand the frames and the inspection.

**Two incidents and one status correction belong here, and both incidents are recorded in full in the register.** The first: a quality lane ran data-writing commands in a worktree whose `data/` was junctioned to the primary, on the coordinator's own instruction, so `npm run data:scene` rewrote the served scene — its second attempt failing after `cleanSceneGeometry` had already deleted it — and `npm run data:markings` re-derived `data/network/network.json` from the live extract, breaking its Review 10 pin. Recovery is verified: the pinned network was restored from a surviving copy, `test/paint-support.test.ts` is 6 of 6, `npm test` is 61 files and 453 tests green, and terrain, roads, pavements, markings and the network are back at their reviewed bytes. **What does not recover is the certificate's scene binding.** The served scene digest is now `20ac847450f9…` where the certificate binds `ddd21ee1…`, and the reason is structural rather than accidental: `sceneTreeDigest` folds `data/scene/manifest.json`, which carries a `builtAt` timestamp, so **any** rebuild moves the digest with byte-identical geometry — the digest is a build identity, not a content identity, and a certificate's scene binding can never be re-derived after a rebuild. The 44 frames and their native inspection remain valid for the bytes that were inspected; what is lost is the gate's ability to re-verify that those bytes are the served city, and the Phase B re-capture re-issues the certificate against the new digest. Normalising the timestamp out of the digest is now a Phase B item, and `docs/policies/local-rules.md` carries both rules: a data-writing lane must not junction `data/`, and any scene rebuild strands the certificate's scene binding until that normalisation lands.

**Quality round, 2026-09-18 — the flythrough's crowd now resolves and walks, the road tile works where it is measured, and the black near field is not fixed.** All five gates are green on `1e3597d`: build, typecheck, 68 files / 494 tests, audit, and a fresh 44-frame certificate (runId `14cdadbafba82120`, same RTX 4090 and driver, three lifecycle records). The controls-driven flythrough passes in the primary at the loaded end of its tick window (46 of 46 distinct digests, judge zero failures), and its crowd leg is the first in this deliverable's history whose subject resolves: about 45-55 distinguishable figures at 35-45 px (1.4-1.8 m, 7.6 head-heights), no visible interpenetration, and the five zero-travel held pairs are live — blobs shift rigidly at 1.05-1.34 m/s, matching the population dump. Two inspection findings are being fixed (the exported `CROWD_CAMERA` constant is 180 degrees out from the pose the flight actually flies, and the lane's aim evidence cites a stale dump), and two are recorded rather than fixed: the ascent still ends 857.4 m out against its 620 m aim, and `stand.targetY` is the world-origin 15.2 m in every frame, up to 6.8 m below the ground under the camera. **The appearance change is a partial success and is reported as one**: the detail tile's neighbouring-texel step went from 0.36% to 6.35% of albedo and the cartographic near-field road went from 99.9% to 63.5% indistinguishable-from-flat with a 14x neighbouring-pixel difference, confirmed by eye on native crops, so the flat-road finding is answered where the bar measures it; while the black satellite near field went from 10.4 to 16.6 of 255 against a floor of 20, so it is not fixed, the palette was shown to be the wrong lever and was not tuned further, and the proposed next change is the dusk light on horizontal surfaces (about 2.5x irradiance, or the road's wetness), which needs its own capture. **Two incidents are recorded in the register and both were the coordinator's**: a lane wrote the primary's served scene through a `data` junction on the coordinator's instruction, and a coordinator's cleanup followed seven junctions an enumeration had missed and emptied seven fetched source directories — recovered, with the network pin restored, `paint-support` 6 of 6, and every fetched source re-downloaded and verified against its pin (the live OSM extract cannot match the reviewed one, and that is named). What remains for the appearance work: the dusk-light lever above, the flicker instrument for the dusk criterion's temporal half, F1's cap (branch `phase-b-terrain-cap`, awaiting a pavement-input review), and a full native re-inspection of the new frames, of which the changed cells are done and the rest still bind to the previous build.
## Coordinator status, 2026-09-18 — the goal closes with the terrain cap implemented and verified against the served bytes, unmerged, and the certified 44-frame set knowingly stale

**Read this block before trusting either the criteria list above or the scene on disk, because the two say different things and both are true.** `origin/main` was `17052ea` when this was written, 2026-09-17T21:21:50-07:00, and this record is the commit directly on top of it. The terrain-hole fix exists, is verified against the bytes the app serves, and is **not on `main`**, so a reader who finds only a branch is not looking at an unstarted fix; and the served scene has already moved past the scene the current certificate binds, so a reader who sees a closed ground in `data/scene/terrain.mesh` is not looking at a landed one either.

**What landed on `main` in this round.** `5515e59` corrected the Phase 10 criterion to say the sweep half is not clean and carried the terrain-hole finding with its evidence — both overhead reports, the background-colour core sampling `rgb(22,24,33)`, the 1.9 px pose reconstruction, the bounding-box match against the 2026-09-15 measurement, and `artifacts/terrain-holes/REPORT.md` as the root cause and census — superseded the crowd caveat with that lane's **resolves** verdict (about 45-55 distinguishable bodies, held frames live at 1.05-1.34 m/s, `Blocking: none`, plus the stale-aim-dump and crushed-near-ground frame-level findings), and added the rule that a citation saying a change landed names a commit reachable from `main`, anchored to an audit of 87 cited commits of which 78 are reachable and 9 resolve but are unreachable, five of them errors established with `git patch-id --stable`. `8ff7367` added the companion rule that a tracked document is written with the line endings it already has, anchored to a two-line `plan.md` correction that reported 469 insertions and 469 deletions under `core.autocrlf=true` with no `.gitattributes`. `213af4c` and `17052ea` changed `quality-register.md` from other lanes, and this record does not touch that file.

**The cap is implemented, gated and verified, and it is on a branch.** `phase2-regen` in the primary checkout and `worker/f1-terrain-cap` in its worktree both sat at `0e7d683` (off `4efaea6`, five commits: `f1b3bc9` the cap and census gate, `38bb0ac` the TIN reader fix, `7ad6ed0` its gate proof and register entry, `d5ead63` the pavement-input re-binding, `0e7d683` the session record); `phase-b-terrain-cap` at `0c76789` is the superseded first attempt, a single commit that sat 33 commits behind `main` when the rebase happened. Phase 1's code is nine files at +1,295/−21, and the four non-visual gates are recorded green on the re-bound revision — build 0 with the three certificate-pinned build files byte-identical, typecheck 0, 72 files / 525 tests, audit 0 (`d5ead63`'s message; the session record at `7ad6ed0` names 72 files / 522 tests at that earlier revision). Six mutations are recorded in `docs/learning/gate-proofs.md`, each watched red, and the one that matters most is the one that first stayed green: removing the cap from `buildTerrain` left all seven cases passing until a fixture-pipeline case drove the builder itself over a test-written 8x8 `dem:TINRelief` grid with one cell omitted. The TIN reader fix is what makes the census correct — 384 triangles were parsed twice — and it took the built mesh's duplicate triangles 65 → 0 and its more-than-two-face edges 195 → 0 while leaving the hole census unmoved at 1,441 boundary edges and 14 interior rims.

**The served scene is regenerated with the cap, and these are the bytes on disk rather than a summary of them.** `npm run data:scene` ran to completion in the primary with the cap in place: `terrain.mesh` `fef57d09…` → `c39d49ed…` (4,413,112 → 4,415,496 B) and `roads.mesh` `3fe126cd…` → `0d429c31…` (6,499,344 → 6,509,676 B; 79,244 → 79,367 triangles; 461 → 338 polygons dropped for reaching past the terrain). Running the repository's own instrument, `node artifacts/terrain-holes/mesh-doctor.mjs data/scene/terrain.mesh`, reports **183,350 triangles, 92,283 vertices, degenerate 0, duplicate 0, more-than-two-face edges 0, boundary 1,214 edges in one loop**, against the backup mesh's 183,188 / 92,269 / 0 / 65 / 195 / 1,441 edges in 15 loops — which is 183,188 − 65 + 227 and 92,269 + 14 exactly. The served `manifest.json` (4,074 B, sha256 `afa6403c…`) records `closedHoleCount` 14, `capTriangleCount` 227 and fourteen `closedRims`; their `rimVertexCount` sums to 227, their `rimAreaM2` sums to **5,187.5 m²** against the 2026-09-15 census's 5,188, four of the fourteen apexes are inside the AOI, and there is no `builtAt`. Determinism holds on the regenerated scene: two consecutive runs produced 117 files / 301,773,802 bytes with **zero differing paths** (`artifacts/f1-cap/evidence/after1.json` and `after2.json`). `dist/` did not move — the three certificate-pinned build files are still `a73d718a…`, `c890299c…` and `index-dvQKBZaf.css` `11dd032c…` (a capital K), and only the unpinned source map differs.

**One number the brief predicted was falsified by the run, and it is the first thing review 34 must test.** `artifacts/f1-cap/BRIEF.md` argued that a moved pavement digest "can only come from the road polygons that now drape over the fourteen holes" — 123 triangles. The measured move is `pavements.mesh` 461,912 → **464,525 triangles** and 12,974,860 → 13,068,136 B, so 2,613 triangles rather than 123. The likely mechanism is ground support for sidewalk pieces that were previously dropped, and `d5ead63` restates the question for review 34 as a falsifiable claim rather than an assertion. The cap added no new defect class there: `degenerate` 25,448 before and after, `duplicate` 99,869 → 100,011.

**The markings compromise is recorded as a compromise, not as a re-derivation.** `npm run data:markings` refuses at `tools/scene/hero-paint.ts:30` because the OSM extract's provenance no longer matches the reviewed network's: `data/osm/shibuya-aoi.osm.json` is `976d848e…` at `timestamp_osm_base` `2026-09-18T01:49:46Z`, while `data/network/network.json` records `9923a9ae…` at `2026-09-07T03:23:56Z`. The reviewed extract exists nowhere on disk — the only `*.osm.json` in the repository is the current one, and hashing all 665 JSON files of 1 MB or more under `artifacts/` (2,109 MB) produced no match for `9923a9ae…` — and a dated Overpass attempt returned a **current** snapshot (`sha256 7805b653…`, `timestamp_osm_base` `2026-09-18T04:11:19Z`), so reconstructing it is unavailable here. The reviewed authored `markings.mesh` `efdba9cb…` (388,992 B) was therefore **restored** rather than re-derived, and the restoration is recorded in `artifacts/f1-cap/evidence/final.json` as the change from the 383,200-byte file the `data:scene` runs wrote. **The bytes are verified and the inheritance is now proved rather than owed**: the file on disk hashes to `efdba9cb…`, the directory this record was briefed to name as the source, `artifacts/scene-backup-20260917/scene/`, holds a different markings mesh (`4daa4283…`, 383,200 B) and is not it, and the only byte-identical copy on disk is `artifacts/network/app-motion/candidate/data/scene/markings.mesh`. `artifacts/f1-cap/probe-inheritance.mjs`, re-run in the primary on the merged revision 2026-09-18, samples all **204 vertices** of the authored paint across the old and new ground and roads and reports **0 samples missing on either side, maximum |old − new| 0.000000000 m on both, paint height difference 0.000000000 m** — so the paint still meets the capped ground and the restoration is sound. The durable decision — re-derive the network from a pinned extract, or narrow the guard to a content check that records both vintages — remains an owner-level source decision, with both costs named in `artifacts/f1-cap/BRIEF.md` §6. **Which command loses the paint is review 34's finding B1 and this record stated it wrongly:** the silent downgrade is `npm run data:scene` alone (`tools/scene/build.ts:159` calls `writeMarkings()` with the default `includeAuthored = false` from `build-markings.ts:82`, which overwrote the authored pair in this batch's own two determinism runs); `npm run data:markings` alone refuses by name at `hero-paint.ts:30`; and a clean `npm run data:setup` re-derives both the network and the paint from the live survey, replacing the reviewed bytes with nothing comparing them. `docs/learning/defect-register.md` carries all three paths.

**What is owed, in priority order.** The merge of `phase2-regen` to `main`, which this session lands; the **one batched 44-frame re-capture**, which the last certificate's own timestamps measure at **273.9 s** (`--begin` 2026-09-18T03:30:51.944Z → `completedAt` 03:35:25.883Z) rather than the three hours this repository's older notes still carry; and the **one re-inspection of those 44 frames** at native resolution, H1's and H2's fan wedges first. Review 34 is written and its findings applied. The markings inheritance proof is **done** — `artifacts/f1-cap/probe-inheritance.mjs`, 204 authored vertices, zero missing and zero movement, re-run in the primary on the merged revision — and the owner-level markings source decision it was blocking is still owed. Owed and separately scheduled: the walking-admission stall, now an approved work item whose fix lives in `src/agents/population/**`, so it moves `dist/` and needs **its own** capture rather than the batched one; its acceptance test is pinned in the signature-shot entry above (diagonal entry **on the green**, completions at or above **338** at 3000/200, `worstOverlappingPairs` at or below **142** at 150/0), and the arm that would demonstrate the located cause is pre-registered and running.

**Phase A's true state, including what is not met.** The frame-budget probe seam, the flythrough clearance bound and the crowd leg's resolution all landed. The flicker instrument's four fixes, plus a fifth defect and a cadence change, are committed on `flicker/measure` (`559bd3f`; the branch tip was `2bd5bba` when this was written) on base `4efaea6`, and **none of it is on `main`**. Its build is recorded byte-identical to `main`'s (`dist/assets/index-DRmtpVMl.js c890299c…` on both sides); `npm test` is recorded 520/522 with the two failures being the batch's pin mismatch — `paint-support` on `data/scene/roads.mesh` and `facade-emission`'s complete-plugin UV cases, both reading `data/scene` while that batch was rebuilding it — while typecheck and audit are asserted in the commit message and the report and have no surviving log, so they are recorded rather than verified. Its crawl verdict is the final-scene set, not the mid-rebuild one: `final-orbit.json` reads **9.9950e-4 to 1.3190e-3** and `final-ascent.json` **1.0369e-3 to 1.3506e-3** per rendered frame, so **9.995e-4 to 1.3506e-3** combined, against a `0.005` bound that stays **provisional** because the data cannot calibrate it. The earlier `1.035e-3`–`1.291e-3` table was taken mid-rebuild and the lane's report withdraws it, though the report was still being rewritten when this block was written and its body still carried the withdrawn figure; bind any citation to the record's digest rather than to a line number. The bound's **form** is flawed and carried rather than fixed: `CRAWL_FRACTION_PER_FRAME` is written per rendered frame and a pair's allowance is `bar × straddled span` (`tools/flicker/judge.ts:500`), so a slower screenshot makes the bar looser — 0.115 of a frame at a 23-frame span against 0.030 at RUN-01's 6-frame span, which is backwards. **The Phase A item "a consecutive-frame capture that can actually answer the dusk criterion's flicker/crawl half" is therefore NOT met**: the achieved gap is **20.5-23 rendered frames**, mid-shutter to mid-shutter, against the criterion's `cadenceFrames: 2`. The viable route is assessed with numbers and unimplemented — an in-page `requestAnimationFrame` grab with `drawImage` into a 2D canvas, recorded as roughly 1-3 ms and 3.7 MB a frame against about 250 ms and 31 rendered frames for `page.screenshot`, adjacency verified by `frameCount`, with `toDataURL` rejected at 30-80 ms of PNG encode; those figures are recorded with "roughly" and were not measured by a probe. The lane also found a defect in shipped `main`: `judge.ts`'s `channelDelta` read **byte** offsets against **pixel** indices, invisible to the 192x108 unit fixtures, which is where RUN-01's `changed = 1.0000` came from — correcting RUN-01's own attribution of that reading to misalignment, while its separate 30-35 px gesture finding stands. **Corrected 2026-09-18, when round 35's record pass landed at `2f276a5`:** the change is on `main` now (`409a57e`), so "none of it is on `main`" above describes the day it was written rather than the tree; the bar's coupling is not merely backwards but **quadratic** — the predicate is `unexplainedFraction > 0.005 × frameGap²` (`tools/flicker/judge.ts:504`, not the `:500` above), so it tolerates more than the whole frame past a 14.14-frame span and **cannot fire at the 20.5–23-frame spans this lane achieves**, which makes the 9.995e-4–1.3506e-3 figures a measurement rather than a class enforced, and makes "3.7x under the shipped 0.005" a comparison against a constant the judge never applies to a pair (the allowance at a 22.5-frame span is 0.1125); the `channelDelta` mechanism described above is also wrong — the reads never left the buffer at any frame size and the mutated call returns a finite `changedFraction` (0.9702 at 1280x720, 0.9930 on the unit frame), so what hid the defect was the unit case's own pre-fix assertion written to the bug's output rather than a fixture too small to reach it; the 27-34-frame shutter figure is the **pair sum**, the per-shutter spans being 16–17 on `orbit` and 14–18 on `ascent`, unchanged from RUN-01's 16–18; the fine pass's refine window is **23 px, not the 41** the comment and the report carry; and **`ascent` emits no wheel event at all**, so it does not change distance and the criterion's "or an ascent" pose is not exercised. The corrected text lives in `quality-register.md`, `docs/learning/defect-register.md`, `docs/learning/gate-proofs.md` and `docs/policies/local-rules.md`.

**The signature shot's cause is corrected in the status subsection above, and it is located to one expression rather than to a queue** — the no-admissible-velocity fallback at `src/agents/population/pedestrians.ts:314-321`, with 133 of the 148 frozen bodies zeroed by it against 14 gate halts. The 700 m cap is read at one site inside the legacy `walk()`, 51.1% of slots already drive a crossing route, and the route-distribution lever is a measured dead end. `docs/learning/defect-register.md` carries the two dead mechanisms this investigation exposed — `spacingQueryRadiusM`, declared at `config.ts:84` and read by nothing, and the `queued` counter, which counts every stopped body rather than gate holds — and `artifacts/signature-routing/STALL.md` §5 carries the correction to the two records that cited the counter.

**The two world styles' "simulation progress" half is no longer a code claim** — `artifacts/style-switch-sim/REPORT.md` measures it met within its bound, as the status subsection now records.

**These artifacts are provenance and this tracked record is the durable home of every number above**, per this repository's rule that raw task output stays ignored and is deleted once no active task needs it. `handoff.md` lists the paths whose loss would leave a claim in this document checkable only by re-deriving it.

## Coordinator status, 2026-09-18 — the cap is merged, the certificate is re-issued, and the eight-lane re-inspection finds the holes closed and the spill prediction not reading

**Read this block before trusting the criteria list above or the two dated blocks above it, because the tree moved twice under the last one and that block's own state line is the state before this batch landed.** `origin/main` was `2f276a5` when this was written, 2026-09-17T22:20-07:00, and this record is the commit directly on top of it. The terrain-hole fix is now **on `main`** — `400065a` merges `phase2-regen`, five commits off `4efaea6` — the served scene and the certificate both describe the capped ground, and the re-inspection the previous block recorded as owed **has run**: eight read-only lanes over the 44 certified frames, every one of them passing with no blocking defect. Nothing in this block came from a gate, a build or a `data:*` command; it records what the certificate, the eight reports under `artifacts/inspection-final2/` and the repository's own documents measure.

**The revision, the gates and what the certificate binds.** `main` = `origin/main` = `2f276a5` as this was written, with the batch merge `400065a` and review 34 (`docs/work/0_shibuya-1km/reviews/34_implementation.md`, landed in `3f1fb77`) beneath it. All five gates ran once on the merged revision and each is green: build 0, typecheck 0, `npm test` **74 files / 535 tests, 0 failed**, `npm run visual` 0 and audit 0, recorded in `docs/devlog/detailed/2026-09-18_terrain-cap-and-census-gate.md`. The certificate is `artifacts/visual/complete.json`, runId **`aa2e07e9902a5dc9`**, `startedAt` `2026-09-18T04:43:05.663Z` → `completedAt` `04:47:44.715Z` and `certifiedAt` `2026-09-18T04:47:44.725Z`, so the 44-frame gate took **279 s**. It binds `sceneTreeDigest dfee0f12bb2e87c1defa7c41c0400e0df4f98ab829fd346d5d689da0c406873c` over **117 files / 301,782,754 B**, build `dist/index.html a73d718a…`, `dist/assets/index-DRmtpVMl.js c890299c…` and `dist/assets/index-dvQKBZaf.css 11dd032c…` (a capital K), both lanes on the RTX 4090 at driver 616.64, with three lifecycle records whose page-error lists are empty and whose teardowns completed. All 44 frame digests verify: the coordinator re-hashed them against the certificate (44 match, 0 mismatched), and lanes independently re-derived the scene digest from the served tree and recomputed the three build hashes from `dist/`.

**The re-inspection: eight groups, all PASS, no blocking defect.** Four lanes took the two styles at the plaza and block distances, two took the two overhead sets, and two took the hero and hero-approach frames; every frame digest was recomputed against the certificate before inspection, and each lane's crops — 48 native 1:1 regions for a sweep subset, eight for a hero frame — were opened at their own size rather than through a contact sheet. The reports are `REPORT-hero.md`, `REPORT-hero-approach.md`, `REPORT-satellite-overhead.md`, `REPORT-satellite-block.md`, `REPORT-satellite-plaza.md`, `REPORT-cartographic-overhead.md`, `REPORT-cartographic-block.md` and `REPORT-cartographic-plaza.md`, all under `artifacts/inspection-final2/`.

**The finding this batch existed to answer is closed, in the mesh and in the pixels.** PLATEAU's DEM TIN had fourteen interior rims; the cap fans each closed with one apex at the rim's own vertex mean, and the served mesh is now **183,350 triangles and 92,283 vertices at 1,214 boundary edges in a single loop with 0 duplicate and 0 more-than-two-face edges**, against the pre-cap mesh's 183,188 triangles / 92,269 vertices / 65 duplicates / 195 more-than-two-face edges / **1,441 boundary edges in 15 loops** — which is 183,188 − 65 + 227 and 92,269 + 14 exactly. In the frames, the number of exact-background pixels inside the rim polygons fell from **940 / 746 / 214 / 606 / 242 / 446** (az000/060/120/180/240/300) to **0** in the six cartographic overhead frames, and the six satellite overhead frames' enclosed background islands fell from **4 / 3 / 3 / 8 / 9 / 13** to **0**, with the count inside every other rim at every azimuth 0 as well; the two az060 patches that survive are open background beyond the terrain's own footprint, identical in size and position in both arms. The largest hole in the mesh — H1, in the AOI at 1,325 m² — is drawn ground at all three azimuths where it was visible.

**The spill prediction does not read as a defect, and the check was made rather than asserted.** `artifacts/f1-cap/BRIEF.md` §5 predicted the invented surface standing up to **+3.25 m** above surveyed ground at H1 (about 42 m²) and **+1.74 m** at H2 (about 19 m²). A lane recovered the pre-cap mesh by stripping the 227 appended cap triangles, which reproduces the pre-cap census exactly — 1,441 boundary edges in 15 loops, and all fourteen rims matching `data/scene/manifest.json`'s `closedRims` on vertex count, perimeter, area and centroid 14 of 14 — and reproduced both heights exactly; its own 1 m grid finds **46** and **23** proud cells rather than 42 and 19, the difference being the rim-boundary binning of a serpentine rim, with the maxima agreeing to the centimetre. It then measured the luminance step across the cap's own boundary against the same frame's ordinary ground texture at every azimuth: at **az300** the step's p99 is **75.3** and its max **82.9** against a control p99 of **87.3** and max of **204.7**, and at **az060** its mean is **3.48** against **7.65** — so the step is at or below the frame's own texture at every one of the six azimuths, which is what rules out a lip, a ridge or a shading discontinuity. The silhouette is unmoved: tracing the outermost non-background pixel of every row and column before and after moves **0 rows and 0 columns** at four of the six azimuths, at most **1 px on three rows** at az120 and at most **4 px on two columns** at az060.

**What does read, and is not a defect, is the fan's own triangle size.** A fan triangle is **17–34 m²** of plan area against the survey's 2.5 m cells, so where it fills a slope the TIN renders as many small steps the frame shows one larger plane: at az000 and az300 the fan's footprint is a **strip a few pixels wide along a street corridor**, slightly cooler in tone than the pavement beside it, visible under 4–6× magnification and essentially invisible at 1:1. The satellite lane reads the same surface at 12× as a smooth straight-edged paved slab whose texture energy is *up* (7.30 → 7.88) and mean luminance *up* (43.09 → 48.19), so it is a change of surface character rather than a flat fill; the hero lane sees that change and no step above the window's own terrain-and-building population. It is the honest appearance of invented-but-plausible ground, and it sits exactly where the hole was.

**The batch's data changes are provably invisible away from the overhead framing, and that is a measurement with controls rather than an assertion.** At the block distance a pose-tolerant residual (±4 px, which absorbs the accepted 0.012 rad pose tolerance instead of reporting it as change) gives a maximum **32×32-block mean of 0.2 luminance levels and zero blocks above 2 of 920** at every azimuth, against the same instrument run between the two pre-batch arms, whose own floor is 0.097–0.219 — so the six block frames differ from the previous scene by no more than two runs of the previous scene differ from each other. The control that shows the instrument works is one shot away: over the overhead frames of the same certified run the identical code finds the change, a 32×32 block at (64,672) on overhead az180 with mean **9.99** and **121 pixels over 24 levels**, on a frame whose pre-batch noise floor is max 8. The six satellite plaza frames are the **same picture** as the pre-batch `grain` after-arm capture to the last bit the comparison can see: a 48-cell residual range of **r 0.9977–0.9999** with fine-band r **0.9978–1.0000**, and per-cell statistics agreeing to three decimals (az000 `near-pavement` reads mean 27.560 / pixel sd 4.429 / residual sd 2.710 / fine 1.129 on both sides). The road's per-cell numbers on the new frames reproduce the reviewed ones digit for digit: az000 `near-pavement` at **mean 77.94, spread 5.91 and adjacent step 2.911** against the pre-batch report's 77.94 / 5.91 / 2.91, with `left-ground` at 78.10 / 5.26 / 2.795 against 78.10 / 5.26 / 2.80.

**Coverage bound 1: the authored west-approach road paint cannot be judged from these frames.** The paint is present and correctly placed — `data/scene/markings.mesh` is `efdba9cb4532db02…`, 388,992 B, whose decoded self-contained tail is exactly **204 vertices / 68 triangles from index 13,671**, with all 68 centroids **4.0–6.0 cm above the highest surface beneath them** (min +0.040 m, median +0.045 m, max +0.060 m, 0 below and 0 unsupported) and `src/scene/roads.ts` adding the mesh unconditionally to the roads group — but no certified frame can show it. At az000, az060, az120 and az180 the whole 79 m treatment is **35–120 m away and edge-on**, compressing the 12 m street to a 2–8 px band, so a 0.15 m lane separator is a 1–2 px line and the **largest single triangle the entire tail projects is 6–9 px²**, with even those centroids outside the frame, which puts **no turn arrow inside the frustum at any of the six azimuths**. At az240 and az300 the camera stands on or beside the paint and 30–38 of the 68 triangles are behind it or blow up at 1 m depth, leaving a 230–371 px² sliver. Where it does fall in frame it is **drawn but faint**: +1 to +17 luminance levels above the asphalt beside it, a 1–3 px-wide local maximum at az180, az240 and az300, and no lane dash or arrow legible at 1:1, 4× or 8× — the legible white dashes in that rectangle are the obliquely-seen zebra stripes, which belong to the procedural crossing paint and to neither markings mask. A frame looking along the west approach from a few metres would settle it, and none of the 44 is one.

**Coverage bound 2: H1 and H2 are visible only in the overhead pair.** Both hero lanes put the rims behind their camera — at the hero-crossing pose H1 lies **174.5°** off the view axis and at the hero-approach pose **152.8°**, with H2 at 175.0° and 155.9°, so no style, hour or magnification brings them into a hero frame — and the approach lane's projection puts thirteen of the fourteen rims behind the camera's own image plane (rim centres at pixel depths of −137 m to −745 m) with the fourteenth, H8, more than 3.7 frame widths off the right edge. At the block poses H1 and H2 are behind the camera at az000 and az060, outside the frame at az120 and az300, and in frame only at az180 and az240, where both land on building fabric in both arms. At the plaza poses all fourteen are behind the camera at az000, az060 and az120, outside the frame at az180, and at az240 and az300 they land on facades between the camera and them. So the overhead framing is the **only** one of the 44 that can see any rim at all, and a reader must carry that bound rather than read it as an omission.

**One appearance finding is carried and unresolved.** At the satellite block az060 framing a large shadow-side plane reproduces its recorded state and is effectively textureless: its **5×5 local-sd median/p90 is 0.00 / 0.34, identical to that frame's clear sky**, and its cross-preset fine-band correlation is **0.022 against the same sky patch's −0.025**, where material surfaces on the same run read **0.70–0.98** (0.979 at az120 `left-ground` asphalt, 0.834 on the az180 street, 0.699 at the az000 junction). Its mean is 28.3 with a spread of 3.2 against the frame's own sky control at 2.1, so the earlier dusk fill has lifted it out of near-black and it is still a smooth two-dimensional gradient with no feature at any scale the lane looked at, at 1:1 or at 6×. It is about 28% of the frame, it is shade and not a hole, and this batch did not touch it — every statistic is identical to the pre-batch rebuild to the printed digit. The measurement to carry is the sky-identity rather than the word "matte"; the plausible cause is ambient-only lighting on a shadow face, and the **open question is which surface it is and whether the lever is its material or the lighting**.

**Five record corrections the lanes made, each of which the next reader would otherwise re-derive.** (1) The two `style-return` frames are at the **approach** pose, not the crossing, and they are **not certificate-bound**: `complete.json` lists 44 entries (8 hero + 36 sweep) and `tools/visual/verify-output.ts` never names `style-return`, while `artifacts/visual/hero/hero.json` carries their digests in `styleReturns[].sha256`; `return-cartographic-dusk` differs from `hero-cartographic-dusk-approach.png` by a mean of **0.38** luminance levels against **30.70** for the crossing frame, and its frame mean 92.63 sits on the approach frame's 92.36 rather than the crossing frame's 117.59. (2) `tools/inspect/crops.ts`'s region names are **screen geometry and carry no material claim at any pose**, and six misreadings across four lanes came from reading one as one — at the block pose the bottom band is roofscape at five of six azimuths, at the hero crossing pose the cell a prior report called road is pavement, and the same crop name is asphalt at `plaza-az000` and pavement at `plaza-az060`; the rule is that a lane classifies a surface from the pixels and says what it based the classification on. (3) "Nothing below luminance 8 in any frame" is a **rounding artefact**: 0.000–0.022% of each frame is below 8, in 0–31 small components, 97–100% of it below 8 pre-batch too. (4) The **frame change is attributed and it is the batch working — the premise was wrong.** The changed pixels are **not** outside the fourteen rims: az300's region `x[1092..1251] y[222..250]` looks at plan x 532.4..723.7, z 492.5..716.6, the corridor carrying H2, H5, H6, H7, H9, H10, H12, H13 and H14, and of its **1,066** changed pixels **505 lie inside a rim footprint** (454 in the eight order-verified rims, 51 in rim1), **532 are pixels where the pre-cap ray met no ground surface at all**, and 13 move to the roads layer and 3 to pavements; az240 (`x[722..759] y[0..65]`) reads 422 changed, 214 in a rim, 225 with no surface before, 3 roads and 2 pavements. The jitter question is settled by control, not argument: the best integer shift within ±6 px is **(0,0)** for all six overhead frames on all three pairings, and where the two pre-cap arms themselves differ across a whole frame at 11,167–14,594 px past 2 levels they differ in these two regions by **3 px and 1 px** against 1,066 and 422 changed, so a whole-frame claim at this framing is worth nothing. Almost none of it is the road drape, which is real but small (**43 px and 3 px**): it is **532 px of cap void becoming ground** (mean pre-cap luminance 42 → 124) plus **473 of the remaining 510 px whose surface is the same layer at the same position** — the ray hit point identical to under 1 cm in plan and `dy 0.0000 m` at all 510 — **but whose stored vertex normal moved, median 5.5° and max 27.2°**, the batch's cull of 65 duplicate terrain triangles, which **review 34 recorded without recording that it moves shading**; z-fighting was tested and rejected (5 of the 510), and `control-hardware.json` and `decorations.json` are excluded on mechanism. It is correct, not a defect: the road drapes on the fresh ground at **14.06–14.92 m** against fan apexes of 9.7–12.4 m, with no seam at the cap and no silhouette move, so the "new content no one attributed" is the former black holes at nine rims plus a normals shift, not mis-draped street. Bounds: a 950 m still cannot say whether the invented heights are the right heights and a street-level view is needed to judge the 1–3 px drape, and two rim polygons could not be order-verified, so "47 of 123 added road triangles outside every rim" is an upper bound. The report and its instruments are `artifacts/inspection-final2/REPORT-unattributed-change.md`. (5) `artifacts/terrain-holes/REPORT.md`'s hole numbers are not area order and `manifest.json`'s `closedRims` is append order, so a reader joining a per-hole figure to the manifest must match on **centroid** rather than on index.

**The batch's own numbers, recorded where they are checkable.** The C5 pavement re-binding is accepted: keyed on positions the terrain's old position array is an exact prefix of the new one, `new.indices[0..549,369)` is the old index array with exactly the 65 reader duplicates deleted in order, and the 227 appended triangles use only old rim vertices plus one apex and each spans two adjacent rim vertices (227 of 227); the roads mesh gains exactly 123 triangles with 0 old triangles absent; and the pavement delta is 2,691 triangles appearing and 78 re-noded in place, 2,691 − 78 = **+2,613** against the brief's predicted 123, every changed triangle within **15.1 m** of a rim ring (median 4.9 m), the whole delta explained by `pavement-recipe.json`'s `offTerrainPolygonCount` falling 197 → 116 (**−81**) and its base `polygonCount` rising 48,555 → 48,636 (**+81**). `pavements.mesh` now sits at **92.9% of its 500,000-triangle ceiling**, which is the batch's budget bound. Review 34's one blocking finding was in the record rather than the code: the register, the devlog, `plan.md` and `handoff.md` all said a clean `data:setup` would silently lose the authored paint, and the three code paths are now stated separately — `npm run data:scene` **alone** silently downgrades the pair to unauthored (`tools/scene/build.ts:159`, `build-markings.ts:82`, which this batch's own two determinism runs did), `npm run data:markings` alone refuses by name (`hero-paint.ts:30`), and a clean `data:setup` re-derives both the network and the paint from the live survey with nothing comparing them.

**What is owed, in priority order.** (1) The **owner-level markings source decision** — re-derive the reviewed network from a pinned OSM extract, or narrow `tools/scene/hero-paint.ts:30`'s guard to a content check that records both vintages; the reviewed extract is on no disk here and a dated Overpass attempt returned a current snapshot, so the authored mesh was restored rather than re-derived and its inheritance is proved (`artifacts/f1-cap/probe-inheritance.mjs`, 204 authored vertices, 0 missing, max |old − new| 0.000000000 m). (2) The **prescribed stall fix** at `src/agents/population/pedestrians.ts:314-321`, the ORCA no-admissible-velocity fallback, located and confirmed by falsification (the ORCA-zeroed count tracks the overlap count at every contact scale, 134 → 19 → 0 against 133 → 19 → 0) with its acceptance test pinned — diagonal entry on the green, `pedestriansCompleted` at or above 338 at 3000/200, `worstOverlappingPairs` at or below 142 at 150/0 — and because it lives under `src/agents/population/**` it moves `dist/` and needs **its own** capture rather than this batch's. (3) The **prescribed fix for the certificate-in-a-worktree evidence loss** — the wrapper certifies wherever it is run, so a gate run inside a worktree issues `complete.json` into an ignored tree that the merge deletes, and the fix belongs in the instrument (refuse to certify outside the primary, or write somewhere durable) rather than in a caution to the next lane. (4) The **prescribed `tools/flicker/judge.ts` docstring correction**: `:681-693` and the duplicate copy above it still state the falsified `channelDelta` mechanism, `test/flicker-judge.test.ts:250-259` repeats it, `:733-736` still says the refine window is 41 px against the real 23, and `:135-137` still quotes the pair sums as per-still spans. (5) The **crawl predicate's quadratic form** — `unexplainedFraction > 0.005 × frameGap²` cannot fire past a 14.14-rendered-frame span and this lane's spans are 20.5–23, so its crawl figure is measured and not gated. (6) The **assessed-but-unimplemented in-page consecutive-frame capture** — a `requestAnimationFrame` callback after the app's own loop with `drawImage` into a 2D canvas, measured at roughly 1–3 ms and 3.7 MB a frame against about 250 ms and 31 rendered frames for `page.screenshot`, adjacency verified by `frameCount`, with `toDataURL` rejected at 30–80 ms of PNG encode. (7) The **identification of the still-unattributed frame change** in the satellite block subset. (8) The **crop-label rule** as a standing one, with `tools/inspect/crops.ts`'s own docstring saying its names are screen geometry. (9) A **decision on the two `style-return` frames** — certify them and add them to the expected set, or state in the certificate's documentation that they are lane evidence and not part of the 44 — which is the quality register's own open row rather than an inspection finding.

**Nothing in this block is a build, a gate, a capture or a code change.** It is documentation written from the certificate, the eight inspection reports, `docs/work/0_shibuya-1km/reviews/34_implementation.md` and the repository's own devlog and register; no `npm run visual`, no `npm run build`, no `data:*` command and no gate ran for it, and the 44 frames the eight lanes inspected are the certificate's own bytes rather than re-rendered ones. The evidence is under the ignored `artifacts/inspection-final2/` and `artifacts/visual/`, and this record is its durable home: once that output is deleted, the figures above are checkable from this document and by re-deriving them from the certificate.
