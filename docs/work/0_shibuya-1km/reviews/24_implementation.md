# Review 24: implementation

## Target

Facade source binding and material preparation. The exact eleven-path facade increment in `artifacts/graphics-facade-increment/worktree`, source freeze `ad367811624b41e94850aa8d056f46ce9cacf4df31b9f95749079c37aed90f7b`. The assigned integration context was main `7f8aeac51357654a4d39d3e6ecc5f8ac0bda79b6`; implementation was based on retirement freeze2 `a8b4e520b8c74c989048ffdd0a34bce2c1f0c06eb3f51431a36997d3a65cf3e7`. This documentation checkpoint uses later main `edd448e` without changing the reviewed target.

The exact target is `artifacts/graphics-facade-increment/handoff.json`, SHA-256 `51047330219fe34791656b33e6e744d9e63742e40bf0c55fcdfc5a19f7163ac6`. Reviewed text sources are retained byte-for-byte under `artifacts/network/facade-b0-checkpoint/candidate-01/recovery/source/`; the consolidated `recovery/reviewed-source.patch` has SHA-256 `5be6da8e380b778de4d06369866a213d07650349c6239420b2e83d54efcc518a` across 46 source files for this checkpoint. The additive patch uses LF transport; exact copies and normalized reconstruction records preserve the original bytes and line endings. Raw output, source assets and earlier counterevidence remain ignored at their pinned handoffs. No experimental code is promoted by this document.

## Reviewers and coverage

Independent facade source reviewer authored the report below independently of the implementation. Root separately read its substantive findings and accepted only the dispositions stated here. The report is `artifacts/graphics-facade-source-review/report.md`, SHA-256 `bde3a61e590b76308b9214f49430b4db11331666ca2fc7918e725cdda378efbb`; its handoff is `3a3c6fc0bbf61f209b1e373384dfc2f6c5ac55f3fa9018a01d571b754c644fd7`, sealed at `2026-09-13T13:31:58.3106110+00:00`. The wrapper is authored by the network worker for root's review; it does not alter the reviewer's body or claim additional independent work.

Rounds 24–26 follow these three handoffs' recorded seal times on 2026-09-13, not completion of their underlying work. Historical Review 23 and the earlier ABBA preparation reviews remain unchanged; these are new source, focused repair and result-interpretation rounds. The full authored text is embedded with ATX heading depth increased by three; inverse transformation is checked byte-for-byte. Local finding labels and priorities are preserved verbatim; permanent IDs occur only in the wrapper dispositions.

## Reports

### Independent facade source reviewer

<!-- authored-report-start sha256=bde3a61e590b76308b9214f49430b4db11331666ca2fc7918e725cdda378efbb -->
Independent facade increment source review — 2026-09-13.

Disposition: no material new source finding in the exact 11-path increment. The finite loader, source-admission, layout and shader-assembly evidence supports moving this candidate to root's next integration gate. This is not native shader, appearance, lifecycle, city, performance or final graphics acceptance. No candidate, main, data, dependency or Git file was changed by this reviewer.

The candidate is `artifacts/graphics-facade-increment/worktree`. The assigned integration context is main `7f8aeac51357654a4d39d3e6ecc5f8ac0bda79b6`; its implementation base is the exact retirement freeze2 `a8b4e520b8c74c989048ffdd0a34bce2c1f0c06eb3f51431a36997d3a65cf3e7`. Source classification and the three-region contract had already been accepted by root from source-study handoff `8bc8277576a87ecab413f29a8c948df83f33046c7405ae456b25472f0a5a7091` and profile `100dd1c65c85838a70a693c045ffee93ce31517450b2f34745b455a031c6fb57`. This review did not repeat that native source-crop review or judge newly rendered appearance.

| Sealed target | SHA-256 |
| --- | --- |
| handoff.json | `51047330219fe34791656b33e6e744d9e63742e40bf0c55fcdfc5a19f7163ac6` |
| REPORT.md | `43627e47ec0afa0244f97275726753a82e4e0e066b2c6dc8c4c786d6776053aa` |
| source.patch | `2c741a9ab7b0866286abca82ceb7d6d3a23c1a97ed099588c0c45382c2c19a64` |
| source-freeze.json | `ad367811624b41e94850aa8d056f46ce9cacf4df31b9f95749079c37aed90f7b` |
| runtime-source-census.json | `dbf3431d55fb26913ed941ec003baf0524cebfbfb570eb445f2a31793547fb46` |

Independent complete pre-check and post-check passes verified all 307 owned records (53,491,155 bytes) and all 260 external records (140,413,907 bytes), with zero mismatches. These are the handoff's finite populations, not a claim to have inventoried the entire machine or every runtime file. All 225 retirement baseline source hashes matched. The candidate has exactly 231 frozen source files, with six new files and five modified baseline files. Its exact changed set is the declared 11 paths: two documentation files, `buildings.ts`, the two new profile modules, `facade-textures.ts`, `procedural-facades.ts`, `tile-materials.ts`, and three tests/fixtures. Every other baseline path remains identical, including app/main/renderer, bridge, lighting, roads, postprocessing, camera, styles/dropdown, emission rules, photographic de-lighting and texture policy. The three exact original photographic gain expressions remain present unchanged.

The actual binding chain was read and exercised. `GLTFExtensionsPlugin` registers `FacadeAtlasBindings.loaderPlugin` on the installed GLTFLoader. Its `afterRoot` checks texture 0's `EXT_texture_webp` image 0, embedded-image MIME/length and SHA-256, actual parser texture association and original 4096×4096 image before placing the original Texture/source/image identity in a private WeakMap. A candidate map with a matching name or dimensions cannot supply that association. No parser, encoded tile or image buffer is intentionally retained in that map. The normal tile fetch verifies the selected tile bytes; processing validates the original UV0 channel, flip and matrix before replacing the texture. No custom fetch/parser or altered Draco/metadata/RTC ordering was introduced.

The source fixture runs the installed B3DMLoader, GLTFLoader and Draco decoder. Its image decode/readback adapter is a placeholder. Fresh controls on the actual cached tile518 reproduced the three accepted patches on 2, 1 and 2 clipped triangles. Interior samples were obtained from each triangle's clipped UV polygon, so an extrapolated patch centre was not counted as positive evidence. The samples retained the fixed 10 mm plane, normal dot 0.999, vertical and UV bounds. Reversed source normals, roof normals, unrelated UVs, an authored-plane offset of 10.1 mm and a point above the vertical interval all rejected. Same-GML sign polygons were disjoint from the facade polygons. Missing loader binding and a wrong original UV channel rejected before replacement. Actual parent522 and tile488 received zero eligible groups despite reused building identities elsewhere in the source population.

The group is an eligibility attribute, not a classified surface. Per-fragment UV, world-plane, Y and outward-normal tests still apply. The shader obtains the source normal from `normalMatrix * objectNormal` and reverses the view rotation; it does not use the camera-facing double-sided normal for profile admission. The inward weight remains zero outside the polygon. Within the blank patch's hard admitted polygon, the shader sets window/emission mask to zero before the colour fade, and the authored blank profile also has zero window mask. This avoids a ring of invented openings at the fade. Native precision, interpolation, edge appearance and nearby azimuth/LOD views still need rendering.

Unknown Cartographic surfaces use GML-seeded authored bay proportions, with a neutral seed when GML is missing. The source bounds are aggregated across primitives before writing floor attributes. Fresh controls verified a two-primitive, 24 m building with six floors produces the same 4 m floor height and common zero origin in both primitives; reordering the actual tile meshes and repeating preparation preserved the attributes. Roof colour remains quiet because the bay mask is wall-gated and the universal roof grid was removed. Satellite has metalness zero, the authored rough unclassified default and roughness changes only inside admitted profiles. The old photo grid and brightness-selected glass mechanism are absent. Authored palette and roughness values remain choices awaiting native appearance review.

Fresh full `processTileModel` execution through the actual plugin replaced the bound source atlas and preserved position, normal, UV, batch and index hashes. Its size-correct resized CPU image was explicitly closed. Three-tile measured added storage exactly reproduced the author census: tile518 has 53,766 vertices and 215,064 new bytes; parent522 has 23,067 vertices and 92,268 bytes; tile488 has 42,258 vertices and 169,032 bytes. Only tile518 carries candidate groups, on 5,424 vertices before per-fragment restriction. The new attribute is four bytes per vertex. Existing three-float floor parameters are not new allocation. These counts are not whole-city GPU memory or performance measurements.

Photo and plain material shader assembly both passed the focused checks. They retain one main function, use the outward source normal, preserve zero blank openings and omit the rejected photo/glass/roof mechanisms. Shared style uniforms round-tripped through Cartographic and Satellite without rebuilding the source or eligibility. This tests the material contract; it does not exercise the actual dropdown or compile a GPU program. The author build/types/364-test evidence and the three old-shader red controls were hash-verified and inspected within the handoff; they were not rerun or relabeled as new reviewer GPU evidence.

Two fresh asynchronous controls covered the added loader boundary. Pausing the exact image bufferView dependency only after the real source scene existed kept GLTFLoader parsing pending until the facade hook completed. Releasing it yielded the bound scene. Injecting a rejection at that same boundary preserved the rejection identity and returned no admitted scene. Installed GLTFLoader awaits `Promise.all(afterRoot)` and routes its rejection to the normal parse error; the tiles path awaits parse and model processing before publishing engine data. Failed selected-source checks therefore do not silently authorize a replacement texture.

There is a lifecycle assurance limit on that error test: the installed loader does not explicitly dispose an already-created Texture on this prepublication `afterRoot` rejection. The CPU fixture observed zero loader-side Texture.dispose events; reviewer finally cleanup disposed its captured placeholder texture and two geometries. This does not establish a naturally occurring failure of SHA-256 on the pinned supported input, a native bitmap leak, GPU allocation, or a new persistent owner. It also does not prove native cleanup of all prepublication failures. The new hook retains no additional explicit native resource. General native failure cleanup remains outside this finite source approval.

A separate disposal hypothesis was tested and rejected rather than promoted into a finding. Reading the installed plugin showed an unconditional null-KTX call inside its `if (this.autoDispose)` branch. The first reviewer assertion incorrectly expected the real owner to throw; it failed. A second assertion incorrectly inferred the plugin was not called; it also failed. The final direct observation resolved both: `TilesRenderer.dispose()` does invoke the plugin's dispose method, but this installed constructor never assigns `options.autoDispose` to the instance, so the field is undefined and that branch does not execute. The existing `buildings.dispose()` follows tiles disposal with explicit Draco disposal. There is no demonstrated null-KTX production failure here. Both failed reviewer assertions and the final imported-module/function/call record are retained.

Verification used 14 focused source/layout/replacement/shader checks, two asynchronous controls and one final installed-owner observation. The reviewer did not rerun the full suite, build, audit, source generator, browser fixture, benchmark, server or GPU work. Tests imported the exact frozen TypeScript through Node 24's native transformation, so no Vite service, worker or cache was needed. The only warning was Node's experimental stripTypeScriptTypes notice. The first two pin-inventory attempts falsely counted an existing author Vitest result under candidate `node_modules/.vite` as a source file; the second retained the exact mismatch. The correction excluded the declared dependency/cache directory while continuing to hash all external handoff pins. Neither failure changed or removed candidate bytes.

All reviewer writes are intentionally retained under the new ignored sibling `artifacts/graphics-facade-source-review`. Probe headers name the existing instrument. No source restoration was required. The final full pin pass again found every owned/external and candidate source hash unchanged. All ten retained reviewer CPU child PIDs, including failed controls, were absent in a successful strict native snapshot at `2026-09-13T13:30:09.1675430Z`; no matching current review process, current child or port4319 listener remained. Each launch was hidden, had a held process handle and a 120-second outer cap, and finally confirmed exit and closed the handle. Main source probes included 110-second cooperative guards. No broad kills or closure signals were issued. Native scene objects created by the fixture were released in reviewer finally paths; deliberately retained evidence is the only remaining review output.

Root still owns integration and the next authorized native checks: complete cached scene binding, actual shader compilation, the eight crossing/approach heroes across styles and times, real dropdown returns, nearby azimuths and LOD transitions, and the complete 44-frame visual gate with native inspection. Existing photographic colour/blur/gain, dusk lighting, roads, AA/TAA, motion, lifecycle and population performance questions remain open. The candidate is still an isolated artifact; no main merge or final graphics completion follows from this source review.

<!-- authored-report-end -->

## Findings and disposition

No material new source finding was reported. Root accepts only the finite loader/source-admission, layout and shader-assembly evidence. Actual native compilation and appearance remain unverified. The prepublication rejection cleanup assurance limit is retained. The null-KTX hypotheses were tested and rejected; they are not promoted into findings.

## Verification

The reviewer freshly completed fourteen source/layout/replacement/shader checks, two asynchronous loader controls and the installed-owner disposal observation. It inspected the author build/types/364-test record and three old-shader controls without rerunning them. Exact photograph gain expressions, geometry bytes and the source admission boundary remain bound. Placeholder image readback and CPU shader assembly are not GPU compilation or native pixels.

Preparation of this documentation checkpoint checks the exact authored/source pins, reversible imports, scoped document headings and links, blob/secret/diff limits, and separate HEAD/WIP preservation. It runs no browser, benchmark, code build, five-gate sequence or source repair. Review 11 remains open. Primary WIP also retains rounds 2, 4 and 12 that are absent from HEAD. This checkpoint neither invents those missing committed rounds nor claims a whole work-docs continuity pass.

## Round outcome

Root accepts this finite source increment for the next integration gate. No facade code, native appearance, city lifecycle, performance or full 44-view acceptance follows from this round. Current delivery status remains in [the plan](../plan.md).
