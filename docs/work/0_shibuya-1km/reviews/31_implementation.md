# Round 31 — independent review of the frozen revision

Reviewer: an independent, read-only lane. Revision reviewed: `30c7b01`, the frozen revision the 44-frame verdict capture is running against.

**This round is numbered 31, not 30, and the brief asked for 30.** `docs/work/0_shibuya-1km/reviews/30_implementation.md` already exists and is a different round — the paint-review wave's adjudication of the road-markings criterion at 1:1, landed on `main` in `e8b0bc1` (`24d2865` is its own commit) and cited by `plan.md:205`. Overwriting it would have destroyed another lane's authored round, so this one takes the next number instead. The collision is not only mine: `plan.md:134`, added during this round, calls this review "Round 30's independent review" while `plan.md:205` uses the same number for the paint round, and `src/ui/style-picker.ts:12` cites `reviews/30_implementation.md` for a measurement that round does not contain — the native-select reachability probe is round 27's (`reviews/27_implementation.md:13`, probe preserved at `artifacts/style-ui/scratch/select-probe/`).

Evidence: nothing was executed. The verdict capture owned the CPU for this whole round, so there is no `npm run visual`, no Playwright run, no `npm run build`, no `npm test`. Every claim below is marked **verified** (I read the code, the bytes or the artefact myself), **read-only** (reasoned from code and records without running anything) or **unverified**, and the deferred commands are listed at the end. The primary checkout received exactly one write: this file. I did not create a worktree, because a read-only round does not need one and building one with `data/` and `node_modules/` junctions is the operation that emptied the primary's `data/` earlier today.

`main` advanced three times while this round ran — from `90acf28` to `5c32286`, `e8b0bc1` and `98f7a64` (tip at the time of writing) — and one of those commits changed the gate while its own capture was running: B4 and B5 below. The frozen revision the capture is bound to has not changed, and neither have the frames.

## Verdict per acceptance criterion

| `plan.md` | Criterion | Verdict |
| --- | --- | --- |
| 181 | AOI renders end to end, several angles and zoom levels, each frame at native resolution | **Not established for `30c7b01`.** The 44-frame set owed for this revision is not certified yet — 3 of 10 hero captures had landed when this round was written and the two sweeps, which carry 36 of the 44, had not started. The native inspection on file binds an earlier capture (`index-BP1Vm0F-`), and the two hero frames inspected against *this* capture are recorded in `plan.md:138` with their digests, which I re-hashed and confirm exactly. The "native resolution" clause is still not literally met by any inspection: the image tool returns 1066×600 previews of 1280×720 sources, disclosed at `plan.md:138` and in `artifacts/frame-inspection/report.md`. |
| 182 | Ground is not flat; buildings sit on it without floating or sinking | **Met.** `data/scene/manifest.json`, rebuilt 2026-09-17T00:26:25Z: terrain 8.71 to 36.35 m, 183,188 triangles, ground at the origin 15.1986 m; the reconciliation spread is 0.094 m over 2,607 buildings; the GSI cross-check gate is proved red at `gate-proofs.md:131`. **Verified** from the manifest and the code; the render half is the coordinator's frame inspection. |
| 183 | Buildings hold up close (floor counts, PBR) | **Met for the graphics landing, not re-established for `30c7b01`.** Settled on the 44 frames "bound to G1" (`plan.md`'s visual acceptance state); no frame-level evidence exists for the frozen revision yet. |
| 184 | A dusk frame of the crossing reads as Shibuya | **Same shape as 183.** The two hero frames inspected so far are the right evidence and are digest-bound; the second time of day and both styles' sweeps are outstanding. |
| 185 | Road markings match reference imagery, signals, guardrails, street furniture | **Not met, and now adjudicated rather than "partly carried": round 30 did the 1:1 work.** Met for the diagonals (both, at their authored bearings, confirmed against GSI to 5.6–7.8 m), the zebra bands (period measured at 1.10 m against 1.05 authored) and the tactile strips; **not met for guardrails**, because `src/scene/street-details.ts` returns `guardrailPosts: 0` as a literal and the nearest source barrier is 295 m from the crossing. The thin-paint half cannot be adjudicated against a 0.486 m/px reference at all, which is a stated bound rather than a crossed one. |
| 186 | Dusk post chain (ACES, bloom, SSAO, TAA) and "holds still — no flicker or crawl over a moving sequence" | **Not met.** The last measurement of the mechanism (`artifacts/gate-timing/REPORT.md`) read `taaAccumulating: false, taaSamples: 0` on all eight hero frames; the rest-frame repair is in the tree but no artefact on this revision reports accumulation engaging, and the "over a moving sequence" half needs the flythrough, which has never been run. New pre-flight checks the post chain is active with its four passes, which is not the same claim. |
| 187 | Vehicles hold lanes, obey signals, turn, spawn and despawn, watched over a run | **Not met.** Round 29's spacing blocker is closed and gated (`test/vehicle-spacing.test.ts`, `5e8c266`), but the running build draws **59 of 200** vehicles and 200 is recorded as infeasible in this network by owner decision (`plan.md:108`). Nothing watches a run: no frame of the reviewed set contains a vehicle. |
| 188 | Pedestrians avoid each other and surge diagonally across the crossing — the signature shot — no interpenetration, no sliding feet | **Not met, on the criterion's own subject.** `plan.md:126`, measured on `f4e96f0`: worst sample **982 distinct positions for 3,000 active bodies**, largest stack **205 at one centimetre**, one sample with **227,214 overlapping pairs**, **no walker ever entered the authored diagonal**, nearest approach **31.228 m**. `worker/scramble` reads **1,580 of 3,000** slots on a crossing route against **0** and is unlanded. By construction the reviewed frames cannot carry this criterion: the verdict lane is `?agents=`-free, so no frame of the 44 contains a pedestrian. What would change the verdict: the crossing route landed with an after-arm measurement of diagonal occupancy, distinct positions and overlapping pairs, plus a rendered populated sequence. |
| 189 | One clock drives both; no frame shows traffic in the scramble while pedestrians are on it | **Not established.** The shared deterministic controller was accepted in round 29; the frame clause is a property of a frame set that contains neither party. Extra: no pedestrian signal lens exists in this city, so the pedestrian stage has nothing on screen to photograph (`gate-proofs.md:474`). |
| 190 | 60 fps at 1920×1080 with 3,000 animated pedestrians and 200 vehicles | **Not met.** `artifacts/frame-budget/wt/artifacts/frame-budget/interval.json`: interval p50 **83.3 ms**, p95 183.4 ms, 12.005 fps at 1920×1080 on the RTX 4090 over D3D11 with 3,000 pedestrians and **59** vehicles drawn; tick `simulationMs` p50 **75.7 ms** against a 16.667 ms target; the frame's own cost is p50 7.7 ms, so the simulation is the limit. **Verified** from the retained JSON. |
| 191 | The scene rebuilds from a clean checkout | **Met for the city, not for the population assets.** I hashed every recovery pin myself: PLATEAU archive 649,322,807 bytes, `data/scene/agents/vehicles.json` `5b29efc7977949db…`, `roads.mesh` `3fe126cd6759…`, `pavements.mesh` `5d2a915047b0…`, `data/network/network.json` `314fac843392de12…` — all match `plan.md:122`. The human agent set did not come back; see B1. |
| 192 | The running app shows its attribution | **Met.** `src/ui/attribution.ts` is installed at boot and throws if its element is missing (`src/main.ts:68`); the attribution line is present in the captured hero frames (`plan.md:138`). **Read-only.** |
| 193 | Sweep and flythrough through the real controls come back clean; independent review passes; merged to main | **Not met.** The sweep is mid-capture, the flythrough has never been run (M6), this round is the review, and criteria 181–190 stay open above. |
| 194 | Cartographic and Satellite hold up at street, block and aerial; the real dropdown switches both ways with pointer and keyboard, preserving camera and progress, from an extensible registry | **Partly established.** The switch is driven through the shipped control in the live app and the capture has passed the option-text and first-switch assertions on the real build; the registry is extensible by construction and tested by the `style-picker.spec.ts` fixture. Owed: the frame-level record, and the pointer half is weaker than the plan says — B3. |

## Blocking findings

**B1 — the payload the gate binds lost 53 files and 157,114,411 bytes, the human agent set is gone, and the correction is now recorded in one place and still wrong in four.** `plan.md:134-136` now carries this finding from my early report, and I have verified its every number: the running capture pins **84 files / 214,114,015 bytes / `ddd21ee1b4feb7f7…`**, which I reproduced from disk as `data/scene` 83 files / 203,874,605 plus `data/network` 1 file / 10,239,410, against the **137 files / 371,228,426 bytes / `0222c0566d507e4b…`** that `plan.md:106`, `gate-proofs.md:313` and `:538`, `docs/devlog/summary.md:7` and `devlog/detailed/2026-09-16_scene-digest-binds-bytes.md:15` still state. `5c32286` corrected the sixth place, the comment at `tools/visual/verify-output.ts:65-70`, which now reads "214,114,015 bytes across 84 files … (It was 371,228,426 bytes across 137 files before that day's rebuild of `data/`)". So the record is now internally contradictory: `plan.md:134` says those figures "are corrected", and `plan.md:106` a few paragraphs above still prints them as current. **Verified** by reading all six and hashing nothing that the earlier round had not already hashed.

The composition is verified exactly as far as it goes: `data/scene/agents/` holds only the three vehicle GLBs and `vehicles.json`, while `src/world/agent-assets.ts:4-9` requests `commuter-male.json`, `office-male.json` and `commuter-female.json`, and `tools/agents/bake-human.py:20,360-369,437` writes that set into the same directory — 3 variants × 3 LODs × {glb, positions.f16, normals.f16} = 27 files, 3 manifests and 3 `-source.blend` = 33. `npm run data:agents` is not in `data:setup`, and `data/agents/` (the MPFB and MakeHuman archives and the Blender user directory that builder needs) went with the incident, so restoring them means re-fetching about 325 MB and running Blender. **Unverified**: that those 33 files account for all 53. About 117 MB and ~20 files are still unnamed, and nothing on disk distinguishes "lost inputs" from "pre-incident leftovers that a rebuild legitimately dropped". What would settle it: a preserved listing of `data/scene` from before the incident, or `npm run data:scene` on a scratch tree to see whether it reproduces 84 files.

**B2 — `plan.md:102` still says the 2026-09-16T13:08:43Z run "certified … three hardware lifecycle runs", and the log of that run says it could not have.** `artifacts/verdict-run4/visual.log` is that run — navigation 158/168/158 ms and replacement 8399/5716/8573 ms match the paragraph, and its build is `index-Wbd7IHvh.js`, the build round 29 found in the certificate. The npm header in the log shows the chain that ran, `npm run build && node tools/visual/verify-output.ts --begin && playwright … && playwright … && node tools/visual/verify-output.ts`, with **no `--reset`**, and it ends on `verifyVisualRun`'s line, `All 44 fresh native-resolution frames survived the complete visual gate with matching hashes.`, with no `Certified run …` line. `966930e`, which added both `--reset` and `lifecycleEvidence`, is dated 2026-09-16 09:47:29 -0700 — three and a half hours **after** that run finished at 06:08:43 local, and the same document says so at `plan.md:96` when it records that none of the five instrument fixes was in place at `04be817`. The paragraph also overstates a range: the three records read 158, 168 and 158 ms, so "navigation 158 to 177 ms" is not the range they support. **Verified** from the log, the three record files and the commit dates. The repair is a sentence, not a re-run: what that run proved is that the pixel lane wrote 44 frames and the lifecycle lane navigated cleanly three times.

**B3 — the freeze-time spec change is defensible, but the pointer half it was made for is still not exercised, and the pre-flight landed tonight shows exactly what is missing.** I read `7a41aed` and `30c7b01` against the shipped control and traced both specs by hand. What holds: `getByRole("combobox", { name: "World style" })` matches the button by accessible-name substring (`aria-labelledby` is the label plus the value), `render()` writes `data-style-id` (`src/ui/style-picker.ts:156`), the live capture has passed both new assertions, and the selection assertion is not the control's own word alone — the frozen bridge's `style().id` is checked on both directions and `hero.spec.ts:332` still requires the two styles to produce different pictures. What does not hold is the claim that driving the real control exercises the pointer path, because `await styleSelect.click()` followed by `Home`/`End` and `Enter` cannot distinguish "the pointer opened the listbox" from "the pointer did nothing": a **closed** control commits on an arrow key, since `move()` ends in `if (open) render(); else choose(option.id)` (`src/ui/style-picker.ts:199-201`). Delete the click handler tomorrow and the browser's default mousedown focus still lands on the button, `Home` still commits cartographic, and both assertions still pass. The option-text assertion does not cover the gap either, because `allTextContents()` does not require visibility, so it passes on a listbox that can never be opened. **Verified** by reading the control and both specs; round 27 (`reviews/27_implementation.md:13`) had already recorded pointer-completed selection as unestablished for the control this replaced.

Two smaller losses came with the same change and are worth recording rather than fixing: the option rows are now matched page-wide (`page.locator('[role="option"]')`) rather than inside the control, and `expect(page.getByText("A third registry entry")).toBeVisible()` became `expect(page.locator(".world-style-picker__description")).toHaveText(…)`, which asserts text without asserting visibility. Neither is wrong today — the app has exactly one listbox — and both are weaker than what they replaced.

The repair is already written in the tree: `tools/visual/smoke.spec.ts`, landed in `5c32286`, drives the pointer-completed selection properly with `await styleControl.click()` then `page.locator('[role="option"][data-style-id="satellite"]').click()`. `hero.spec.ts` should do the same in the same place, and that edit belongs to the **next** capture, not this one: changing a capture spec while its run is in flight is the thing B4 is about.

**B4 — the certificate binds the build and the scene, and nothing binds the harness — and a harness commit landed twenty minutes into the three-hour capture.** `beginVisualRun` hashes `dist/index.html` plus `dist/assets/*.{js,css}` (`verify-output.ts:195-202`) and `certifyVisualRun` re-derives the scene digest, so a reader can tell which bytes and which city the frames came from; nothing hashes `tools/visual/**`, and the certificate therefore cannot say which harness drew them. `plan.md:53` requires exactly that of the final verification — "Final candidate verification must bind the complete source/build/data/harness closure before and after the run" — so the instrument is weaker than the plan says the acceptance must be. This stopped being theoretical at 17:55:29 local, when `5c32286` changed `tools/visual/verify-output.ts` and added two files under `tools/visual/` while the capture that started at 17:35:33 was still running. **Verified** from the wrapper's code, the plan line and the commit timestamp.

The capture is not invalidated by it, and this is worth stating precisely because the opposite conclusion is easy to reach: `playwright.config.ts` fixes its test list when the run starts, so a spec added twenty minutes later cannot enter the run in flight, the build bytes are pinned in `run.json` and unchanged, and the scene data has not moved. What is now lost is the ability to *show* that, from the certificate alone. Minimum repair, to be done at `--end` for this run: record the harness revision and a `tools/` tree digest in `run.json` and `complete.json`, and state in the round that no file under `tools/visual/` changed between `--begin` and `--end`.

**B5 — `playwright.config.ts` collects the new pre-flight into the pixel lane, which is the opposite of what the pre-flight was built for.** The pixel lane's selection is `testDir: "./tools/visual"` with `testIgnore: ["lifecycle.spec.ts"]`, so `tools/visual/smoke.spec.ts` — added tonight with its own config, its own port 4336 and its own `visual:smoke` script — is also collected by `playwright test --config playwright.config.ts`. The smoke spec's own header says the opposite in as many words: "It is a separate configuration rather than a flag on `playwright.config.ts` for the reason the gate's own comments give about renderer switches: a check that can be turned into the evidence lane by an environment variable is a check that will eventually be run as evidence." It is not a flag that puts it there; it is the default `testDir`. Consequences: the next `npm run visual` runs a fourth, unplanned specification inside the evidence lane on the same port 4319, with `maxFailures: 1` and `workers: 1`, so a smoke failure stops the capture; and `docs/policies/local-rules.md:107`'s "the pixel lane … runs `hero.spec.ts`, `style-picker.spec.ts` and `sweep.spec.ts`" becomes false. The smoke spec itself is good and asserts the right things — renderer through the shared predicate, the post chain's four passes, the tileset refining, both style directions, the simulation still running — and on SwiftShader it should pass; the defect is that it is in a lane nobody put it in. **Verified** by reading all three configs and the spec. The fix is one line: add `smoke.spec.ts` to the pixel lane's `testIgnore`, or set an explicit `testMatch`.

## The gate instrument's five checks, against this revision

| Check | Where | Verdict |
| --- | --- | --- |
| A certificate cannot be issued for a run that did not happen | `package.json`'s chain, `visualRunPhase`, `resetVisualRun`, `certifyVisualRun`, `test/visual-instrument.test.ts` (31 cases, counted) | **Holds.** `--reset` precedes the build, `--begin` writes `run.json` after it, `--end` refuses without a `runId`, a bare invocation is refused rather than re-certifying, and the frame-only certificate is deleted before the lifecycle evidence can fail. Residual, already recorded: a crash between `verifyVisualRun`'s write and its delete leaves a frame-only `complete.json`. |
| The scene digest binds the served bytes | `sceneTreeDigest`, `assertSceneUnchanged` | **Holds in code**: path + SHA-256 of every served file, pinned at `--begin`, re-derived before a frame is read, and the red control for it is recorded at `gate-proofs.md:478-538`. Bounds: `/draco/` is served from `node_modules` and is outside it, and the comment at `verify-output.ts:36-45` calls the two mounts "the two directories the preview server serves" while `tools/vite/serve-scene-data.ts:52-73` answers three. The numbers quoted for the payload were wrong until tonight (B1). |
| Each lane refuses the other's renderer | `lane.ts`, both capture specs, `certifyVisualRun`, `lifecycle.spec.ts:96-104` | **Holds.** Positive predicate at the first frame of both capture specs, denylist on the lifecycle side before the expensive preparation, and the wrapper re-asserts both over the manifests it certifies and records the two strings. |
| An empty page-error list and a completed teardown are asserted | `page-errors.ts`, `src/harness/teardown.ts`, `lifecycle.spec.ts:152-166`, wrapper `:435-454` | **Holds**, with the disclosed bound that the listeners cannot see a `pagehide` throw. One prose defect: `teardown.ts:49-50` says that when session storage is unavailable "the record is simply absent", but both the try and the catch call `setItem`, so an unavailable storage throws out of the handler; the reader's outcome is the same refusal, the sentence is not. |
| The 44 frames are fresh and hash-matched | `expectedFrames()`, `readManifest`, `verifyVisualRun`, `progress.ts` | **Holds**, with the bound named: freshness is **per manifest** — three `capturedAt` values against `run.startedAt` — not per frame. What makes the frames fresh is that both specs delete their own output directory first, write the manifest last, and assert their own ledger count (10 and 18). Frame hashes, native 1280×720 and the 44-name set are checked per frame. |

Every `docs/…` claim in `test/` and `src/` resolves on this revision except one: `src/ui/attribution.ts:22` → the frame-that-did-not-render entry (2.20, `gate-proofs.md:54`); `src/world/building-attributes.ts:36` → the sentinel entry; `test/vehicle-spacing.test.ts:27` → the spacing entry now at `gate-proofs.md:540`; `test/visual-instrument.test.ts:444` → the two CLI red controls in the certificate entry. The exception is `src/ui/style-picker.ts:12`, which cites `reviews/30_implementation.md` — a file that exists, is about paint, and never carried that measurement; round 27 does. `gate-proofs.md:361` is also an entry with no `##` heading: the Phase 6 network gate sits under four blank lines with no title.

## The two unlanded branches

`worker/scramble` at `7a9538c` (1 commit, base `f4e96f0`, **36 commits behind `main`**) is the more valuable of the two and the more dangerous to land unmeasured. I checked the structural claim its anti-deadlock argument rests on and it holds: `sampleRoute` offsets along `(cos h, −sin h)` with `h = headingAt = atan2(dx, dz)`, which is exactly the right axis `tools/agents/spacing-metrics.ts:126-128` defines, and the lateral fraction is always in `[0,1)`, so the two directed sections of one corridor — headings π apart — land on **opposite** sides of the centreline. The routes it makes are still bounded by `MAXIMUM_CENTRE_ROUTE_LENGTH_M` inside `descend`, so the +140 m crossing preference cannot smuggle a walk past the 1,000 m cap. **Verified** by reading. What is missing is what its own message says is missing, plus one thing it does not: it lands **no gate**. `tools/agents/crowd-spread.ts` is an instrument, not a check, and the pedestrian-spacing gate is still unlanded in `artifacts/spacing/wt`, so the crowd's overlap count would land as prose for the third time. Before it is landable it needs the rebase, the after-arm run (diagonal occupancy, distinct positions, overlapping pairs, crossings, completions at the same seed and network), a landed gate over the overlap class, and — new tonight — the human assets, without which no after-arm can run in a browser at all.

`worker/flythrough2` at `bdb2248` (2 commits, base `e74375e`, **35 commits behind `main`**) is a well-built lane that has never been run, and the plan describes it as though it had. The branch's own message says "Not yet run … no frame has been captured and no gate has been run"; `plan.md:128` says it "carries 46 frames over four legs through the real controls on the hardware renderer". The 46 is the plan's frame count (`plan.ts`: 11 + 12 + 12 + 11) and no frame exists. Two citations inside the branch do not resolve either: `plan.ts` names `artifacts/flythrough2/aim.json` written by `node artifacts/flythrough2/aim.ts`, and neither exists — the tool is `tools/flythrough/aim.ts`, whose default output is `tools/flythrough/aim.json` (`aim.ts:70`), and no `aim.json` is anywhere in the repository. What I could verify is the part that matters: the retained dumps reproduce the plan's vehicle figure exactly — **45 vehicles at tick 7,200** — and the anchor `CROWD_TARGET = (-431, -466)` holds **196 distinct pedestrian positions within 60 m** at that tick against a largest 120 m cell of 199, so the flight really is aimed at one of the densest knots rather than at the crossing. The plan's "59 at tick 2,400 and 14 at tick 20,000" cannot be reproduced: no dump exists at either tick. The lane's structure is sound by reading — own root, own port 4323, `MAPS_VISUAL_GPU=hardware`, all four axes driven through `page.mouse`, reads only the frozen bridge, `assertCertifiable` refusing the lane by name, and `test/flythrough-frames.test.ts` making every sequence claim red including the zero-delta input control. To land it: the rebase, the human assets (it captures `?agents=1` with a `pedestriansDrawn` floor of 100), one real run with its frames inspected at their own size, and the two citations corrected.

## The plan's own numbers: what I reproduced, and what I could not

Reproduced exactly from the artefacts: the 2026-09-16 recovery pins at `plan.md:122` (`vehicles.json` `5b29efc7…`, `network.json` `314fac84…`, `roads.mesh` `3fe126cd…`, `pavements.mesh` `5d2a9150…`, archive 649,322,807 bytes) — hashed by me; the population-cost batch at `plan.md:100` (2.409 / 0.526 / 1.007 / 4.346 ms, 2,582 requests, 27 grants) from `artifacts/population-cost/report.md`; the frame interval and **every** phase share at `plan.md:124` (83.3/183.4 ms, 12.005 fps, 59 vehicles, `simulationMs` 75.7, 44.1% avoid at 5.521 of 12.524 ms, 17.7%, 10.4%, 9.1%, 7.5%, 7.2%) from `interval.json` and `phases-subsplit.json`; the "31 cases" of `test/visual-instrument.test.ts`; the two hero-frame digests at `plan.md:138`, both of which I re-hashed and both of which match; the "45 at tick 7,200" behind the flythrough aim; and the "56 `*.test.ts`" half of `plan.md:122`.

Not reproduced, with the reason: **`plan.md:106`'s 137 files / 371,228,426 bytes** (false — B1); **`plan.md:102`'s "navigation 158 to 177 ms"** (the three records read 158, 168, 158 — B2); **`plan.md:124`'s "152 of 262 frames hit the loop's five-step catch-up cap"** — the retained JSON gives `steps` p50 5, p95 5, mean 4.546 over n = 262, which bounds the count to 143–202, so 152 is consistent but not readable from the record; **`plan.md:100`'s "27 grants against 2,582 requests, so 80.8% of the population queues"** — the report's 80.8% is 2,586 queued of 3,200 active, a different denominator from the pair it is attached to, which is 99.0%; **`plan.md:126`'s crowd numbers** (982 / 205 / 227,214 / 31.228 m), which come from `tools/agents/crowd-spread.ts` — a tool that exists only on `worker/scramble`, so the plan quotes an unlanded instrument as evidence for the defect its own branch fixes, and no run of it is retained under `artifacts/`; **`plan.md:128`'s far-LOD numbers** (201 pixels, 4 and 7 pixels, per-level counts `0/0/3000`) and the vehicle counts at ticks 2,400 and 20,000, for which I found no frames or dumps in this phase; and **`plan.md:122`'s "401 tests"**, which needs `npm test` (the file count holds at 56).

## What held exactly, checked by hand

The five instrument checks above, each with its bound named; the chain order in `package.json` and its 31-case pin; `lane.ts`'s four lanes with `assertCertifiable` refusing every one but the verdict; the scene digest's content binding and its recorded red control; the lifecycle lane's renderer refusal, page-error assertion, teardown reader and build-hash comparison; the ledger in both capture specs; the five recovery pins; every phase share at `plan.md:124`; the two hero-frame digests at `plan.md:138`; the flythrough's aim anchor against its retained dumps; the lateral-axis convention the crossing fix depends on; and the constant discipline in `lifecycle-record.ts` and `lane.ts`, where a lane cannot be added without a root and a refusal string.

## Deferred commands

Not run, and wanted: `npm test` and `npm run typecheck` (to reproduce 56 files/401 tests and clean types); `npm run build` plus `Get-FileHash dist/assets/*.js` (to bind `30c7b01`'s bytes against what the certificate will pin); `npm run visual` (never for this round — the capture owns it); `npm run visual:smoke` (to see the pre-flight from the lane it was written for, and to see whether the pixel lane's collection of it changes the outcome); `npm run artifacts:size`; `node tools/agents/population-route-mix.ts --pedestrians 3000 --vehicles 200 --ticks 6000` on `main` and on `worker/scramble` (the only way to reproduce 0 versus 1,580); `node tools/agents/crowd-spread.ts` on the branch; `node tools/agents/vehicle-spacing.ts --vehicles 200 --pedestrians 0 --ticks 3600`; `npm run data:scene` on a scratch tree (to see whether a rebuild reproduces 84 files or restores 137 — what would settle B1's unnamed remainder); `npm run data:agents` (blocked: the archives it needs were themselves in `data/`); `npx playwright test --config playwright.flythrough.config.ts` (blocked by B1); a fresh probe of the native-select popup, to retire or confirm round 27's reachability finding; and the two-arm `workers` measurement `plan.md:130` asks for, which needs a machine with no verdict capture on it.

## Disposition

The instrument is in better shape than any earlier round found it, and it is being repaired while it runs: the five checks that matter are present, each has a recorded mutation, the payload figures were corrected in the code the moment they were found wrong, and the two hero frames inspected so far are bound to their digests, which I re-hashed and confirm. The deliverable is **not** complete, and the criteria that fail, fail on their own subject: nobody is on the crossing, the crowd holds 982 distinct positions for 3,000 bodies, the frame rate is 12 fps with 59 of 200 vehicles, and the temporal accumulation the dusk criterion names has never been seen to engage on this revision. The record is wrong in four places about the payload the gate binds and in one about what a certificate carried, and the gate's own collection rule has just put a pre-flight into the evidence lane.

Nothing here says the running capture is worthless. It is the first capture whose spec-side assertions are being watched to pass against the shipped control, its build and scene bytes are pinned, and its first two frames are already the evidence criteria 181, 183 and 184 have been waiting for. It should run to certification, and then be inspected at native resolution, which is the one thing no instrument here can do. The two changes this round asks for — the harness digest at `--end` and the pixel lane's `testIgnore` — are both small, and **neither should be applied while the capture is in flight**; they belong to the landing that follows it, together with `hero.spec.ts`'s pointer-completed selection.
