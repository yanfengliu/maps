# Review 67: design

## Target

Independent review of the conditional birth-prefix geometry and proposed gap-edge nomination. Author: `causal_support_prefix`, detached at `d7c8eea43c1cbfc6e85bcd01306fd43898be6b66` in `artifacts/causal-support-prefix/wt`. Reviewer: `contact_design_review`, detached at current main `86c08b210d67a68261cd2cad3c2310caea9f4d83` in `artifacts/causal-support-prefix-review/wt`. Root owns admission and integration.

Author `HANDOFF-FREEZE.json`, SHA-256 `848b41c2802c0c8d6652c8bff419767d00ccca32632f6e0327fcf9347f776115`, binds 51 files totalling 22,040,596 bytes. The exact authored report is [67_causal-support-prefix.md](../snapshots/67_causal-support-prefix.md), SHA-256 `2f6db1112363196223de4d953e8317366b6a14ea2d26b68e59256580264fe986`. The first strict graph remains separately bound by nineteen-file freeze `f762b48d8ab028aa61d482ff37f4999e9109e975fb310fddf06c1e5a0fab08b7`. Every target member was copied unchanged into this review's ignored evidence. This review edits none of those targets.

## Reviewers and coverage

Codex `contact_design_review` read the current instructions, local rules, lessons and accepted Review 57 before inspecting the author scripts, pre-execution contracts, full nine-case records, source matches, graph edges, rational audit, competitors and proposed roster. I authored none of the target. The existing retained constructor/population instruments answer birth and route identity; frozen graph records answer this proposal without running another graph. New private checks therefore use those actual APIs, the raw current meshes and bound source blocks, plus an independent exact barycentric clipping calculation.

The bounded population recovery is the same 150-pedestrian, zero-vehicle, seed-5970698, 200-tick identity observation. It is not a population acceptance gate. No revised graph, foot/controller replay, source policy, data writer, install, network request, browser, GPU job, native capture, full product gate, primary edit, commit or remote action occurred. The primary full-gate owner retained that resource. Review 65's native work and Review 66's shoe integration are separate targets and supply no acceptance here.

## Reports

### Codex contact_design_review

**Accept the reproduced conditional geometry within its stated bounds. Recommend the proposed thirty-edge-per-row nomination rule after correcting F36 in a separately frozen author handoff. Reject the report's claim that the original and proposed edges cross different bank families.** The reporting error does not change the ninety enumerated edge identities, measured intervals, actual banks or the rule's result. Root accepted this diagnosis and will assign the minimal wording correction and focused re-review before any nomination. The original report and graph remain counterevidence.

**F36 — The failed chord's starting triangle was called the departed gap bank.** The report says the new 38594→38628 gaps differ from the original 38611→38628 seam pair. Its own frozen `seam-bank-witnesses.json` instead names 38594→38628 for both original slots 62 and 83. Independent exact rational clipping of the original attempted chords confirms that record: the chord starts on 38611, crosses real selected geometry, and finally leaves 38594 before reaching 38628. Chord-start support and the bank immediately before the gap are different observations.

| Original slot | Chord-start triangle | Actual departed bank | Actual resumed bank | Independently measured route-direction gap |
|---|---:|---:|---:|---:|
| 62 | 38611 | 38594 | 38628 | 0.1247079568642666 mm |
| 83 | 38611 | 38594 | 38628 | 0.10880193704410171 mm |

Both remain on `tran_27df5e50-4cf9-4f56-a187-ede010eca6b1`. The proposed frontier chords cross this same bank pair at different positions and angles. Their longer route-direction gaps do not create a different source-feature family, and the old tiny widths do not bound every new chord. The failed expectation of bank 38611 is retained as `bank-label-red.mjs` and `bank-label-red.stderr.txt`; `independent-result.json` records the exact rational endpoints for both original chords. This is a substantive correction because the different-family claim informed the proposed next contract. Correct the account, not the geometry, and do not rerun or replace the strict graph to repair prose.

The nine conditional prefixes are independently reproducible. All 150 route identities and all 150 first-active observations match; each of the nine complete routes also equals the earlier frozen route. Slot 35 first activates at fixed tick 2; the other eight activate at tick 1. All are generation 1, and the displayed Float32 scales and actual variants match. The separate candidate initialization remains source travel zero, physical travel zero, speed zero and phase zero. That is the agreed diagnostic convention, not an inferred tick-200 phase or a claim about an already executed candidate. Source-zero XZ matches the retained sampler. Current selected triangle Y supplies the proposed body-origin datum without the separate 80 mm actor lift.

Each prefix covers source 0–28 m in 152 or 153 recorded pieces, with fourteen real triangles and thirteen exact common-3D-edge transitions. I checked all 117 transitions against current raw road vertices. Every recorded point lies along its actual common edge within the disclosed arithmetic residual bound. The maximum join-coordinate difference and independently evaluated Y difference are each `3.552713678800501e-15` m. Those are evaluation roundoff on real shared geometry, not permission to bridge a physical hole. The independently summed prefix arcs range from 27.983933699812553 to 27.99682671796602 m and equal the stored values. Nine explicit prefix-to-entry witnesses remain bound; the two original seam paths also retain 97 adjacent covered-piece joins each, with at most that same rounding difference and one unsupported interval per row.

All 64 nominated current triangles match their recorded raw mesh vertices and a selected source face. I independently located each named source road/polygon in its bound byte range, checked the road's selected LOD, projected its ring, and recovered the recorded triangulated face. These are identities in the current builder's output. They do not turn its 0.20 m road presentation derivation, the original constructor's nearest-height choices, or absent OSM level tags into physical or source-selection authority. The conditional classification is appropriate under Review 57. Competitors and `sourceAuthority: unestablished` must remain visible.

The strict graph's three zero-candidate outcomes remain unchanged. Independent traversal of its recorded edges finds the first unreachable frontier at source 43.345796803396354–43.554640688022985 m for slots 88, 87 and 146. Every one of their 42 blocked edges is included: 126 edges and 47/50/51 unsupported intervals. Independent exact clipping agrees with every stored rational interval endpoint and departed/resumed bank roster. Exactly thirty edges per row have only complete internal gaps with actual 38594→38628 banks: from-node ordinals 3–8 paired with to-node ordinals 0–4. All twelve other edges per row remain excluded, including any edge carrying an open interval or a tiny terminal remnant. No numerical width threshold produced eligibility.

The five tiny terminal remnants per row remain real uncovered intervals for the represented binary64 constructed endpoint coordinates under exact arithmetic. Their roughly `2.18e-14`–`3.92e-14` m extent is below construction roundoff and does not establish a physical mesh hole. Exact clipping and exact point construction are distinct questions. `floatingFragments: 0` answers only the former. Keeping these edges excluded is a valid conservative boundary for this nomination; nothing here requires filling their remnants with an epsilon.

A separate full-mesh bounding inventory examined 79,367 road, 464,525 pavement and 183,350 terrain triangles. Fourteen triangles intersect the actual combined gap bounding box. Independent clipping reproduces the competitor IDs in all 148 gap records, including the different-feature lower roads; recomputed bank heights and normals match exactly. This verifies the retained finite gap inventory, not an obstruction envelope for a shoe or body. Lower geometry stays a competitor, never a fallback support value.

The proposal can admit a later **kinematic body-centre segment between the real bank endpoints**, with each unsupported interval and both selected banks retained. It must never manufacture a support triangle, return an interpolated gap height through a support query, weld the mesh or label the bridge physically supported. The reported bank-height differences do not waive this distinction. A real finite shoe may bridge an unsupported interior under the accepted declared model; centreline coverage is neither a sole-contact test nor proof that a foot cannot span the gap.

Any later nominated complete candidate must fit its supported XYZ and explicitly labelled bridge XYZ before independently summing physical arc and publishing a monotone source-to-physical mapping. All current rows correctly keep that complete mapping null. Freeze the unchanged route, offsets outside the repair, gate/occurrence data, original station/lateral roster, thirty eligible identities per row and geometric ranking before foot results. Retain the predeclared 1,024-station, 128-lateral and sixteen-candidate bounds and all refusals. Do not insert edges outside this roster if a later frontier also fails. A further failure is a bounded result. Birth-to-exit causal timing, actual variant/scale, F26 holds and F27's unresolved coupling must still be handled separately. Contact remains absolute normal distance below 15 mm, planted Euclidean drift below 5 mm, and separate nonpenetration with measured uncertainty. No native, anatomical or production verdict follows from nomination.

The summary field `firstRefusal.sourceM: 28` in these three rows names the start of the failed local graph domain. It is not the location of the first geometric obstruction. Use the detailed measured frontier above for that question. The frozen summary is preserved, and this review explicitly supplies the label distinction. A later consumer should retain separate `localDomainStartM` and frontier interval fields rather than expose 28 m as the hole location.

**Next discriminator for the four exit misses, proposed only.** Slots 35, 109, 91 and 72 fail all 57 predeclared exit candidates from 46 through 60 m at 0.25 m spacing. That does not establish absence of a later rejoin, much less an impossible route. Their next bounded diagnostic should overlay each unchanged preferred track against the same 57-triangle selected sheet over the complete remainder of occurrence 0, `walk:1465365729:0:0:ground0:f`: source 28 through 141.53886586122212 m. Retain the existing source-polyline derivative boundaries and 0.25 m constructor chords, and clip all spans into maximal covered and uncovered source intervals. Do not perform another lateral graph or choose an endpoint from foot results.

That interval output should distinguish three outcomes. A later covered interval with a continuous selected-surface construction supporting a full 0.25 m incoming and outgoing source witness can nominate a geometric exit after 60 m and at or before 141.28886586122212 m, selected by a rule frozen before execution. Coverage only in intervals too short or with unresolved height transitions is `rejoin-contact-boundary-unresolved`. No such interval before the first occurrence ends is `no-same-sheet-rejoin-in-occurrence`, with the complete uncovered spans and terminal evidence retained. The last result calls for a source-assignment/transition decision; it does not prove that every later occurrence or different explicitly assigned sheet fails. Preserve the four original exit-roster failures, all routes, preferred offsets, gates, scales and nine-case denominator. This recommendation is not permission to execute the overlay or adopt another surface.

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
|---|---|---|---|
| F36 | The report calls 38611 an original gap bank and falsely distinguishes the new bank family. | Confirmed by exact original-chord clipping; root accepted the diagnosis. Actual banks are 38594→38628 in both original seams and the proposed roster. | Correct and separately freeze the authored explanation, preserving the original target and all geometry. Focused re-review before nomination. |
| W2/W3 | Current source correspondence and actual foot support remain unestablished. | Conditional prefix and edge geometry are acceptable within Review 57; no nearest-Y authority or foot acceptance transfers. | Keep selected intervals, competitors, missing inputs and unsupported spans explicit during any later admitted experiment. |
| W3 | Four fixed exit rosters failed without establishing complete same-sheet rejoin absence. | Correct bounded refusal, not a new defect or proof of route impossibility. | Root may separately admit the finite occurrence-wide interval overlay proposed above. |

## Verification

`prepare.mjs` exited zero and checked all 51 target members, all nineteen strict-graph members and all 211 recorded shared inputs before copying the target. The authored snapshot is byte-identical. `independent.mjs` exited zero on Node v24.12.0 with the complete identity, source-face, prefix, exact 126-edge and original-two-chord checks described above. `geometry-checks.mjs` exited zero with all 148 competitor sets, real bank fields, adjacent seam joins and four analytic controls: closed shared edge, retained 0.12 mm slit, coincident XZ with different Y refused, and overlapping exact coplanar geometry accepted. No proposed edge was inserted or graph rerun.

Two reviewer instrument corrections are retained. The initial source lookup assumed one surface block per LOD inside a road; searching every matching block recovered the named polygon without changing the target. A subsequent byte-exact birth/prefix Y assertion found a `3.552713678800501e-15` m difference between two evaluations; XZ stays exact and the measured Y difference is explicitly retained under an arithmetic bound. The independent exact clipping had already passed in that attempt. The later bank-label failure is F36, not an instrument defect: its expectation came from the reviewed report and contradicted both its frozen bank record and the independently evaluated original chords.

Private target copies, scripts, successful outputs, preserved failed controls, before/after input checks and the final freeze are under `artifacts/causal-support-prefix-review/wt/artifacts/review67/`. Final checks rehash source inputs and both author freezes, validate this report's six sections and exact snapshot, inspect the two new documentation files for whitespace/size and named high-signal secret patterns, and record owned-resource cleanup. These checks do not claim a general absence-of-secrets proof. The repository document checker still refuses the inherited top-level `docs/work/0_shibuya-1km/handoff.md` shape; that unrelated failure remains disclosed.

The worktree contains only this new report and its authored snapshot as proposed tracked changes. No junction was created. All bounded Node commands were waited; no browser, server, native helper or GPU process was launched. The task-owned process inventory and its availability are recorded with the evidence. No resource is intentionally left running; no other worktree or shared output was removed.

## Round outcome

The geometry and exact ninety-edge eligibility result pass this bounded independent review. F36 blocks acceptance of the authored explanation as written. Root will obtain a minimally corrected frozen handoff and focused re-review before any revised nomination; this round grants no execution or foot/contact acceptance. The four failed exit rosters remain unresolved, with a concrete proposed discriminator rather than another repetition of the same search. The review report, exact rejected target snapshot and frozen evidence are released for root integration. This review worktree remains at `artifacts/causal-support-prefix-review/wt` until its evidence is preserved and root removes it after delivery.
