# Review 23: implementation

## Target

B0 city diagnostic preparation. The original B0 file-only supervisor, held trace reader, publisher attribution and future-build binder at main `7f8aeac`, using retirement freeze2 `a8b4e520b8c74c989048ffdd0a34bce2c1f0c06eb3f51431a36997d3a65cf3e7`. No future build or city run existed in this reviewed target.

The exact target is `artifacts/graphics-lifecycle-rethink/retirement-witness-prep-20260913/preparation-handoff.json`, SHA-256 `3285d5b2f85a7615c659038d5ba8d6f61ab8a28aa4b0440edb0baa780e48fdfd`. Reviewed text sources are retained byte-for-byte under `artifacts/network/retirement-pose-checkpoint/candidate-01/recovery/source/`; the consolidated `recovery/reviewed-source.patch` has SHA-256 `dd2fe770ac1368dbf21ee7bf3bd3033f2564f03b8141f63633c94478fa0114d9` across 61 source files for this checkpoint. The additive patch uses LF transport; exact copies and normalized reconstruction records preserve the original bytes and line endings. Raw output, source assets and earlier counterevidence remain ignored at their pinned handoffs. No experimental code is promoted by this document.

## Reviewers and coverage

Independent B0 reviewer authored the report below independently of the implementation. Root separately read its substantive findings and accepted only the dispositions stated here. The report is `artifacts/graphics-b0-review/report.md`, SHA-256 `507df963ae9a16aeb5aa8b150dbb77b7ce8352cb912b27a95f4bfda40f89d953`; its handoff is `85ef5735552857a7bb7945a857ad50d647c8876603dda00df582d6ebbdcde6cf`, sealed at `2026-09-13T13:00:05.5613216+00:00`. The wrapper is authored by the network worker for root's review; it does not alter the reviewer's body or claim additional independent work.

Rounds 18–23 follow the six handoffs' recorded seal times on 2026-09-13, not completion of their underlying implementation work. The original ABBA review and focused re-review remain separate rounds. The full authored text is embedded with ATX heading depth increased by three; inverse transformation is checked byte-for-byte. Local finding labels and priorities are preserved verbatim; permanent IDs occur only in the wrapper dispositions.

## Reports

### Independent B0 reviewer

<!-- authored-report-start sha256=507df963ae9a16aeb5aa8b150dbb77b7ce8352cb912b27a95f4bfda40f89d953 -->
Independent finite review of sealed B0 city diagnostic preparation — 2026-09-13.

Disposition: preparation needs three narrow corrections before the proposed city diagnostic is relied on. B01 misattributes a replacement-document marker. B02 permits admission after the observer has already read the matching navigation completion. B03 admits unexpected noncanonical debug-log paths into the future baseline. These are diagnostic preparation findings, not evidence that the ordinary city lifecycle passed or failed. No candidate or production implementation was changed by this review.

The integration owner assigned this read-only review against main `7f8aeac51357654a4d39d3e6ecc5f8ac0bda79b6` and retirement freeze2 `a8b4e520b8c74c989048ffdd0a34bce2c1f0c06eb3f51431a36997d3a65cf3e7`. Scope was the file-only B0 supervisor, trace classifier, held reader and publisher identity, mechanical lifecycle wrapper, prepared-input binder and future-build capture. The previously reviewed tiny preparation remains separate. No browser, server, GPU, production build, city run, population benchmark, lease acquisition, dependency, Git or source mutation occurred here.

Exact target envelopes under `artifacts/graphics-lifecycle-rethink/retirement-witness-prep-20260913`:

| Target | SHA-256 |
| --- | --- |
| b0-report.md | `923250cf8b547c7348015075864c4d8398e20483002bca2fb2283cd110afd378` |
| preparation-handoff.json | `3285d5b2f85a7615c659038d5ba8d6f61ab8a28aa4b0440edb0baa780e48fdfd` |
| prepared-inputs.json | `2758fdef2e01c2646fae47c1322d22c180f302cdf8d220f45c994897425620b9` |
| tiny-handoff.json | `c3ee4bd04b6d57b29dbde3cf62067c0a08bbda178dbdbfc454a57712c2c427df` |
| tiny-inputs.json | `3c672bf0442e5bc93fbba80231f953ccb0f9b8ef52c1e3ea9db5a82e93acf4e9` |

Both independent full pin passes verified all 463 preparation records (15,387,171 bytes), all 25 tiny handoff records (2,113,999 bytes), and all 6,162 tiny input records (502,146,006 bytes), with zero mismatches in those populations. Both checked all 17,377 prepared input records (2,260,294,806 original bytes). Exactly one prepared row changed: the canonical Chromium debug log. All 17,376 other prepared rows matched. The source and evidence files remained frozen, and the tiny controls were read and hash-verified without rerunning them.

The changed log is `browser-runtime/chromium_headless_shell-1243/chrome-headless-shell-win64/debug.log`. Its prepared 29,219 bytes hash to `732a3b83303ec8e77e133eb682d5a597ea4c5adc9d74be7189ca9e93db91c339`. Its observed 29,585 bytes hash to `21455710a511bf3ad726a68cb0f0f62a6c006de745f342838962da00f87b57e7`. Independent prefix hashing proves the prepared bytes remain intact. The 366-byte suffix hashes to `53a48c821ff692256df81937dd3684d9c93375ecff23d42e8d286c7166a28aba`. This is byte accounting only. Attribution to the separately authorized tiny launch belongs to that launch's owner and retained evidence. This review does not claim all original runtime bytes stayed unchanged.

B01 [P2] — Preserve the earliest captured noon snapshot when limiting first-city attribution. In `b0-core.ps1:118`, every matching noon main-frame snapshot overwrites `ReplacementSnapshot`. `Get-B0Result` then takes the minimum of the API end and that last stored timestamp. The report promises the earlier boundary established by the first captured noon main-frame snapshot. The exact classifier was exercised with noon snapshots at 900103 and 900105, a lost marker at 900104, and API completion at 900110. With only the first snapshot, the marker is correctly listed as later-document evidence and the result is unavailable: zero first-city markers, window end 900103. Adding the second snapshot, without changing the marker or API end, yields `observed`, one first-city marker, no later marker, and window end 900105. This is a false first-city attribution in the diagnostic. Retain the earliest matching boundary, including a regression control with at least two replacement snapshots and an intervening marker. Evidence: `result-snapshots-1.json`, `result-snapshots-2.json`, `controls.json`, and the retained fixture traces. The finite classifier reproduction does not assert that an actual ordinary city trace produced this sequence.

B02 [P2] — Reject a new spawn after matching navigation completion has already been observed. In `b0-core.ps1:37`, completion is recorded as unavailable only when the before snapshot has not yet arrived. A poll that reads the complete ordered sequence `noon before API → matching before snapshot → noon after API` still creates the trigger and invokes the launch callback. The independent control writes all three records before polling and observes one callback with no issue. The reversed order `before API → after API → before snapshot` correctly remains unavailable with no callback. This violates the required trigger-before-observed-completion rule and permits retrospective admission that the preparation text describes as active-call admission. Track completion regardless of snapshot presence and refuse a first spawn once that completion was observed. The enforceable requirement is an observer-order guard. A file observer cannot prove that its eventual physical open happened before the actual API completed, because producer buffering and scheduling can race between poll and reader admission. The repaired report must retain that limitation and must not promise physical synchronization. Evidence: `result-completed-False.json`, `result-completed-True.json`, and `controls.json`; the callback only counted attempts and launched no process.

B03 [P2] — Limit the prepared-input exception to the exact canonical browser log key. `bind-city-inputs.mjs:33` skips hashes for every browser-runtime path ending in `/debug.log`; line 73 uses the same broad suffix to omit paths from prepared population equality. Later runtime-output classification only exempts one exact canonical key. Evaluating the actual extracted predicate against the complete prepared record population shows that adding `chromium_headless_shell-1243/chrome-headless-shell-win64/nested/debug.log` does not change the prepared population projection, while adding `nested/other.log` does. Such a new log can be accepted into `inputs-before.json` and then remain unchanged through the run without an input-change finding. This contradicts the binder's claim that no other path is exempt. The actual sealed population contains exactly one canonical debug log, so this is a prospective coverage gap, not evidence that an unexpected file was present or changed during a measured run. Use the same exact canonical key for prepared hash/population checks and later output classification. Evidence: the six source/predicate controls in `pins-before.json` and `pins-after.json`. The complete binder was not run because it requires a future authorized build; the reproduced predicate and its complete surrounding source establish the narrower preparation issue without manufacturing build pins.

The remaining reviewed design is coherent within its declared limits. Exact version-9 library trace header fields, test title, base URL, fresh header/file creation, initial dusk navigation and successful after snapshot bind the target context, page and main frame. The supervisor selects a unique already-retained live owned Node worker through complete Windows argument parsing and the installed worker-entry path. The child independently pins process creation ticks and executable, opens one physical trace identity, verifies header bytes and replays from the exact navigation offset. This is source-grounded collocated publisher attribution, not an operating-system observation of which process holds a particular file. It does not distinguish JavaScript execution contexts from the stable Playwright page identity.

The long supervisor is separate from the one-shot reader clock. Reader work is limited to 110 seconds from child start, including compilation/admission, inside a fixed 120-second parent cap. A failed admission consumes the single attempt; no later retry, reopened path, rescue source or extended wait cycle is supplied. Poll, record, reader-byte, record-count and retained-evidence limits fail unavailable. Completion of the npm parent alone is not accepted as trace EOF. The exact retained publisher must exit, then the held file must be drained to its current end with no partial final line.

The file is opened with `ReadWrite|Delete` sharing. An independent Windows control deleted the name, recreated a different file at the same path, and verified that the old handle remained delete-pending, had a different physical identity, and still reread the original five records. A separate control reached EOF while a retained current-process publisher remained alive and received `publisher-active`. These controls support the file-handle mechanism, not the production publisher mapping. Holding a deleted file extends its physical storage lifetime until the reader closes it; this and the extra polling/compilation are real observer costs. Zero performance impact is not claimed.

Marker status remains separate from captured-window errors and the ordinary Playwright result. Same-page console errors/pageErrors in the captured window fail despite a lost marker; unsupported, already-lost, malformed, duplicate and missing evidence remain distinct. The installed Playwright `willCloseBrowserContext` calls `_stopTracing` before final context closure. Consequently `pageClosed` is optional, and the code requires validated navigation/before records, a completed noon API record and publisher closure/final drain instead. Its result explicitly limits absence-of-error claims to captured trace bytes. It does not establish absence of cleanup errors after tracing stopped, delivery of a native asynchronous WebGL event, native resource destruction timing, full-page cleanup, or success of the separate 15-second navigation and 60-second replacement-render checks.

The wrapper generator was evaluated with a capture-only writer and its generated text was byte-identical to sealed `run-city.ps1`. The existing ordinary lifecycle command, style/camera setup, recording path, test spec and timeout values remain unchanged. The B0 additions have their own diagnostic records and preserve the acquired-owner finally cleanup. Fresh empty output and fresh Playwright artifact-root checks remain explicit. Known retained ownership limits remain: a sampled inventory is not a proof of historical processes it never admitted; unresolved ownership omissions, publisher errors, listener or profile/temp cleanup must remain visible in the eventual run. This review did not launch that wrapper.

The future-build script only records existing dist files, refuses overwrites and binds the source freeze; it never builds. The before/after binder verifies the reviewed source delta, complete source/data/runtime populations, witness sources and actual future dist population/hashes, apart from B03. There is no authorized build, future build manifest or city before/after run evidence from this review. A later build needs separate authorization and root inspection of its actual pins; later city evidence needs its own ordinary test outcome, reader result, runtime log accounting and native owned-resource closure. The old B0 scheduling concern is substantially addressed by the long supervisor, subject to B01/B02 correction and honest file-observer limits.

Fresh verification was deliberately finite: seven actual PowerShell classifier/Windows-handle controls and six binder/generator controls. All executed as expected, including the three adverse reproductions. No full author suite, 16-million-point oracle, tiny browser control or population timing was rerun. Probe source headers name the existing instrument. Prior author failures, original review counterevidence and all current fixture bytes/results are retained.

Cleanup: single-process probes used hidden launches with retained handles, a 120-second outer cap and 110-second cooperative work guard. Reviewer child 44784 exited at 12:55:01.199Z, child 44188 at 12:56:47.003Z, and child 43340 at 12:57:37.679Z, all with exit code 0 and confirmed handle closure. A strict native CIM snapshot at 12:57:53.121Z found all three PIDs absent, no matching current review process or children, and no listener on port 4319. No signals were sent by the closure check. Its evidence is point-in-time and says nothing about unrelated owners. Every review output is retained intentionally under this ignored sibling folder for findings/handoff; no disposable process-owned resources remain. No source restoration was required, and post-check target pin verification confirmed the same frozen files. CPU review work is now complete; no further tests, hashes or builds will run during root's quiet window.

<!-- authored-report-end -->

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
| --- | --- | --- | --- |
| F17 (original B01) | A later noon snapshot overwrote the earliest replacement-document boundary. | OPEN. Root accepts the P2 finding; no repair is accepted here. | Retain the earliest captured boundary and reject the preserved two-snapshot/intervening-marker control. |
| F18 (original B02) | The supervisor could first spawn after it had already read matching navigation completion. | OPEN. Root accepts the P2 finding; no repair is accepted here. | Refuse admission after observed completion; retain buffering/scheduling limits rather than promise physical synchronization. |
| F19 (original B03) | Prepared hash/population checks exempted any browser path ending in debug.log. | OPEN. Root accepts the P2 finding; no repair is accepted here. | Use only the exact canonical browser log key in every relevant prepared/binding check. |

## Verification

The reviewer ran seven PowerShell classifier/Windows-handle controls and six binder/generator controls, including the three concrete adverse cases. It verified the frozen records and preserved the sole canonical log append as changed runtime output. No city browser, build or native delivery run was performed. Root read the full report, verified all 29 owned pins, accepted all three findings and assigned a narrow successor instrument repair; that assignment is not a fix.

Preparation of this documentation checkpoint checks the exact authored/source pins, reversible imports, scoped document headings and links, blob/secret/diff limits, and separate HEAD/WIP preservation. It runs no browser, benchmark, code build, five-gate sequence or source repair. Review 11 remains open. Primary WIP also retains rounds 2, 4 and 12 that are absent from HEAD. This checkpoint neither invents those missing committed rounds nor claims a whole work-docs continuity pass.

## Round outcome

The original B0 preparation is rejected pending F17/F18/F19 repair and focused re-review. The ordinary city 15-second navigation, 60-second replacement readiness and full 44-view graphics gate remain separate unfulfilled acceptance criteria. Current delivery status remains in [the plan](../plan.md).
