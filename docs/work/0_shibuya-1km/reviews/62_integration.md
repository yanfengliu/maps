# Review 62: integration

## Target

The applied, uncommitted walking-observer milestone in maps, based on `d7c8eea43c1cbfc6e85bcd01306fd43898be6b66`. The exact 21-path target is bound by `artifacts/walking-observer-milestone-20260919/freeze-post-gates.json`, SHA-256 `1aa2699430a0f6de8b37bec0114e61ea02baf3054deeefd5c5a45c039cce8829`. It contains five source/test additions, thirteen exact Reviews 57–61 documents and snapshots, and three canonical documents. The complete patch is `280ca6681d6739ee1f5ae4d2d520c5e77e6f723076ec0e06949c6e2980a77705`; the post-gate manifest is `932774e069ac70918bb75606d2473467dbb30dfd8e6e9c2ce451f6c361c8238d`.

The exact integration report is retained as [62_walking-observer-integration.md](../snapshots/62_walking-observer-integration.md), SHA-256 `8636fe86334410ca668ceda63c0bae50b674e3076a24cdbc603b255d3003ec5b`. A copy of the target freeze, full patch, report, manifest and canonical delta is retained with the ignored review evidence under `artifacts/walking-observer-integration-review/frozen/artifacts/review62/target/`. Root owns acceptance, staging and delivery.

## Reviewers and coverage

Codex `walking_integration_review` is independent of the component author and integration owner. I read AGENTS.md, local rules, lessons, the fleet work-document format, accepted Review 59, the actual observer-only delta, support/corridor contracts, transfer and gate instruments, raw gate outputs, preservation records and post-gate canonical amendments. I inspected the supplied source bytes and replayed read-only binding checks from an isolated review worktree at the same base.

No primary file was edited. No build, full test suite, audit, browser, GPU capture, native fixture, server, fetch, dependency install, commit or push was run by this review. I used the completed primary gate receipts and independently checked their inputs and outputs. No new native-resolution image review or product motion claim is made.

## Reports

### Codex walking_integration_review

**Recommend scoped acceptance of this exact integration. No material new finding was reproduced.** All 21 target files and 99 frozen evidence members match their recorded lengths and hashes. The five code/test files and thirteen imported documents also match their actual source locations byte-for-byte. The primary HEAD is unchanged, the index is empty, and the complete unstaged/untracked scope is exactly the 21 assigned paths. Both forward application against the review's base and reverse application against primary pass as dry runs.

The code remains the exact Review 59 target: path `99f78810e51a0b03de8ea81a34f095250ce88abaf725edaccfe86ba5331f75e3`, surfaces `9c0274aa6cf2ec0f2362b30f9c1d486531937527e725448d1da9a35198259674`, corridor `18d119d735c64373a6ce8598a6d81b57f9ac572dacf3a043b8f5b3d5f626922a`, original test `8acdf6aab744cd6f853dcd68f754d4c44a5eff126dc0c5e138ab5602a306271b`, and observer test `4a1ac323821934b628313a1d6d08105a333fe454819d34498781e76b60fb78db`. The four original F26 imports remain recoverable separately from the observer delta. Only the path gains observation hooks; the new test file adds fourteen cases. I checked the original four-file manifest and both preserved patches. This is an observation of the completed calculation, with detached frozen records and propagated callback exceptions; it does not add an admission transaction.

The 24 transitive dependencies retain the exact recorded raw hashes in primary, author, retained baseline and integration preparation. Their contents match after only CRLF-to-LF normalization. This is content compatibility, not a claim that differently ended raw files are identical. No normalization was written back. The added path explicitly remains `productionReady: false`. The observed browser closure does not reach any of the five additions, and the actual source import search reveals no production caller of the new walking APIs.

The visual transfer satisfies the repository's stated three-part rule. Fresh primary build output equals the original complete four-file dist, including the three certified members and the source map. Read-only calls to the shipped `sceneTreeDigest` and `harnessTreeDigest` reproduce the original certificate's complete content digests: scene `dfee0f12bb2e87c1defa7c41c0400e0df4f98ab829fd346d5d689da0c406873c` over 117 files / 301,782,754 bytes, and harness `45aeaf15464fbc2a34e0252f3c06b6e8bd44969103aeb65bdd7d21dab53d0593` over 24 files / 219,520 bytes. Replaying the inspected TypeScript AST walker against primary reproduces the exact 88-file closure from its two HTML roots, with zero candidate reaches, unresolved imports or nonliteral dynamic imports. The replay changes only the explicit input root and dependency location; its output stays in the reviewer tree.

All 109 preserved visual/build files match both their retained and current counterparts, totaling 71,137,154 bytes. All 44 certified PNGs match their hashes and 1280×720 dimensions, and all three lifecycle records match, name completed teardown, have no console/page errors and bind the same certified build. Certificate `8d70420e07afbfa7`, SHA-256 `9cdb592428903b0d1fdff4daf5cec4c375a1ad21cf9e1c122a679ac55722f611`, remains the original evidence from `05ad1a4830e5876be03116e468199370504a6693`. Review 47 and the existing Review 55 transfer record establish the provenance and agents-OFF still-image review. No certificate was issued again. This transfers that existing bound only; it proves no pedestrian contact, controller behavior, temporal stability, source adoption, populated scene or performance result.

All 101 earlier history documents equal both their recorded hashes and their bytes in Git at the integration base. The 241,649-byte historical plan suffix remains exact at `30b5ba3b571debac1012657ed6c47ead5ac90a6a2b5a7fcf4313924882604fde`. After the four primary gates, only the current plan, one summary entry and this session's detailed devlog changed. Their amendment accurately distinguishes verified unwired code from pending integration/delivery. Review 60's F34 rejection and the old run02 exit-evidence limit remain intact. The run03 statements are attributed root observations pending a separate native-results review at the time this target was frozen. I verified the separate report and freeze pointer hashes; I did not independently accept that native run here.

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
| --- | --- | --- | --- |
| F26 | Preserve the bounded physical/source walking contract through integration. | No integration regression found. The accepted five-file target, its dependency contents and separation from production are preserved. | Root may accept and deliver the exact reviewed milestone. |
| F34 | The separate shoe observer can falsely accept an uncertainty-sensitive support hull. | Remains rejected in exact Review 60 history. No shoe code is in this integration. | The separate repair and re-review remain outside this acceptance. |
| F28/F30/F32 | Native launcher, survivor and capture obligations. | No acceptance transferred from this code milestone. Review 61 is preserved as an admission to one bounded diagnostic; results need their own disposition. | Keep the separate native result review and later launcher/capture acceptance distinct. |

No new finding ID is requested. Existing source-selection, moving-controller, contact and whole-city acceptance gaps are not closed by this report.

## Verification

The frozen primary receipts directly record awaited process exit 0 for build, types, unit tests and high-threshold audit on Node v24.12.0. Their raw logs agree: 84 files / 723 tests pass; the build retains its chunk-size warning; audit retains two moderate Vitest advisories, with no dependency change. The gate runner checks all 21 applied hashes before each sequential command and after completion, invokes the actual npm CLI, and stores the original child return code rather than a pipeline's last status. I compared those gate-target copies to final primary; the three declared canonical amendments are the only later changes. No gate was rerun by the reviewer.

Independent checks passed for the target/evidence rosters, actual source imports, baseline and delta pins, dependency compatibility, historical preservation, complete patch applicability, all 109 current/retained visual members, PNG and lifecycle records, actual served/harness digests, exact AST closure replay, seven additional build/config inputs, all 36 listed local Markdown links and the 256 KiB file bound. A scan of four selected credential patterns found no match in the full patch; root still owns final staged inspection. The inherited top-level `handoff.md` structural refusal remains disclosed; no repository-wide document-check pass is claimed.

The initial reviewer checker compared a digest object with a certificate object that additionally contains mount metadata; the actual digest fields already agreed. A second local checker initially used Windows' default cp1252 to decode a UTF-8 patch. Both instrument errors stopped before a pass and are retained with their corrected successful results. They were not target failures. The first Git metadata and CIM queries were refused by sandbox permissions; their scoped approved retries succeeded. The read-only process census found zero Node, Python or browser processes naming this review scope. Every spawned command was awaited. No junction, browser, server or native fixture was created.

Ignored review scripts, inputs and results are retained under `artifacts/walking-observer-integration-review/frozen/artifacts/review62/` with an exact final freeze and cleanup receipt. The report and exact snapshot are the only authored tracked-path additions from this review.

## Round outcome

Recommend acceptance of the exact frozen 21-path integration and its proved original visual transfer. No material integration finding remains from this round. Root must preserve these code bytes, identify the inherited visual revision and proof in the code commit message, inspect the final stage, and complete main/remote delivery. This report does not claim that delivery already happened or that the Shibuya goal, source policy, walking controller, shoes, launcher or moving visual acceptance is complete.
