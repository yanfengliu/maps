# Round 33 — independent review of the acceptance evidence chain

Reviewer: an independent, read-only lane. Revision reviewed: `39ca459`, with main advancing to `144766b` (docs-only) during the round; all bindings unchanged. Authored under `artifacts/review-acceptance-evidence/r2.md` and preserved here on 2026-09-17 because authored review rounds belong on `main`. Verdict: **no blocking code finding**; its two blocking findings are criterion-unmet statements — the signature shot and performance as written — both recorded in the plan with their causes. Its record corrections are applied: the certified hero frames' TAA reading (now cited from `artifacts/visual/hero/hero.json`) and the populated capture's noon time. The scene-identity finding — the certificate matches the 84-file agent-free tree it was captured against, and the 33 restored human-agent files are a later, intended difference the frames cannot depend on — is recorded in the plan and the devlog.

# r2 — independent review of the acceptance evidence

Reviewer: read-only, delegated. Subject: the acceptance evidence the Shibuya deliverable rests on, not code style. No tracked file was modified; all re-derivations were run from `C:\Users\38909\Documents\github\maps` with Node 24 and PowerShell, and nothing was written outside `artifacts/review-acceptance-evidence/`.

Revision note. The brief says main is at `39ca459`. It was when this review started; a docs-only commit landed during the review, so `HEAD` is now `144766b` ("docs: the session's record …"), which changes only `docs/learning/defect-register.md` (+24) and `docs/work/0_shibuya-1km/plan.md` (+33/−1). Every binding the certificate carries is unaffected: `dist` bytes, the harness digest and the scene digest are all unchanged between the two, and the plan/register text quoted below is `144766b`'s. `git worktree list` shows one worktree (the primary, at `main`); `git branch -a` shows `main` and `origin/main` only, so the "nothing left on a branch" claim holds.

---

## Claim 1 — the 44 certified frames (`artifacts/visual/complete.json`, runId `94fc1aa78b0af8f9`)

**Artifacts checked:** `artifacts/visual/complete.json`, `artifacts/visual/run.json`, `tools/visual/verify-output.ts` (`sceneTreeDigest`, `harnessTreeDigest`, `HARNESS_ROOTS`), `data/scene/` + `data/network/`, `dist/`, `src/world/aoi.ts`.

**Build bytes.** I ran `npm run build` on this checkout (`39ca459` + docs-only `144766b`) and hashed the result: `dist/index.html` `782e2ddec658ac03265d7c4c8df056cdf4441da26bd8ce2a5ad6559f27c446d7`, `dist/assets/index-B3qA34nq.js` `b50735b70fca1adab00b88575aa85167fa12e4776bc95485f69b3f9414eff01c`, `dist/assets/index-dvQKBZaf.css` `11dd032c51b536a500e2265a9acdcdda9070f73e6c0a8f31e75b6ad5ab7ba929`. All three equal the certificate's `build` array. The bundle is reproducible from the reviewed revision.

**Scene digest.** I re-implemented `sceneTreeDigest` exactly (recursive `readdir().sort()`, key `route + relative path`, per-file `sha256(bytes)`, entries joined with `\n`, digest over the joined text) and ran it over `data/scene` + `data/network`:

- whole served tree today: **117 files / 301,674,250 bytes → `dd637661f6ebf56c88c46761d98ec421ea7475a82df6e0107204028b608fd913`** — not the certificate's value;
- the same tree with the 33 delivered human-agent files removed (3 humans × 11 assets: `source.blend`, near/medium/far × positions/normals/glb, and the manifest json): **84 files / 214,114,015 bytes → `ddd21ee1b4feb7f7eb1e2bcda4c39bede72757756fc582686aa488870257d500`** — exactly the certificate's `scene` block.

So the certificate's scene binding is a faithful digest of the tree it was captured against, and the difference from today's served data is exactly the human agent set: 33 files / 87,560,235 bytes. The 4 vehicle files (`vehicle-kei/taxi/bus.glb` + `vehicles.json`, 1,180,212 bytes) were present at capture and are still present, which is why the certificate is 84 files and not 80. Timeline from file mtimes (machine is PDT): capture ran 22:49:31–22:55:15 PDT; the human bake wrote 22:55–22:59 PDT. `npm run visual`'s `--end` refuses if the scene digest moved, so the bake could only start after certification — which is what the plan says happened (plan.md, post-capture runbook).

**Could the frames depend on the absent human assets?** No. The verdict lane runs without `?agents=`, so the population is off and `src/world/agent-assets.ts` never requests those files; `verify-output.ts` records that a run without `?agents=` "never loads the human assets". The inspections agree on the pixels: both hero reports and the sweep reports record **no pedestrians or vehicles visible in any crop**. The scene digest is now unreproducible from the current `data/` tree, so a future certificate cannot be compared against this one on scene identity — that is a bound, not a defect in the frames.

**Harness digest.** I re-implemented `harnessTreeDigest` over the certificate's own `roots` (`tools/visual`, `playwright.config.ts`, `playwright.lifecycle.config.ts`, `vite.config.ts`, `tools/vite/serve-scene-data.ts`): **23 files / 214,395 bytes → `d9c8b8791e166048b24c0d67d7fb5c6a23bb35546f14b8f5a501f1e02318c1c3`**, equal to the certificate. `tools/visual` was last committed at `f741fa7c` on 2026-09-16 22:46:02 PDT — three minutes before the capture began — and has not changed since, so the harness binding is intact at `144766b`.

**Frame set.** I hashed all 44 PNGs named in `complete.json`: **44/44 match their recorded SHA-256**. `verifyVisualRun` also requires native 1280×720, which the crop manifests independently record (each frame's 8 regions tile 1280×720 and reassemble byte-for-byte).

**Verdict: holds.** Build, harness and frame bindings are all reproducible from the reviewed revision. One bound is real and disclosed: the scene digest no longer matches the served data (human agent set restored after certification), and the certificate is of an agent-free city, which the frames confirm and which the plan records.

## Claim 2 — the native inspection (`artifacts/inspection-gpu/**`, `artifacts/inspection-populated/**`)

**Artifacts checked:** all eight GPU reports (`inspection-gpu/hero/REPORT-crossing.md`, `inspection-gpu/hero/REPORT-approach.md`, and the six `inspection-gpu/sweep/REPORT-*.md`), both populated reports, every crop `manifest.json` under both trees, and the frames themselves.

**Re-derivation.** I recomputed, from the bytes on disk:

- all 44 certificate frames against `complete.json`: 44/44 match;
- every crop manifest under `artifacts/inspection-gpu` and `artifacts/inspection-populated/crops` — **58 manifests, 464 crops**: frame record (path, bytes, sha256) matches the file in 58/58 cases, crop sha256 and byte length match in 464/464 cases, **zero mismatches**;
- every 64-hex digest *printed in a report* against the certificate or the populated manifest. Seven of the eight GPU reports print only digests that are in `complete.json` (6 frame digests each). `REPORT-satellite-overhead.md` additionally prints all 48 crop digests (crops, not frames — they match their own manifests). `REPORT-approach.md` prints two digests not in the certificate — `7185d516…` (`return-cartographic-dusk`) and `449bc5e6…` (`return-cartographic-noon`) — and the report itself says they are outside `complete.json` by construction and bound to their crop manifests; those manifests exist under `inspection-gpu/hero/return-*/` and their frame records and 16 crops recompute clean. In the populated set, `REPORT-poses.md`'s 24 frame digests are all in `artifacts/populated-capture/manifest.json` (24/24), `REPORT-crossing.md`'s one frame digest `f8e32bfe…` is the manifest's value for all 14 crossing frames, and its one non-manifest digest is a crop (`22935599…`), not a frame.

**Spot-checks (three reports, two crops each, opened at native size and read).** (1) `inspection-gpu/hero/hero-satellite-dusk-crossing`: `left-ground` (320×180) is the tan paving-slab grid with the yellow line fragment; `centre-crossing` (640×360) is the scramble with both yellow diagonal arms, the zebra bands, `TOKYO`×2, `TSUTAYA` and no agents — the report's description exactly. (2) `inspection-gpu/sweep/cartographic/overhead-az180`: `left-ground` carries the report's F1 black blob on pale ground; `left-lower` is dense pale city from above with a clean rim. (3) `inspection-gpu/sweep/satellite/plaza-az000`: `left-ground` and `near-pavement` are effectively uniform near-black with a faint gradient — the report's "severe … uniform black", with the attribution line still legible at the bottom of `near-pavement`. Digests recomputed for each of these crops and frames match their manifests and the certificate.

**Did each report open every crop at native size?** Yes for seven of the eight GPU reports, in substance and often verbatim ("each crop opened at its own native size (320x180, 320x360, 640x180, 640x360, 320x540 per manifest)"; "48 of 48 crops opened and read at their own size"; hero crossing "all 32 crops then opened and read at their own size"; approach "every crop opened at its own size"). Two weaknesses in the text, not in the binding: `REPORT-satellite-plaza.md` says "at each crop's own pixel size" but prints only 8-hex-prefix crop digests, and `REPORT-cartographic-plaza.md` prints no crop digests at all, so their crop-level binding cannot be re-checked from the report alone — I re-checked it from the manifests instead (clean). For the populated set the coverage is narrower and is reported honestly: `REPORT-poses.md` opened all 24 of its frames and 32 region crops (4 crop directories), `REPORT-crossing.md` opened 3 of its 14 frames directly (`crossing-00/06/13`) and 8 crops of one, hashing the other 11.

**Findings the reports carry, by severity as the reports themselves grade them.**

GPU set (all under `complete.json`, none blocking):
- *finding (minor)* — cartographic `plaza-az000` `left-ground`, one flat colour across the whole 320×180 cell (grazing-angle wash-out at its extreme); `note`-level at `plaza-az120` (three cells) and `plaza-az180` (two cells).
- *findings* — satellite `plaza-az000` near-field detail loss severe (`left-ground` and `near-pavement` uniform black); `plaza-az120`/`az180` notable; satellite `plaza-az060` facade atlas softness severe in `left-upper`, and a bright wash band across the carriageway in `left-lower`.
- *finding* — satellite `block-az060`: the shadow side of the foreground tower fills roughly the right third of the frame as near-featureless black; coherent with the tower silhouette, read as deep dusk shade (cartographic at the same pose keeps structure).
- **F1** (cartographic overhead): an irregular pure-black patch on interior open ground, seen at `az060`, `az180`, `az240`; adjudicated below in Claim 5.
- *notes* — near-field wash-out at grazing angles; facade atlas softness; mottled aerial photography over LOD2 roofs; pale roof treatment under low sun; azimuth-dependent sky gradient; bright ground glints; pale rectangular fragments at the outer rim (`az300`); 1–2 px red specks; a static junction glow; the World style panel and attribution line as application chrome (never defects); dark-but-structured dusk shadow sides; crossing occluded by foreground massing at block distance.

Populated set (bound to `artifacts/populated-capture/manifest.json`, which carries `certifiable: false`):
- **BLOCKING** (`REPORT-crossing.md`) — 14 of 14 crossing frames byte-identical (`f8e32bfe…`) across 26.15 simulated seconds spanning the whole pedestrian green, zero pedestrians and zero vehicles in frame: "the signature shot is an empty junction", "the signature acceptance criterion … is not met". The report verifies this is a true depiction (3,000 rendered, 0 within 60 m, crossing events accumulating elsewhere) and corrects an earlier misreading of the lit red lens.
- *findings* (`REPORT-poses.md`) — pedestrians packed in knots at the west-edge portal and not walking for 14 s (`corridor`); no vehicle visible in any of 8 frames at the `corridor` pose (16 active, `vehiclesCrossed` flat); the `boundary` pose cannot observe its criterion (portal hidden behind rooftops, lifecycle counts constant, no spawn/despawn in the window, 2 px of change).
- *notes* — noon not dusk; blurry satellite facades; a shadowed walkers street. The `walkers` pose passes: pedestrians at 1–2 m/s, mid-stride, feet planted, no visible interpenetration.

**Verdict: holds, with bounds.** The digest binding is real and I reproduced it wholesale (44 frames, 464 crops, every printed digest); the reports do describe opening their crops at native size, and my six-crop spot-check matched their prose. Bounds: three reports do not print full crop digests; the populated inspection opens 27 of 38 frames at native size (11 crossing frames are hash-only, justified by byte-identity); and both populated reports bind to an iteration-lane manifest with no certificate.

## Claim 3 — the performance measurement (`artifacts/frame-budget/REPORT-20260917.md`, `interval.json`, `sweep.json`)

**Artifacts checked:** the report, both JSONs, `docs/learning/defect-register.md`, `index.html`, `dist/assets/index-B3qA34nq.js`, git history.

**Numbers re-derived from the JSONs (all match the report).** Acceptance arm: `frameIntervalMs` p50 **16.7** / p95 **33.4** (n=930 interior gaps; p05 16.6, max 316.7, mean 22.06), `fpsFromMedianInterval` **59.88**, target 16.667; simulation (fixed step) p50 11.8 / p95 27.6; `advance` p50 17.5 / p95 34.0; frame-own p50 5.6 / p95 6.7; headroom 4.867 ms; steps histogram 0:3, 1:664, 2:248, 3:8, 4:1, 5:3 over 927 frames → catch-up 260/927 = 28.0%, 3 at the cap; population drawn 3,000 / 59 at open and close; lifecycle spawned 3,068 / retired 9 / active 3,059; authority violations 0; window 20,576 ms, 933 frames, ticks 106→1,314, simulated 1.77→21.90 s, camera travelled 273.4 m. Sweep arms (2,500 / 2,000 / 1,500 / 1,250 / 1,000, in that order): every arm p50 **16.7**, p95 16.8/16.8/16.7/16.7/16.8, 51 vehicles drawn, histograms and catch-up shares exactly as the table prints them (2.9% / 1.3% / 1.6% / 6.5% / 2.9%). `holdsSixtyFps` is `false` for every arm and `largestHoldingArm` is `null`, exactly as the report says; the report calls that an artifact of the 100 µs `performance.now()` clamp rather than a finding.

**The instrument defect.** I verified the report's §"Instrument defect" independently: `index.html` contains no `probe-entry` line; `git log --all -S "probe-entry" -- index.html` is empty; `index.html` was last committed by `0ba1e12` (2026-09-06); the certified bundle contains no `__frameBudgetProbe`; `tools/frame-budget/probe-entry.ts` exists but no served page references it (the only tracked references are in `tools/frame-budget/run.ts` and the defect register). So the lane as committed cannot run against the certified build, the numbers describe worktree build `11c5d868…` (`index-CyjzInZt.js`, 975,953 bytes) = the certified bytes `b50735b7…` **plus** an observation-only seam, and no on-disk artifact of that build survives (`artifacts/frame-budget/base-wt` is a partial copy with no `index.html`, no `dist`, no `.git`). The register entry recording the defect and the owed permanent fix is committed, but in `144766b`, one docs-only commit after the revision under review.

**Is the bound stated honestly?** Yes. §"Machine and build" and §(d) name both digests, say the numbers describe the certified bytes plus the seam, and say the 2026-09-16 numbers are not independently checkable because that worktree was destroyed. `interval.json` and `sweep.json` themselves carry `"certifiable": false` and `"evidence": "not evidence: an iteration lane with no certificate, refused by tools/visual/verify-output.ts"`. Load is tabulated at four sample points around the arms (11.8–42.4% CPU, against 65.8–76.7% before any run), which supports the report's claim; the plan's phrase "load tabulated per arm" is a slight overstatement — the table is per sample point, not per arm.

**Does the acceptance claim survive?** "The median holds 60 fps at 3,000" holds only as: the median frame interval reads 16.7 ms, which a 60 Hz vsync frame reads as under a 0.1 ms clock; the more precise statement the JSON supports is "p50 sits on the vsync quantum, 59.88 fps from the median, with p95 33.4 ms one full frame late". The claim is about **3,000 pedestrians and 59 drawn vehicles**, not the 200 the criterion names, and it is measured on a build that has never existed on `main`. The criterion's signal-obedience half is separately unmeasurable in this fixture.

**Verdict: holds with a bound — and the criterion it serves is not met.** The arithmetic is honest and reproducible from the retained JSONs; the artifact measured is not on `main` and not retained; the vehicle half of the population is 59 (acceptance) / 51 (sweep) of 200.

## Claim 4 — the flythrough (`artifacts/flythrough2/manifest.json`)

**Artifacts checked:** the manifest (46 frames), its per-frame records, `tools/flythrough/frames.ts` (`judgeSequence`), `tools/flythrough/plan.ts` (the scored crowd pose), `tools/flythrough/flythrough.spec.ts` (what is asserted and what is written), and the artifact tree for a retained console and a frame inspection.

**Re-derivation.** I parsed the manifest and hashed every frame file: **46 frames — overview 11, approach 12, crowd 12, ascent 11 — 46/46 digests match disk, all 46 digests distinct (100%, floor 95%)**. Repaired-check semantics are all present in the record: each leg's first frame carries `cameraTravelM: null` (4/4); 42 real pairs all clear the 1 mm floor (`stillDriven` 0); the crowd leg's 6 held pairs (`cameraHeld: true`, steps 6–11, travel 0.386 → 0.110 → 0) each have a digest differing from the previous frame and advancing `frameCountAfter` and `ticksBefore`, so the "scene stays alive while the camera is held" branch is exercised; no `failures` key is present, which the spec writes only when `judgeSequence` returns failures, and the spec then asserts `failures == []`, 46 frames on disk, no console errors, every frame on the dusk preset and on the live post chain (all of which the manifest's own records satisfy). Poses check out: the crowd leg opens at azimuth **2.2250 rad = 127.48°** and distance 25.99998 m, which is `plan.ts`'s scored pair (`CROWD_CAMERA`/`CROWD_TARGET`, `CROWD_AZIMUTH`, 26 m asserted within 0.5 m), with clearance **3.64 m**; every frame is 1280×720 and `targetY` is 15.2 within float tolerance. The run's build array is the certified bytes `b50735b7…`, its query is `?agents=1&style=satellite&time=dusk`, and it drew 3,000 pedestrians and 50–66 vehicles.

**What is weaker.** (a) The run's own console is not retained: the only console in `artifacts/flythrough2` is `run2-console.*` (06:00 local, the failing second run), so "exit 0" rests on the plan's sentence, not on an artifact; the manifest alone cannot distinguish a pass from a failed assertion after the manifest was written (the spec writes the ledger before asserting). (b) No frame inspection of the 46 frames exists: crops for all 46 frames are under `artifacts/inspection-flythrough/` but there is no report, matching the plan's "its frame inspection is in flight". (c) The manifest declares itself `certifiable: false` / "iteration only: these frames are not the verdict set and no review covers them". (d) TAA reads 0 of 46 accumulating, by construction in a populated run.

**Verdict: holds with a bound.** It is a pass of the criterion's mechanical half — a camera driven through the real controls (synthesised pointer, wheel and key input, with per-frame input records) whose sequence check accepts movement, distinctness, held-frame liveness, clearance and viewport — but not yet a pass of "comes back clean" as a visual judgement, because that inspection has not been written, and the exit-0 record is not kept.

## Claim 5 — the F1 terrain hole (`artifacts/inspection-gpu/sweep/REPORT-cartographic-overhead.md`, appended adjudication)

**Artifacts checked:** the report's adjudication section, `data/scene/terrain.mesh`, `src/world/aoi.ts`, the `-az180 left-ground` crop.

**Re-derivation.** I decoded `terrain.mesh` myself (92,269 vertices, 183,188 triangles) and ran an unindexed point-in-triangle test:

- **no triangle covers (580, −404)**;
- a 1 m grid over x 560–600, z −420…−388 finds **515 m² uncovered** (the report says ~510 m²); a finer window over 570–600 × −420…−396 finds 415 m²;
- the report's four in-AOI holes all reproduce: (427, 391), (329, 309), (277, 251), (497, 461) are each uncovered;
- a boundary-edge census (edges used once, union-find components, perimeter and rim contact) finds **exactly 14 interior loops with perimeter > 20 m**, of which 4 are inside ±498.65 m — the largest 135.0×120.0 m at (426.8, 393.9), matching the report's "135x120 m" — and 10 are in the margin ring, including **89.5 m perimeter at (579.3, −403.6)**, the F1 hole; the remaining 64 interior components are ≤5 m TIN slivers;
- the AOI's east half-width from `aoi.ts` (`AOI_EXTENT_M.eastWest` 997.3) is 498.65 m, so x=+580 is **81.35 m outside the AOI**;
- `terrain.mesh` mtime is 2026-09-16 17:25, so the prescribed cap is not landed, and the hole is visible at 1:1 in `cartographic/overhead-az180/left-ground` (I opened it: an isolated black blob on pale ground).

**Verdict: holds; the disposition is defensible as a scope argument, not as a closure.** The report's locate/name/explain/verdict is accurate, its minimal fix (`tools/scene/build-terrain.ts` caps interior loops over their own rim vertices, and `npm run data:scene` refuses uncapped loops) is the right class of fix, and its explanation of why satellite hides it (sky dome below-horizon colour against olive ground) matches the measured contrast (cartographic 37.6 vs 82.9; satellite 25.7 vs 30.3). But "upstream data gap 80 m outside the AOI" does not make it invisible: it is drawn in three of the 44 certified frames, and the criterion it sits under is "Cartographic and Satellite each hold up at street, block and aerial distances". To close it the review would require either (i) the cap landed, `data:scene` re-run, the visual gate re-run to a new certificate, and the affected frames re-inspected; or (ii) an explicit accepted-limitation entry that names the hole, its 81 m offset, the three frames that show it, and the AOI scope argument, with the watertightness census added to `npm run data:scene` as the gate that stops the class recurring (with a red control). Leaving it as "recorded, not landed, the review's to make" is honest but not closed.

## Claim 6 — what the evidence does not cover

- **The certified 44 frames are of an agent-free city.** Recorded: `verify-output.ts`'s scene-digest comment, the plan's runbook ("the 44 frames are of the agent-free city and the population never loads without `?agents=`"), and the reports ("no pedestrians or vehicles visible"). This is a coverage gap for anything requiring agents, not an unmet criterion — and the human agent set is now restored, so the certificate's scene digest no longer matches disk.
- **The signature criterion's crossing is empty.** Recorded and graded **blocking** by `REPORT-crossing.md`, and recorded in the plan as an unmet criterion with routing named as the cause (700 m route cap against a >1,000 m round trip; 0 within 60 m of the camera). **Unmet criterion**, not merely a coverage gap.
- **The populated capture is noon while some criteria name dusk.** The capture's query is `?agents=1&seed=9137&style=satellite&time=noon`; `REPORT-crossing.md` says "Dusk rendering: not applicable — this capture is noon" and `REPORT-poses.md` lists it as a note. The plan's populated paragraph does **not** name the time of day; the artifacts do. Coverage gap for the dusk criterion (which the flythrough, at dusk, partly covers).
- **The corridor pose shows no vehicle.** Recorded in the plan and as a *finding* in `REPORT-poses.md` (no vehicle in any of 8 frames, 16 active, `vehiclesCrossed` flat). Coverage gap for the vehicles criterion; combined with the unmeasurable signals clause it leaves that criterion unmet.
- **The boundary pose cannot see its portal.** Recorded in the plan and as a *finding* (portal behind rooftops; constant lifecycle counts; 2 px of change). Coverage gap.
- **TAA.** The flythrough's `taa` reads **0 of 46** accumulating, recorded in the plan as "by construction in a populated run". The brief's second half — "0 in the hero frames" — is **not true of this certificate**: `artifacts/visual/hero/hero.json` records `taaAccumulating: true` with `taaSamples` 25 and 32 for all eight hero captures of run `94fc1aa78b0af8f9`. The 0-on-hero reading belongs to the 2026-09-15 capture (`artifacts/gate-timing/REPORT.md`, plan's visual-acceptance-state entry) and was superseded by the `controls-rest.ts` fix. The real gap is different and smaller: the hero stills record accumulation with the manifest's own caveat "before screenshot; not a convergence assertion", and a still cannot show flicker or crawl. The "holds still over a moving sequence" clause therefore rests on the flythrough's frame inspection, which is not written. **Unmet/owed.**
- **Style switch with a population running.** The plan records that "preserves simulation progress" is a code claim, not measured. Coverage gap.
- **Guardrails** remain unmet (`guardrailPosts: 0`), and the signals clause of the vehicles criterion is unmeasurable in this fixture. Recorded; unmet.
- **Performance as written** (3,000 pedestrians **and** 200 vehicles): 59 (acceptance) / 51 (sweep) vehicles drawn; recorded as the owner's vehicle-half infeasibility. **Unmet criterion.**

---

## Blocking findings

1. **The signature criterion is unmet.** The populated crossing capture is 14/14 byte-identical frames with zero pedestrians in frame for the whole pedestrian green; the inspection itself grades it blocking and the plan records it as not met, with routing as the cause. The deliverable cannot be accepted with its own signature shot empty. (The evidence is honest about this; the criterion is what fails.)
2. **The performance criterion is unmet as written.** The acceptance arm measures 3,000 pedestrians with **59 of 200** vehicles drawn (51 in the sweep arms), and its p50 16.7 ms is a vsync-clamped reading — `holdsSixtyFps` is `false` for every arm in `sweep.json`. The number also describes a build that has never existed on `main` (certified bytes plus a probe seam, `11c5d868…`, not retained), which the report and defect register disclose, and the permanent fix is owed.

Nothing else in the evidence chain is blocking: every binding I could re-derive does re-derive.

## Non-blocking findings

1. **F1**: a ~515 m² interior terrain hole at (580, −404), 81.35 m outside the AOI, visible as a black blob in three certified cartographic overhead frames; fix prescribed, not landed, disposition deferred. Needs the fix-and-recertify path or a named accepted limitation plus the `data:scene` watertightness gate. (Independently reproduced, including the 14-hole census.)
2. **Certificate scene identity is no longer reproducible**: the scene digest matches the served tree minus the 33 human-agent files; the human set has since been restored (`data/scene` is now 117 files / 301,674,250 bytes). Frames are unaffected (agent-free lane, no agents in frame), but a future certificate cannot be compared on scene identity.
3. **Flythrough pass is bounded**: no retained console/exit record for the passing run (only the failing second run's console), no frame-inspection report for the 46 frames (crops only), and the manifest is `certifiable: false` with no `failures` key — the absence of a key is the pass record.
4. **Populated inspection coverage**: 27 of 38 frames opened at native size (11 crossing frames hash-only, justified by byte-identity); crop digests are asserted through the tool-written manifests rather than recomputed in the reports.
5. **Report text vs bindings**: `REPORT-satellite-plaza.md` prints only 8-hex crop-digest prefixes and `REPORT-cartographic-plaza.md` prints none, so their crop binding cannot be checked from the report alone (it checks out from the manifests); `REPORT-satellite-overhead.md`'s summary says "Notes: 4" while five per-frame notes exist.
6. **Populated `REPORT-poses.md` corridor height error**: it describes the corridor camera as "about 28 m above the ground"; the manifest's own `poseSolution` gives `clearanceM` 8.48 m (camera y 43.45 over ground ~34.97). The other three poses' heights match. The "still life" reading is made from a viewpoint the report mis-states by ~20 m.
7. **Plan-level record accuracy**: the plan says the frame-budget load is "tabulated per arm" where the report tabulates four sample points around the arms; the plan's populated-set paragraph does not record that the capture is noon.
8. **Housekeeping**: `artifacts/frame-budget/base-wt/` is a partial checkout copy (no `.git`, no `index.html`, no `dist`) with a `node_modules` junction; it is not a registered git worktree (`git worktree list` shows only the primary), but it is a leftover tree whose evidence status is unstated.
9. **Timing of the register entry**: the frame-budget instrument-defect register entry and the plan's session record are in `144766b`, one docs-only commit after the reviewed `39ca459`. Evidence bindings are unaffected.

## Unmet criteria (explicit)

- **Line 267 road markings** — met except guardrails (`guardrailPosts: 0`); still open.
- **Line 268 dusk preset with ACES, bloom, SSAO and TAA that holds still over a moving sequence** — the post chain is live and the hero stills record TAA accumulating, but no inspection of the 46 dusk flythrough frames exists to judge flicker or crawl, and the flythrough's TAA is 0/46. **Unmet/owed.**
- **Line 269 vehicles** — signals clause unmeasurable in this fixture (zero governed entries); the `corridor` pose shows no vehicle at all; the `boundary` pose cannot see its portal and recorded no spawn/despawn. **Unmet.**
- **Line 270 pedestrians and the signature shot** — the crossing is empty; the walking half is evidenced at the `walkers` pose only. **Unmet.**
- **Line 271 one clock, no frame showing traffic through the scramble while pedestrians are on it** — the clock half holds and is gated; the frame half is unmeasurable (no governed vehicle entries). **Unmet.**
- **Line 272 60 fps at 1920×1080 with 3,000 pedestrians and 200 vehicles** — median at the vsync quantum for 3,000 pedestrians and 59 vehicles on a non-certifiable build with a seam; 200 vehicles never drawn; p95 33.4 ms. **Unmet as written.**
- **Line 275 sweep + flythrough + independent review + merge** — the sweep is inspected and bound; the flythrough check passed but its frame inspection is owed; this review is one of the two the plan requires. **Open.**
- **Line 276 two world styles** — the frame half is met (36 frames, both directions, digests bound); "preserves simulation progress" under a switch with a running population is unmeasured. **Partly open.**

## Bounds of this review

I re-derived digests, tree digests, builds, the terrain census and the manifest arithmetic from the artifacts themselves. I did not re-run the three-hour `npm run visual` gate, the frame-budget lane, the flythrough or the populated capture, so I confirm what the artifacts bind and whether their claims are internally consistent and reproducible, not that a fresh run would reproduce them byte-for-byte. I read six crops at native size across three reports rather than all 464; the remaining crop bindings were verified by digest, not by eye. Where a report's prose and a manifest disagree I have said so (finding 6); where the brief's premise disagrees with the artifact I have said that too (TAA in the hero frames).
