# Review 64: implementation

## Target

Independent review of the F34 finite-shoe contact repair against maps base `d7c8eea43c1cbfc6e85bcd01306fd43898be6b66`. Author: `shoe_contact_repair`, workspace `artifacts/shoe-contact-observer-v2/wt`. Reviewer: `shoe_observer_review`, new detached workspace `artifacts/shoe-observer-v2-review/wt`. Review 60, the rejected v1 candidate and their evidence remain unchanged. Root owns admission, integration and canonical status.

The exact authored handoff is [64_shoe-observer-v2.md](../snapshots/64_shoe-observer-v2.md), SHA-256 `a870e72e91d5fdccec05487e451ad591f69b72ee9773d45b43ea6f7aeca01f0e`. The accepted design is [64_shoe-observer-v2-design.md](../snapshots/64_shoe-observer-v2-design.md), SHA-256 `32818fc451b9ccf9b48648b7cf9a1dfa2001eb3a659187ec88b86818c24b0339`. The author's 57-member handoff freeze is `9d2a97ba10117ddf0a2e2763975fce6ff158845f42398bfaee9e84e7fb60034a`; all members and its 49 final inputs were independently checked before and after execution.

The proposed source `tools/contact/finite-shoe-contact.ts` is SHA-256 `f779d813817614e1e70a0480f719e752a0357754d17a0a0bf7335819c0004599`; `test/finite-shoe-contact.test.ts` is `24e4b5bdf28f08d68d0a6f18873f1fd9c196f11bda03f3d7e1e8a60b6b14bccd`. The recoverable full candidate patch is `859081f5034911e6c0a86ccc7a0cbf72806622ebf7bbfbb5a11954e6af4defa8`; the delta against the rejected source is `6f46a67ed577beaa3d7586542ea4ad6f439d8cdaae8d5368d1fdf0d4acdfea68`. Both patches are retained in the author's freeze and this review's ignored evidence. No code commit is asserted by this target.

## Reviewers and coverage

Codex `shoe_observer_review` authored none of the repair and retains the independent role from Review 60. I inspected the instructions, lessons, accepted design, exact delta, final source/test, boundary runner, original counterexamples and source-control bindings. The existing actual-source runner answers the source/control question and was rerun unchanged; private API checks answer the uncertainty-boundary question. Source correspondence was not reinterpreted or promoted into a city claim.

Coverage includes the geometric inner/outer argument, both normal-band signs, strict thresholds, support-edge uncertainty, empty inner regions, point/segment outer hulls, original F34 inputs, neighboring positive/negative decisions and preserved source denominators. No compiler toolchain for native code, native fixture, browser, GUI, GPU, server, dependency install, data writer, full-product gate, commit or push was used. Focused TypeScript and Vitest run on Node v24.12.0. The scope is a finite supplied-pose diagnostic with exact selected support triangles and exact supplied load query; `packingErrorM` bounds shoe vertices only.

## Reports

### Codex shoe_observer_review

**Accept the bounded F34 repair at the exact source and test hashes above, subject to root's integration checks. No new material finding is identified.** Both rejected false-pass cases now remain uncertain while their actual displaced/rounded negative controls fail. The repair preserves useful positive and negative decisions rather than rejecting every empty inner region. This recommendation does not accept a real-city stance, source policy, motion controller or broader collision proof.

The implementation matches the accepted argument. A point represented by barycentric weights on a supplied shoe triangle moves by at most the maximum vertex error when those same weights are applied to perturbed vertices. Orthogonal projection onto a fixed support plane and X/Z projection do not increase that error. Insetting the support-edge and both signed normal-band inequalities therefore identifies nominal representatives whose corresponding actual points remain contacts. The projected representatives can still move, so the passing hull separately requires the exact load query to lie more than epsilon inside every edge. This second margin pays for point displacement; it does not stand in for the band inset that v1 omitted.

The outer inequalities correctly provide a separate impossibility test. Every actual contact has a barycentric nominal preimage within the expanded region, and its projected actual point lies within epsilon of that nominal representative. The complete possible contact hull is therefore contained in the outer hull expanded by epsilon. A load farther away cannot be supported. A load between the guaranteed and possible decisions remains uncertain. Point/segment outer hulls are retained and tested by distance; their zero nominal area is not misreported as absent possible contact. Required banks use resolved inner presence versus possible outer presence.

This argument is a statement about exact geometry under the declared per-vertex error bound. The implementation retains its existing double-precision arithmetic guard and perimeter-based area guard; this review does not turn those conventions into a directed-rounding certificate. Clearance and uncovered nominal pieces retain their prior algorithms. In particular, acceptance of this contact/hull repair is not a new universal proof for uncertain collision geometry. The exact-load contract is explicit in the API and report, so no unaccounted load-point error is silently spent.

The original assumed-error case now has an inner maximum X of `0.24999990525952295`, nominal maximum `0.3333333333314059` and possible maximum `0.4166667614032888`. Load X 0.30 is uncertain; the actual upward displacement still fails. X 0.20 still passes. X 0.40 is uncertain because admissible geometry can support it; treating that point as a definite failure would repeat the opposite error. X 0.45 remains a definite negative.

The measured Float32 example preserves the original error `3.900141719997974e-10` m. The nominal load X 0.0375 is uncertain; supplying the actual rounded vertices with zero remaining packing error still fails. The 0.02 interior passes and 0.06 exterior fails. Nominal patch vertices and `nominalHull` remain inspectable, while the passing `hull` uses resolved inner geometry. Nominal `patches[].verdict` means that a corresponding inner region exists; its nominal boundary is not promoted into a guaranteed hull.

Independent checks reused all 14 frozen Review 60 analytic inputs and both measured-rounding inputs without rebuilding their coordinates. Thirty additional observations applied the almost-parallel case to support planes tilted 0, 30 and 60 degrees, for both normal signs, at four load positions and the adverse displacement. All contact/hull expectations held; negative-normal poses still separately failed clearance. A support-edge case with no nominal overlap stayed uncertain, while an admissible displacement created an actual positive-area patch and passed. A clearly exterior query failed. Outer clipping that leaves only one point or one segment stayed uncertain at the possible support and failed outside it. Empty-inner neighbors retained uncertain, positive and negative outcomes. These 56 asserted observations supplement the author's 369 finite observations; they are finite corroboration, not exhaustive numerical verification.

The unchanged actual-source runner reproduced all 49 expected outcomes. Its complete results equal the author's repaired final results, and its check rows and source-reference evidence equal the preserved v1 run. The complete decompressed observation payload also matches. Both shoes and both scales retain the 807-vertex, 1,552-whole-triangle denominators, fixed 492/497 sole triangles and 368/372 samples. Source correspondence, wrong-bind and calf controls keep their original bounds. The proposed pure observer still depends on an external caller that validates source bytes and actual membership; the inspected artifact adapter remains an experimental caller, not new source authority.

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
|---|---|---|---|
| F34 | Nominal contact-band boundary could certify support removed by allowed packing error. | Reviewer recommends resolved for this exact v2 candidate. The inner/outer construction propagates error through band membership, support edges and the hull; both original cases are now uncertain, and independent positive/negative neighbors remain useful. Root owns final disposition and admission. | Integrate the exact source/test after repository delivery checks; preserve Review 60 and the old-source failing controls. No new F-ID is requested. |

## Verification

`node artifacts/review64/prepare.mjs` verified 57 author members and 49 final inputs before copying the exact source, tests and runners. `node artifacts/contact/check.mjs review64` passed 49/49 expected actual-source outcomes. `node artifacts/contact/boundaries.mjs` reproduced the author's complete 369-observation record exactly. `node artifacts/review64/independent.mjs` passed 56 independently asserted API observations.

Focused strict TypeScript over the two candidate files exited zero; Vitest passed 15/15. The exact old source `ad97626d…` and the preserved original regression file `341e5e7e…` were copied to a separate private scratch root and rerun: Vitest exited 1 with four failures and nine passes. The failures were both original false passes, the lower-normal-band hull and the possible support-edge case. All eight original unit cases passed against the old source. This observed negative control establishes that the regression checks detect the former behavior; a copied log alone is not the proof.

`node artifacts/review64/verify.mjs` rechecked all 57 target members, 49 final inputs, 62 preserved predecessor/reviewer members and 38 actual-loader inputs. Repaired results equal the author result, and expected source check rows remain identical to v1. Decompressed observations contain 51,212,832 bytes with SHA-256 `fb39fae2d6c80b935d14eda35e84bc97b50f991edf32e9bee914b5003c51e969`. Private scripts, exact input/output records, source rerun, original rejection proof and before/after hashes remain under ignored `artifacts/review64/` and `artifacts/contact/`, bound by the final review freeze.

The default worktree creation hit the Git-metadata sandbox boundary; its scoped approved retry succeeded. Focused Vitest and the read-only process census used scoped approved execution because this same sandbox had already blocked their configuration/CIM access during Review 60. The final census found zero processes naming this review's workspace. All commands were awaited. No browser, GUI, server or junction was created. Task-owned Vitest caches were removed after resolving their absolute paths inside this new worktree and rejecting reparse points; retained evidence was preserved. The inherited top-level `docs/work/0_shibuya-1km/handoff.md` shape violation still blocks the fleet document checker, so no repository-wide document pass is claimed. The new report has the required six sections and its two snapshot links resolve.

## Round outcome

Recommend accepting the exact F34 v2 repair for the declared finite contact/hull diagnostic and marking F34 resolved when root admits and integrates those bytes. The recommendation is bounded to fixed selected upward height-field supports, the exact supplied load query, complete caller-bound shoe geometry, per-vertex uncertainty and the existing finite arithmetic conventions. Physical thresholds, source authority, real plant lifecycle, whole-city contact, force/anatomical plausibility, continuous collision and native motion acceptance remain unchanged.

The three retained review documents are ready for root integration; no code or document was committed by this review. The detached worktree remains at `artifacts/shoe-observer-v2-review/wt` for evidence handoff and preservation. Root owns full delivery gates and removal after the review target and old counterevidence are safely retained. Nothing is intentionally left running.
