# Review 58: implementation

## Target

Read-only review of the first Windows ownership-primitive experiment and its proposed measurement correction, based on maps `927250018e86db4c89c9063a8f5f166ee15eb391`. This is not a launcher or capture approval. The native author's worktree remained unchanged at `artifacts/launcher-native-primitive/wt`. The complete reviewed source is `artifacts/native-proof/run-01/frozen/native-proof.cpp`, 371 lines, SHA-256 `aae779c24cf59b0ecc2f696ffd87a27929814d4bfdb00fc2d1c750c8ba09c784`; executable `3a6ee0d2485fd4448f9d57419a15bc78cb57971d2fc8809a67615e0325a7f1b1` was hashed, never executed by this review.

The exact authored targets are preserved as [run-01 report](../snapshots/58_native-run-01-report.md), SHA-256 `4621c266698c7eab0b075091a07f671ccce79ec467045fc7793d29179e7a013f`, and [diagnosis addendum](../snapshots/58_native-run-01-diagnosis-addendum.md), `ea0e32a6cdc7e070f8f319c085ee6b443abbf2695cea8c1fe427bfe9237eb21d`. The addendum supersedes the report's proposed owner-handle release. The 24-member `HANDOFF-FREEZE-02.json` is `eedc4d7fbb013f37254176d515f255977ac6f9bf902b534feba4276a75f185ae`; raw `run-01/events.tsv` is `240300ab309dd1df3596877c39782843590ca77432924781a60a2447b9ea34c8`; the 14-member pre-execution `INPUT-FREEZE.json` is `b06b13831859e85b4e4ea1a507da3366f20907a9101c9da9ff3e505d72a4fdeb`.

Review56's frozen text `928593d95901044189b4e0740f4afbb78ec9e3b81d2e2c4c0888b8a3972ea5d2` and the earlier fixed launcher acceptance `acf9a4e761b819bf97c41243931ff5c19e6a2751a27031d0b58d87f7bf1cf9cb` remain the requirements. A reviewed copy of every handoff member, source-model controls, CLI abstention and byte-check results are retained under `artifacts/native-instrument-review/frozen/`. The original failed source, executable, trace, cases and timers remain recoverable there and in the author's run-01 directory.

## Reviewers and coverage

Independent reviewer: `native_instrument_review`, in a separate detached worktree at the stated base. Coverage: complete frozen C++ source, exact authored report and correction, raw scores and event ordering, ownership and exception paths, Review56 schedules/disqualifiers, current Microsoft API contracts, byte comparisons and seven bounded scoring models. Root owns dispositions, any next native execution, canonical status and integration.

The secondary Claude CLI was freshly checked at version 2.1.263. A finite, hidden, stdin-fed read-only request using configured defaults exited 1 after 5.14 seconds because its OAuth session had expired and refresh failed. It produced no substantive review and has no vote. No authentication repair, installation or model fallback occurred; a model change cannot repair authentication. Its process exited, and final process checks are recorded with the handoff.

No native fixture, compiler, browser, CDP, server, GPU, full suite, dependency install, product edit, commit or remote operation ran. CPU source models cannot establish actual Windows accounting timing, kernel interruption, pipe framing or Chromium behavior.

## Reports

### Independent instrument and ownership judgment

**Recommend the addendum's retained-handle repeated-accounting change for one separately authorized diagnostic run, with the exact conditions below. Reject interpreting that run as complete primitive acceptance while its survivor controls remain unverified. Run 01 stays failed.** Its different observation paths do not justify releasing handles, replacing kernel membership, or abandoning the strategy.

The report's central measured facts are supported. The frozen executable returned 1. The raw roster has 28 unique cases: 19 FAIL, five PASS, three EXPECTED_RED and one UNAVAILABLE. Each FAIL's recorded outer count is 1; each PASS's count is 0. All 28 later rescue records have root wait, job zero, separate EOF, closed handles and a live outside sentinel. All case records show the runner's kernel handle count 63 to 63. The final receipt shows zero instrument-owned handles, and the retained final census found no exact `native-proof` basename. These are scoped cleanup observations after scoring, not successful candidate outcomes or an independent measurement made by this reviewer.

**F31 — normal-arm observation is prematurely decisive and insufficiently recorded.** Source lines 327–333 wait the original owner, read its exit code, query outer accounting once and require code 0, shared verdict 1 and count 0. Lines 319–325 terminate and wait the original owner, then query repeatedly for up to 500 ms, retaining the same process/thread handles. The five reached crash cases obtain zero with those handles held. Thus termination mode and observation schedule change together. The data cannot attribute the 19 failures to held handles, prove that releasing them is necessary, or prove that additional observations would pass. The absence of an ERROR record confirms that normal-arm wait and exit-code query reached the accounting call; their raw values and the shared verdict were not persisted. Adding them is necessary to interpret a second failure.

Microsoft describes ActiveProcesses as current job associations, then discusses temporary increments on admission-limit failure followed by decrement after exit/reference release. The paragraph does not settle ordinary-exit accounting timing here. A successful job-state query and zero remain required; API failure is unknown, never zero. These contracts justify treating one nonzero read as incomplete observation under an acceptance rule that allows pending drain. They do not establish the cause of this run's count. [Accounting fields](https://learn.microsoft.com/en-us/windows/win32/api/winnt/ns-winnt-jobobject_basic_accounting_information), [query contract](https://learn.microsoft.com/en-us/windows/win32/api/jobapi2/nf-jobapi2-queryinformationjobobject).

**F32 — three raw expected-red labels do not independently establish a surviving fixture.** Lines 322–324 label a positive outer accounting value EXPECTED_RED after owner death. The post-assignment, inherited-job-handle and breakaway arms record counts 1, 2 and 2 respectively. Breakaway creation succeeded before the kill; that establishes creation, not post-kill liveness. A live survivor and delayed association accounting can feed the same positive number to this expression. The positive-arm discrepancy makes that distinction material. The report and addendum correctly refrain from accepting these labels. They must remain raw labels in a diagnostic successor until a separate observer demonstrates their intended failure before outer rescue.

The held-pipe control is stronger evidence within its scope. The raw trace records inner job zero, pipe drainage pending for 100 ms, a failed/nonfinal verdict, then EOF only after the separate writer is closed. Source lines 271 and 274 explicitly assert the zero-versus-EOF distinction and preserve the failed verdict across rescue. Its outer FAIL remains unchanged. This demonstrates why process zero alone is insufficient; it is not CDP or overlapped-I/O coverage.

**F33 — the final exit predicate drops unavailable coverage.** Source line 343 increments `unavailable`, and line 352 prints it, but line 353 returns 0 when failures are zero and cleanup succeeds, regardless of `unavailable`. A source-extracted expression returns 0 for `{failures:0, unavailable:1, sentinelClean:true, openCount:0}`. This did not conceal run 01: its exit was 1 and its report explicitly declared incomplete coverage. It could produce an ambiguous green command after normal-arm scoring is repaired. The successor needs an explicit partial outcome and a nonzero partial exit, distinct from a failed case and from full success.

The creation-window record must remain UNAVAILABLE. Its point-11 signal precedes `CreateProcessW`; killing after that signal cannot prove interruption inside the call. The observed five owner-loss barriers remain useful measurements. Unavailable in-kernel timing does not require drivers or manufactured interruption evidence and does not by itself establish that an isolated pipe experiment is unsafe or impossible. Microsoft documents job-list assignment during creation as the remedy for the separate create-then-assign crash gap. Any later admission relying on that contract must name it as an OS premise rather than measured timing coverage. [Creation-time job assignment](https://devblogs.microsoft.com/oldnewthing/20230209-00/?p=107812).

Source inspection supports the bounded ownership design. The ordinary path creates an unnamed, non-inherited, non-breakaway kill-on-close inner job and supplies it in the creation attributes. Attribute values survive attribute-list destruction; returned process/thread handles enter the ledger before later checks. Only the labeled post-assignment negative uses `AssignProcessToJobObject`; there is no PID-to-handle open or numeric-PID kill path. The observer holds the separate outer rescue job, not the tested inner job. Windows requires inheritable entries for the explicit handle list and requires their backing values through attribute-list deletion; the inspected paths follow these contracts. [Attribute contract](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-updateprocthreadattribute).

`Scope::~Scope` joins rescue until `closed`; `drain` stops acquisition, destroys attributes, requests termination, waits the original root, repeatedly observes accounting, drains the pipe and closes owned handles. Ordinary failure returns preserve the error vector; expected-fault arms assert failure before rescue. No concrete executed Scope exception/RAII cleanup violation was found in this frozen run, and indefinite visible pending rescue is permitted by acceptance. This is not a claim about arbitrary allocation failures or every harness exception, nor does an in-process synchronous scope establish later Node/native binding or cancellation safety. Termination requests alone remain insufficient; the original-handle wait matters because Windows process termination can be asynchronous. [Termination contract](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-terminateprocess).

### Exact smallest next experiment

Admit the following instrument correction for root consideration, not execution by this review:

1. Preserve run 01 unchanged. Freeze a new source/executable/manifest in a new run directory. Keep all 28 case names and schedules, expected failures, inner drains, 15-second normal owner wait, five-second crash owner wait, and existing rescue semantics. No timeout becomes cleanup success.
2. Retain original owner process/thread handles through scoring. Record raw owner wait result, exit-code API success/value, shared verdict, and each outer accounting API success/error/count with monotonic elapsed time. Record both arms' observations directly in the runner output; runner `shared` is null, so merely calling the existing `log()` helper would silently record nothing.
3. Replace only the normal arm's single accounting observation with the existing crash arm's 500 ms observation budget and 5 ms cadence. Query immediately; accept only an actually successful zero query before rescue. An API error ends this score as failed/unknown; elapsed nonzero remains failed/pending. Owner exit code 0 and shared verdict 1 remain required. Do not call outer termination or release owner handles to make this measurement converge. Log the existing crash loop too without changing its limits.
4. Keep the three survivor controls' original raw labels, but mark their independent validation `not-established` in a machine-readable summary. Retain the held-pipe control and all cleanup/sentinel assertions. Record 28 executed cases, the exact unavailable case and unvalidated negatives. Return failure when a required arm/cleanup fails, a distinct nonzero partial status when coverage or negative validation is unresolved, and success only when the declared scope's checks are complete. No whole-primitive certificate is emitted by this diagnostic-only run.
5. Re-run the same 28-case roster once after root separately admits the exact diff. This experiment can determine whether retained-handle repeated observation reaches zero on these schedules. A pass supports that observation contract only; it proves neither the cause of run 01 nor the necessity of handle release. If normal counts remain nonzero, hand back the raw successful wait/exit and accounting sequence before proposing another mechanism or timer change.

A process-ID list is optional read-only diagnosis and adds no stop authority or liveness proof. It is unnecessary for the minimal correction. A handle-release comparison, changed timeouts or a new observer is a separate experiment, not an unrecorded addition to this one.

The smallest separate survivor-observer proposal should demonstrate, after the original owner is signaled and before rescue, that a fixture whose original creation identity is known remains unsignaled. A retained duplicate of an originally returned fixture process handle can provide that observation without opening a PID or retaining the inner job; its transfer, acquisition ordering, failure cleanup and closure must receive source review before execution. The post-assignment root is suspended, so requiring a running acknowledgement would change that schedule. For running leak/breakaway fixtures, a fresh post-death challenge and acknowledgement is another possible discriminator, but a pre-death readiness flag is insufficient. Apply a matched observer to the relevant candidate and negative arms, prove the broken controls remain red before rescue, and separately join its handles/pipe operations. No such observer has been implemented or admitted here.

### Stage decisions

Truthful machine status, a later isolated pipe experiment, and whole-launcher acceptance are different decisions. Fix the first now through the reviewed correction. This run does not yet supply the positive observation and discriminating negatives needed to recommend the next pipe stage. Once those are independently reviewed, root may consider a narrowly owned pipe experiment under outer rescue, explicitly stating both measured bounds and OS API premises while retaining the unavailable kernel cut. Whole-launcher/native capture remains blocked on F28/F30 and its original acceptance cases, joins, source authority and separate capture admission. No distinct competing ownership mechanism is required by this evidence; no proof here establishes that kernel membership cannot meet the requirements.

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
| --- | --- | --- | --- |
| F31 | One immediate normal-arm query and missing raw wait/exit observations cannot distinguish incomplete accounting from a primitive failure. | Reviewer recommends the narrow retained-handle correction; no causal handle-release claim accepted. Root owns final disposition. | Exact diagnostic conditions above; preserve failed run 01 and independently inspect the new diff before native execution. |
| F32 | Positive accounting alone does not validate three intended live-survivor negative controls. | Unresolved; report/addendum already disclose it honestly. | Keep labels raw; separately review original-handle or fresh-acknowledgement observation before treating the controls as validated. |
| F33 | Final exit expression ignores unavailable coverage. | Confirmed static false-success possibility; not an observed false report in run 01. | Explicit partial summary and nonzero partial exit; preserve the kernel-window UNAVAILABLE. |
| F28 / F30 | Whole-launcher descendant ownership, unsafe SDK paths and joined cleanup remain unaccepted. | Unresolved; this primitive and review cannot retire them. | Preserve the original launcher checks and later native/CDP admission sequence. |

## Verification

On Node `v24.12.0`, `node artifacts/review58/check-inputs.mjs` exited 0 and verified all 24 handoff members against the supplied hashes and original author bytes, including the pre-execution 14-member manifest and exact source/executable. It independently parsed the 28 scores, all rescue and runner handle-balance rows, and the held-pipe event order. `node artifacts/review58/static-controls.mjs` exited 0: seven independently generated observation histories preserve the exit/shared-verdict/zero predicates; the delayed-zero history fails the original first-read model and passes bounded observation; query-error-to-zero mutation is rejected. Extracted final-exit and negative-score expressions demonstrate F33 and F32's ambiguity. These are seven source-model controls, not extra native cases or evidence that Windows will produce those histories.

All frozen native inputs matched again after the secondary CLI attempt. Its only result was authentication failure; no vote is counted. Exact report/addendum snapshot digests match their targets. Review docs use the required six headings. No application gates were run because no product code changed and the assignment excluded them. No native rerun or second scored result exists in this review. The first sandbox process census met access denial and its apparent zero count is explicitly invalidated in the retained record. The follow-up read-only query succeeded with approved elevation and found zero matching task-owned processes. Final handoff records retain both observations, source checks, model results and the CLI failure; no browser/server resources were created. The isolated review worktree is released for root integration, with no commit or merge claimed by the reviewer.

## Round outcome

Review complete with F31–F33 open and a concrete minimal diagnostic correction recommended for root admission. The original 28-arm run remains failed; five owner-loss observations and the held-pipe distinction retain their stated bounds. The native primitive, pipe stage, launcher and capture are not accepted by this round. The pending kernel-window measurement is reported honestly and is not a demand for impossible instrumentation or a proof against all further progress.
