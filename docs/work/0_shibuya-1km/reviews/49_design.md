# Review 49: design

## Target

Independent review of the bounded source-lineage experiment and its proposed next contract. The review checkout is `artifacts/source-lineage-review/wt`, detached at delivered main `05ad1a4830e5876be03116e468199370504a6693`. The author checkout is `artifacts/source-lineage/wt`, pinned to generator revision `1ca4552ceee25e34ac62335114bbb5377a4d6470`. The exact author report is retained as [49_source-lineage-author.md](../snapshots/49_source-lineage-author.md), SHA-256 `9f772df7733a32c28352928850da08ef1771d32c49dcaada0c9d2c82e1651b1c`. Author freeze `b6fd638a9b13e1651ee2464409c70518cf42da047879dd7f2fb61d9b0ffa3c8e` binds 31 files; every file and the freeze were copied unchanged into this review's ignored `artifacts/review49/target/` before inspection.

The accepted network is `314fac843392de12c8264cbf6b1647935d2b7e9d7194a3c29835e46445537677`. The experiment uses original terrain `fef57d0960c3d08c5d31f0f56f64cff3b046b6d577b36473b7c046295249b381`, original roads `3fe126cd675949daf4b337b890c99478c730a4f956c7f4241e7fe883cf967490`, historical response `e5e3f1acc6b951914ab28536fa1b9210ced80ad55d650356989ea9ba4dd7d141`, and current-source control `976d848ee620b7d4c4780f3fe7d15e58f2ccbf0a653a195ae2f07d0d08bf6d23`. Original raw source `9923a9ae93afbc73f82332bb5a03f06f76ed587cbc143541158d4c192b4b8671` has not been recovered.

## Reviewers and coverage

Codex `lease_retirement` independently inspected the author scripts, complete saved networks, request/response bindings, old generator/import closure, dependencies, source filters, current consumer need and raw-hash paint guard. This reviewer authored none of the source-lineage experiment. The review used one private historical generation and one private current-source control, plus comparisons without generation. It made no network request, installed no dependency, imported no writer, wrote no shared data and launched no browser, server or GPU process. No second CLI review was run in this bounded round; no earlier CLI result is transferred as approval.

The report evaluates reproducibility and the next source contract. It does not approve source adoption, a new network, current-surface support, paint regeneration, walking production use or a scene rebuild. Root retains those decisions.

## Reports

### Codex lease_retirement

The experiment's central claim is independently reproduced. All ten non-provenance top-level fields match the accepted network in full, including array order: `version`, `admissionBounds`, `boundary`, `nodes`, `lanes`, `walks`, `junctions`, `physical`, `portals` and `diagnostics`. No sample, prefix or count-only comparison substitutes for that result. The historical output matches the author's complete generated bytes, SHA-256 `30a0c16a84a4299dca480e027dd88196e81cb7a7cbb706a7e10b49671f237554`; ordered payload SHA-256 is `c5f3c338b4049cc2b7796af73ee952292578fca9235de3879c0510e0ade15288`.

Exactly two provenance fields differ from the accepted network. `osmSha256` truthfully names response `e5e3f1ac…` instead of unrecovered `9923a9ae…`; `osmTimestamp` records the actual response watermark `2026-09-20T01:54:02Z` instead of the accepted file's `2026-09-07T03:23:56Z`. Terrain hash, road hash and the complete method string are identical. The query's explicit historical cutoff is separately `2026-09-07T03:23:56Z`. The retained request body matches the pinned ordinary query exactly after removing that one date clause. Its body SHA-256 matches both the request sidecar and frozen bytes. A response watermark is not evidence that the historical cutoff was ignored, and replacing that watermark with the cutoff would destroy the observed distinction. This review preserves both values and makes no claim to recover the original raw response.

The current-source control independently produces exact author output `8bca0ac45a92ad89f4a9e7f97557cc6631ba270ea9320a6e2de0f37c23e6d7d5`. It also passes both validators and has 5,094 nodes, 4,855 lanes, 1,740 walks and 213 junctions. Its whole payload differs. A separate Python traversal reproduces 161,332 ordered structural comparison locations: 13,841 nodes, 147,481 lanes, two walks and eight diagnostics. These are comparison locations, not changed geographic-feature counts. The two walk locations are `/walks/814/from` and `/walks/815/to`, changing `walk:n8415255153` to `walk:n8402189748`. Equal headline counts and green validators therefore do not establish equivalence. Six deliberately changed payload controls also fail equality: a late lane point, last walk length, reversed node order, late diagnostic field, removed physical field and changed version. Those controls exercise the comparison boundary; they are not graph-validity tests.

The reviewer extracted the pinned source from Git into a private directory and verified all 15 inspected files against the frozen author bytes after CRLF normalization. All 15 Review 5 counterparts, all 12 available Review 9 counterparts and 14 app-motion counterparts agree; the app-motion writer's added explanatory comment is outside the runtime closure. An independent TypeScript AST traversal accounts for 14 source/type modules and one external import, `three`, with no dynamic module loader and no imported `tools/network/build.ts`. Type-only `.js` specifiers are resolved to their bound `.ts` source. The Three 0.180.0 package metadata, module and core bytes match the local cached tarball, whose SHA-512 matches the pinned lockfile. Old surfaces also match independently retained app-motion copies. No generator, filter, radius, population or physical topology was changed.

The current scene is a separate unresolved boundary. Terrain `c39d49edd405e266373e0b48e0202967a01f546e7d11d2dffd5497c14d692773` and roads `0d429c3145be6c7b96fdff6001e49fbdad07db0d0bb4cf966785668bf8fedabc` differ from the old surfaces used above. Current pavement `3b24c5ef71f864fc6a90c768d8850b2f72ba1eb41255cf40dc37fd940115fb62` is also bound in the review input record, but this replay does not test its support intervals. All current scene inputs, authored markings pair and shared source/network bytes remained unchanged. Exact network payload equality under old meshes establishes no safe contact interval on the current meshes and grants no walking or vehicle behavior acceptance.

### Minimal next contract and adoption proposal

The next increment should be a small producer-owned historical-facts derivation, reviewed before production wiring. Keep accepted network `314fac84…`, original provenance and all existing evidence intact. Retain a compact lineage record that separately names the historical query cutoff, response watermark, exact raw response digest, query digest, pinned producer revision/import/dependency bindings, old surface digests, accepted network digest, truthful replay network digest and complete payload-equivalence result. This records a newly reviewed historical replay; it must not label `e5e3f1ac…` as recovered `9923a9ae…` or infer that every source tag is identical merely because this generator produced an equal payload.

The immediate consumer is the walking candidate's occurrence/interval authority boundary. Derive a small offline fact table keyed by existing walk-edge ID and `sourceWayId`, using the same first-occurrence OSM indexing and producer filters as the pinned generator. Include only the fields needed to explain admitted source class and level: the relevant highway/foot/access/area and bridge/tunnel/indoor/layer/level facts, the producer's filter outcome, and the replay/producer binding. The runtime receives those facts with the scene/network inputs; it does not fetch raw OSM or infer historical eligibility from the available current tags. Route occurrences may refer to an edge's record without changing route choice or population randomness.

The finite source inventory makes this concrete: 1,738 accepted walk edges map to 561 historical source-way IDs, with no `surfaceExclusion` refusal among them. The remaining two edges are the explicitly authored scramble diagonal, with `sourceWayId:null`. Preserve their authored identity and existing endpoint/geometry contract; do not fabricate an OSM way or let a null-source fallback inherit historical eligibility. Missing, duplicate, mismatched or incompatible fact bindings must refuse. A future derivation gate should compare the complete edge roster, preserve the two authored records, show old/current-source mismatch refusal and catch changed filter/level facts. This proposal adds no fact schema or producer code in this round.

Historical eligibility and current physical support need separate acceptance. An eligible source way does not select one of two overlapping current geometric sheets or justify a nearest-Y fallback. The next support-policy increment must bind the current mesh hashes and justify the interval assigned to the actual occurrence/body corridor, preserving the existing level ambiguity, covered ground, step and missing-support refusals until each is resolved by evidence. Do not restore old meshes, flatten a curb, widen contact tolerances or treat the replay as support certification.

Paired source/network adoption is optional and later. If root chooses it after independent review, the concrete candidate pair is historical response `e5e3f1ac…` with truthful network `30a0c16a…`, retaining the accepted original pair and exact payload comparison. That candidate changes provenance honestly; it is not a current-mesh regeneration. A reviewed installer must preserve the old files and prevent consumers from observing a mismatched pair. `hero-paint.ts:30` can retain its strict raw SHA equality: the truthful pair satisfies that check without weakening it. Its success would still not authorize authored paint replacement or override the landed Q4 preservation guard. Any paint preview/regeneration and current-surface adoption need their own preserved inputs and acceptance. The local rule that says Overpass output cannot be hashed should be clarified in that later source-contract change to distinguish an exact response digest from a reproducible fetch; no rule was edited here.

## Findings and disposition

No material defect was found in the author's bounded equivalence claim. The independent results support accepting this experiment as evidence for the proposed source-contract decision. They do not authorize the decision itself. Original raw-source recovery, current-surface interval authority, paired installation, authored paint regeneration and production walking remain open. The proposal above is deliberately limited to the actual consumer boundary and preserves the existing strict raw-hash guard.

## Verification

`python artifacts/review49/prepare.py` exited zero: 31 target files, 15 pinned source files, archive comparisons, three dependency members, five generation inputs, current scene bindings and the exact authored-report snapshot checked. `node artifacts/review49/independent.mjs` exited zero with exactly two generator calls; the historical arm took about 1.60 s and the control about 1.38 s on Node v24.12.0. These are incidental review timings, not performance acceptance. Both generated networks match the complete author outputs byte-for-byte and pass `validateShibuyaNetwork` and `validateCompoundClearance`.

`python artifacts/review49/compare.py` exited zero with no generation, reproducing the full equality/control counts and source-way inventory. `node artifacts/review49/check-query.mjs` exited zero with no request or generation, checking the exact historical query addition and independent old-surface copies. The first AST preflight stopped before generation because the review instrument initially treated a type-only `.js` specifier as a runtime path; that instrument was corrected. The separate query check first stopped on a wrong relative import and then a missing explicit Git working-directory selection; both were corrected before its zero-execution check completed. These are disclosed review-instrument failures, not product failures or additional generation attempts.

The independent probe is SHA-256 `7deed1a382af8592b758d8ca509ffa09cc4fe1cf5e6386c0cc3544f768e9f42f`; its result is `7c2ebfc4d08721b205b69b6dae58b0778e2f7ad0f7826ef82debca6463fe121e`, and the separate comparison result is `8f3d364b39adb8d0e75b3522f658948ea01f35d2bee96abad80ad622f7da9853`. The complete ignored review manifest records exact file hashes and commands. The author tree and private inputs were rechecked after the run; no writer was imported or invoked. No full gate, browser capture, fresh source request or dependency installation was needed for this read-only review.

The fleet work-docs checker again refused the inherited top-level `docs/work/0_shibuya-1km/handoff.md` path. This report has the required six review sections and retains the exact authored target, but does not claim the unrelated repository-wide structural failure is resolved.

## Round outcome

Accept the bounded historical network replay as independently reproduced evidence. The exact non-provenance payload result and sensitive current-source control support a minimal historical-facts contract review. Source adoption, guard relaxation, actual-city rebuilding and current-surface/walking acceptance are not granted. Root owns the next decision. The review tree and ignored evidence remain available for the integration owner; this round makes no primary change or commit.
