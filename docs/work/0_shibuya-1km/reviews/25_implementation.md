# Review 25: implementation

## Target

Focused B0 witness repair. The isolated six-script B0 repair, with unchanged reader and supervisor, at original main context `7f8aeac51357654a4d39d3e6ecc5f8ac0bda79b6`. The product remains retirement freeze2 `a8b4e520b8c74c989048ffdd0a34bce2c1f0c06eb3f51431a36997d3a65cf3e7`. Original Review 23 and its open historical disposition are unchanged; this later round records the repair. Root discovered and assigned the additional oversized-tail case F20/B04 after the original review. The author reproduced it on the original, fixed it in the successor and retained both results; no fictional extra review round is inserted.

The exact target is `artifacts/graphics-lifecycle-rethink/b0-repair-20260913/handoff.json`, SHA-256 `2fe5580c6e3ce8fe5f3dbb9bae2eab2bd28137d705077d0bdd86ae742c2d1c65`. Reviewed text sources are retained byte-for-byte under `artifacts/network/facade-b0-checkpoint/candidate-01/recovery/source/`; the consolidated `recovery/reviewed-source.patch` has SHA-256 `5be6da8e380b778de4d06369866a213d07650349c6239420b2e83d54efcc518a` across 46 source files for this checkpoint. The additive patch uses LF transport; exact copies and normalized reconstruction records preserve the original bytes and line endings. Raw output, source assets and earlier counterevidence remain ignored at their pinned handoffs. No experimental code is promoted by this document.

## Reviewers and coverage

Independent B0 repair reviewer authored the report below independently of the implementation. Root separately read its substantive findings and accepted only the dispositions stated here. The report is `artifacts/graphics-b0-repair-review/report.md`, SHA-256 `470970cb9c1ed3158ad60e458269d54afb30efd7d4a0df6a475235c55aba7990`; its handoff is `a439bf5939ff3bb4373bb6e48e86e687bbfc32d203ddfad32276d782629ed6d6`, sealed at `2026-09-13T13:49:33.0296575+00:00`. The wrapper is authored by the network worker for root's review; it does not alter the reviewer's body or claim additional independent work.

Rounds 24–26 follow these three handoffs' recorded seal times on 2026-09-13, not completion of their underlying work. Historical Review 23 and the earlier ABBA preparation reviews remain unchanged; these are new source, focused repair and result-interpretation rounds. The full authored text is embedded with ATX heading depth increased by three; inverse transformation is checked byte-for-byte. Local finding labels and priorities are preserved verbatim; permanent IDs occur only in the wrapper dispositions.

## Reports

### Independent B0 repair reviewer

<!-- authored-report-start sha256=470970cb9c1ed3158ad60e458269d54afb30efd7d4a0df6a475235c55aba7990 -->
Independent focused review of the four B0 witness repairs — 2026-09-13.

Disposition: F17/B01, F18/B02, F19/B03 and F20/B04 are resolved within the finite diagnostic preparation contract. No additional material finding emerged. The exact successor is suitable for root's next preparation gate; this is not permission to build or launch a browser and is not a city lifecycle or native retirement verdict. Only this new ignored review sibling was written. Product source, candidate files, data, dependencies and Git were untouched.

The target is `artifacts/graphics-lifecycle-rethink/b0-repair-20260913`. Its handoff SHA-256 is `2fe5580c6e3ce8fe5f3dbb9bae2eab2bd28137d705077d0bdd86ae742c2d1c65`, report is `8382bfdba695207b79760e3ba7b8205c0b976cfbb55f1038c019e10cb27e1d71`, and prepared-input manifest is `703ae3534774fcf56cad626b9c128611e7b29dd271b1ecd4f03234fd0eb7f29b`. The immutable predecessor handoff remains `3285d5b2f85a7615c659038d5ba8d6f61ab8a28aa4b0440edb0baa780e48fdfd`. The original independent review remains `graphics-b0-review/report.md`, SHA `507df963ae9a16aeb5aa8b150dbb77b7ce8352cb912b27a95f4bfda40f89d953`. Root assigned main `7f8aeac51357654a4d39d3e6ecc5f8ac0bda79b6` as context; the actual product target remains retirement freeze2 `a8b4e520b8c74c989048ffdd0a34bce2c1f0c06eb3f51431a36997d3a65cf3e7`.

Independent pre-check and final post-check passes verified every one of the 146 handoff records, 9,304,856 bytes, with zero mismatches. They also verified 885 directly relevant live inputs, 348,727,101 bytes: all 225 reused product source files, all 658 pinned PowerShell runtime files, the Node executable and the installed browser registry. The unchanged approximately 2 GB data/browser/npm population was not needlessly reread. Instead, its complete 17,377-record old/new manifest populations and content pins were compared, with the author's separately retained full-input verification still distinct from this review's live hashes. No missing key or unexpected content pin was found. The 11,229 backslash-to-forward-slash resolved-path formatting changes refer to the same paths; they are recorded separately from content changes.

| Changed witness source | SHA-256 |
| --- | --- |
| b0-core.ps1 | `588ccb48f771dbd6a0ce8432796411f931fb181e0b8d0428213e8d85f5dd3563` |
| SharedTrace.cs | `a5c231c6359bdcdc9db5495e2658969644531672326faa2dcafb24a26cd6349e` |
| b0-publisher.ps1 | `a97f54cb9651b7aba1215697dc1137fda79c0ba0b26ad46fe91e59b5c1f5181a` |
| run-city.ps1 | `7faad239f0fa740891766ef0a32c095bf5adedc3f99de352b50d6e08e7648133` |
| freeze-future-build.mjs | `c1637f3a3d93879b78b783049a6569152406f83794df5e228989aad9a6d592f8` |
| bind-city-inputs.mjs | `1cb66dd0ac618d736fc0eab459cccc91e51986a82688413d1fe31e3be6d69f95` |

All eight witness scripts were independently reconstructed from predecessor bytes using the 23 declared exact substitutions and checked against the actual successor bytes. There are exactly six changed scripts. Each original script also matches its own predecessor handoff pin. `b0-reader.ps1` remains `6eda767de1d1e0f854b0990f698137b181eba6d270cfdcdbf63e83a9747adda8`; `b0-supervisor.ps1` remains `d3198332870af8f195b336927561ca899230239a719a8f0858842dded7329d66`. Their byte identity limits the scope of this re-review. The exact before/after script pins and reconstruction results are in `pins-binder-after.json`.

F17/B01 — accepted finite repair. The original classifier replaced its first noon snapshot with each later matching snapshot, moving the attribution boundary and accepting a replacement-document marker as first-city retirement. The successor accepts only finite numeric matching timestamps and retains their minimum. The independent chronological control places a marker at 900020 with matching snapshots at 900010 and 900030. The original yields the false observed first-city result; the successor excludes that marker at boundary 900010. A reordered sequence 900030, 900010, 900040 has the same corrected minimum. Equal-minimum and fractional timestamps remain valid; null and string timestamps remain unavailable in the successor. The old string timestamp case additionally demonstrates why explicit numeric typing matters. This accepts the boundary rule, not real browser document identity beyond the previously reviewed page/frame attribution limits.

F18/B02 — accepted finite repair. The original could create or use a trigger after already reading the matching noon completion. The successor latches completion regardless of whether the before snapshot exists, checks it after polling before trigger creation, and checks again at the first spawn attempt. Independent `Nav → Before → After` same-poll and direct completion-after-trigger-before-spawn controls now produce zero launch callbacks; both were launchable on the predecessor. Early admission still produces exactly one callback. A deliberately nonterminated, unobserved completion record still permits that one attempt. That positive case is intentional: the guard concerns complete records already observed. It cannot synchronize the reader's physical open with actual API completion, queued writes or producer buffering. No such physical guarantee is claimed.

F19/B03 — accepted finite repair. The prior prepared hash/population filters excluded every browser-runtime `/debug.log` suffix, allowing an unexpected nested log into the future baseline even though later accounting exempted only one exact key. The successor derives one canonical key from the installed browser registry before any of these stages and uses the identical key in the hash loop, population filter and after comparison. Independent execution of the exact extracted production bodies rejects nested debug.log, a similar debug.log.1 name and another input kind in both prepared checks. The predecessor fails the nested-debug hash and population requirements. Canonical append and canonical new-file cases remain accepted as runtime output; corrupt prefix, canonical deletion and a newly added nested log remain input changes. Both variants' 13 scenarios, extraction hashes and result objects are retained. These are exact binder bodies with controlled reads and the complete 17,377-record population; the complete future binder was not invoked because there is no newly authorized build.

The current canonical browser log exactly matches the successor's retained 29,585-byte backup and SHA `21455710a511bf3ad726a68cb0f0f62a6c006de745f342838962da00f87b57e7`. Independent prefix verification preserves all 29,219 predecessor bytes. The already documented 366-byte append hashes to `53a48c821ff692256df81937dd3684d9c93375ecff23d42e8d286c7166a28aba`. This is the only content-pin change between the prepared manifests. The old manifest remains unchanged. This review does not ascribe the append to a new browser run or renew tiny-run authority.

F20/B04 — accepted finite repair. An oversized unfinished line switches SharedTrace into discard mode and clears the byte buffer. Previously, `PendingBytes == 0` then allowed clean publisher EOF and an observed result from the valid prefix. The single new `HasIncompleteRecord` getter covers discard mode OR nonempty pending bytes; the closure classifier uses it without falsifying the numeric buffer count. Independent tests read in 4,096-byte chunks through actual SharedTrace and use an actual held, exited task-owned Node process as the closure stand-in. Complete tail remains observed. Small partial and exact-262,144-byte partial tails reject. The 262,145-byte nonterminated tail reproduces the old false observed result; the successor reports `HasIncompleteRecord=true`, `PendingBytes=0`, `truncated-final-record`, and unavailable. Terminating that oversized line still reports the existing record-size error and remains unavailable. This proves the C#/closure/result path at the specified limits, not the real browser-to-file publisher association.

The fresh classifier/tail instrument contains 15 requirements per variant. The predecessor fails six and the successor passes all 15. The fresh binder instrument contains 13 scenarios per variant; the predecessor fails two and the successor passes all 13. Old failure objects, fixtures, partial bytes, original/new results and stdout/stderr are preserved. The author controls and their old-red/new-green ledgers were read and hash-verified, not substituted for these independent reproductions. The first probe names the actual B0 classifier, SharedTrace and publisher instrument. No unrelated retirement, tiny, facade or population suite was rerun.

Path wiring is consistent. The successor wrapper's working-directory guard points to the unchanged predecessor worktree, while its sourced supervisor/core/reader resolve in the successor witness folder. Capture output remains a fresh, empty task-owned directory inside the reused worktree's artifacts root. Future-build capture reads that same worktree and the unchanged predecessor source freeze, but writes the successor's future-build manifest. The successor binder reads original copied-input/delta/source lineage, new witness/prepared/future-build files, and writes its before/after records into the successor folder. Its instrument manifest explicitly pins both new scripts and reused predecessor references. The product freeze still names retirement-only source; incorporating any facade or other product increment would require a new reviewed binding. These scripts do not silently authorize that integration.

The repair leaves the ordinary recording-on control sequence and 15-second navigation/60-second replacement criteria intact, along with the 2,400-second wrapper cap, 120-second reader cap and 110-second reader work budget. Publisher PID/creation/executable admission, one-shot behavior, bounded backfill/bytes/records, shared delete-capable file handle and final publisher drain are unchanged. A held deleted file can delay physical storage reclamation; it is still an observer cost. Captured errors remain separate from the marker and can invalidate it. The installed tracing stop precedes final context close, so pageClosed remains optional and absence of captured errors does not prove absence of later cleanup errors. Native WebGL event delivery, resource teardown timing and ordinary city success remain outside this review.

Reviewer failure preservation: the first two metadata comparison attempts treated path separator normalization as a semantic path change. The second retained the exact mismatch summary: 11,230 raw row differences, of which 11,229 are separator formatting and one is the known log append. The corrected comparison normalizes separators only; content hashes, sizes, population and all other path characters remain checked. No target files were changed. The initial closure record's copied description incorrectly said the probes had no children; its actual eight-PID inventory already included both publisher children. The corrected final snapshot states the accurate scope, and the earlier record remains preserved.

Cleanup is complete. Six direct CPU children and two finite publisher children were launched with hidden windows and held process handles. Direct children had 120-second outer caps; CPU loops had 110-second cooperative guards; the two publisher stand-ins were expected to exit within 10 seconds and had their own finally kill/wait/handle-close records. All eight recorded PIDs were absent in a successful strict native inventory at `2026-09-13T13:47:22.5008565Z`, with no matching review process, current child or port4319 listener. The inventory sent no signals. This is current recorded-owner closure, not a reconstructed historical OS census. All evidence remains intentionally retained under `artifacts/graphics-b0-repair-review`; no disposable resource remains. Final source/runtime pins matched, and no restoration was needed.

The scoped review is complete and the CPU slot is released. Root owns the subsequent authorized build, actual dist inspection/binding, browser lease, native witness and ordinary lifecycle acceptance. No browser, server, GPU, build, city or full44 run occurred in this review.

<!-- authored-report-end -->

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
| --- | --- | --- | --- |
| F17 (original B01) | A later noon snapshot replaced the earliest document boundary. | Resolved within this finite repair. | Keep the minimum finite matching timestamp; original two-snapshot and reordered cases remain adverse evidence. |
| F18 (original B02) | A first reader could be admitted after matching completion had already been observed. | Resolved within this finite repair. | Latch completion and check it at trigger creation and first spawn; producer buffering remains outside the ordering guarantee. |
| F19 (original B03) | Prepared input checks exempted noncanonical debug-log suffixes. | Resolved within this finite repair. | Use the same installed-registry canonical key across hash, population and output checks. |
| F20 (original B04) | Discarding an oversized unfinished record emptied PendingBytes and admitted false clean EOF. | Resolved within this finite repair. | HasIncompleteRecord covers discard mode or pending bytes; the original 262,145-byte unterminated tail remains a reproduced failure. |

## Verification

The reviewer reproduced fifteen classifier/tail requirements and thirteen binder scenarios per variant: the predecessor fails six and two, while the successor passes all. It independently reconstructed eight scripts using the declared substitutions; exactly six changed. It verified 146 handoff records and 885 relevant live inputs, while comparing the unchanged full manifest populations separately. Exact SharedTrace/closure controls do not establish browser publisher attribution, native retirement or city success.

Preparation of this documentation checkpoint checks the exact authored/source pins, reversible imports, scoped document headings and links, blob/secret/diff limits, and separate HEAD/WIP preservation. It runs no browser, benchmark, code build, five-gate sequence or source repair. Review 11 remains open. Primary WIP also retains rounds 2, 4 and 12 that are absent from HEAD. This checkpoint neither invents those missing committed rounds nor claims a whole work-docs continuity pass.

## Round outcome

Root accepts F17–F20 only within the finite B0 diagnostic preparation contract. The ordinary recording-on controls, 15-second navigation and 60-second replacement criteria, 2,400-second outer cap and 120/110-second reader bounds remain intact. Actual build/dist binding, native observation and ordinary city acceptance remain separate. Current delivery status remains in [the plan](../plan.md).
