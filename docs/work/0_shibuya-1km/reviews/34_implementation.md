# Round 34 — independent review of the terrain cap and the re-bound data pipeline

Reviewer: an independent, read-only lane. Revision reviewed: **`d5ead63`** (the code and the acceptance gates) with **`0e7d683`** (docs-only) above it — branch `phase2-regen`, five commits over base `4efaea6`, **unmerged**. `main` was `17052ea` when the round started and advanced twice while it ran: to **`1a7b780`** (docs-only) and then to **`409a57e`** (the flicker lane's merge — `tools/flicker/**`, `test/flicker-*`, `docs/learning/gate-proofs.md`, `docs/policies/local-rules.md`, `docs/devlog/**`, `docs/learning/defect-register.md`; it touches none of the batch's files, no pin and no served byte). Every claim below was measured against the branch revision; where another revision's text is quoted it is named, and the one place where two revisions disagree is finding 7.

Worktree `artifacts/review-f1-cap/wt` on branch `review/round34`, `data/` and `node_modules/` junctioned to the primary for reading. The one tracked file this round writes is this one. Every probe and every raw output lives under the ignored `artifacts/review-f1-cap/`, preserved in the primary checkout so that removing this round's worktree does not strand them. No `data:*` command ran, `npm run visual` was not run (the flicker lane holds that gate), and no file under `tools/`, `test/`, `src/` or the pins was modified, so the revision under review is byte-identical to what I measured.

Verdict: **one blocking finding, and it is in the record rather than in the code.** The C5 pavement re-binding is **accepted** — see the last section for what that acceptance rests on and what it does not cover.

## What I ran, and what I read

Gates, in my worktree checked out at `d5ead63` (host ports 4319 and 4320 were free before any of them): `npm run build` exit 0; `npm run typecheck` exit 0; `npm test` **72 files / 525 tests, 0 failed**; `npm run audit` exit 0 (2 moderate, below the high threshold). The build's three certificate-pinned files hash to `a73d718a467779d9…`, `c890299ce2547882…` and `11dd032c51b536a5…` — the certificate's own three values, byte for byte. They were taken before I switched the worktree, and `git checkout review/round34` afterwards left the tree clean. **Integration arm:** merging `main` (`409a57e`) into `d5ead63` on a scratch branch takes main's four flicker files cleanly and leaves **typecheck exit 0 and `npm test` 74 files / 535 tests, 0 failed** — the batch's 525 cases plus the flicker lane's ten, together, on the code the integrator will actually get. What does not merge cleanly is two documents, finding 7.

Read with `git show` rather than the working tree: all five commits of the batch, `4efaea6..0e7d683` in full, `tools/scene/terrain-watertight.ts`, `tools/scene/build-terrain.ts`, `tools/scene/build.ts`, `tools/scene/pavement-recipe.ts`, `tools/scene/hero-paint.ts`, `tools/scene/build-markings.ts`, `tools/geo/plateau-tin.ts` (the reader diff), `test/terrain-watertight.test.ts` and `test/paint-support.test.ts` at `d5ead63`, `docs/learning/gate-proofs.md:1340-1400`, the register's two new entries, `artifacts/f1-cap/BRIEF.md`, `artifacts/f1-cap/EVIDENCE-review34.md`, and the four enumerations under `artifacts/f1-cap/evidence/`.

Measured myself, all read-only, with my own mesh decoder (`artifacts/review-f1-cap/meshlib.mjs`, written from the format comment in `src/world/mesh.ts` rather than importing it): the terrain additivity probe, the pavement delta keyed on **positions** (not vertex indices — the compose pool renumbers, and an index-keyed comparison reports false change; I hit that trap and discarded the first result), the adjacency of the changed pavement to the fourteen rims, the roads insertion and the seam fixture against the shipping `createPaintSupport`, the TIN-reader reconstruction, the eighteen-file digest recomputation of `sceneTreeDigest`, the three `control-hardware.json` vintages, the authored-markings tail, and the network's three `undrapableSegments` against the old and new terrain samplers. Re-ran the author's own `artifacts/f1-cap/probe-inheritance.mjs` and `probe-seams.mjs` from my own tree instead of quoting their numbers.

## 1. "The cap is purely additive" — verified on the bytes, including the part the brief got wrong

Claim tested: the cap appends one apex vertex per rim and one triangle per rim edge and removes nothing, so a changed pavement digest can only come from geometry that newly drapes.

`artifacts/scene-backup-20260917/scene/terrain.mesh` (`fef57d09…`, the pre-batch served bytes) against `data/scene/terrain.mesh` (`c39d49ed…`):

- **positions: the old array is an exact prefix of the new one, float for float.** Nothing moved.
- vertices 92,269 → 92,283, exactly `+14`; triangles 183,188 → 183,350, exactly `+227` cap triangles minus the 65 reader duplicates the same batch removes (`183,188 − 65 + 227 = 183,350`).
- the 65 old triangles that vanish are exact duplicates (same three vertices as an earlier triangle), and `new.indices[0 .. 549,369)` is **old.indices with exactly those 65 deleted, in order**. So the reader fix removed those and nothing else.
- the 227 appended triangles use **only** old rim vertices plus a new apex, and **each spans two adjacent rim vertices** (227 of 227), so the fan is the shape the module documents. The 14 apexes carry 6, 6, 6, 6, 8, 8, 14, 14, 15, 16, 22, 24, 28 and 54 triangles — 227 in sum, and the 54 is H1.
- each apex is the float32 rounding of that rim's own vertex mean: the largest deviation I could find between the stored apex and the float64 mean is 2.0e-5 m, at 579 m from the origin, which is float32 resolution and not a different number.
- independent census, my own and then `node artifacts/terrain-holes/mesh-doctor.mjs`: old **1,441 rim edges / 15 loops / duplicate 65 / nonManifold>2 195 / degenerate 0**; new **1,214 / 1 / 0 / 0 / 0**.

The author's own rebuild pair settles the reader half without my having to build anything: `artifacts/f1-cap/wt/artifacts/f1-cap-probe/rebuilt/uncapped.mesh` (`3a8545cc…`, 183,123 triangles) has **1,441 rim edges, 15 loops, 0 duplicates, 0 non-manifold edges** — the hole census does not move — and its index buffer is byte-identical, in order, to my independent reconstruction "old indices minus the 65 duplicate triangles", with positions identical over all 92,269 vertices. Its `capped.mesh` is byte-identical to the served `data/scene/terrain.mesh`. So the pipeline's output is the cap's output, and the two defects are independent rather than one hiding the other.

**The roads half is additive too, but not in the form the brief assumed.** Keyed on positions: **0** of 79,244 old road triangles is absent from the new mesh and exactly **+123** appear; vertices +369 = 3 × 123; old vertex positions absent from the new mesh: **0**. In index order the arrays share a 11,736-triangle prefix and a 48,861-triangle suffix with the 123 new triangles inside the middle block, and the vertex array **differs from vertex 33,523 on** — the pool is rebuilt per polygon, which is why 231,537 new vertices sit at only 33,223 distinct positions. That last fact matters: any index-level comparison between the two road meshes reports thousands of "changed" triangles that are the same geometry, which is how the brief's 123-triangle prediction and the true 2,613-triangle delta can both be arithmetically honest and still fail to describe the same thing. The seam probes' identities move by +123 precisely because the insertion precedes triangle 54,518.

## 2. The pavements delta: the restated claim survives in substance and fails as worded

Measured: `pavements.mesh` 461,912 → 464,525 triangles, `+2,613`, against the brief's predicted 123.

Keyed on positions, the delta decomposes exactly: **2,691 new triangles appear and 78 old ones disappear**, with no multiplicity change on either side (`2,691 − 78 = 2,613`). The 78 are **re-noded in place, not removed**: 46 of them have an added triangle's centroid within 1 cm, all 78 within 1 m (median 0.0000 m), so the same pavement surface is still there with a slightly different triangulation. Every changed triangle's nearest vertex is **within 15.1 m of a rim ring** (median 4.9 m among the 2,691 added, 2.7 m among the 78 replaced, 404 of the added inside 0.5 m), and the nearest-rim histogram is rim 0 (1,451), rim 1 (575), rim 2 (414), rim 10 (251); the 2,218 added triangles with no vertex inside any rim footprint have centroids in x 300.8–545.9, z 279.5–513.9; **no changed triangle is more than 65 m from a rim centroid and none is anywhere near the crossing**.

Where the mechanism actually is, and why the triangle-level claim was never going to hold: **`pavement-recipe.json`'s own report explains the entire delta.** Diffing the pre-batch report (`artifacts/scene-backup-20260917/scene/pavement-recipe.json`, `6d9ad2a2…`) against the post-batch one (`0750f79e…`) leaves **only** these differences: `inputs.terrain`/`inputs.roads`/`inputs.adaptiveBase`, `output.*`, the base's `polygonCount` 48,555 → 48,636 (**+81**), `triangleCount` 414,963 → 417,576 (**+2,613**), `vertexCount` 276,379 → 278,959 (+2,580), `offTerrainPolygonCount` 197 → **116** (**−81**), `lowestVertexM`, and 22,285 `replacement.ledger[*].triangle` indices. The `hero` and `lower` blocks are byte-identical, `parentIds` and `coverageLimit` unchanged, `replacement.inputTriangles`/`unchangedTriangles`/`retainedOutputTriangles`/`outputTriangles` all move by exactly +2,613, and `pavement-source.json` did not move at all (`artifacts/f1-cap/evidence/before.json` vs `final.json`).

So the true statement is one level up from the one written: **81 source polygons that had no ground support are now draped, and they carry 2,613 triangles** (32.3 each, against a base average of 8.6 — they are the larger multi-vertex road polygons that spanned a hole). At the triangle level the claim as worded is false twice: 2,218 of the 2,691 added triangles have no vertex inside any rim footprint (they are the rest of the recovered polygon, which extends past the hole — median 4.9 m off the ring), and 78 triangles' geometry *did* change where no rim covers them. Both are within the band the newly kept polygons reach, which is why the conclusion holds even though the sentence does not.

`pavements.mesh`'s pre-existing defect classes are untouched by the change: `degenerate` is **25,448 in both**; `duplicate` 99,869 → 100,011 (+142), `nonManifold(>2)` 107,615 → 107,739 (+124), boundary edges 211,126 → 213,259 (+2,133), loops 30,349 → 30,672 (+323) — proportional to +0.57% triangles in a mesh whose producer already emits 21.6% duplicate and 5.5% degenerate triangles at `5d2a9150…`, which Review 8 accepted. No new class.

## 3. The three reviewed artifacts that moved, one site at a time

**`pavements.mesh` `5d2a9150…` → `3b24c5ef…`** — §2. Sites quoting the old digest: `docs/devlog/detailed/2026-09-12_graphics-diagnostics.md:17` and `docs/devlog/detailed/2026-09-17_phase-b-appearance-batch.md:35` are devlog history and stay as written; `docs/devlog/summary.md:3`, `docs/learning/defect-register.md:38` and `docs/reference/pavement-recipe.md:5` record the move; `docs/learning/defect-register.md:133` and `docs/work/0_shibuya-1km/plan.md:124` are the 2026-09-16 incident record; `reviews/8_implementation.md:44`, `reviews/10_implementation.md:31`, `reviews/30_implementation.md:132` and `reviews/31_implementation.md:25,70` are reviews bound to the bytes they inspected, which the local rules say keep their digest. `docs/work/0_shibuya-1km/quality-register.md:37` carried both digests and the wrong `+3,164 B`; main's row now says `3,060 B`, and the batch never touched that file, so a merge cannot resurrect it.

**`control-hardware.json` `5f710c5b…` → `a65058ee…`** (same 99,943 B) — the move is fully explained and **nothing was placed differently**: `5f710c5b` → `a65058ee` differs in exactly 134 leaves, which are `inputs.roads`, `inputs.pavements` and 132 `records[*].evidence.{pavement,support}Triangles` indices; no position, status, count or placement field moves, so the same 119 records / 23 placed / 96 unplaced hold, and the index shifts are the insertions of §1–2 seen from the support side (`supportTriangles` +123 on the roads, `pavementTriangles` +1,563 to +2,613 depending on where the record sits relative to the inserted blocks). **But the review it is attributed to does not record `5f710c5b`.** `reviews/30_implementation.md:132` records `e6b3e1d387807f43…`, and that digest survives on disk in three trees (`artifacts/network/f9-repair/candidate/data/scene/control-hardware.json`, `artifacts/network/review9-frozen/source/…`, `artifacts/recovery-realism-20260908/candidate6-probe/…`). Its difference from the pre-batch `5f710c5b` is **one leaf — `.inputs.vehicles`, `4cd6e59b…` against the accepted `5b29efc7…`** — so Review 30's value predates the 2026-09-16 recovery that restored the vehicles manifest. The evidence pack's ``(Review 30)`` and the register's `` `control-hardware.json` (Review 30)`` therefore attribute the wrong digest to that review; the file the batch moved from is the one `artifacts/f1-cap/evidence/before.json` records, `5f710c5b`.

**`markings-provenance.json` `764c659f…` → `efad82e8…`** (same 14,383 B) — I hashed **every** `markings-provenance.json` in the repository including every worktree: three exist, `efad82e8…` twice (the served file and `artifacts/network/app-motion/candidate/data/scene/`) and `2be1bd51…` (the unauthored 11,223 B file in `artifacts/scene-backup-20260917/scene/`). The batch's claim that no copy of `764c659f…` survives is correct, and the served file now equals `reviews/30_implementation.md:132`'s recorded digest. The only tracked sites quoting either value are `reviews/30_implementation.md:132` and the register's new entry, and both are now consistent with disk.

## 4. The gate's claim, its bound, and the one part of it nobody has watched

`tools/scene/build.ts:116-119` reads `data/scene/terrain.mesh` **back off disk** (`readFile(join(SCENE_ROOT, "terrain.mesh"))`) and hands those bytes and the path string `"data/scene/terrain.mesh"` to `verifyTerrainIsWatertight`, which censuses directed edges and throws if any rim other than the outer one survives. The claim is genuinely topological and the header says so in its own words (`tools/scene/build-terrain.ts:240-260`): it does not claim the fan resembles the survey, does not check projection coverage, **cannot see a hole whose rim was welded into a seam or a T-junction**, and puts the outer silhouette out of scope. The refusal names what happened, which input, and what would satisfy it — path, hole count, the outer rim, and every hole worst-first with its vertex count, perimeter, position and XZ area, closing with the file and the function to fix it and the command to re-run. I reproduced that message against the pre-batch mesh through the census in `docs/learning/gate-proofs.md:1351-1369` (the batch revision's lines — `main` does not carry that section yet), and its fourteen lines match my own census rim for rim. The payload names the invention: `data/scene/manifest.json` carries `closedHoleCount` 14, `capTriangleCount` 227 and fourteen `closedRims` records, and each record's vertex count, perimeter, apex and area matches my independent census (H1 `54 / 369.4 m / (426.82, 11.83, 391.25) / 1,325 m²`; F1 `15 / 89.5 m / (578.98, 26.42, −403.06) / 512.5 m²`). `test/terrain-watertight.test.ts` goes further than the author claims: it reconstructs the uncapped ground from the served mesh by dropping the fourteen apexes and requires the fourteen interior rims to come back, which is the additivity property of §1 asserted in the suite.

**The refusal has never been watched firing end-to-end, and that bound is acceptable but not free.** What exists: the unit case that drives the pipeline's own `buildTerrain` over a fixture DEM with one missing cell in both arms (`leaveHolesOpen` on and off) and reads the bytes it hands back; the same function refusing a 1-hole mesh; the census over the real pre-batch served mesh producing the real fourteen-line message; and the six mutations in `docs/learning/gate-proofs.md:1373-1400`, including the one that mattered — removing the cap from the pipeline left the first version of the suite green, which is why the fixture arm exists. What does not exist is a run in which `npm run data:scene` itself aborts on a mesh that reached disk. That is unreachable without mutating `buildTerrain`, which the instruction forbade, and a mutation would prove no more than the fixture arm already does — except for one thing it would not prove either: that the call site is reached and that nothing downstream is written first. Its reachability rests on a **source-text assertion** (`test/terrain-watertight.test.ts:231-237` counts one occurrence of `readFile(join(SCENE_ROOT, "terrain.mesh"))` in `tools/scene/build.ts` and requires `decodeMesh(terrain.bytes)` to be absent), which is a weaker instrument than a run and is the kind of check that can be satisfied by moving the read rather than by keeping the gate. I record it as a bound, not as a defect: the ordering is visible in the source — the gate sits at `tools/scene/build.ts:116`, after `terrain.mesh` is written and before roads, pavements, markings and buildings — and no reachable real-file path produces a holed mesh, because `buildTerrain` caps and self-verifies before returning.

## 5. The seam probes are re-derived evidence, and the arithmetic is exact

`test/fixtures/paint-road-seams.json` against the pre-batch fixture preserved at `artifacts/f1-cap/evidence/paint-road-seams.before.json`: 11 probes, the same `source` and the same `evidenceSha256` (`91f98cf9…`), the same `x`, `z` and `expectedY` to the last digit. Every identity moved by **exactly +123 road triangles with the edge index unchanged** (sorted by triangle: `roads:54518:0 roads:54526:2` → `roads:54641:0 roads:54649:2`, 11 of 11), and `|gapWidthM|` moves by 1.76e-11 to 9.01e-11 m against the test's 1e-6 m tolerance. The stored edge-list order changed on all 11 probes, which is harmless because `test/paint-support.test.ts:132` sorts both sides by triangle, and it is itself a sign of regeneration: I re-derived all eleven probes with the **shipping** `createPaintSupport` over the served meshes and got the fixture's identity sets (11/11) and its `gapWidthM` values **bit-identical** (11/11), zero probes gone. The fixture is what the code produces, not a hand-edited number.

## 6. The markings compromise: the proof exists, I ran it, and the record names the wrong failure

**The inheritance proof reproduces.** `node artifacts/f1-cap/probe-inheritance.mjs` from the primary, 2026-09-18, my own run:

```
authored provenance: 5 ways, 68 triangles, 204 vertices
markings.mesh: 13875 vertices, 4625 triangles; authored vertices 13671..13874
authored vertices sampled: 204
ground:  0 with no terrain on one side; max |old - new| 0.000000000 m at null
roads:   0 with no road on one side; max |old - new| 0.000000000 m at null
paint height difference under the authored vertices: 0.000000000 m
VERDICT: the authored paint's support is unchanged, so the restored bytes are what a re-derivation would produce there
```

The script is runnable as written, its `204 / 5 ways / 68 triangles` marker reproduces, and the zeros are bit-exact rather than rounded. Two checks make the sample the right population rather than a hopeful one: the served `markings.mesh` (`efdba9cb…`, 13,875 vertices / 4,625 triangles) is the unauthored `4daa4283…` mesh (13,671 / 4,557) with its **position array and index buffer as exact prefixes** and a self-contained 204-vertex / 68-triangle tail whose lowest index is exactly 13,671 — which is what `heroPaint` appends — and the served file is byte-identical to `artifacts/network/app-motion/candidate/data/scene/markings.mesh`. So the two places the batch says the authored pair survives really do hold it, and `artifacts/scene-backup-20260917/scene/` really is the unauthored pair, as the evidence pack corrects. There is a stronger argument than the sample and it is worth recording beside it: the cap is additive on positions (§1), so the height field outside the rims is bit-identical and no authored vertex *can* have moved; the 204 samples confirm a structural fact rather than extrapolating from a sample. `main`'s new text (`docs/work/0_shibuya-1km/plan.md:482`, `handoff.md:33`) records the restoration as "verified by digest, unproven by inheritance" because no output for that script existed; the output now exists and this round is it.

**The consequential claim is wrong about which command loses the paint, and that is the blocking finding (finding B1 below).** The register says a clean `data:setup` "would produce the unauthored markings from live OSM and silently lose the authored west-approach paint" (`docs/learning/defect-register.md:16`, repeated in `0e7d683`'s message and in the evidence pack's §6). `package.json:27` runs `npm run data:network` **before** `npm run data:markings`; `tools/network/build.ts:28` writes `osmSha256: hash(osmBytes)` for the extract it read; and `tools/scene/hero-paint.ts:30` throws only when that recorded digest differs from the extract on disk. On a clean checkout both come from the same `data:fetch`, so the guard's equality holds, `data:markings` proceeds, and the authored paint is **re-derived**, not dropped. What a clean `data:setup` actually loses is the reviewed *bytes* — the network pin is silently replaced by a re-derivation from the live survey, and the paint that comes back is derived from that different survey with nothing comparing it to `efdba9cb…`. The silent-loss path the batch is looking for is one command earlier and in the batch's own evidence: `tools/scene/build.ts:159` calls `writeMarkings()` with the default `includeAuthored = false` (`tools/scene/build-markings.ts:82`), so **a plain `npm run data:scene` overwrites the authored pair with the unauthored one** — which is exactly what `artifacts/f1-cap/evidence/after1.json` and `after2.json` record (`markings.mesh 4daa4283…`, `markings-provenance.json 2be1bd51…`) and what `final.json` then restores to `efdba9cb…`/`efad82e8…`. That is the exposure worth naming: two of this batch's own runs silently downgraded the paint, and nothing in the pipeline notices.

## 7. Determinism, the digest chain, and the network decision

**The determinism claim reproduces, and it reproduces all the way to the certificate.** `after1.json` and `after2.json` are file-for-file identical over all 117 entries; recomputing `sceneTreeDigest` from the enumerations with the shipping walk (mount order, `readdir` sorted, recurse inline, entries `<route><relative>\0<sha256>` joined with `\n`) gives **`09105dd79921ff5e…`** for both, `dfee0f12bb2e87c1…` for `final.json`, and **`1ebda66251e5e023…` for `before.json` — the certificate's own `scene.digest` at 117 files / 301,674,209 B** (`artifacts/visual/complete.json`, runId `e6b7b9d8de0faf69`). So the certificate is provably bound to the pre-batch tree, the two `data:scene` runs are provably byte-identical, and the served tree is provably `dfee0f12…` at 117 / 301,782,754 B. `before.json` → `final.json` moves exactly eight files with the digests the evidence pack lists, and `after2.json` → `final.json` differs in exactly the authored markings pair.

**Leaving `data/network/network.json` at its bytes is the right decision, and the staleness is bigger than the batch records.** The file is `314fac84…` in `before.json` and `final.json`, unmoved. Its `diagnostics.undrapableSegments` holds exactly three entries, all carrying "Terrain absent under walking path": ways `1317354768` at (387.5, 354.2), `1317354769` at (421.9, 391.4) and `1317354773` at (379.7, 346.1). I queried both terrain samplers at those points: **all three are inside H1's rim footprint, all three are `ABSENT` on the old ground, and all three read back 12.194 / 12.817 / 12.913 m on the capped ground.** So the recorded reason is now false for every entry, and the 47.196 m of "lost" walking length is ground that exists. Re-deriving would turn the fan's invented ground into walkable ground against `tools/network/generate.ts`'s stated policy and would need a live extract that cannot reproduce the reviewed graph, so recording the staleness and leaving the bytes is the cheaper correct call. Two things the batch does not record: `network.provenance` still names `terrainSha256 fef57d09…` and `roadsSha256 3fe126cd…`, so the file records *inputs* that no longer exist on disk; and the live reference document that publishes those facts, `docs/reference/network-contract.md:71` (digests) and `:79` ("Missing terrain removes 47.196 m … ways `1317354768`, `1317354769`, `1317354773`. Their first missing world points are `(387.535,354.217)`, `(421.929,391.408)`, `(379.657,346.100)`"), was not touched by the batch and is now false on all three named ways. The four `docs/work/0_shibuya-1km/snapshots/*_network-contract.md` copies are historical snapshots and stay as written.

## 8. Scope discipline

- **`data:network` did not run.** `data/network/network.json` is `314fac84…` in both `before.json` and `final.json`, and its `provenance` still records the pre-batch terrain and road digests — a re-run would have written the new ones. The batch touched no file under `tools/network/`.
- **The three certificate-pinned build files are untouched.** `git diff --stat 4efaea6 0e7d683 -- src/` is `src/world/scene-data.ts` only, and its 43 added lines are an interface and its field documentation — erased by TypeScript. Built at `d5ead63`, the three files hash to the certificate's `a73d718a…`, `c890299c…` and `11dd032c…`; `package.json` is unchanged, so no data command was added. Only the unpinned source map differs, which the evidence pack already states.
- **The TIN-reader fix leaves the hole census unmoved.** Old served mesh: 1,441 rim edges / 15 loops / 14 interior rims, duplicate 65, nonManifold>2 195. Rebuilt uncapped: 1,441 / 15 / 14 interior rims, duplicate **0**, nonManifold>2 **0** — verified both by `mesh-doctor` and by my reconstruction of "old minus its 65 duplicate triangles", which equals the author's uncapped index buffer byte for byte.
- **The gates at the batch revision are green as claimed**: 72 files / 525 tests, 0 failed, matching `EVIDENCE-review34.md` §8 exactly; build, typecheck and audit exit 0.

## Blocking findings

**B1. The markings consequential claim names the wrong command, and the record it drives is wrong in the direction that matters.** `docs/learning/defect-register.md:16`, `0e7d683`'s commit message and `artifacts/f1-cap/EVIDENCE-review34.md` §6 all say a clean `data:setup` "would produce the unauthored markings from live OSM and silently lose the authored west-approach paint". It would not: `package.json:27` orders `data:network` before `data:markings`, `tools/network/build.ts:28` re-records the extract's digest, and `tools/scene/hero-paint.ts:30` therefore passes on a clean checkout, so `data:markings` re-derives the paint. The real silent loss is `npm run data:scene` alone — `tools/scene/build.ts:159` calls `writeMarkings()` with `includeAuthored = false` (`tools/scene/build-markings.ts:82`) — which the batch's own two determinism runs performed, recorded in `artifacts/f1-cap/evidence/after1.json`/`after2.json` as `markings.mesh 4daa4283…` and `markings-provenance.json 2be1bd51…`, before `final.json` restored the authored pair. This is a record defect, not a code defect: the data and the code may land as they stand, but the sentence is the batch's own headline ("the most consequential finding in this batch") and the fix it points at is the wrong one. Would resolve it: state the three paths separately — `data:scene` alone silently downgrades the pair to unauthored; `data:markings` alone refuses by name in this tree because the extract and the reviewed network disagree; a clean `data:setup` re-derives both the network and the paint from the live survey, so the reviewed bytes are replaced with nothing comparing them — and keep the two remediations already costed in `BRIEF.md` §6.

## Non-blocking findings

1. **The restated pavement claim is written one level too low.** "The added triangles are exactly the pavement pieces that gained ground support, and no pavement geometry moved where the support did not change" is false as worded: 2,218 of the 2,691 added triangles have no vertex inside any rim footprint, and 78 triangles were re-noded at points no rim covers. The measured statement is at the polygon: `pavement-recipe.json`'s base gains exactly 81 polygons and exactly 2,613 triangles while `offTerrainPolygonCount` falls 197 → 116, every changed triangle is within 15.1 m of a rim ring (nearest rims 0, 1, 2 and 10), and the 78 replaced triangles each have a replacement within 1 m (46 within 1 cm), so no pavement surface was removed or displaced. Would resolve it: say "81 newly supported polygons, 2,613 triangles, nothing outside a 15 m band around the four rims the pavement layer reaches".
2. **`control-hardware.json` is attributed to the wrong review.** `EVIDENCE-review34.md` §5 and `docs/learning/defect-register.md:38` name Review 30 as recording `5f710c5b…`; `reviews/30_implementation.md:132` records `e6b3e1d3…`, which differs from `5f710c5b…` in exactly `.inputs.vehicles` (`4cd6e59b…` vs the accepted `5b29efc7…`) and survives in three artifact trees. The batch's own move (`5f710c5b… → a65058ee…`) is fully explained and changes no placement: 134 leaves, all of them the two mesh input digests and 132 `records[*].evidence.*Triangles` indices.
3. **`docs/reference/pavement-recipe.md:5` quotes a base digest that no longer describes the recipe.** It says "The previous adaptive base also reproduces exactly, SHA-256 `bf44e124…`", while `data/scene/pavement-recipe.json`'s own `inputs.adaptiveBase` is now `c1ce290c5e91f18a…` and `base.bytes` 11,706,564. The sentence was true of the old inputs and is presented beside the updated output digest as current evidence. Would resolve it: mark it as the previous binding's base, or record the new one.
4. **`docs/reference/network-contract.md:71` and `:79` are now false and were not touched.** The live contract still publishes `Terrain SHA-256 fef57d09…; roads SHA-256 3fe126cd…` as "Current full-source evidence", and states that missing terrain removes 47.196 m across the three ways I measured to be covered at 12.19–12.91 m. §7 of this review records the decision to leave the graph alone; the contract's summary of the world is a separate sentence and should be corrected, with the register's staleness entry as the pointer.
5. **The pipeline-level refusal is pinned by a source-text assertion rather than a run.** §4. Acceptable as a bound for this round; worth a case that drives the pipeline's entry over a holed mesh written to a temporary scene root when someone next touches that file.
6. **Budget headroom is thinning.** `pavements.mesh` is now 464,525 of the recipe's 500,000-triangle ceiling (92.9%) and 13,068,136 of 16,000,000 bytes (81.7%). Nothing here is near a limit, but the next ground-support fix has 35,475 triangles of room, and the ceiling's own error message says to investigate the source partition rather than raise it.
7. **Landing this batch on current `main` conflicts in two documents, and both conflicts are the same class.** Merging `main` (`409a57e`) into `d5ead63` on a scratch branch takes every code file cleanly but leaves `docs/devlog/summary.md` and `docs/learning/defect-register.md` conflicted: both are newest-first logs that the batch and the flicker lane each prepended to, so the conflict is `docs/devlog/summary.md:3-7` (the batch's 2026-09-18 bullet against the flicker lane's) and `docs/learning/defect-register.md:3-79` (the markings entry and the terrain entry against the flicker lane's new entries). The resolution is mechanical — keep both lanes' entries, newest first — but it drops a whole defect entry if it is resolved by taking either side, and the batch's markings entry is the one this round's blocking finding asks to be rewritten. The integrator should resolve that file once, with B1 applied, rather than resolving the conflict and then correcting it.

## Verdict on the C5 pavement re-binding: accept

The re-binding is `tools/scene/pavement-recipe.ts:24-25` naming `terrain.mesh c39d49edd405e266…` and `roads.mesh 0d429c3145be6c7b…`, carried into `test/paint-support.test.ts:92-94,115-116`, `docs/reference/pavement-recipe.md:5` and `test/fixtures/paint-road-seams.json`. I accept it, on these measured grounds and no others:

- the two inputs that moved did so **additively** — the terrain by appended apexes and rim triangles with positions as an exact prefix and nothing removed but the reader's own 65 duplicates, the roads by exactly 123 triangles with no old triangle or vertex position lost;
- the pavement delta is **entirely the adaptive base**: `pavement-source.json` is byte-identical, `hero` and `lower` are byte-identical, and the report's own counters account for every triangle (+81 polygons, 2,613 triangles, off-terrain 197 → 116);
- every changed pavement triangle is inside a 15.1 m band around four rim rings, and the 78 triangles whose triangulation changed are replaced within 1 m (46 within 1 cm), so nothing moved where the support did not reach;
- the bounded-strength guarantee the review exists to protect still holds: the source/layer selection did not move, the 16 MB / 500,000-triangle budget is respected, and the standing source-dependent gate — the 100-row paint census with its rise-over-run and source-deviation bounds, the authored diagonal's placement ledger and the eleven re-derived seam probes — **passes on the new bytes** (`npm test` at `d5ead63`: 72 files / 525 tests, 0 failed), which is what re-establishes on the new bytes the protection Review 8 and Review 10 accepted on the old ones.

Acceptance is conditional on the record, not on the data: finding **B1** must be corrected, and findings 1–4 and 7 are owed to the tracked documents. None of them changes a byte under `data/` or in the code, and none of them would be fixed by re-running a data command.

## What I could not check, and why

- **No `data:*` command and no `npm run visual`.** The determinism claim is verified by recomputing the digest from the author's enumerations to the certificate's and the served tree's own values, not by two fresh runs; a `data:scene` run I did not perform could in principle differ from both. And there is **no frame evidence at all in this round**: I did not open a pixel, the certificate (`e6b7b9d8de0faf69`) is knowingly stale against the served scene (`1ebda662…` vs `dfee0f12…`), and the brief's falsifiable prediction — H1's and H2's fans spilling up to 3.25 m and 1.74 m over the surveyed ground, ~42 m² and ~19 m² of invented surface proud of the survey, at azimuths the certified overhead frames already show — is untested here. That re-capture and its native re-inspection remain owed, and the prediction is the first thing it should answer.
- **The two document conflicts were detected, not resolved.** Finding 7 names them and their line ranges; I aborted the scratch merge rather than writing a resolution into the integrator's file. The 74-file / 535-test integration result is unaffected, because no test reads those two documents, but it is a test of the merged *code*, not of the merged tree an integrator would commit.
- **The base drape was not re-run.** The polygon-level mechanism (81 polygons → 2,613 triangles) is read from `pavement-recipe.json`'s own counters, not from an independent rebuild of `buildRoads`/`buildPavementPresentation`; I verified the counters are internally consistent (polygon, triangle, vertex, off-terrain and ledger arithmetic all close) but I did not reproduce them.
- **`data:setup`'s behaviour on a clean checkout is a code-path argument**, from `package.json:27`, `tools/network/build.ts:28` and `tools/scene/hero-paint.ts:30`, not an observed run. The conclusion in B1 turns on those three lines and would be overturned by anything that makes the guard's two digests disagree on a clean setup (a fetch that skips Overpass when a file exists, or a network build that records a digest of something other than the extract).
- **`764c659f…` cannot be explained, only bounded.** No copy survives anywhere I can reach — I hashed every `markings-provenance.json` in the repository and every worktree — so I can say what it was not: it is not the unauthored file (that is 11,223 B, not 14,383), and its size equals the authored one, which is what a rewrite of the same JSON with different extract provenance or a different key order would weigh. The batch records it as unresolved; that is the right disposition, and closing it needs either the file or a re-derivation whose provenance is recorded.
- **The recovery that moved `control-hardware.json` from `e6b3e1d3…` to `5f710c5b…` is inferred**, not observed: the one-leaf difference is `.inputs.vehicles`, and the accepted manifest `5b29efc7…` is the one the 2026-09-16 recovery restored, so the order follows — but I did not watch it.
- **The pixels the pavement change produces** are unmeasured. §2 says the change is confined to a band around four rims far from the crossing, which bounds it geometrically; whether the re-noded street surface reads correctly at 1:1 is a frame question and belongs to the re-capture.

## What would change this verdict

- **A failing source-dependent gate at the batch revision**, or the 100-row census or the seam probes passing only because a pin was moved with them. This round's evidence that they are meaningful is that `test/paint-support.test.ts` refuses a moved input by name — on `main`, whose pins are the pre-batch ones, it fails against the regenerated `data/` with exactly that message (`Paint regression input data/scene/roads.mesh changed from its Review10 pin … to 0d429c31…`), which is the check working.
- **A pavement triangle whose geometry changed outside the band the support change reaches** — anything beyond ~65 m of a rim centroid, or near the crossing where the authored paint and the hero halves live. I found none: every changed triangle is within 15.1 m of a rim ring, and the nearest rims are 0, 1, 2 and 10.
- **An old pavement or road triangle or vertex position absent from the new mesh that is not a re-noded replacement within 1 m.** I found 122 absent pavement vertex positions and zero absent road ones; the 122 come with the 78 replaced triangles and are consistent with re-noding, but a re-noding that moved a surface by more than a centimetre would be a different finding, and my 1 cm count (46 of 78) leaves 32 triangles whose nearest replacement is 1 cm to 1 m away.
- **Evidence that a clean `data:setup` does drop the authored paint**, which would restore B1 as a code finding rather than a record one.
- **The visual re-capture showing the H1 or H2 fan wedges as new surface**, which is a different question — the cap's fidelity — that this review does not settle and the register's own prediction already names.

## Closing statement

Revision reviewed: `d5ead63` (code and gates) plus `0e7d683` (docs), branch `phase2-regen`, off base `4efaea6`, unmerged. `main` advanced from `17052ea` through `1a7b780` to `409a57e` during the round; neither advance touches the batch's files, its pins or the served data, and the integration arm in "What I ran" measures the merged code directly. This review is rebased onto current `main` before it is pushed, so what lands on `main` is this file against `409a57e`.

Everything in §1–§5, §7 and §8 was measured on this machine in this session, and the probes and their raw output are under `artifacts/review-f1-cap/` (`additive.txt`, `reader.txt`, `pavement2.txt`, `pavement3.txt`, `pavement4.txt`, `roads-seams2.txt`, `recipe.txt`, `digest.txt`, `evidence.txt`, `hardware.txt`, `vertices.txt`, `network-osm.txt`, `markings-tail.txt`, `inheritance-mine.txt`, `seams-rerun.txt`). Nothing I could not reproduce is repeated as a claim; where the record states something I measured to be otherwise, the measurement is quoted beside it. No tracked file other than this one was written, and the working tree was clean at `d5ead63` after the gate run.
