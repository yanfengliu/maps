# Finite commuter-male shoe observer

Owner: shoe_contact_observer. Base: d7c8eea43c1cbfc6e85bcd01306fd43898be6b66. This is a CPU-only finite-pose diagnostic instrument, not a controller, source-policy, naturalness or city acceptance change.

Harness: reuse the frozen source-reference loader, complete-weight adapter and renderer selectedHumanParts admission. Read exact GLB triangle indices and retained 807-vertex shoe groups. Pin the source/model/VAT/palette/weights, drawn membership, scale and reference identity before tests. No asset is rewritten. Existing native44 source/packed correspondence provides the method; the new run repeats the relevant measurements rather than inheriting a number.

The observer accepts explicit named support triangles and nearby obstruction triangles. It never picks a sheet. It clips actual sole triangles to normal projections inside the named supports and the strict 15 mm band. It retains normal and vertical residuals, positive-area contact patches, an actual foot-joint load projection, their convex hull, and uncovered sole pieces. Whole-shoe clearance is a separate signed-height check over triangle-pair intersections, including degenerate shoe edges. A vertical support it cannot interpret is unresolved. The committed plant's immutable vertex IDs and positions persist across support changes; drift is Euclidean and strictly below 5 mm. Packing uncertainty produces uncertain results, never a tolerance for sinking.

Expected controls, fixed before measurement: (1) actual left and right near shoes on a broad plane 0.5 mm below the lowest sole point pass the declared rigid/contact model; (2) actual shoes spanning a 0.125 mm opposed-bank slit pass with both banks and uncovered slit retained; (3) one-bank contacts that exclude the load point fail hull support; (4) actual shoe over a finite interior rectangular hole may pass the declared rigid hull while retaining positive uncovered area; (5) support shifted 20 mm down fails contact band; (6) support shifted 20 mm up fails penetration; (7) wrong sheet, (8) cap substitution, (9) variant, (10) scale, and (11) reference identity mismatch are rejected; (12) 6 mm plant translation fails drift even when triangle IDs change; (13) 4 mm translation remains below the drift bound; (14) support inside packing uncertainty reports uncertain; (15) a height step preserves both original heights without interpolated support; its contact outcome is measured and not prespecified as an anatomical pass; (16) a tiny raised support under a shoe-triangle interior penetrates while that triangle's original vertices miss its footprint; (17) changing plant sample membership is rejected; (18) near-zero contact-patch area is uncertain; (19) analytic sloped surface reports different signed normal and vertical residuals and requires normal projection inside; (20) a degenerate projected shoe face crossing support fails clearance. Missing cases stay visible and fail the control runner.

The actual rig controls use one predeclared idle-zero reference, actor scales 1 and 0.9, with the same pose and normalizing transform for every synthetic scene. The 0.5 mm lift is a disclosed synthetic fixture placement and is never a production foot correction. Sole selection is fixed from source geometry before scene outcomes: downward-facing actual shoe triangles in the idle-zero reference, including their full original vertex IDs. The complete shoe is retained for penetration checks. No favorable phase search or patch enlargement is permitted. Selected scale is applied exactly once; 15/5 mm thresholds remain world metres.

Run focused unit and actual-source controls; record complete denominators and exact input/output hashes. No full gates, browser, GPU, server, fetch, install, code commit or push. The root owns independent review and delivery. Preserve this plan and failed runs unchanged with the final handoff.
