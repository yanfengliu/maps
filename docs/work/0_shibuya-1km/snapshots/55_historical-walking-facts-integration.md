# Historical-facts primary integration — Review 55 target

Applied root-approved preparation `dec61b39885d5b4a1bc9387ee8cce0ec79e249ed7c7e50a82caa492486c1fceb` to clean primary `main` at `82ee91772f9533ab3e8661f73af2eb4eb1e2bd3c`. This is an uncommitted 13-path target. Root accepts the exact Review 53 offline component; independent integration Review 55 and final staged acceptance remain pending. No staging, commit or push occurred.

## Scope and immutable history

The four source/test hashes are unchanged from accepted v2. Exact Review 52 plus two original snapshots and Review 53 plus its snapshot preserve both rejected and accepted targets. Shared changes are plan current status, summary, the F29 gate-proof section and one detailed devlog. Only plan/summary/detailed devlog changed after gates, as root authorized; `post-gate-docs.json` binds before/after, and `applied-gate-target/` retains all13 gate-time files. All86 older reviews/snapshots remain exact. Historical plan body remains SHA `30b5ba3b571debac1012657ed6c47ead5ac90a6a2b5a7fcf4313924882604fde`; plan is 259171 B, below256KiB. No source-pair adoption, current support policy, walking runtime, data writer, guard relaxation or dependency change was added.

Q7 records root's separate modeled v4 PID-reuse rejection with final Review54 reporting pending and native capture still held; no launcher source/report was imported. The top-level inherited `handoff.md` structural refusal remains unchanged.

## Actual verification

Node v24.12.0. Four fresh primary commands completed with actual exit0 and retained stdout/stderr: build2.81s, types3.22s, fullunit19.26s (**82 files / 685 tests**), high-threshold audit7.68s. Audit reports the same two moderate Vitest advisories. The build retains its existing chunk-size warning. No fix, dependency install or validation relaxation was attempted.

Before rebuilding, the entire visual tree and complete dist were preserved as **109 SHA-verified files / 71,137,154 B** under `previous-visual-8d70420e07afbfa7/`, without traversing reparse points. The actual dist roster is four files: the certificate's three plus the existing JS source map. Fresh build reproduces allfour exactly. The certified three remain bound to source revision `05ad1a4830e5876be03116e468199370504a6693` and certificate `8d70420e07afbfa7`, SHA `9cdb592428903b0d1fdff4daf5cec4c375a1ad21cf9e1c122a679ac55722f611`.

The shipped read-only digest functions independently read actual primary served inputs before/after:117files/301,782,754B/digest`dfee0f12bb2e87c1defa7c41c0400e0df4f98ab829fd346d5d689da0c406873c`, and harness24files/219,520B/digest`45aeaf15464fbc2a34e0252f3c06b6e8bd44969103aeb65bdd7d21dab53d0593`. Both actual HTML module roots produce an88-file closure with no candidate reach, unresolved local module or nonliteral import. Its exact primary bytes and package/lock/config hashes remain unchanged. All44 original1280x720 PNGs and three complete lifecycle record bytes match the preserved originals. No certificate write, GPU, browser, server or fresh capture occurred. The transferred result retains Review47/root's old agents-OFF still-image bound; no new visual inspection, motion/flicker, population, support, source or performance acceptance is claimed.

## Instrument corrections and cleanup

The first preservation instrument stopped after copy because it incorrectly asserted three total dist files. `apply-preserve.py` and `preparation-correction.json` retain that assumption; read-only `finish-preservation.py` verified the realfour without recopy or source edits. The first raw closure comparison also stopped because a fresh worktree uses different pre-existing CRLF representations in 58 files. The exact frozen parser then ran against primary itself. `closure-eol-before/after.json` explicitly verifies normalized equality for those representations while the actual88primary raw hashes stay fixed; no file was normalized. These stops preceded all gates and are measurement corrections, not code failures or relaxed build equality.

The initial restricted CIM scan was denied, so its zero arrays are explicitly unavailable evidence. The subsequently available read-only scan completed and found zero known runner/command IDs, current direct descendants, unique task-marker processes and port4319listeners. Every gate command was waited to completion; no browser/server was launched or process killed. This finite scan is not an OS job-object or arbitrary escaped-process proof. Receipts are `process-cleanup.json` and the retained invalid first scan.

`cleanup-plan.json` inventories four potentially removable facts trees, with no deletion. The original `historical-walking-facts/wt` is deliberately retained because the unresolved support study reads its artifact and all60frozen files at their original paths. Source-lineage/support/walking/launcher/stall/route/motion trees remain untouched. Later preservation/removal needs refreshed reader checks and explicit reviewer release.

## Handoff

`freeze-post-gates.json` binds these13candidate paths and all retained milestone evidence. `candidate-post-gates.patch` is complete and reverse-checks against primary. Source4 and importedhistory5 hashes are unchanged; secret-pattern, file-size and CR-aware diff checks pass. No raw task output is proposed for Git. `gates/results.json`, `transfer-before/after.json`, `preservation.json`, actual logs and both exact closure instruments provide the audit trail. Primary is frozen for Review55; root owns final disposition and delivery.
