# Recorded turn visual attempt: instrument refusal before capture

The authorized single capture attempt produced **0/36 frames**. It stopped before opening the diagnostic page because the launcher could not round-trip the browser creation identity through Windows PowerShell JSON. There is no native appearance inspection or renderer acceptance from this run. No retry or launcher repair was made.

The unchanged turn target is candidate `4afb9a13f2b7c5ffbfee8bfdbafbbc65f26d2247071632357a2df34d151a9649`, based on `27c71865bef31a7c9dcc622db16e6721be839dc3`. The exact prelaunch input freeze is `0daa924e7d41e0ad9f91ec4b2100c7da1049391102b75b23165ca0a99587a75f`. A previous prelaunch freeze is retained; before launch, only an empty data favicon was added to the diagnostic HTML/generator so an unrelated favicon request would not enter the console-error verdict. Neither prelaunch version was used for an earlier browser attempt.

## What ran

The exporter reused the original matching full-mesh palette and prepared six-influence geometry, with the selected GLB's unchanged material properties. It reconstructed the exact positive90/negative90 recorded rows through scenePose and made zero controller updates. All 242 retained poses and 5,092,648 complete drawn-vertex comparisons ran. Maximum packed error was 1.572777e-7 m; including the browser geometry's final Float32 rounding, maximum error was 2.078371e-7 m, below the unchanged 1e-5 m bound. Actual body XYZ/yaw, source/physical distance, source phase and feet came from the frozen rows without offsets or picture-specific corrections. Existing wrong-reference and single-calf controls ran and remained red/green as expected. These are geometric export results, not rendered results.

The isolated Vite build completed in 796 ms. Hardware identity returned NVIDIA GeForce RTX 4090, driver 616.64. A single configured Chromium process was launched headlessly with D3D11 flags and a dedicated profile. There was no page, browser renderer-string observation or captured PNG. The manifest consequently has complete false and viewerDisposed false; the viewer was never created.

## Exact failure and boundary

The launcher invokes `powershell.exe`, whose ConvertTo-Json serializes DateTime as `/Date(1789869845656)/`. The tool-level read-only preflight had used the host's PowerShell, which emitted ISO dates, so observing access alone did not verify this serialization boundary. The exact-identity classifier accepts DateTimeOffset-compatible creation times and refused this Windows PowerShell string at its first owned-child classification. The retained diagnostic failure is `Cannot convert value "/Date(1789869845656)/" to type "System.DateTimeOffset"`.

The browser root's preliminary JavaScript `Date.parse(value) < launchTime - 1000` comparison also failed to reject a non-finite parse: NaN makes that comparison false. This is an instrument defect, not permission to weaken identity checks. The subsequent classifier stopped the attempt. The smallest separately reviewed repair would serialize CreationDate explicitly as UTC ISO with its available precision, require a finite parse before comparison, and test exact round-trip identity plus invalid-date rejection using the actual configured powershell.exe without launching a browser. No such repair/test or capture rerun was performed in this attempt.

## Cleanup

The finally path closed the retained browserServer handle and the in-process HTTP server; the dedicated profile was removed. Its own last-child inventory was unverifiable for the same serialization reason, so the manifest remains incomplete even though it recorded no surviving known root. No shared browser was stopped and no historical parent PID was used to infer ownership.

A separate scoped read-only CIM check at 2026-09-20T02:05:17.6240777Z found zero processes from the exact configured Chromium directory, with the task's exact profile, or at the recorded browser/launcher PIDs. The profile was absent. Its known current observer PID was excluded because the observer's own command text contains the profile search literal; the first check showing that self-match is retained beside the corrected absence check. Neither check stopped a process. The GPU lease was released after this absence check.

## Retained evidence and limits

The input freeze, copied/adapted harness, source exporter, prepared pose bytes, build, stdout/stderr, incomplete manifest and both cleanup observations are retained and hashed. The approved 36 native-frame inspection remains entirely incomplete. The successful separate pending-withdrawal check does not change this result. There is no city, production wiring, source-policy, shader-normal, full-motion, walking-resumption or performance acceptance; no model, controller, original freeze, production source/test/doc or dependency was changed.
