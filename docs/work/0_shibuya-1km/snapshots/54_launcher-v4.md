# Launcher v4: joined public tree disposal

The first frozen candidate passes all 33 planned lifecycle control verdicts (the original 22 replay plus 11 v4 arms) and six additional descendant/observation controls. This is CPU synthetic evidence, pending independent Review54. It establishes no native Windows cleanup or capture success. All old failures remain preserved. No candidate repair or rerun was needed.

Owner: walking_contract_review. Scope: ignored launcher lifecycle/resource receipts only in `artifacts/walking-path-v2`, base `e55f1c76dae791563b64e3e3c7a5db9b1c9f5231`; primary `82ee91772f9533ab3e8661f73af2eb4eb1e2bd3c` contains later docs only. No production, dependency, controller, geometry, helper or frame-adapter change. No actual browser, server, GPU, native stop, network or full gate was run. The original 36-frame block remains byte-identical; the future 44-frame adapter is separate.

## Exact target

- `candidate/capture.mjs`: `feb9825c2ebbd8b10aa078ed4de172c2672077ed1351ae5ae21f47e6e2f2e06a` (18,349 bytes).
- `candidate/freeze-before-scoring.json`: `05488d763fc47c09121b3aeaca697cd0d6de015e3899bc452b410abd62476898`, written before any candidate scoring. It binds the launcher and four unchanged helpers; syntax check exited 0.
- `candidate.patch`: `36519af242cda3420fe7950a1ae0d226f3d10d9c941b703f34373e0ae6e01f04`, against frozen v3 `71acc4f9f81a7de7ccaf10cb8ce673af3632c1dc8a08de02c30fe1810e4a78bb`.
- `checks.json`: `5f4a1ed9b76aae61c12a2807d5a2a1c5778f923c083520fa5a2f55f3732587d1`; individual results, counts, statuses and unchanged-input checks.
- `v4-controls.patch`: `700b9677096798c6db8c328603e03fc7ae6a9112e3735e44973707bb618795d3`; harness changes are separate from the candidate patch.

## Mechanism and ownership boundary

Fresh exact-identity observations precede retained BrowserServer close and escalation, including cancelled work. The retained BrowserServer public `close()` and `kill()` own disposal of their launched process tree and profile. V4 removes the direct parent `child.kill()` that invalidated Playwright's tree cleanup in v3. All late acquisitions are retained, work is joined, closure and escalation promises are joined, and final evidence follows disposal. Pending HTTP listen must settle before final close, so an early close cannot falsely mark a later listener disposed.

The 120-second deadline limits work, not process-return time. A pending build or API/cleanup operation may exceed it; the pending receipt remains non-final, no later work acquisition starts after cancellation, and finalization waits for required settlement. The seven-second close budget starts public tree escalation but does not pretend that settlement is complete at seven seconds. An API that never settles can keep this joined scope pending. No new cancellable helper or hard process-return claim is introduced.

Missing CIM observation, an incomplete identity row or a mismatched parent creation timestamp makes observation `unknown`, cleanup `unverified`, exit status nonzero and capture incomplete. It does not revoke the retained public BrowserServer resource handle or excuse leaving its acquired resources undrained. The public API is still awaited. That API uses Playwright's own process ownership; this experiment does not prove native OS behavior when a parent exits or a PID is reused. It does not use failed observation to authorize a broad PID fallback. The separate existing exact-identity fallback remains limited to already admitted rows and rechecks creation identity in the unchanged PowerShell helper immediately before a stop.

`remainingKnown` is explicitly the admitted-identity subset, not a global descendant census. An empty array alone cannot establish complete coverage. Successful observation is named `observed-within-admitted-live-ancestry`; successful cleanup is `observed-clean-within-owned-tree-contract`. The model's late child can be absent from this known subset yet be removed by public tree disposal. That is the API contract being tested, not evidence that CIM observed every child.

## Instrument and controls

The admitted instrument freeze remains `154e9d833f36cdd21d713936f9f05e2325a0a3cd33da766b3851464fd538e12d` (48 unchanged members). Its factory `796b75e01960a2da5f1127d1a8ea28acf707effd9df81346ed8e813b1442f914` extracts the installed Playwright launch/close/kill mapping and executes it with fake processes, clock and profile disposal. No real taskkill, process stop or browser launch occurs. The profile join and a child born after the last inventory distinguish the reviewed defect. Installed source and the independently prepared alternative mechanism are pinned by the instrument. Runner reuse remains viable but deferred.

`controls/baseline/launcher-v3/control-final.mjs` is the exact original 22-arm harness. `controls/v4/control.mjs` runs the 11 candidate arms: delayed build, listen, launch, context and page; rejected build and launch; delayed browser close resolve/reject; delayed HTTP close resolve; and normal. Run with `node --experimental-vm-modules <entry>`. `lifecycle-commands.json` and stdout/stderr retain both invocations and exit 0. The original 22 includes eight expected old-v2 red arms; 33/33 therefore means control verdicts, not 33 successful captures. Only the normal v4 arm completes a 36-shot synthetic capture. The ten other v4 arms stay incomplete with nonzero status and no model resources left. Generic injected close rejection remains synthetic stress, not an asserted OS EPERM or real HTTP defect.

`controls/descendant/control.mjs` executes six additional arms and retains every result in `controls/descendant/results/`. Both child schedules dispose the modeled descendant through the public tree API. In the after-last-inventory case, child birth is event 34 after inventory event 29 contained only parent 222; child 333 is never added to known identities. Inventory refusal, incomplete identity and creation-date mismatch all retain unknown/unverified outcomes while disposing the modeled owned tree. The full-36 observation-refusal arm produces all 36 synthetic frames but remains incomplete. All six leave foreign process 444 alive, use zero direct parent kills and dispose every modeled task resource. These six are additional to the fixed 33 lifecycle denominator.

The filename `cancel-context-recycled-identity.json` describes a synthetic observer returning the same PID with a creation timestamp shifted by 1 ms. It does not model a real Windows PID being recycled beneath Playwright's retained process. Foreign/recycled PID classification is separately pinned by exact prior Review50 evidence under `references/review50/`: 11 pure classifier cases include recycled parent, submillisecond mismatch, foreign executable prefix and unallowed identity refusal. The earlier actual PowerShell identity round-trip also tested finite UTC date transport (13 invalid, 3 valid dates). Those results were not rerun in this round. `references/identity-provenance.json` verifies the unchanged helper bytes and exact prior freeze membership; this narrow transfer must not be read as fresh OS cleanup evidence.

The instrument's unchanged old-v3 red schedules remain `instrument/old-v3-red/cancel-context-parent-only-escalation.json` (`fbfe7a709b1d6fa6f1a1f5b8e1b707213e603de189a420048da4706305b620a1`) and `cancel-context-after-last-inventory.json` (`8350c63d749c97a43fda9df19264f05c7e2bcf1707ea7f957d35fb17f754f32e`). Both expose the surviving child after direct parent kill. No failed evidence was replaced.

## Preservation and remaining limits

The final checks rehash all 48 admitted instrument members, all 93 v3 members, all 27 F26/launcher-v2 repair members and all 41 F27 gait members. Four helpers remain byte-identical. The original shot block hash remains `0740096d5e2a9d31168d5baf1d884b8cc3118398b3d58b27dbcb58bddfafa56b`. The top-level freeze binds this handoff, full candidate, patches, scripts, results and transferred references. Retain this ignored evidence and its worktree until root disposition; nothing is staged or committed.

Independent Review54, native owned-process cleanup, an accepted minimal 44-frame adapter, 242-pose precheck and explicit GPU lease remain required before the next real diagnostic. Synthetic green does not establish gait naturalness, city support, source policy, performance or production readiness. No native task resource was created by this round, so no native cleanup was needed.
