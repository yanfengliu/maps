# Live pending-turn withdrawal discrimination

The separately authorized harness-only check passes on unchanged candidate `4afb9a13f2b7c5ffbfee8bfdbafbbc65f26d2247071632357a2df34d151a9649`, based on `27c71865bef31a7c9dcc622db16e6721be839dc3`. Both signs execute the missing live waiting→withdrawal transition. The prior one-attempt report, raw local pass fields and freeze remain unchanged; this is new evidence for that specific missing case, not a reinterpretation of the old initialization-only controls.

Each of six arms executes all 121/121 poses and 120 fixed update/pose callbacks at 60 Hz: 726/726 observed poses and 720 updates total. Two are delayed references, two are withdrawals, and two are harness-only negative controls that retain a withdrawn request in the delivered input. There is one attempt, no controller change and no retry.

At tick 0, init still does not create turnControl. At tick 1 (0.016667 s), a live ±90° request with actual yaw 0 creates status waiting, cancelled false and zero foot events. Reference tick 2 retains it at yaw 0. Reference tick 3 (0.05 s) supplies the first actual ±3° body increment and observes the first departure. The reference reaches ±90° at tick 32 and both feet align at tick 63 (1.05 s), with three liftoffs/plants each, zero final foot heading error and at most 2.8e-17 m anchor residual.

Each withdrawal arm removes the request at tick 2 (0.033333 s), after observed live waiting and exactly one tick before its sign's measured reference departure. It observes cancelled true, status aligned, targetYaw 0 and no pending request. That remains true through tick 120. All foot anchors/headings remain exactly equal to their initial values; there are no foot events or foot-admission calls. Body XYZ/yaw, speed, source distance and physical distance remain zero throughout.

The ignored-withdrawal mutant changes only the harness-delivered request after tick 2. The command remains withdrawn and all body/time/source/physical inputs stay identical. Both mutants fail specifically at tick 2 with withdrawal-not-settled: status waiting, cancelled false, targetYaw ±π/2 and pending true. Each still executes 121/121 poses. This control would pass a check of stationary body/feet alone, so the observed red distinguishes cancellation semantics from absence of travel.

The references remain below 0.081525 mm maximum planted horizontal drift over the original 317 drawn sole vertices per foot. Withdrawal and mutant arms have zero measured planted drift. All six have zero absolute planted-foot minimum Y and zero measured whole-shoe penetration. Every pose checks both complete 807-vertex shoes. The unchanged full-mesh check compares 883,848 drawn vertices at initial, live-wait, withdrawal, departure, contact and retained endpoint rows; maximum packed correspondence error is 1.524739e-7 m. The same source-reference controls run: no out-of-quantization components for idle/walk phase zero, wrong-bind palette 0.517492 m and 41,797 failed components, and the single-calf perturbation moves 1,223 vertices while packed error remains below 1.505e-7 m.

This closes only the named finite pending-withdrawal gap on the explicit flat-support fixture, at one rig, scale and initial phase. Walking resumption, city support, arbitrary cancellations, appearance, general controller acceptance and performance remain untested. There was no browser/GPU, source-data writer, production wiring, model/palette/threshold change or commit. Execution took about 1.75 seconds; this is a resource observation, not a benchmark.
