# Prepared terminal and capture integration

Prepared in `artifacts/population-capture-integration/wt`, branch `codex/population-capture-integration`, from `27c71865bef31a7c9dcc622db16e6721be839dc3`. Root accepted the scoped Review 41 lease and Review 43 capture components. Root acceptance of this combined freeze is still required before primary mutation or any gate. No code commit, primary write, browser, server, data writer or product gate ran during this preparation.

- `combined.patch` SHA-256: `0ad68eb7d857b3b65ef2fd9096398159a9eabd69821d5528fa4ac7ffaa059f5e`.
- `freeze.json` SHA-256: `9be56b16cfaa53f1e6ea96e9819af0fed40d91aee80478765865235854f7a84a`.
- Canonical plan SHA-256: `cbcafd622f625cb8ff1a66ea919ff64dae09ea8bf48fd0be32fbb7f1c1ec11a3`.

The 35-file freeze contains 16 non-document files. Every one matches the accepted source/test/tool hash from lease patch `b41f3dd3fdc9a3d4ee55f3e86e9bfccf78ee0017171f9d26fd62063cf9358bb9` or capture patch `d0e7972ec58c02cc9ef13a0bea17c081daab73cf05eb6a02ef060598ff95e1d0`. There are no overlapping non-document paths and no experimental walking, earlier solver/lateral or Q4 code. The freeze records each source origin and digest; author manifests and every frozen author file were rechecked unchanged.

Twelve review/snapshot files were copied exactly: normalized Review 39 plus its original authored snapshot; Review 40 plus two design/result targets; Review 41 plus the lease author report; Review 42 plus its original Q3 record; and Review 43 plus the Q3 v2 record and repair handoff. Their original paths and hashes are in `input-manifests.json` and the freeze. The old rejected reviews and targets remain explicit; no attributed report was edited.

Documentation reconciliation used Q3's current-main base and appended only lease's verified older-base additions. The shared summary, defect register and gate proofs retain all newer primary additions, checked as exact blocks against the changes between `244632c` and `27c7186`. No old-base full document replaced newer Q2 history. The two separate detailed devlogs remain. Canonical status is updated only in the plan under root disposition: lease component accepted within its finite bounds, Q3 capture component accepted within its finite bounds, no stall/AOI egress/flicker completion, walking pure candidate still unsupported, and Q4 preservation guard separate with source policy unchanged.

Root directed one integration-only path mapping. The fresh top-level `docs/work/0_shibuya-1km/q3-motion-capture.md` is not introduced. Its exact authored bytes instead remain at `snapshots/q3-motion-capture-review43.md`, SHA-256 `71e5cbd57aae23a02c6f09bf787dd72cab31650bacacde04c9480316053dad08`. The newly added plan, devlog and policy links point to that snapshot. Review 42's original snapshot remains separately recoverable. No author or reviewer worktree was changed by this mapping.

`verification.json` records the source/review checks, newer-primary block checks, named credential-pattern scan with no matches, and size/diff checks. All 35 files are below 256 KiB; the largest is the existing canonical plan at 254,189 bytes. `git diff --check` passes. The common work-docs structure check still exits 1 at inherited top-level `handoff.md`; its exact output and status are retained in `work-docs-check.log` and `.json`. No structural pass is claimed and unrelated historical documents were not silently moved. Source hashes establish component transfer, not combined runtime verification.

The earlier `combined-pre-review43.patch` and `freeze-pre-review43.json` are retained only as superseded preparation evidence. Apply the final freeze by exact bytes and recheck hashes; a successful Git patch application can convert LF to CRLF and is not proof of byte identity on this machine.

## Gate preparation, not executed

Root selected the existing primary `artifacts/certificate-primary/gates/run-owned-visual.ps1`. Its inspected SHA-256 is `900522c109029f6e30551d7bdaabb56e4adc6ad410eb0b3e7c5d389f10326ad0`. It launches the normal `npm run visual` with hidden windows, retains verified live process handles and executable/creation identities, and cleans only those handles in finally. It does not broadly terminate browsers by PID or image name. Its exact bytes must be checked again before use.

Before any build or visual reset, preserve current certificate `d11360e0649ffb15` and the entire current `artifacts/visual` tree at a new ignored milestone path. Check all 44 certified PNG hashes against `complete.json` before and after copying; also compare the complete copied file list and hashes. Preserve the currently bound `dist` bytes before a build overwrites them. Refuse an existing destination, and do not replace the earlier preserved `aa2e07e9902a5dc9` evidence. This preparation has made no backup and deleted no certificate.

After root authorizes exact primary application, run build, types, the full unit suite and audit with separate real stdout/stderr and captured process exit codes. Do not derive success from a trailing output command. Run only one visual GPU lane through the inspected owned wrapper from the primary checkout, then require its zero exit, the new certificate's bindings, all owned runtime exits and no owned listener. Inspect all 44 new frames individually at native size and retain exact image digests. The lease source changes the app bundle, so the previous visual gate is not transferable merely because capture tools are outside that bundle. Final combined acceptance, commit, main integration, push and resource removal remain root-owned steps.
