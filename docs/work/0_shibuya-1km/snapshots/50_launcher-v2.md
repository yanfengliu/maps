# Walking v2 bounded repair handoff

Base: `e55f1c76dae791563b64e3e3c7a5db9b1c9f5231`, branch `codex/walking-path-v2`, isolated `artifacts/walking-path-v2`. The four pure files were copied byte-for-byte from the rejected original before editing. Original pure/gait/cancellation/failed-visual freezes remain untouched. No production wiring, data/dependency changes, commit, browser, GPU or full gate ran here.

F26 is repaired in the pure advancer by computing the lower inverse of the path's monotone source mapping. A hold at route source station 2 on the analytic two-occurrence offset join maps to physical station 2, the beginning of its one-metre constant-source connector. It no longer permits the connector to consume distance through physical station 3. The same hold applied after an agent is already inside/past that interval stops it at its current position without rewinding. A reached source hold conservatively stops further step progress, including a queued turn; rectangle sweeps still independently bound motion before that hold.

The exact old source, with the new regression cases, fails four of 24 cases: starts before/on/inside the plateau move to physical 3 instead of 2/2/2.4, and release reports only 2 m remaining instead of the correct 3 m. The repaired source passes 24/24 in 38 ms, and the complete TypeScript check passes. The extra controls cover an already-past start, repeated holding, an earlier rectangle collision at physical 1.4 m, and authority release through the remaining join. The original rotation-sweep and actual-arc controls remain unchanged. The first sandboxed test invocation failed while resolving configuration; its startup error is retained separately and is not counted as semantic red evidence.

The F27 ordered mechanism is written in `artifacts/gait-v2/ORDERED-TRANSITION-PLAN.md` and accepted by the root for implementation after Review47. No gait controller edit has occurred. An update beginning airborne will only advance/land that flight under its actual retarget/freeze policy, then a later planted-begin update can admit a fresh departure. The forthcoming proposal must describe the actual segment/coefficients used, not merely rename an advance carrying a mixed flight.

The copied ignored launcher now explicitly serializes PowerShell CreationDate as UTC ISO, preserving available precision. JavaScript validates ISO shape, finite parse and calendar normalization before any creation-time comparison or stored identity. The browser-free probe invokes actual `powershell.exe`: old transport/classifier round-trip exits 1; the old NaN lower-bound comparison evaluates false; the new exact-identity round-trip succeeds; all seven invalid-date cases and a recycled creation identity are rejected. The check observes only its own Node identity and performs no process-stop action. These checks do not validate a GPU capture or its full cleanup lifecycle. The v2 renderer inputs have not been prepared or launched.

Only `walking-path.ts` and its test changed relative to the copied pure candidate; the surface and corridor modules remain exact. The constructor/source-policy/locomotion limits in the original handoff still apply. New source-hold tests plus the timestamp controls are bounded repair evidence, not production path acceptance. Independent review is required before integration; the v2 files remain unmerged in this worktree while the author performs the separately assigned Review47.
