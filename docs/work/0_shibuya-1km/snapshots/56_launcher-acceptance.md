# Fixed launcher cleanup acceptance before option comparison

Design only. Preserve frozen v3 `71acc4f9f81a7de7ccaf10cb8ce673af3632c1dc8a08de02c30fe1810e4a78bb` and freeze `4f0b666d7119155c1ca757762e3b59a168cc61f9e3b6c1c5c76d9a032b17d5e3`. No new capture, controller change or launcher implementation is authorized by this document. Root owns the next decision and will arrange an independent bounded option judgment.

The fixed material reproduction is Review51's `artifacts/launcher-v3-review/wt/artifacts/review51/independent/cancel-context-parent-only-escalation.json`. A new Chromium child 333 exists after the first inventory and before cancellation. Close escalation kills retained parent 222 only. That exit makes installed Playwright skip its own tree kill; cancellation skips the later owned inventory. The module returns a failed capture manifest, with `remainingOwned: []` for its incomplete known subset while child333 remains live. This is a cleanup failure, not a false successful capture result.

Acceptance retains every existing v3 joined-scope lifecycle/date/identity check. The 22 browser-free arms must still execute and preserve their declared denominators, original red outputs and failure statuses. Delayed build/listen/launch/context/page must retain late handles before checking cancellation, launch no later work after cancellation, join acquisition and disposal, and keep unresolved drainage explicitly non-final. A pre-listening close cannot mark a pending HTTP listen disposed. All existing finite ISO and exact creation-identity checks remain unchanged. No threshold, timer, case or assertion is weakened.

The additional material check must run the exact old and candidate ESM lifecycle bodies against Review51's child-creation/escalation schedule. Old v3 must reproduce the remaining child. Candidate cleanup must obtain a fresh owned inventory before any browser close/kill, retain exact descendant creation identities, preserve the installed owned-browser tree-disposal route, and account for cancellation-time inventory without treating known-subset emptiness as complete observation. Normal and cancelled final records must distinguish successful complete inventory from unavailable/refused observation. All required cleanup work must be joined before any final result; a pending operation has only a non-final pending record.

After independent CPU acceptance and separate root authorization, the eventual native diagnostic must capture its full frozen 44-frame denominator and verify a complete clean owned inventory, profile removal and closed server. This design does not grant that authorization or claim that browser-free models prove real GPU/OS cleanup.

Disqualifiers: a timer or no-op hides failure; the defect passes only because the delayed phase or required capture did not execute; a child survives its parent; only known PIDs are checked while unobserved ownership is called clear; acquisition/disposal promises are abandoned; a missing final manifest is reported as success; cleanup failure status is lost; a broad kill or foreign/recycled PID is admitted; exact creation identity or the capture denominator is weakened; an unbounded drain is described as a hard 120-second process-return bound; a new framework relocates the same unresolved lifecycle problem.

Review51's synthetic child.kill-throws fixture is generic exception stress. Node24's observed EPERM path emits an error/returns false with Playwright's listener; the throw fixture is not a second reproduced Windows defect. Arbitrary HTTP-close rejection likewise is not asserted as the installed API's behavior. Preserve these stress results and their honest bounds; do not expand the required repair from their labels.

Compare two separate mechanisms before choosing: (A) the existing isolated BrowserServer with its installed tree disposal, fresh pre-close ownership observation and joined final inventory; (B) reuse of the shipped Playwright fixture/runner cleanup. Inspect each actual dependency/fixture rather than assuming guarantees. Record concrete reuse gaps; do not declare either strategy impossible without evidence. No code from one option is mixed into the other during this design review.
