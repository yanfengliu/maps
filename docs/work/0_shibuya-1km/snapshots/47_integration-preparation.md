# Q4 preservation integration preparation

Prepared from main `e55f1c76dae791563b64e3e3c7a5db9b1c9f5231` in `artifacts/rebuild-preservation-integration/wt`, branch `codex/rebuild-preservation-integration`. Primary remains clean and unchanged. This handoff requests root inspection of the exact integration; it does not claim application, integration gates or Review 47 acceptance.

The 11-file candidate is `combined.patch`, SHA-256 `7819b25cf1e84e7b015ca6d926ddd60f91edfbfb5a0261100cd1dc2b6cf6abe6`, and `freeze.json`, SHA-256 `cf44a8ea83b1054b89f271c2ac510b63de9a3ff7e374ad09ab5694a9bc110136`. Apply from the byte-checked frozen files, then verify every destination hash. Git patch materialization can change line endings on this checkout; a successful patch command is not a byte-transfer proof.

Exactly three executable/test files match the independently accepted author target: `tools/scene/build.ts` (`e94a8e9bea1f567df6d10a03ccc66ad44b2e77a2d20d2337010e68336d9d24f5`), `tools/scene/rebuild-preservation.ts` (`6b01b2bb5ea70c8357c377e403eb10837b6b9611bf49eba64e3a166c2f97430a`) and `test/scene-rebuild-preservation.test.ts` (`149ca2417991e718fa5178972f47e17996fad998a3b5afd749a26cc5507cc556`). No source correction was introduced during integration. The exact authored detailed devlog is also copied without rewriting its historical pending-review statements.

The four shared documents each receive only the author's one additive block from its older base: local policy, defect register, gate proofs and summary. Removing each inserted block reconstructs the complete current primary file byte-for-byte, so no newer terminal/capture history is overwritten. The fifth shared document, the canonical plan, updates only its current queue: it records the delivered terminal/capture commit and verified archive cleanup, the accepted Q4 component, and the still-pending Q4 integration/gate boundary. Everything from `## Problem and outcome` onward remains byte-exact. `canonical-plan.diff` carries that small status delta separately.

Review 45 and its authored snapshot are copied exactly: report SHA-256 `9370573dd2fe25fc6411edd8b9b5a6be6ddfce230084e968dc3575bfb4c8006b`, snapshot SHA-256 `e9b33d5321a8fb9ffb6034ab1c49ab14b8ed2847205e2a52f40cd734c9d19df9`. The report's fresh Claude authentication abstention remains unchanged. Reviews 39–44 and their snapshots are untouched. There is no new top-level work-status document.

The component evidence remains scoped: 27 focused tests and types pass; both destructive-order controls reproduce 23 failures and three passes before exact restoration; the independent real-primary preflight refuses the authored markings without changing either hash; actual producer output, ten additional private refusals and a private shipped-entrypoint check pass their intended contracts. None of this establishes a successful city rebuild, source authentication, source-vintage acceptance or whole-rebuild transactionality.

Preparation checks: all 11 files are text below 256 KiB (canonical plan 257,422 bytes); named private-key/OpenAI/GitHub/AWS token-pattern scans find zero matches; the diff check recognizing existing CRLF terminators passes; only the three expected non-document files differ. The source author freeze and all eight author files were rechecked before transfer. The inherited top-level `handoff.md` work-docs issue remains disclosed; no structural-check pass is claimed.

No build, full unit suite, audit, visual gate, data writer or source fetch ran during preparation. Root must first accept the freeze. After exact primary application, preserve certificate `7f68c3883cc3f1ce` and its visual/build bytes before any build/reset, then run all five gates with the previously reviewed owned launcher only after the walking capture releases its GPU lease. Review 47 and native inspection of all 44 fresh certified frames precede delivery. Existing source/provenance guards are not bypassed and actual primary `data:scene` is not run.
