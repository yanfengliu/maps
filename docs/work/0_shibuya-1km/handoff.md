# Handoff — the Shibuya 1km deliverable, 2026-09-18

This note is the session-to-session delta. The canonical status is [`plan.md`](plan.md) — its closing dated block, "Coordinator status, 2026-09-18 — the cap is merged, the certificate is re-issued, and the eight-lane re-inspection finds the holes closed and the spill prediction not reading", and the acceptance criteria status subsection are current, and the dated blocks above that one are the history of how the finding got here. The standing defect list is [`../../learning/defect-register.md`](../../learning/defect-register.md) and the prioritized work list is [`quality-register.md`](quality-register.md). Read those after this note.

## 0. Where the tree is, and what a reader would otherwise get wrong

`origin/main` is `2f276a5`, and this note is the commit directly on top of it. The primary is clean and so is this note's worktree: `git diff HEAD` is empty in both.

**The terrain cap is on `main` and the certificate describes the capped scene, so the previous handoff's "knowingly stale" state is over.** `400065a` merges `phase2-regen` (five commits off `4efaea6`), review 34 is landed as [`reviews/34_implementation.md`](reviews/34_implementation.md) with its one blocking finding in the record rather than in the code and applied, and the old branches `phase2-regen`, `worker/f1-terrain-cap` and `phase-b-terrain-cap` are gone. Read the criteria list and the dated blocks under it as history; the closing block is the current state.

Uncommitted work: none. Live trees: the primary on `main` at `2f276a5`, and `artifacts/docs-final2/wt` on `worker/record-reinspection`, this note's own worktree, removed once its branch merges. Nothing in it junctions `data/` or `node_modules/` — this pass is documentation only, ran no gate, no build and no `data:*` command, and never needed either. `worker/flicker-review` is merged (`1a7eb51`) and its branch still exists on disk; that is housekeeping, not unlanded work.

## 1. What is verified, and by which record

**The certificate binds the capped scene, and all 44 frames verify.** `artifacts/visual/complete.json`, runId **`aa2e07e9902a5dc9`**, `startedAt` `2026-09-18T04:43:05.663Z` → `completedAt` `04:47:44.715Z`, `certifiedAt` `04:47:44.725Z`, so the gate took **279 s** for the 44 frames; `sceneTreeDigest dfee0f12bb2e87c1defa7c41c0400e0df4f98ab829fd346d5d689da0c406873c` over **117 files / 301,782,754 B**; build `dist/index.html a73d718a…`, `dist/assets/index-DRmtpVMl.js c890299c…` and `dist/assets/index-dvQKBZaf.css 11dd032c…` (a capital K, not `dvQKbZaf`); both lanes on the RTX 4090 at driver 616.64; three lifecycle records with empty page-error lists and completed teardowns. The coordinator re-hashed all 44 frames against it (44 match, 0 mismatched), `REPORT-hero-approach.md` re-ran the whole 44 independently and also got 44 of 44, and two lanes re-derived `sceneTreeDigest` from the served tree and recomputed the three build hashes from `dist/`.

**The served mesh.** The lanes' own census of `data/scene/terrain.mesh` (`node artifacts/terrain-holes/mesh-doctor.mjs`) reports **183,350 triangles, 92,283 vertices, degenerate 0, duplicate 0, more-than-two-face edges 0, and 1,214 boundary edges in one loop**; the pre-cap backup reports 183,188 / 92,269 / 0 / 65 / 195 / **1,441 edges in 15 loops**, which is 183,188 − 65 + 227 and 92,269 + 14 exactly.

**The five gates on the merged revision** are recorded in `docs/devlog/detailed/2026-09-18_terrain-cap-and-census-gate.md`: `npm run build` 0 (reproducing the three certificate-pinned files), `npm run typecheck` 0, `npm test` **74 files / 535 tests, 0 failed**, `npm run audit` 0 (2 moderate, 0 high) and `npm run visual` 0. **This pass did not re-run them and I did not re-verify them**; they are read from that record, which is the session that also captured the certificate.

**Review 34's verdict**, in one line: the C5 pavement re-binding is accepted — the terrain's old position array is an exact prefix of the new one, `new.indices[0..549,369)` is the old index array minus exactly the 65 reader duplicates, each of the 227 cap triangles spans two adjacent rim vertices, the roads mesh gains exactly 123 triangles with 0 old ones absent, and the pavement delta is 2,691 triangles appearing and 78 re-noded in place, **+2,613** — and its one blocking finding was the record's false claim that a clean `data:setup` would silently lose the authored paint.

## 2. What the eight re-inspection lanes concluded

All eight are read-only, all write under the ignored `artifacts/inspection-final2/`, and all eight pass with **no blocking defect**. Every one recomputed its frame digests against the certificate before inspecting, and every one opened its crops at native size rather than through a contact sheet.

- `REPORT-cartographic-overhead.md` — the cap landed and the H1/H2 prediction does not read as a defect: background pixels inside the rim polygons **940 / 746 / 214 / 606 / 242 / 446 → 0**, the pre-cap mesh recovered by stripping the 227 cap triangles, the luminance-step seam test, the silhouette trace.
- `REPORT-satellite-overhead.md` — enclosed background islands **4 / 3 / 3 / 8 / 9 / 13 → 0**, every differing blob in all six frames lands on a recorded rim, and it censused the served mesh independently. It also records an instrument fault it found and fixed: its first diff matcher indexed a per-pixel mask by byte offset and reported no difference anywhere.
- `REPORT-cartographic-block.md` — a measured null at the block distance: a pose-tolerant residual's maximum 32×32-block mean is **0.2 luminance levels with 0 blocks above 2 of 920**, against the pre-batch arms' own 0.097–0.219 floor, with the same run's overhead az180 block (mean 9.99, 121 px over 24 levels) as the positive control. No rim is visible at this framing.
- `REPORT-satellite-block.md` — the same null, with pixels moved by ≥1.5 levels at 0.60–2.85% of a frame; H1 and H2 are in frame only at az180 and az240 and land on building fabric; the carried az060 finding below; and three corrections to the earlier record.
- `REPORT-cartographic-plaza.md` — the road's per-cell numbers reproduce the reviewed ones digit for digit (az000 `near-pavement` 77.94 / 5.91 / step 2.911); the authored west-approach paint is in the scene and in the pixels but not legible; no rim is visible.
- `REPORT-satellite-plaza.md` — the six satellite plaza frames are the same picture as the pre-batch `grain` after-arm capture (48-cell residual **r 0.9977–0.9999**, fine-band r 0.9978–1.0000, per-cell statistics agreeing to three decimals), and it reproduces the road bound rather than contradicting it.
- `REPORT-hero.md` and `REPORT-hero-approach.md` — no geometry gap and **zero** background-coloured pixels in the whole hero lane; both find H1 and H2 behind the camera at the hero poses (H1 174.5° off the crossing axis and 152.8° off the approach axis), so this framing has no power over the cap in either direction and says so.

## 3. The two coverage bounds the record must carry

**The authored west-approach road paint cannot be judged from the certified frames.** It is present and correctly placed — `data/scene/markings.mesh` `efdba9cb4532db02…` at 388,992 B, its decoded self-contained tail exactly **204 vertices / 68 triangles from index 13,671**, all 68 centroids **4.0–6.0 cm above the highest surface beneath them**, and `src/scene/roads.ts` adding the mesh unconditionally — but at az000/060/120/180 the whole 79 m treatment is **35–120 m away and edge-on** (a 0.15 m separator is a 1–2 px line, and the largest single triangle the entire tail projects is **6–9 px²**, so **no turn arrow is inside the frustum at any of the six azimuths**), and at az240/az300 the camera stands on or beside it with 30–38 of the 68 triangles behind it or blown up at 1 m depth and only a 230–371 px² sliver in view. Where it does fall in frame it is **drawn but faint**: +1 to +17 levels above the asphalt beside it and a 1–3 px local maximum, with no dash or arrow legible at 1:1, 4× or 8×. Settling it needs a frame looking along the west approach from a few metres, and none of the 44 is one.

**H1 and H2 are visible only in the overhead pair.** The hero lanes put them behind the camera (174.5°/152.8° at the two hero poses, H2 at 175.0°/155.9°); the approach lane's own projection puts thirteen of the fourteen rims behind the camera's image plane and the fourteenth, H8, more than 3.7 frame widths off the right edge; the block lanes have them behind the camera at az000/az060, outside the frame at az120/az300, and in frame only at az180/az240 where both land on building fabric in both arms; the plaza lanes have them behind the camera or outside the frame at az000/060/120/180 and on facades between the camera and them at az240/az300. The overhead framing is therefore the **only** one of the 44 that can see any rim, and a reader must carry that rather than read it as an omission.

## 4. The carried appearance finding

At the satellite-block az060 framing a large shadow-side plane reproduces its recorded state and is effectively textureless: its **5×5 local-sd median/p90 is 0.00 / 0.34, identical to that frame's clear sky**, and its cross-preset fine-band correlation is **0.022 against the same sky patch's −0.025**, where material surfaces on the same run read **0.70–0.98** (0.979 at az120 `left-ground` asphalt, 0.834 on the az180 street, 0.699 at the az000 junction). Mean 28.3 and spread 3.2 against the frame's own sky control at 2.1 — so the earlier dusk fill has lifted it out of near-black and it is still a smooth two-dimensional gradient with no feature at any scale the lane looked at, at 1:1 or at 6×. It is about 28% of the frame, it is shade and not a hole, and this batch did not touch it, every statistic being identical to the pre-batch rebuild to the printed digit. Carry the **sky-identity measurement rather than the word "matte"**. The plausible cause is ambient-only lighting on a shadow face; the **open question is which surface it is and whether the lever is its material or the lighting**.

## 5. What is owed, in priority order

1. **The owner-level markings source decision** — re-derive the reviewed network from a pinned OSM extract, or narrow `tools/scene/hero-paint.ts:30`'s guard to a content check that records both vintages. The reviewed extract is on no disk here and a dated Overpass attempt returned a current snapshot, so the authored mesh was restored rather than re-derived and its inheritance is proved (`artifacts/f1-cap/probe-inheritance.mjs`, 204 authored vertices, 0 missing, max |old − new| `0.000000000 m`). Separately: `npm run data:scene` **alone** silently downgrades the pair to unauthored (`tools/scene/build.ts:159`, `build-markings.ts:82`), `npm run data:markings` alone refuses by name, and a clean `data:setup` re-derives both from the live survey with nothing comparing them.
2. **The prescribed stall fix** at `src/agents/population/pedestrians.ts:314-321`, the ORCA no-admissible-velocity fallback, located and confirmed by falsification (the ORCA-zeroed count tracks the overlap count at every contact scale, 134 → 19 → 0 against 133 → 19 → 0). It lives under `src/agents/population/**`, so it moves `dist/` and needs **its own** capture rather than the batch's. Acceptance test, pinned: diagonal entry on the green (`committedAtScramble > 0`), `pedestriansCompleted` at or above **338** at 3000/200, `worstOverlappingPairs` at or below **142** at 150/0.
3. **The prescribed fix for the certificate-in-a-worktree evidence loss** — the wrapper certifies wherever it is run, so a gate run inside a worktree writes `complete.json` into an ignored tree that the merge deletes. The fix belongs in the instrument (refuse to certify outside the primary, or write somewhere durable), not in a caution to the next lane.
4. **The prescribed `tools/flicker/judge.ts` docstring correction** — `:681-693` and the duplicate copy above it still state the falsified `channelDelta` mechanism (the reads never left the buffer at any frame size and the mutated call returns a finite `changedFraction`), `test/flicker-judge.test.ts:250-259` repeats it, `:733-736` still says the refine window is 41 px against the real **23**, and `:135-137` still quotes the pair sums as per-still spans.
5. **The crawl predicate's quadratic form** — `unexplainedFraction > 0.005 × frameGap²` cannot fire past a `sqrt(1/0.005) = 14.14`-rendered-frame span, and this lane's measured spans are 20.5–23, so its crawl figure is measured and not gated and a `no failures` line must not be read as the class having been enforced.
6. **The assessed-but-unimplemented in-page consecutive-frame capture** — a `requestAnimationFrame` callback after the app's own loop with `drawImage` into a 2D canvas, measured at roughly 1–3 ms and 3.7 MB a frame against about 250 ms and 31 rendered frames for `page.screenshot`, adjacency verified by `frameCount`, with `toDataURL` rejected at 30–80 ms of PNG encode.
7. **The frame change is attributed, and this item is not owed** — `artifacts/inspection-final2/REPORT-unattributed-change.md` measured the overhead az300 and az240 regions against the two surviving pre-cap arms: the changed pixels are the rims rather than outside them (az300's `x[1092..1251] y[222..250]` holds 1,066 changed pixels, 532 of them cap void becoming ground), the best integer shift is `(0,0)` for all six overhead frames on all three pairings, and 473 of the remaining 510 px are the batch's 65-duplicate-triangle cull moving the stored vertex normal at unchanged positions (median 5.5°, max 27.2°) — so the change is the cap and the reader fix, not an unattributed defect. What this item called the same change at the satellite **block** framing (0.60–2.85% of pixels moved by ≥1.5 levels, largest cluster 423 px) is not re-measured by that report, whose bound is the overhead pair. Two of the report's summary figures do not match its own instrument files: `cluster-rims.txt` and `classes.txt` give **505** changed pixels inside a rim footprint at az300 (454 in the eight order-verified rims plus rim1's 51) where the report says 510 (459 + 51), and `pixel-controls.txt` gives an arm-versus-arm whole-frame floor of **11,167–14,594 px** past 2 levels where the report says 12,334–15,298; `plan.md`'s item (4) carries the instrument values.
8. **The crop-label rule as a standing one**, with `tools/inspect/crops.ts`'s own docstring saying its region names are screen geometry: six misreadings across four lanes came from reading a name as a material claim, and a lane must classify a surface from the pixels and say what it based the classification on.
9. **A decision on the two `style-return` frames** — certify them and add them to the expected set, or state in the certificate's documentation that they are lane evidence and not part of the 44. That is the quality register's own open row, and it is a decision rather than an inspection finding.

## 6. Corrections this pass made, and the flicker claims it checked

**Three sentences in `plan.md` said "both return frames" in a way that implied the two `style-return` frames are certified.** They are not, and they are at the **approach** pose rather than the crossing: `complete.json` lists 44 entries (8 hero + 36 sweep) and `tools/visual/verify-output.ts` never names `style-return`, while `artifacts/visual/hero/hero.json` carries them in `styleReturns[].sha256`; and `return-cartographic-dusk` differs from `hero-cartographic-dusk-approach.png` by a mean of **0.38** luminance levels against **30.70** for the crossing frame, with its frame mean 92.63 on the approach frame's 92.36 rather than the crossing frame's 117.59. Corrected at lines 140, 288 and 447, each citing those two measurements.

**Of the four superseded flicker claims the correction lane listed, `plan.md` and `handoff.md` carried one.** The crawl bar's "a slower screenshot makes the bar looser" reading was present in the closing dated block's Phase A paragraph, together with a citation to `tools/flicker/judge.ts:500`; both are corrected in place at line 486 (the predicate is quadratic, the line is `:504`), and the criterion's own status entry at line 283 now records that the crawl class cannot fire at the lane's cadence. The other three — the per-still shutter span, the 41 px refine window and `ascent`'s missing wheel event — were **not present** in either file, so nothing was changed for them; they appear here only as owed text work (item 4). That same paragraph also carried the falsified `channelDelta` mechanism ("invisible to the 192x108 unit fixtures"), which is corrected there too.

**The record corrections the lanes made are carried rather than re-derived**: `artifacts/terrain-holes/REPORT.md`'s hole numbers are not area order, so joining a per-hole figure to `manifest.json`'s `closedRims` must match on **centroid**; `tools/inspect/crops.ts`'s region names are screen geometry; and "nothing below luminance 8 in any frame" is a rounding artefact (0.000–0.022% of each frame is below 8, in 0–31 small components, 97–100% of it also below 8 pre-batch).

## 7. What would strand what

A `data/scene` write strands the certified 44-frame set bound to `aa2e07e9902a5dc9`, exactly as it stranded the pre-batch `e6b7b9d8de0faf69`, and so does any `dist/` change — the stall fix (item 2) will be the next one to do it. Any `npm run data:markings`, `data:network` or `data:setup` needs item 1's decision first, and none of them may run in a worktree whose `data/` is junctioned to the primary. `npm run visual` owns port 4319 and one machine's GPU: exactly one run at a time. The scene digest is a content identity now, so a rebuild re-derives the digest rather than inventing one — but it still re-issues the certificate rather than inheriting it, and it re-derives `roads.mesh`, which must then agree with the `roadsSha256` embedded in the pinned `network.json`.

## 8. Facts the next session needs

- **Machine**: RTX 4090 at driver 616.64 as the certificate records it, 32 logical cores. One browser lane at a time; `npm run visual` takes about five minutes.
- **Ports**: 4319 verdict/lifecycle, 4320 hardware, 4321 populated, 4322 render-defects, 4323 flythrough, 4324 flicker, 4335 frame-budget, 4336 smoke.
- **Data**: the capped scene at 117 files / 301,782,754 B under `sceneTreeDigest dfee0f12…`, with `markings.mesh efdba9cb…` (388,992 B), `terrain.mesh c39d49ed…` (4,415,496 B), `roads.mesh 0d429c31…` (6,509,676 B) and `pavements.mesh 3b24c5ef…` (13,068,136 B, 92.9% of its 500,000-triangle ceiling); `data/network/network.json 314fac84…` still at its Review 10 pin and untouched by the cap; `data/osm/shibuya-aoi.osm.json 976d848e…` is **not** the extract the reviewed network was built from.
- **The frame-set header trap**: `complete.json` keys its frames as `sweep\satellite\plaza-azNNN.png` with backslashes and no `artifacts/visual/` prefix, so a basename lookup collides with the cartographic set and returns "0 of 6" — a lookup bug that reads exactly like a digest failure. Match on the certificate's own `file` field.
- **Never run a data-writing command in a worktree with `data/` junctioned, never install into a worktree with `node_modules/` junctioned, and never delete a directory that has held a junction without enumerating the reparse points loudly, unlinking every one with `rmdir`, and then confirming the primary's own directory counts.**

## 9. What this pass wrote as unverified, so it is not inherited as verified

The five gate results, the mesh census, the certificate's fields and every per-lane measurement above are **read from the record that produced them** — this pass ran no gate, no build, no capture and no `data:*` command, and re-derived none of those numbers itself. The `0.70–0.98` material-correlation range is the quality register's summary of a lane's 0.699 / 0.834 / 0.979, not a band the lane printed. The "six misreadings across four lanes" count is the correction lane's, not a count this pass made. The **ambient-only lighting** cause for the az060 shadow plane is a plausible cause recorded in the register, not a measurement. The `+3.25 m`, `+1.74 m`, ~42 m² and ~19 m² prediction and areas come from `artifacts/f1-cap/BRIEF.md` §5 rather than from this pass's own geometry.

## 10. Provenance whose loss would strand a claim

All of it under the ignored `artifacts/`, none of it promoted, because the tracked record is the durable home of these numbers: the eight lane reports and their instruments under `artifacts/inspection-final2/` (`REPORT-hero.md`, `REPORT-hero-approach.md`, `REPORT-satellite-overhead.md`, `REPORT-satellite-block.md`, `REPORT-satellite-plaza.md`, `REPORT-cartographic-overhead.md`, `REPORT-cartographic-block.md`, `REPORT-cartographic-plaza.md`, plus `spill.mjs`/`spill.json`, `seam.mjs`/`seam.json`, `measure-block.mjs`/`measurements.txt`, `measure.mjs`/`measure.json`/`measure-prebatch.json`, `masked-corr.txt`, `paint-support.txt` and the crop manifests); the certificate and its 44 frames at `artifacts/visual/complete.json` and `artifacts/visual/**`; the pre-cap reference arms `artifacts/grain/after-arm/` and `artifacts/dusk-light/after-arm/`; `artifacts/f1-cap/` (`BRIEF.md`, `evidence/after1.json`, `after2.json`, `final.json`, `probe-inheritance.mjs`); `artifacts/terrain-holes/REPORT.md` and its untracked `mesh-doctor.mjs`; `artifacts/signature-routing/STALL.md` and `BRIEF.md`; `artifacts/style-switch-sim/REPORT.md`; and `artifacts/flicker/` (`RUN-01-REPORT.md`, `RUN-02-REPORT.md` SHA-256 `2d76b7f1800aa46d01ff965b5435121eaad7951509fd55ebcd9c2d3b7d0e2114`, `REGISTER-ROW.md`, `capture/final-{orbit,ascent}.json`, `capture/manifest-{orbit,ascent}.json`, the 23 captured PNGs and `inspect-run02/`) — and **the register cited those under `artifacts/flicker/wt/artifacts/flicker/`, a worktree that has since been removed; that citation now points at the live copies above**; `RUN-02-REPORT.md` was rewritten while the round-35 review read it, so bind any citation to the record's digest rather than to a line number.
