# Handoff — the Shibuya 1km deliverable, 2026-09-18

This note is the session-to-session delta. The canonical status is [`plan.md`](plan.md) — its closing dated block, "Coordinator status, 2026-09-18", and the acceptance criteria status subsection are current — the standing defect list is [`../../learning/defect-register.md`](../../learning/defect-register.md), and the prioritized work list is [`quality-register.md`](quality-register.md). Read those after this note.

## 0. Where the tree is, and what a reader would otherwise get wrong

`origin/main` is `17052ea`, and this note is the commit directly on top of it. The round opened at `8ff7367` and the tree moved twice under it — `213af4c` (the quality register's rewrite, another lane) and `17052ea` (the flicker lane's two closing findings) — while `phase2-regen` in the primary advanced `7ad6ed0` → `d5ead63` → `0e7d683` in the same window, so every revision named below names when it was read.

**The terrain-hole fix is implemented, gated and verified against the bytes the app serves, and it is not on `main`; the served `data/scene` has already been regenerated with it, so the certified 44-frame set is knowingly stale relative to the scene the app now serves.** Both halves of that matter. A reader who finds only a branch is not looking at an unstarted fix, and a reader who finds closed ground in `data/scene/terrain.mesh` is not looking at a landed one.

Uncommitted work: none in the primary. The pavement-input pin edit an earlier brief called uncommitted was committed as `d5ead63` while this note was being written, with the old and new digests in its message and the published digest updated in `docs/reference/pavement-recipe.md`; `git diff HEAD` in the primary is empty.

Live trees: the primary on `phase2-regen` at `0e7d683`; `artifacts/f1-cap/wt` on `worker/f1-terrain-cap` at `7ad6ed0`, one commit behind its branch and clean; `artifacts/flicker/wt` on `flicker/measure` at `2bd5bba`, **dirty** (`docs/learning/gate-proofs.md` edited, and an untracked `test/zz-final.test.ts` that is scratch and must be deleted before that lane closes) and **running `npm run visual` in its own worktree since about 21:21:28**, so the appearance port and the GPU are held and no other lane may capture; and `artifacts/docs-closeout/wt`, this note's own worktree, removed once its branch merges. `phase-b-terrain-cap` at `0c76789` is a superseded parked branch with no worktree and is safe to delete once `phase2-regen` lands.

## 1. What is verified, and with which commands

The served scene, read from the primary's `data/`: `node artifacts/terrain-holes/mesh-doctor.mjs data/scene/terrain.mesh` reports **183,350 triangles, 92,283 vertices, degenerate 0, duplicate 0, more-than-two-face edges 0, and 1,214 boundary edges in one loop**; the same command on `artifacts/scene-backup-20260917/scene/terrain.mesh` reports 183,188 / 92,269 / 0 / 65 / 195 / 1,441 edges in 15 loops, which is 183,188 − 65 + 227 and 92,269 + 14.

`Get-FileHash -Algorithm SHA256` on the served files gives `terrain.mesh c39d49edd405e266…`, `roads.mesh 0d429c3145be6c7b…`, `pavements.mesh 3b24c5ef71f864fc…` (464,525 triangles against the backup's 461,912, with `degenerate` 25,448 before and after and `duplicate` 99,869 → 100,011), `markings.mesh efdba9cb4532db02…` (388,992 B), `manifest.json afa6403c7bc36019…` (4,074 B, no `builtAt`, `closedHoleCount` 14, `capTriangleCount` 227, fourteen `closedRims` whose vertex counts sum to 227 and whose areas sum to 5,187.5 m² against the 2026-09-15 census's 5,188, four apexes inside the AOI), `data/osm/shibuya-aoi.osm.json 976d848ee620b7d4…`, `data/network/network.json 314fac843392de12…`, and `data/scene/agents/vehicles.json 5b29efc7977949db…`.

The determinism claim is two records rather than a memory: `artifacts/f1-cap/evidence/after1.json` and `after2.json` are 117 files / 301,773,802 bytes with **zero differing paths**.

`dist/` did not move. `dist/index.html a73d718a…`, `dist/assets/index-DRmtpVMl.js c890299c…` and `dist/assets/index-dvQKBZaf.css 11dd032c…` are the three files the certificate pins and they match it byte for byte; only the unpinned `index-DRmtpVMl.js.map` differs. So the F1 batch moves the certificate's **scene binding only**, not its build binding.

The certificate is `e6b7b9d8de0faf69` in `artifacts/visual/complete.json`, captured `2026-09-18T03:30:51.944Z` → `03:35:25.883Z`, binding scene digest `1ebda66251e5e023…` at 117 files / 301,674,209 bytes — the pre-cap scene. Those two timestamps are also the measurement of the re-capture's cost: **273.9 s for 44 frames**, not the three hours this repository's older notes still carry.

The cap's own numbers are recorded rather than re-run here: nine code files at +1,295/−21 (`git diff --stat 4efaea6 38bb0ac`), four non-visual gates at 72 files / 525 tests in `d5ead63`'s message (the earlier session record at `7ad6ed0` names 72 files / 522 tests at that revision), and six mutations each watched red in `docs/learning/gate-proofs.md`.

Claims that are recorded rather than verified, so do not inherit them as facts: typecheck and audit on the flicker lane (asserted in a commit message and a report, with no surviving log); the in-page capture figures "roughly 1-3 ms and 3.7 MB a frame" (written with "roughly", measured by no probe); and the markings inheritance proof, which does not exist.

## 2. What is owed, in priority order

1. **The markings inheritance proof**, and with it the owner-level source decision: re-derive the reviewed network from a pinned OSM extract, or narrow `tools/scene/hero-paint.ts:30`'s guard to a content check that records both vintages. `npm run data:markings` refuses today because `data/osm/shibuya-aoi.osm.json` is `976d848e…` at `2026-09-18T01:49:46Z` while the reviewed `network.json` records `9923a9ae…` at `2026-09-07T03:23:56Z`; the reviewed extract is on no disk here (the only `*.osm.json` in the repository is the current one, and none of the 665 JSON files of 1 MB or more under `artifacts/` hashes to it), and a dated Overpass attempt returned a current snapshot (`7805b653…`, `2026-09-18T04:11:19Z`). The authored `markings.mesh efdba9cb…` was **restored** from a byte-identical copy rather than re-derived: verified by digest, unproven by inheritance, and the directory an earlier brief named as the source holds a different mesh (`4daa4283…`, 383,200 B).
2. **Review 34** of the re-bound pavement inputs. Its item 1 is the prediction the run falsified: `pavements.mesh` moved 461,912 → 464,525 triangles where `artifacts/f1-cap/BRIEF.md` accounted for 123 from the newly draped roads.
3. **The merge of `phase2-regen` (`0e7d683`) to `main`** — five commits off `4efaea6` — and delete `phase-b-terrain-cap` at `0c76789` in the same pass.
4. **The one batched 44-frame re-capture**, then **the one re-inspection of those 44 frames** at native resolution, H1's and H2's fan wedges first. Start it only when no other lane holds the port: the flicker lane is running the gate in its own worktree right now.
5. **The walking-admission stall**, already an approved work item with an acceptance test (walkers on the diagonal **on the green**, completions not collapsing, overlap not rising). Its fix lives in `src/agents/population/**`, so it moves `dist/` and needs **its own** capture rather than the batched one.
6. **The signature shot stays not met** until 5 lands, and the route-distribution lever is closed: driving the crossing share 51.1% → 93.9% (2,816 of 3,000 slots) buys 1 → 4 diagonal entries and 2 → 6 peak-within-60 m in the 150-walker arm while that arm's completions fall 27 → 2.
7. **Phase A's flicker item stays not met**: the achieved gap is 20.5-23 rendered frames mid-shutter to mid-shutter against the criterion's `cadenceFrames: 2`, and the measured route to close it — an in-page `requestAnimationFrame` grab with `drawImage` into a 2D canvas — is unimplemented.

**Provenance whose loss would strand a claim**, all of it under the ignored `artifacts/` and none of it promoted, because the tracked record is the durable home of these numbers: `artifacts/signature-routing/` (`BRIEF.md` sha256 `71b9badd…`, plus the `occ-p3000-*`, `route-mix-*`, `spread-*` and `stall-*` JSON), `artifacts/f1-cap/BRIEF.md` and `evidence/` (`after1.json`, `after2.json`, `final.json`, `osm-dated.json`), `artifacts/flicker/wt/artifacts/flicker/` (`RUN-01-REPORT.md`, `RUN-02-REPORT.md`, `REGISTER-ROW.md`, `capture/final-orbit.json`, `capture/final-ascent.json`), `artifacts/style-switch-sim/REPORT.md`, `artifacts/inspection-final/REPORT-cartographic-overhead.md` and `REPORT-satellite-overhead.md`, `artifacts/terrain-holes/REPORT.md`, `artifacts/scene-backup-20260917/hashes-stateA.txt` and `hashes-stateB.txt` with its `scene/` copies, and the untracked instrument `artifacts/terrain-holes/mesh-doctor.mjs`. Losing `RUN-02-REPORT.md` costs most: it was being rewritten while this note was written and its body still carried a crawl figure its own headline withdraws, so bind any citation to the record's digest rather than to a line number.

## 3. What would strand what

A `data/scene` write strands the certified 44-frame set and every inspection bound to runId `e6b7b9d8de0faf69`; that has already happened, which is why the re-capture is owed, and no inspection bound to that runId should be extended to the served scene. A `dist/` change strands it too, and the stall fix will be the next change to do it. Any `npm run data:markings`, `data:network` or `data:setup` needs the item-1 decision first, and none of them may run in a worktree whose `data/` is junctioned to the primary. `npm run visual` owns the port: exactly one run at a time, and the flicker lane holds it now.

Two rows in `quality-register.md` carry figures the measurements contradict, and they are **not** mine to edit because that file belongs to another lane: the F1 row's "30×25 m" and "80.43 m outside the AOI" are not reproducible — the gate's own measurement is a **512.5 m² XZ area and a 25.5 m diameter**, about 80 m outside the AOI's east edge — and the "Rejected or deferred" entry naming the signature shot's routing fix as the lever contradicts the measured cause now recorded in `plan.md`.

## 4. Facts the next session needs

- **Machine**: RTX 4090 with driver version 616.64 as the certificate records it, 32 logical cores. One browser lane at a time; `npm run visual` takes about five minutes.
- **Ports**: 4319 verdict/lifecycle, 4320 hardware, 4321 populated, 4322 render-defects, 4323 flythrough, 4324 flicker, 4335 frame-budget, 4336 smoke.
- **Data**: `data/scene` at `terrain c39d49ed…`, `roads 0d429c31…`, `pavements 3b24c5ef…`, `markings efdba9cb…`, `agents/vehicles.json 5b29efc7…`, manifest 4,074 B with no `builtAt`; `data/network/network.json 314fac84…` at its Review 10 pin and untouched by the cap; `data/osm/shibuya-aoi.osm.json 976d848e…` is **not** the extract the reviewed network was built from.
- **The scene digest is a content identity now**: two consecutive `data:scene` runs return the same 117 files / 301,773,802 bytes with zero differing paths, so a rebuild re-derives a digest rather than inventing one — but it still re-issues the certificate rather than inheriting it, and it re-derives `roads.mesh`, which must then agree with the `roadsSha256` embedded in the pinned `network.json`.
- **Never run a data-writing command in a worktree with `data/` junctioned, never install into a worktree with `node_modules/` junctioned, and never delete a directory that has held a junction without enumerating the reparse points loudly, unlinking every one with `rmdir`, and then confirming the primary's own directory counts.**
