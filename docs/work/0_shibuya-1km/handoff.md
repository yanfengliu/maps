# Handoff — the Shibuya 1km deliverable, 2026-09-18

This note is the session-to-session delta. The canonical status is [`plan.md`](plan.md) (its dated status block at the end is current, plus the "Acceptance criteria status, 2026-09-17" subsection), the standing defect list is [`../../learning/defect-register.md`](../../learning/defect-register.md), and the prioritized work list is [`quality-register.md`](quality-register.md). Read those after this note.

## 0. Where the deliverable stands

`main` is `6907963`, pushed, and it is the only branch except `phase-b-terrain-cap` (a deliberate parked branch, below). Five gates are green on `1e3597d` (the last code merge): build, typecheck, **68 files / 494 tests**, audit, and a fresh 44-frame certificate **runId `14cdadbafba82120`** on the RTX 4090 with three lifecycle records. The controls-driven flythrough **passes** in the primary (46 of 46 distinct digests, judge zero failures) and its crowd leg is the first whose subject actually resolves: ~45–55 distinguishable figures at 35–45 px (1.4–1.8 m), no visible interpenetration, and its five zero-travel held pairs are live (blobs shift rigidly at 1.05–1.34 m/s). The road/ground detail tile now delivers measurable structure where the bar measures it (cartographic near-field road 99.9% → 63.5% indistinguishable-from-flat, 14× neighbouring-pixel difference, confirmed by eye); the black **satellite** near-field road is *not* fixed (10.4 → 16.6 of 255 against a floor of 20), and the palette was shown to be the wrong lever.

## 1. What changed in this round, and what it cost

Three Phase A instruments landed: the flythrough's clearance check now reads every recorded moment rather than only the capture; the approach leg's vertical control finishes its correction inside the step (the camera's worst recorded moment went 0.194 m → 2.908 m); and the flythrough's aim scorer now scores bodies at their own ground against the ray the renderer actually uses, with an anchor chosen by its **worst** tick (152/99/95/100/100/10 walking bodies across ticks 3,000–7,200) rather than its best — the previous anchor held 225 at tick 3,000 and 4 at 5,400, which is why an earlier run photographed an empty street and the lane correctly refused it. Phase B landed the appearance batch: a seeded surface-detail tile (now 6.35% neighbouring-texel step at mip 0, corner-aligned sampling, per-surface spectra, anisotropy 8, and a shader patch that refuses a missing anchor), the dead `guardrailPosts` field removed, the frame-budget probe seam gated behind `?frameBudgetProbe=1` so the delivered page installs nothing, and **`builtAt` removed from the served manifest** so the scene digest is a content identity — two consecutive `data:scene` runs now return the same digest.

Two incidents, both the coordinator's, are recorded in full in the register and both are worth reading before touching `data/` or a worktree: a lane wrote the primary's served scene through a `data` junction **on the coordinator's instruction**, and a coordinator's cleanup followed seven junctions an enumeration had missed (`-ErrorAction SilentlyContinue` hid them) and emptied `data/network`, `3dtiles`, `agents`, `decorations`, `gsi`, `osm` and `plateau`. Both are recovered — the network pin restored (`314fac84…`), `paint-support` 6 of 6, every fetched source re-downloaded and verified against its pin — and the local rules now forbid a silent error action in the reparse enumeration and recursive delete on any directory that has ever held a junction.

## 2. What is owed, in priority order

1. **The black near-field road.** It is the road mesh's albedo (`roads:plateau-tran`, 0x32363c against the pavement's 0x999991 in one frame; normals 100% up; no shadows at dusk so light cannot explain it), and the albedo lift took it only to 16.6 of 255. The measured lever is the dusk light on horizontal surfaces — about ×2.5 irradiance, or the road's wetness (0.72, which drives roughness to ~0.46) — and it needs its own capture with a tone review of the pavement, facades and the cartographic near-white look. Do not tune the hex.
2. **The dusk criterion's temporal half.** TAA cannot accumulate while the camera moves in this app by construction, so flicker/crawl must be judged on the non-accumulating path from consecutive-frame captures; no lane has captured consecutive rendered frames yet, and the flythrough's pairs are 285–2,202 ms apart.
3. **F1's cap.** It works on branch `phase-b-terrain-cap` (centroid fan, +227 triangles / +14 vertices, with a census gate that refuses uncapped terrain by name) but it moves `roads.mesh` and the pavement stage refuses because `REVIEWED_INPUTS` binds the old terrain and roads. It needs its own review of that binding before it can land, and it must not be bundled with an appearance capture.
4. **The two flythrough inspection findings being fixed right now** (in flight on `fix/flythrough-anchor-constant`): the exported `CROWD_CAMERA` constant is 180° out from the pose the flight actually flies (53 m on the opposite side; the *flown* pose is the sound one), and the lane's aim evidence cites a stale 2026-09-16 dump.
5. **The ascent's closure claim and `stand.targetY`.** The ascent still ends 857.4 m out against its 620 m aim (the plan's "closed loop" sentence is corrected), and the orbit target's height is the world-origin 15.2 m in every frame, up to 6.8 m below the ground under the camera — which is also why the crowd leg had to be re-aimed onto low ground.
6. **A full native re-inspection of the new frames.** The changed cells are being inspected against the new certificate; the rest of the 44 still bind to the previous build's frames.

## 3. Facts the next session needs

- **Machine**: 32 logical cores, RTX 4090 (driver 32.0.16.1664 / vendor 616.64), a sibling `3d-maker` checkout keeps `chrome-headless-shell` processes alive. One browser lane at a time; `npm run visual` now takes about five minutes.
- **Ports**: 4319 verdict/lifecycle, 4320 hardware, 4321 populated, 4322 render-defects, 4323 flythrough, 4335 frame-budget, 4336 smoke; 4329 is a foreign hold.
- **Data**: `data/scene` 116 files at its pins (`terrain fef57d09…`, `roads 3fe126cd…`, `pavements 5d2a9150…`, `markings efdba9cb…`, `agents/vehicles.json 5b29efc7…`), `data/network/network.json` at the Review 10 pin `314fac84…`, `data/raw` holds the pinned PLATEAU archive, and all fetched sources are restored. **Never run a data-writing command in a worktree with `data` junctioned, and never delete a directory that has held a junction without enumerating the reparse points loudly and unlinking every one.**
- **Certificates**: the current one is `14cdadbafba82120` (frames under `artifacts/visual/`); the previous, fully inspected one is `fb07b11ea3b8296f` with its eight-lane inspection under `artifacts/inspection-gpu2/`. A scene rebuild moves the digest, so any change to `data/scene` strands a certificate.
- **Evidence**: `artifacts/inspection-flythrough2/REPORT-crowd.md` (the crowd resolves and walks) and `REPORT-other-legs.md`; `artifacts/inspection-gpu2/` (the last full 44-frame inspection); `artifacts/appearance-2-evidence/` (the appearance lane's instruments, measurements and gate logs — its crops were in the worktree and are gone); `artifacts/quality-audit/register.md` (the verified prioritized audit).
