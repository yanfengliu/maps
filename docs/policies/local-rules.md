# maps — repo-only rules

No rule yet. The repo has no stack, no code, and no map data source, so nothing here would be more than a restatement of the fleet canon in `AGENTS.md`.

The first entries are expected to come from two decisions the owner or the first substantial code commit will make: the stack and toolchain, and which map data sources are used along with the terms they carry. Write them here when they are made, not before.
